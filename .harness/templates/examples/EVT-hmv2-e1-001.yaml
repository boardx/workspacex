# EVT-hmv2-e1-001.yaml — TPL-EVT-001 的一个真实实例，充当"骨架已经能跑"的活证据。
#
# 不是虚构 fixture：这份文件会被 `pnpm harness templates doctor` 真的扫描、
# 真的校验，是全仓第一份符合 HMV2-008 公共元数据 schema 的实例文档。
# 它记录的正是 Epic E1 骨架落地这件事本身——用这套系统记录它自己的第一次交付，
# 是最小可信的 dogfooding。
template_id: TPL-EVT-001
template_version: 1
instance_id: EVT-hmv2-e1-001
status: active
scope:
  project: workspacex
  phase: null
refs: []

# 以下字段是 §10.1 定义的 Work Event 载荷（尚未单独 schema 化，E3 会把它收进
# TPL-EVT-001 自己的深度 schema；此刻只作为人类可读的补充信息，不参与 E1 的校验）。
kind: progress
actor: coord-architecture
facts:
  - "PROP-HARNESS-MODEL-001 Epic E1（HMV2-006~012）骨架落地"
delta:
  - "template-model.ts / template-id.ts / template-scan.ts / template-doctor.ts 实现完成"
  - "registry.yaml 按 Proposal §8 seed 了 23 个模板类型"
  - "pnpm harness templates doctor / templates allocate 两条命令可用"
blockers: []
next_action:
  owner: coord-architecture
  action: "跑 doctor 验证本文件自己能被扫描到并通过校验，作为 E1 的第一个反证"
