{
  "version": 2,
  "requiredChecks": [
    "verify-control-plane",
    "verify-affected",
    "verify-full-compile",
    "merge-gate",
    "backend-required"
  ],
  "aggregates": {
    "backend-required": [
      "gates-fast",
      "gates-test",
      "native-document-chain",
      "native-runtime-lane",
      "gates-runtime",
      "prototype-audit"
    ]
  },
  "allowedMergeMethods": [
    "merge",
    "squash"
  ]
}
