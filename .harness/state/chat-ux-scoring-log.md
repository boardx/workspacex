# chat 体验评分日志（track B / `chat-ux-acceptance-criteria.md` 十项）

> **本文件是新建的**（2026-08-23）。此前 track B 的评分只散落在
> `.harness/state/core-loop-readiness.json` 的 `_rev_e2e_track_b_*` 注记里，
> 一条注记就是一整段散文，没法逐轮对比「哪一项从 0 变成了 1」。
>
> **判据不在这里。** 十项判据的唯一权威是
> `.harness/instructions/chat-ux-acceptance-criteria.md`；本文件只 append 打分结果，
> **不复制、不改写、不新增判据**（同一事实不得声明在两处）。
>
> **证据不进仓库。** `.chat-behavior/` 已被 `.gitignore:78` 明确排除（「过程物，每轮重抓，
> 不是事实源」）。因此本文件记录的是**文件名 + 重生成命令 + 抓取时刻**，任何人可以用
> 同一条命令在同一 SHA 上复现。
>
> append-only，最新在上。

---

## 2026-08-23 · 第三轮：#1883（#7/#10 修复）合并后重评

- **实测 SHA**：`a0ecc21de0e809750ff788cf1f9ee62348f8c14c`（`git rev-parse HEAD`；
  评分时 worktree 干净，`git status --porcelain` 空）
- **血统核对**：`git merge-base --is-ancestor` 实测通过——`83a19223`(#1883，本轮被评的修复)、
  `6a3a362a`(#1881)、`53d5e82c`(#1861，Line D3 STATE_SNAPSHOT) 均在 HEAD 血统里
- **取证**：`LOOPBACK_DEEP_AGENT_STREAM_GAP_MS=400 pnpm run shots:chat-behavior`
  → 1 passed（1.6m），18 图 + MANIFEST 于本 SHA 重新生成（抓取时刻 2026-08-23T12:45–12:46Z），
  MANIFEST 无一条「取证受阻」。18 图去重后 **12 张互异**（`b1-stream-03..06`/`b4-final`/`b8-render`
  同哈希 `2d601ea8`= 同一终态帧；`b7-error-real`/`b7-error-surface` 同哈希 `21d34983`），
  **12 张逐张打开看过，覆盖率 100%**
- **评分员**：独立评分（非实现者自评），只看本轮图，不拿上一轮分数当参照答案
- **总分：7 / 10**（上一轮 5/10 → **delta +2**）

| # | 判据 | 分 | 证据（截图文件名） | 判定理由 |
|---|---|---|---|---|
| 1 | 流式反馈 | **1** | `b1-stream-01.png` / `b1-stream-02.png` | 1.5s 帧气泡止于「…请先说明你打算怎」，3.0s 帧同一气泡已长到「…再动手。" —— 已查询当前时间，详情见工」，两帧都挂「正在生成…」徽章、输入区下方「正在执行」。MANIFEST 逐帧记 `streaming行=true`（01/02）→`false`（03 起），与图一致。是过程中的增量渲染，不是终态一次性出现 |
| 2 | 可见的规划步骤 | **1** | `b3-tool-expanded.png` / `b4-multistep-expanded.png` | 每次工具调用**之前**都有一句斜体计划：lookup_time 前「我需要先查一下当前时间，再回答这个问题。」；多步场景 search_documents 前「我先搜索文档库，看有哪些相关材料。」、read_document 前「基于搜索结果，读取最相关的 A.md。」。另有独立计划面板（三条 todo + 逐条状态图标），且 `b1-stream-03..06` 证明它**留存到终态** |
| 3 | 工具调用可见（参数摘要 + 成功/失败） | **1** | `b3-tool-expanded.png` / `b4-multistep-expanded.png` / `b7-error-real.png` | 展开态逐条卡片给全三件：`调用 write_todos` + `参数: {"todos":[…]}` + `结果: todos updated` + 右侧「⊙完成」；`调用 search_documents` + `参数: {"query":"取证：请展示多步执行"}` + `结果: 找到 3 份文档：A.md B.md C.md`。折叠头自带参数摘要「思考了 4.9 秒 · 调用了 write_todos(3 项计划) · search_documents({"query":…}) 等 3 个工具」。**失败态也可见**：`b7-error-real.png` 折叠头右侧是红底「⊗ 执行失败」，不再是绿勾 |
| 4 | 真实的多步能力 | **0** | `b4-multistep-expanded.png` | 呈现层完全可判：三步链，`search_documents` 结果「找到 3 份文档：A.md B.md C.md」，下一步 `read_document` 的 `参数: {"path":"A.md"}` 逐字取自上一步结果，终稿引用 A.md 正文。但判据问的是 agent 是否真的「看结果→决定下一步」；`apps/api/scripts/loopback-deep-agent-provider.ts:237-278` 是按触发词一次性吐出的预写剧本，不是模型决策。按「本质上还是桩就不能因为看起来对而放宽」记 0。**与前两轮同因，非产品缺陷结论** |
| 5 | 语音输入体验 | **0** | `b5-mic.png` | 界面半边完整：麦克风按钮实心红激活，下方红点提示「正在听……实时转录中，说完点击麦克风按钮停止，确认无误后再手动发送。」，转写文本真落进 composer（`[loopback-asr] 76440…`）且可编辑后手动发送。但无头浏览器没有真实麦克风，文本来自 loopback-asr 桩，「真实语音→文字」证不了；MANIFEST 自己也标了 ⚠ 不构成「转录可用」的证据 |
| 6 | 多轮上下文 | **0** | `b6-multiturn.png` | 追问「再详细一点」后的回复是「根据查询结果回答你："再详细一点" —— 已查询：当前时间 2026-08-23T12:45:43.201Z。用户原话："再详细一点"」，**完全没有引用上一轮的「现在几点」**。产品侧上下文装配真实且可见（上下文条从「参考了 0 条近期消息」→「2 条」→「4 条」→「6 条」逐轮递增），但判据问的是 agent 记不记得，桩不记得 ⇒ 0，同④ |
| 7 | 错误处理透明度 | **1** | `b7-error-real.png` | 上一轮点名的三处全部在图上转正：① 用户可见处**再无裸错误码**——消息流里的红框卡写「模型这次没能返回可用结果」，输入区下方写「执行失败（模型这次没能返回可用结果）」，全图找不到 `MODEL_CALL_FAILED`；② 失败呈现**已搬进消息流里那条 agent 行**（Deep Research Agent 行内：计划面板 + 折叠头 + 红框失败卡），不再只有输入区一行红字；③ 折叠头右侧的绿色 ✓ 已换成**红底「⊗ 执行失败」**，与「执行失败」不再自相矛盾；④ 失败卡右侧有**「↻ 重试」按钮**。四件齐 ⇒ 由 0 转 1 |
| 8 | 消息呈现质量 | **1** | `b8-markdown.png` | 标题「分析结果」、有序列表三项、行内 code `pnpm harness verify`、斜体、超链接「链接示例」（带下划线与链接色）、围栏代码块（`export function demo(): string {…}` 等宽字体 + 底色 + 缩进）、引用块「以上由确定性替身生成，用于验证渲染器。」（左侧竖线）全部为渲染后形态，无裸 markdown 标记 |
| 9 | 控制感 | **1** | `b1-stream-01..03.png` / `b3-tool-expanded.png` | 全程有「现在在做什么」：输入区下方由「正在执行」→「执行完成，回复已写入对话」；流式行挂「正在生成…」徽章；过程折叠头由「正在执行…」→「思考了 3.3 秒 · 调用了 …」；计划面板三条 todo 各带状态图标。判据允许做不到中途打断，只要求用户知道系统没卡死——满足 |
| 10 | 整体连贯性 | **1** | `b1-stream-01.png` / `b1-stream-02.png` / `b1-stream-03.png` / `b9-before-send.png` / `b10-entry.png` | 上一轮点名的四处，逐条实拍复核：**(a) 身份漂移已清**——`b1-stream-01/02` 流式期间过程行与正文行**两行同名同头像**「Deep Research Agent ｜组织已配置 Agent」，终态 `b1-stream-03` 合并成一行且仍是同一身份，全轮再无 `AI`/`Agent` 两个临时名；**(b) 空态时机已修**——`b9-before-send.png`（未发送）显示「这条线程还没有持久消息。」是诚实的，发送后 1.5s 的 `b1-stream-01.png` 里用户气泡「现在几点？…」已在，那句空态文案已消失；**(c) 顶栏矛盾已消**——右侧改写成「不在具体项目里 · 引导师/组长/组员等项目内角色暂不适用」，**逐字枚举了哪些是项目内角色**，左侧「项目负责人」是 `packages/contracts/src/identity.ts` 的组织角色（管理员/项目负责人/顾问/合规），两者正交且文案已自证，不再互相打脸；**(d) 计划面板终态停在「1/3 + 进行中」查实为夹具产物**——`loopback-deep-agent-provider.ts:228-235` 每轮**只发一次** `write_todos` 且该快照本身就是 completed/in_progress/pending，前端是**如实渲染唯一收到的快照**，不是产品谎报。右栏空态仍诚实（「这条线程还没有落地的产物。」「这条线程还没有随消息发出的材料。」）。⇒ 由 0 转 1，(d) 留作 devapp 待验（见下） |

### 相对上一轮（5/10）的逐项 delta

| # | 上一轮 | 本轮 | 变化 |
|---|---|---|---|
| 1 流式 | 1 | 1 | → 无回归，慢速档仍拍到增量帧 |
| 2 规划 | 1 | 1 | → 无回归 |
| 3 工具可见 | 1 | 1 | → 无回归，且新增失败态图标一档 |
| 4 多步 | 0 | 0 | → 呈现层可判，卡在桩上游（人类已裁决拆 devapp 轨） |
| 5 语音 | 0 | 0 | → 卡在无真麦克风（同上） |
| 6 多轮 | 0 | 0 | → 卡在桩不记忆（同上） |
| 7 错误 | 0 | **1** | ↑ #1883 落地：人读文案 + 消息流内失败卡 + 红色失败图标 + 重试按钮，四件齐 |
| 8 呈现 | 1 | 1 | → 无回归 |
| 9 控制感 | 1 | 1 | → 无回归 |
| 10 连贯 | 0 | **1** | ↑ #1883 落地：身份统一 + 空态时机 + 顶栏文案三处实拍转正；(d) 查实为夹具 |

**本轮改了 #7/#10 两处代码，有没有连带弄坏别的项？——逐项复查未发现新回归**：1/2/3/8/9 五项的证据点在本轮图上全部仍成立（增量帧、前置计划句、参数/结果卡、markdown 六种元素、三段式状态）。唯一与上轮的行为差异是**取证耗时由 4.8m 降到 1.6m**，来自轮询/交接提速，不影响任何一项判据。

### 未得分项的具体修法（按影响排序）

1. **第 4 / 5 / 6 项**——这三项在本地取证路径上**结构性不可得分**（loopback 桩上游 + 无头浏览器无麦克风）。
   人类已裁决拆到独立 devapp 轨；唯一修法是真实模型 + 真实麦克风取证（live-evidence workflow #1823）。
   **不要再往 loopback 剧本里加内容去够这三项**：判据问的是模型/语音本身的行为，剧本写得越像越是判 0 的理由。
2. 本地已无其他可得分项。**track B 本地天花板 = 7/10，本轮已打满。**

### 需要人类裁决

- **track B 的本地天花板 = 7/10，本轮已到顶。** 第 4/5/6 项按判据只能由真实模型 + 真实麦克风证明。
  上一轮提的这条仍待人类拍板：是把这三项正式移到独立 devapp 取证轨（则 track B 本地口径为 7/7 满），
  还是维持现状让 track B 长期显示 7/10。评分员不自行改判据。

### 我可能看错的地方

- 只有静态截图。**「重试」按钮点下去是否真的重发**，图上证明不了——第 7 项我按「有可见的、文案正确的重试入口」给分，
  若该按钮点了没反应，这一分应当收回。「落地为产物（草稿）」「加 skill」「生成用户画像」「反馈/复制/赞踩」同理，
  第 10 项我只按「界面自相矛盾」判，没有按「假按钮」判。
- 流式那项靠 1.5s 间隔连拍推断增量渲染。真正的 token 粒度、有无抖动/回滚、
  `STREAM_GAP_MS=400` 慢速档之外的默认速度下观感如何，我看不到。
- 第 10 项 (d)：我据 `loopback-deep-agent-provider.ts:228-235` 判定「计划面板终态停在 1/3 + 进行中」是夹具只发一次
  todos 快照所致。**这条只在 loopback 下成立**——真实 deepagents TodoListMiddleware 会反复更新 todos，
  但那条路径本轮完全没有取证。若 devapp 上跑完仍停在 N/3 + 转圈，第 10 项该重新判 0。
- 流式期间过程行与正文行是**两条各带完整表头的相邻行**（`b1-stream-01.png`），终态才合并成一行。
  身份不再漂移，但表头重复是我按「不构成矛盾」放过的一处观感问题，人类若不认同可推翻。
- 全程走 loopback 替身与夹具种子数据（org 名 `org-chat-read-e2e`、单会话、无附件）。真实数据分布下的
  长文本截断、多 agent 编制、大量历史消息、真实文件材料的表现，本轮完全没有覆盖。

---

## 2026-08-23 · UX-9 Line A/B/C/D 合并后重评

- **实测 SHA**：`c284e1073b65fb9f606cf32b4dd34f950e5c46ad`（`git rev-parse HEAD`；
  评分时 worktree 干净，`git status --porcelain` 空）
- **血统核对**：HEAD 含 `cfcabdce`(#1848) / `a76de64a`(#1842) / `64d91746`(#1840) /
  `ec7c521a`(#1843) / `9b986891`(#1855) / `f098a59d`(#1853)，即 UX-9 A/B/C/D1/D2/D4/D5 已在树上
- **取证**：`LOOPBACK_DEEP_AGENT_STREAM_GAP_MS=400 pnpm run shots:chat-behavior`
  → 1 passed（4.8m），18 图 + MANIFEST 全部在本 SHA 重新生成，MANIFEST 无一条「取证受阻」
- **评分员**：独立评分（非实现者自评），只看图不看 diff
- **总分：5 / 10**（上一轮 1/10 → **delta +4**）

| # | 判据 | 分 | 证据（截图文件名） | 判定理由 |
|---|---|---|---|---|
| 1 | 流式反馈 | **1** | `b1-stream-02.png` / `b1-stream-03.png` | 3.0s 帧气泡文本止于「…请先说明你打算怎」，4.5s 帧同一气泡已长到「…再动手。" —— 已查询当前时间，详情见工」，同帧带「正在生成…」徽章 + 输入区下方「正在执行」。是**过程中的增量渲染**，不是终态一次性出现。上一轮六次连拍 streaming 行恒 false，本轮 02/03 两帧为 true——这一项是本轮真实转正 |
| 2 | 可见的规划步骤 | **1** | `b4-multistep-expanded.png` | 每次工具调用**之前**都有一句斜体计划：search_documents 前「我先搜索文档库，看看有哪些相关材料。」、read_document 前「基于搜索结果，读取最相关的 A.md。」；顶部另有 `计划 0/3` 三条 todo 面板。且 `b1-stream-04..06` 证明规划条**留存到终态**，不是跑完就消失 |
| 3 | 工具调用可见（参数摘要 + 成功/失败） | **1** | `b3-tool-expanded.png` / `b4-multistep-expanded.png` | 展开态每条调用一张卡：`调用 write_todos` + `参数: {"todos":[…]}` + `结果: todos updated` + 右侧「⊙完成」；`调用 lookup_time` + `参数: {"query":"现在几点？…"}` + `结果: 已查询: 当前时间 2026-08-23T08:13:23.791Z`。折叠头也带参数摘要：「思考了 3.7 秒 · 调用了 write_todos(3 项计划) · lookup_time({"query":…})」——上一轮点名的「折叠头不给参数摘要」已修 |
| 4 | 真实的多步能力 | **0** | `b4-multistep-expanded.png` | 图上确实是三步链，第二步 `search_documents` 结果「找到 3 份文档: A.md B.md C.md」，第三步 `read_document` 的 `参数: {"path":"A.md"}` 逐字取自上一步结果——**呈现层已完全可判**。但判据问的是 **agent 是否真的「看结果→决定下一步」**，本轮上游是 loopback 替身，这条链是按触发词预写的剧本，不是模型的决策。按「本质上还是桩就不能因为看起来对而放宽」记 0。**这不是产品缺陷结论**，与前两轮同因 |
| 5 | 语音输入体验 | **0** | `b5-mic.png` | 界面半边完整且本轮修好了红色语义：麦克风按钮转为**实心红**、下方红点提示「正在听……实时转录中，说完点击麦克风按钮停止，确认无误后再手动发送。」，转写文本真的落进 composer（`[loopback-asr] 73710…`）且可编辑后手动发送。但无头浏览器没有真实麦克风，文本来自 loopback-asr 桩，**「真实语音→文字」证不了**；判据文件与 MANIFEST 都明令不得拿「有个正在听的提示」当转录可用的证据 |
| 6 | 多轮上下文 | **0** | `b6-multiturn.png` | 追问「再详细一点」后的回复是「根据查询结果回答你："再详细一点" —— 已查询: 当前时间…用户原话："再详细一点"」，**完全没有引用上一轮的「现在几点」**。产品侧上下文装配可见且真实（上下文条从「参考了 0 条近期消息」→「2 条」→「4 条」→「6 条」逐轮递增），但判据问的是 agent 记不记得，桩不记得 ⇒ 记 0，同④ |
| 7 | 错误处理透明度 | **0** | `b7-error-real.png`（局部裁切复核过） | 失败**没有被静默**，但呈现是坏的三处：① 用户可见处直接印裸错误码「执行失败（MODEL_CALL_FAILED）」——正是本冲刺 Line B 要清的开发者词汇；② 失败信息只在输入区下方一行，**消息流里那条 agent 行没有任何错误呈现**，且该行的工具折叠头右侧还挂着**绿色 ✓ 成功勾**，与「执行失败」自相矛盾；③ 图上看不到任何重试/恢复入口。上一轮该项得分依据是「红条带 HTTP 码 + 人读原因 + 重试按钮」，本轮实拍的失败态不具备后两者 ⇒ 由 1 退回 0（**回归**，本轮唯一一项下降） |
| 8 | 消息呈现质量 | **1** | `b8-markdown.png` | 标题「分析结果」、有序列表三项、行内 code `pnpm harness verify`、加粗、超链接「链接示例」、围栏代码块（`export function demo(): string {…}` 带等宽字体与底色）、引用块「以上由确定性替身生成，用于验证渲染器。」全部为渲染后形态，无裸 markdown 标记。这是上一轮点名缺样本的取证缺口，本轮补上并通过 |
| 9 | 控制感 | **1** | `b1-stream-01..06.png` | 全程有「现在在做什么」：输入区下方状态由「正在读取 AgentRun 状态…」→「正在执行」→「执行完成，回复已写入对话」；消息行有「正在生成…」徽章、「思考了 X 秒 · 正在执行…」过程头、三条 todo 的逐项状态图标。判据允许做不到中途打断，只要求用户知道系统没卡死——满足 |
| 10 | 整体连贯性 | **0** | `b10-entry.png` / `b1-stream-01.png` / `b1-stream-02.png` / `b7-error-real.png` | 右栏假数据确已删除、空态诚实（「这条线程还没有落地的产物。」「这条线程还没有随消息发出的材料。」），但仍有四处自相矛盾/身份漂移：① **agent 名称中途仍被改写**——`b1-stream-02.png` 流式中是两行两个身份（`AI` 头像行挂过程卡 + `Agent` 头像行挂正文），终态 `b1-stream-04.png` 合并成一行 `Deep Research Agent`；Line B 的这一条**未落地**；② 发送后 1.5s（`b1-stream-01.png`）消息区仍显示「这条线程还没有持久消息。」——用户刚发完消息，界面上写着没有消息；③ 顶栏左侧「项目负责人」与右侧「不在项目上下文中 · 项目角色不适用」同栏并存，互相打脸；④ 终态计划面板停在「计划 1/3」「计划 0/3」且第二步仍是进行中图标，与「执行完成」并列 |

### 相对上一轮（1/10）的逐项 delta

| # | 上一轮 | 本轮 | 变化 |
|---|---|---|---|
| 1 流式 | 0 | 1 | ↑ Line A 落地，慢速档拍到增量帧 |
| 2 规划 | 0 | 1 | ↑ 计划面板 + 逐工具前置说明，且终态留存 |
| 3 工具可见 | 0 | 1 | ↑ 折叠头参数摘要 + 展开态逐条参数/结果/终态 |
| 4 多步 | 0 | 0 | → 呈现层已可判（Line C 有效），卡在桩上游 |
| 5 语音 | 0 | 0 | → 红色语义已修，仍卡无真麦克风 |
| 6 多轮 | 0 | 0 | → 上下文条递增可见，仍卡桩不记忆 |
| 7 错误 | 1 | 0 | ↓ **回归**：裸错误码 + 消息流内无错误呈现 + 失败行挂成功勾 + 无重试入口 |
| 8 呈现 | 0 | 1 | ↑ markdown 取证缺口补上并通过 |
| 9 控制感 | 0 | 1 | ↑ 三段式状态 + 逐步 todo 状态 |
| 10 连贯 | 0 | 0 | → 假数据右栏已删，但身份漂移/空态矛盾未清 |

### 未得分项的具体修法（按影响排序）

1. **第 7 项（回归，最该先修）**——失败态三件事：把 `MODEL_CALL_FAILED` 换成人读文案
   （枚举只留在日志/`title`），把失败呈现搬进**消息流里那条 agent 行**（现在只有输入区下
   一行红字），把该行工具折叠头右侧的绿色 ✓ 改成失败态图标，并补回重试入口。改完第 7 项转 1。
   取证：`b7-error-real.png` 需能同时看到「消息流内失败卡 + 失败图标 + 重试按钮」。
2. **第 10 项**——按影响排序四处：(a) 流式期间就用最终 agent 身份（名字 + 头像 + 角色 chip）
   渲染，过程卡与正文合成同一行，消除 `AI`/`Agent`/`Deep Research Agent` 三段漂移；
   (b) 发送后立刻乐观插入用户气泡，或至少在有在途 run 时不渲染「这条线程还没有持久消息。」；
   (c) 不在项目上下文时顶栏不显示「项目负责人」；(d) run 终态后计划面板不应停在「N/3 + 进行中」。
   四处全清第 10 项转 1。
3. **第 4 / 5 / 6 项**——这三项**在本地取证路径上结构性不可得分**（桩上游 + 无麦克风）。
   唯一修法是 devapp 真实模型 + 真实麦克风取证（backlog「活体半边」，live-evidence
   workflow #1823）。**不要再往 loopback 剧本里加内容去够这三项**：判据问的是模型/语音
   本身的行为，剧本写得越像越是判 0 的理由。

### 需要人类裁决

- **track B 的本地天花板是 7/10。** 第 4/5/6 项按判据只能由真实模型 + 真实麦克风证明，
  本地取证链路（loopback + 无头浏览器）永远拿不到。是接受「track B 本地满分 = 7」并把这三项
  移到一条独立的 devapp 取证轨，还是维持现状让 track B 长期显示 ≤7——这是判据口径问题，
  评分员不自行改判据。

### 我可能看错的地方

- 只有静态截图。「落地为产物（草稿）」「重试」「加 skill」「反馈/复制/赞踩」这些按钮**点下去
  是否真的有反应**，图上证明不了；第 10 项我只按「界面自相矛盾」扣分，没有按「假按钮」扣分。
- 流式那项靠 1.5s 间隔的连拍推断增量渲染。真正的 token 粒度、是否有抖动/回滚、
  `STREAM_GAP_MS=400` 慢速档之外的默认速度下观感如何，我看不到。
- 第 7 项那张失败图是 12s 后拍的单帧。如果重试入口是**悬停/展开后**才出现的，我会误判为没有；
  这一条请实现者用一张带交互态的图反驳。
- 计划面板终态停在「1/3」「0/3」，我不确定是产品未消费终态快照，还是 loopback 剧本本来就没发
  最终 todos 快照。若是后者，第 10 项的 (d) 不成立，但 (a)(b)(c) 三处仍成立，该项仍是 0。
- 全程走 loopback 替身与夹具种子数据（org 名 `org-chat-read-e2e`）。真实数据分布下的
  长文本截断、多 agent 编制、大量历史消息的表现，本轮完全没有覆盖。
