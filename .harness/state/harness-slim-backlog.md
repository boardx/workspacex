# 控制平面瘦身 Backlog（目标：5.6 万行 → ≈3.3 万行，判别力一条不减）

> 创建于 2026-08-18，作者 coord-architecture。
>
> 本文件是**执行清单**，不是论证文档。每一条都带**实测数字**与**验收方式**；
> 标「估」的是尚未实测的预估，做之前必须先测出真数再更新本表。
>
> 硬约束（继承 AGENTS.md）：
> - **不得制造第二份事实副本**——本表里的行数不复述到别处，别处引用本文件。
> - **没有反证的删除不算删除**：每一条删除/收敛动作，必须先有能红的变异反证基线（见 H-00），
>   动作前后跑同一批变异，捕获结果逐条一致才算通过。
> - 一条 backlog 一个 issue 一个 PR（AGENTS.md 完成定义 5/6）。

## 为什么做这件事

不是「代码太多」，是**同一个形态被重复实现，以及为不存在的数据预建了治理机器**。
两条根因，四次实证（2026-08-18 同一会话内）：

| 模块 | 建的时候的假设 | 真实数据 | 代价 |
|---|---|---|---|
| H3A 治理机器 | 为 Epic E3 预建 | 0 task / 0 review / 2 event / 1 domain | 7,872 行守 3 个文件 |
| merge-gate approve 检查 | native APPROVE 是真实信号 | 100 个已合并 PR，**0** 个有 | 事后加 suspend 开关 |
| merge-gate verdict 标签检查 | `review:*-ok` 标签是真实信号 | 300 个 PR，**0** 个有 | 事后加 suspend 开关 |
| verify-risk 高风险分类器 | 对 feature 元数据子串匹配 | 304 个 feature，4/7 标记零命中 | 整个重写 |

## 现状实测（口径：`git ls-files`，2026-08-18）

**scope 修正（2026-08-18 采纳外部意见）**：本 backlog 初版只看 `.harness`，
漏了同属控制平面的 `docs/` 与 `.agents/skills/`。补测：

```
40,518  .harness
15,390  docs/
 ├ 6,277  superpowers/（41 个日期命名的 plan 文件）
 ├ 5,557  proposals/（29 篇）
 ├ 2,561  adr/（26 篇）
 └   624  architecture/
 4,186  .agents/skills/
───────
60,094  控制平面合计
```


```
40,518  行  总计
 ├ 12,702  门禁层（17 道门，见下表）
 ├  9,389  H3A 全簇（代码 8,996 + 数据平面 393）※ 与门禁层重叠约 7,900
 ├  8,632  非 H3A 测试
 ├  3,181  instructions/*.md
 ├  1,321  state/（生成物，却进了 git）
 ├    995  vm/*.sh 真实部署脚本
 └  其余   lib / agents / rubrics / templates / config
```

### 17 道门只有 6 种形态（这是最大的压缩点）

| 形态 | 门数 | 行数 | 平均/门 | 处置 |
|---|---:|---:|---:|---|
| **A 实例校验**（读 YAML → schema → 唯一性 → 交叉引用） | **8** | **9,270** | **1,159** | 删 7 + 收敛 1 |
| B 集合关系（引用集 ⊆/== 实存集、可达性） | 5 | 1,573 | 315 | 收敛 |
| C 棘轮（allowlist 只减不增） | 1 | 111 | — | 保留 |
| D git 追踪状态 | 1 | 154 | — | 保留 |
| E 动态反证探针 | 1 | 417 | — | **保留，全仓最有价值的门** |
| F 聚合报告 | 1 | 1,177 | — | 保留 |
| | **17** | **12,702** | | |

形态 A 每门 1,159 行 vs 形态 B 每门 315 行——差距不是复杂度，是**同一件事写了 8 遍**。

### 门禁耗时实测（本机，冷启）

```
lint:verification        22.33s  ← 占 verify:harness 链 58%
lint:readiness            3.83s
lint:spec-gate-coverage   2.35s
lint:templates-doctor     1.24s
9 个 H3A doctor 合计      ≈7.5s
其余 5 个                 ≈1.3s
```

### 变异反证实测：这些门是真的

造缺陷看门抓不抓，**没有一道空转**：

| 变异 | 结果 |
|---|---|
| workflow-event：instance_id 清空 / kind 非法 / ID 重复 / 删 task_id | 4/4 捕获 |
| terminology：造重复 TERM ID | 捕获（并连带抓出歧义映射） |
| domains：删 owner 字段 | 捕获 |
| graph-authority：把投影文件 `git add -f` | 捕获 |
| skills-doctor：加一条反引号包住的不存在路径 | 捕获 |

**结论修正**：H3A 不是坏门，是**好门守着不存在的东西**。所以 H-01 删的是
「机器 + 它守的空数据平面」整体，不是「删掉一个有用的检查」。

---

## Backlog

状态字段：`not_started` → `measured`（实测替换掉「估」）→ `in_progress` → `pr_open` → `done`

### H-00 变异反证基线（**所有删除/收敛动作的前置**）
- **状态**：not_started
- **动作**：把 2026-08-18 手工跑的变异电池固化成 `.harness/scripts/gate-mutation-probe.ts`，
  对每道门登记「注入什么缺陷 → 必须非 0 退出」，一条命令跑完全部。
- **减少行数**：**+300（净增）**——这是买保险，不是省钱。
- **验收**：全部门禁在基线上「变异必红、无变异必绿」；任何一道门若变异后仍绿，
  当场记为发现，不许绕过。
- **为什么先做**：H-01/H-04 的「不影响项目」不能靠人看代码判断，只能靠这个。

### H-01 删 H3A 全簇（机器 + 空数据平面）
- **状态**：**done**（2026-08-19，PR #1596）。实测 −9,732 行 / −56 文件，
  与预估 −9,389 基本吻合。CI 裁决：合入 commit 上 `verify-control-plane` /
  `verify-affected` / `verify-full-compile` 全绿（红的 `e2e-full`/`fullstack-smoke`
  在 H-01 之前的 commit 上同样红，与本次无关）。
- ~~**依赖**：H-00~~
- **动作**：删 52 个代码文件 + 10 个数据文件（task-assignment / workflow-event /
  review-decision / role-authorization / role-freeze / graph-authority※ / domain-skill /
  portable-role / role-scorecard / terminology 全簇 + `.harness/{tasks,events,reviews,domains,model,contracts,terminology}`），
  同步摘掉 `verify:harness:raw` 里对应的 9 条 `lint:*` 与 `cli.ts` 的 case 分支。
  ※ graph-authority 是形态 D、实测有效且只有 154 行 → **保留，不在删除范围**。
- **减少行数**：**−9,389**（实测）｜ **CI 提速**：≈−7.5s
- **验收**：H-00 基线里 H3A 之外的门捕获结果逐条不变；`./init.sh --full` 绿；
  CI 全绿；`git grep` 无残留引用。
- **风险**：低。删前必须跑一次静态可达性扫描 + 动态全 CI 路径确认零触达，
  **不是凭判断说它死了**。git history 永久可回溯，删除不是丢失。

### H-02 删零/低引用小工具 —— **done，但估算错了 8 倍**
- **状态**：done（2026-08-19）
- **原估算 −762 是错的，实测只有 −97。** 逐条查完消费者：

| 候选 | 实测消费者 | 结论 |
|---|---|---|
| `merge-fl-parts` | 无 | **删**（97 行） |
| `record-readiness-evidence` | **CI 调用 2 次**（`.github/workflows/harness-verify.yml`） | 活的 |
| `verify-readiness-evidence` | `fullstack-smoke.test.ts` 断言它的内容 | 活的 |
| `templates-allocate` | `templates/registry.yaml` 明写"新增模板用它"，23 个 template_id 是活的 | 活的 |
| `templates-render` | 已随 H-01 删除（依赖 workflow-event-model） | — |
| `templates-doctor` | 10 处引用，在 verify 链上 | 活的 |
| `migrate-labels` | `instructions/multi-agent-coordination.md` | 活的 |
| `sweep-unblock` | `lib/types.ts` + phase-00 sprint 文档 | 活的 |
| `dep-graph` | 2 个 SKILL.md + `lint-rewrite-coverage.mjs` | 活的 |
| `verify-timing-report` | `lib/verify-timing.ts` 是 verify 的活依赖 | 活的 |
| `skills-doctor` | 在 verify 链上，变异实测能抓 | 活的 |

**教训见下面「引用扫描方法」一节——原估算之所以错这么多，是扫描方法本身有系统性盲区。**

### H-03 `state/` 出仓
- **状态**：not_started
- **动作**：`.harness/state/` 里的生成物（`.cache/`、`core-loop-readiness.json`、
  `dep-graph.md` 等）加进 `.gitignore`；真正的人写事实（`PROGRESS.md`、各 backlog、
  `roadmap.yaml`、allowlist）留下。**先逐个文件分类，不批量。**
- **减少行数**：**−1,321**（上限，实际取决于分类结果）
- **验收**：新 clone + `./init.sh` 后所有命令仍可用（生成物能重建）。
- **注意**：`graph-authority` 门就是守这条线的——出仓后它应当仍然绿。

### H-04 形态 A+B 剩余 6 道门收敛成通用执行器
- **状态**：not_started ｜ **依赖**：H-00、H-01
- **动作**：H-01 之后形态 A 只剩 templates-doctor（若 H-02 未删）；连同形态 B 的
  5 道（ui-material / nav-reachability / spec-gate-coverage / third-artifact / skills-doctor）
  收敛成 **1 个通用执行器 + 6 份声明式规格**：
  - 实例校验规格：`{ 目录, schema, 唯一键, 交叉引用字段 }`
  - 集合关系规格：`{ 左集提取器, 右集提取器, 关系(⊆/==), 豁免清单 }`
- **减少行数**：**≈−2,000（估）**——做之前先写执行器原型测真实收益，再更新本行。
- **验收**：H-00 基线捕获结果**逐条一致**（这是这条的全部意义）。
- **警告**：这是重写不是删除，是本 backlog 里唯一有实质回归风险的一条。
  若原型显示收益 < 1,000 行，**放弃这条**，别为了数字重写在跑的门。

### H-05 instructions 文档去重 + 生成式图
- **状态**：not_started
- **动作**：3,181 行 md 里查同一事实的多处副本（AGENTS.md 自己点名已漂移 5 次的病），
  收敛为单一事实源 + 机械门控。同时把三处散文换成**从代码生成**的图：
  1. **PR 状态机 → mermaid stateDiagram**，源 = `PR_QUEUE_STATES` + `classifyPr` 分支。
     先例现成：`lib/pr-queue.ts` 头注释已写明「枚举权威，coordinator-sop.md 的状态表由
     pr-queue.test.ts 机械比对」——把表升级成图，成本近零。
  2. **17 道门执行链 → mermaid flowchart**，源 = `package.json` 的 `verify:*` 依赖 +
     CI job 拓扑。今天这条链只存在于一行 800 字符的 `&&` 串里。
  3. **H3A 条款覆盖矩阵**（见 H-06）。
- **减少行数**：**≈−1,000（估）**
- **铁律**：**图只画结构（状态、依赖、覆盖），不画规则。** 手画的图 = 第二份会漂的
  事实副本，正是本仓漂移过 5 次的模式。图必须生成 + CI 断言无漂移。

### H-06 「未实现条款」覆盖矩阵
- **状态**：not_started ｜ **优先级：高，且可与 H-01 并行**
- **动作**：10 个 doctor 每次运行合计打印 **38 条**「本条款尚未实现，如实列出不是遗漏」
  的散文自陈，散落 10 处、每次刷屏，没有任何人能回答「H3A 一共多少条款、实现了几条」。
  收敛成一张生成的 `条款 × 实现状态` 矩阵。
- **减少行数**：**≈−300（估）**，但真正价值不在行数。
- **为什么高优先级**：这张矩阵做出来之后，**才第一次能用数字**决定 H3A 剩下的部分
  值不值得继续投——包括 H-01 到底该删多少。若与 H-01 冲突，先做本条。

### H-07 pr-queue / merge-gate 判定规则收敛成声明式规则表
- **状态**：not_started ｜ **依赖**：PR #1563 合并
- **动作**：#1563 已抽出共享**事实计算**（`issueTraceabilityFailure` / `classifyApprovals`），
  但**规则本身仍是两份手写代码**，靠注释与测试维持语义一致——下次事故驱动的补丁
  大概率又不同步（本会话已因此打了 4 轮补丁）。收敛成一份
  `{ id, check(facts) => pass | fail | advisory }` 规则表，两模块喂不同 facts 跑同一份表，
  **结构上不可能分叉**。
- **减少行数**：**≈−500（估）**
- **验收**：拿真实历史回放（已有原型：60 个真实已合并 PR，54 通过 / 6 失败，
  6 个全部是真·无 issue 关联，零误判），收敛前后判定分布必须完全一致。

### H-08 review 信号三选一
- **状态**：blocked ｜ **阻塞于**：`task_3e1337b9`（`review:*-ok` 标签机制是否有真实消费者）
- **动作**：native APPROVE（实测 0/100）、`review:*-ok` 标签（实测 0/300）、
  `.harness/reviews/` 文件（实测 0 实例）——三条平行通路选 1 条，**删掉另外两条**。
- **为什么不能一直 suspend**：现在两条挂着 `*_SUSPENDED = true` + advisory 输出。
  暂停不删、advisory 没人看，本身就是下一个 #848（一道门静默失效 5 天没人发现，
  最后大家绕过工具）的雏形。

### H-09 删 `docs/superpowers/`（**最干净的一条**）
- **状态**：not_started ｜ **优先级：最高，无依赖，可立刻做**
- **实测**：41 个文件 6,277 行，**外部引用数 = 0**——扫遍 `AGENTS.md`/`CLAUDE.md`/
  `.harness`/`.agents`/`docs/adr`/`docs/architecture`/`apps`/`packages`，
  没有任何一处链接它；只有 plan 文件之间互引。
- **减少行数**：**−6,277**（实测）
- **注意**：它是**活跃写入**的（最近 30 天 50 次提交，最新 2026-08-15），
  不是遗留垃圾。所以动作不是「删掉一个没人写的目录」，而是
  **给它建立退出机制**：feature 合入 main 后 plan 归档出活跃树。
  先跟仍在写它的 agent 确认写入是否还有消费者，再动。
- **验收**：`git grep` 零残留引用；无门禁引用它。

### H-10 `docs/proposals/` 随 H-01 一起收敛（**顺序不能反**）
- **状态**：blocked ｜ **阻塞于**：H-01
- **实测反证**：外部意见建议「已实施的提案直接删除」。**这条会切断活引用链**——
  22 个在跑的 `.harness` 脚本把 `PROP-HARNESS-AGENT-001` 当条款权威源
  （`H3A-009`/`H3A-033`/`H3A-022` 这些编号全指向它）。先删提案 =
  doctor 的条款编号变孤儿。
- **正确顺序**：**先删 doctor（H-01），提案才随之可删。两者是一个包，不是两个批次。**
- **减少行数**：≈−3,000（估，需逐篇核活引用后更新）

### H-11 防反弹棘轮（**不是行数上限**）
- **状态**：not_started ｜ **依赖**：H-01…H-03 落地
- **实测反证**：外部意见建议立「.harness ≤6,500 行、docs ≤2,500 行、单文件 ≤350 行」
  的硬上限。两个问题：
  1. **硬 LOC 上限的失败模式是明确的：最便宜的减行方式就是删测试。**
     一道逼人减行的门，会把人推向删掉唯一有价值的那部分。
  2. **「单文件 ≤350 行」制造第二份事实副本**——`AGENTS.md` 第 61 行已有
     「业务源文件不超过 2000 行」。同一事实两处声明，正是本仓点名已漂移 5 次的病。
     且实测全仓 2,695 个源文件里 **200 个 >350 行**，立门当场红 7.4%。
- **改成棘轮**：`.harness` + `docs` 总行数**相对 merge-base 只能减不能增**，
  增加须在 PR 正文写明理由。仓库已有这个模式且实测在正常工作——
  `lint-rewrite-coverage` 今天还报了一条真实的「allowlist 有 1 条已经不缺了，请删掉」。
  棘轮防反弹，但不制造「删测试最划算」的激励。
- **减少行数**：+150（净增，买保险）

---

## 引用扫描方法（2026-08-19 血的教训，做 H-04 以后每一条都要照这个来）

H-02 估算错 8 倍的根因不是判断失误，是**扫描命令本身有系统性盲区**：

```bash
# ✗ 错的（我用了一整轮的写法）
grep -rn "$f" --include="*.yml" ... . | grep -v "scripts/$f"
```

`grep -v "scripts/$f"` 本意是"排除文件自身"，实际把**所有调用这个文件的行**
也一起过滤掉了——因为调用行长这样：

```
.github/workflows/harness-verify.yml:311:  pnpm exec tsx .harness/scripts/record-readiness-evidence.ts \
```

它包含 `scripts/record-readiness-evidence`，于是被自己的排除规则吃掉。
**这个过滤器精确地隐藏了它本该找到的东西。**

```bash
# ✓ 对的：按路径排除，不按内容
grep -rn "$f" . | grep -v node_modules | grep -v "^\./\.harness/scripts/$f\.ts"
#   或用 git 的路径限定
git grep -ln "$f" -- '.github/**' 'apps/**' 'packages/**'
```

同一轮里，机械检查三次抓到我扫描漏掉的东西（`tsc` 抓到两条 import 耦合、
`skills doctor` 抓到两处文档死链、CI grep 抓到 record-readiness-evidence）。
**结论：静态扫描的结论必须由编译器/门禁/CI 复核，不能单独采信。**
这与 AGENTS.md「静态痕迹 ≠ 动态事实」是同一条，只是这次栽在扫描命令的写法上。

---

## 外部意见评估（2026-08-18）

收到一份外部瘦身方案，逐条实测后：**采纳 3 条，反证 4 条。**

### 采纳
| 意见 | 处置 |
|---|---|
| scope 应含 `docs/` 与 `.agents/skills/` | **对，我漏了**。已扩 scope，多出 15,390 + 4,186 行可作用面 |
| `docs/superpowers` 6,277 行可清理 | **实测确认外部引用 = 0**，比本 backlog 原有任何一条都干净 → H-09 |
| 需要防反弹门禁 | 思想对，实现改为棘轮 → H-11 |

### 反证（有实测数据）
| 意见 | 实测 |
|---|---|
| 「测试 12,525 → 1,500（−88%），因为每条规则构造 50 行 Mock」 | **前提不成立**。实测 **912 个用例 / 12,077 行 = 13.2 行/例**；压到 1,500 行 = **1.64 行/例**，不可能。逐个查大文件：design-signoff 11.2、lint-ui-material 12.7、core-loop-readiness 7.9 行每例。且 76 个测试文件里 **30 个已在用表驱动/循环**。**这条最危险**：本会话全部真发现（0/100 native approve、4/7 零命中、5 类变异捕获、60 PR 回放）都出自测试与反证 |
| 「已实施的 proposals 直接删除」 | **会切断活引用链**，22 个在跑的脚本依赖它 → 改为 H-10，顺序倒过来 |
| 「单文件 ≤350 行」硬门 | **第二份事实副本**（AGENTS.md 已有 2000 行规则）+ 200 个文件当场红 → 改为棘轮 |
| 「上下文加载速度与命中率提升 400%」 | **不可证伪**，命中率无基线测量。本 backlog 的纪律：没有实测就标「估」 |

### 净结果
外部意见让**低风险实测删除量从 11,472 提到 ≈17,750**（多出 superpowers 6,277）。
这是真实增量，值。但 10,000 行目标仍不可达，原因不变且更清楚：
12,077 行测试是唯一有效的发现机制、995 行真实部署脚本、约 15,000 行日跑路径。

---

## 累计效果

| 步 | 减少 | 累计 | 数据来源 |
|---|---:|---:|---|
**口径修正（2026-08-19）**：初版的 60,094 是**在过时的树上量的**——当时用主 checkout
的工作树（停在某个 feature 分支）而不是 origin/main，导致 docs 少算了约 2,500 行。
以下全部改为**声明 SHA 的实测**，这是 AGENTS.md「静态痕迹 ≠ 动态事实」的同一条。

| 口径 | SHA | .harness | docs | .agents/skills | 合计 |
|---|---|---:|---:|---:|---:|
| 起点 | `8977b2eb` | 41,554 | 17,894 | 4,187 | **63,635** |
| 现在 | `d1ca9673` | 33,870 | 18,013 | 4,186 | **56,069** |
| 差 | | **−7,684** | +119 | −1 | **−7,566** |

（`.harness` 的 −7,684 = H-01 的 −9,732 减去这两天别人新增的约 2,000 行。
docs 两天只涨 119 行——初版说的「docs 一天涨 2,623」是过时树造成的假象，不成立。）

| 步 | 减少 | 状态 |
|---|---:|---|
| H-00 变异基线 | +480 | **已合入** |
| H-01 删 H3A 全簇 | **−9,732** | **已合入 #1596** |
| H-02 删小工具 | **−97** | 实测（原估 −762，错 8 倍，见上表） |
| H-09 删 superpowers | −6,277 | **blocked**（等写入者确认） |
| H-03 state 出仓 | −1,321 | 估（上限） |
| H-10 proposals 随 H-01 | −3,000 | 39,645 | 估 |
| H-04 形态收敛 | −2,000 | 37,645 | 估 |
| H-05 文档去重 + 生成式图 | −1,000 | 36,645 | 估 |
| H-06 覆盖矩阵 | −300 | 36,345 | 估 |
| H-07 规则表收敛 | −500 | 35,845 | 估 |
| H-11 棘轮 | +150 | 35,995 | 估 |

**落点 ≈36,000，−40%。其中 −17,749（H-09/01/02/03 四步）是实测数字且全是低风险删除。**

## 为什么到不了 10,000

剩下的 36,000 里：12,077 是测试——本会话全部真发现（0/100 native approve、
4/7 风险标记零命中、6 个真失败样本、5 类变异捕获）都是靠回放真实数据 + 变异反证抓出来的，
砍测试等于砍掉唯一有效的机制；995 是真实部署脚本；约 15,000 是
`verify`（全仓引用 174 次）、`doctor`（46）、`sync`、`claim`、`readiness` 这些每天在跑的
路径及其 lib；2,561 是 ADR（决策档案，删了就没有「为什么这么定」的记录）；
4,186 是 skill 活知识库。**再砍就是砍在跑的东西，与「不影响项目」直接冲突。**

有人会说「压到 1.64 行一个测试用例」——那不是压缩，那是删掉门。

## 执行顺序建议

```
H-09（删 superpowers，零引用，无依赖，可立刻做）
H-00（基线，其余全部动作的前置）
  ├─→ H-06（覆盖矩阵，产出决策依据）──┐
  ├─→ H-01（删 H3A）←─────────────────┘ 依矩阵调整删除范围
  ├─→ H-02（删小工具）
  └─→ H-03（state 出仓）        ← 到这里 −11,472，全部实测、低风险
        └─→ H-04（形态收敛，先做原型验收益，收益不足就放弃）
        └─→ H-05（文档去重 + 生成式图）
              H-07（等 #1563 合并）
              H-08（等 task_3e1337b9）
        H-10（proposals，严格在 H-01 之后）
        H-11（棘轮，等前面落地后再立，否则棘轮锁死在胖状态）
```
