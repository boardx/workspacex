# 验证清单

1. 正例能触发该技能；相邻但不相关的反例不应触发。
2. SKILL.md中每个脚本、参考资料和模板路径存在，映射保持相对规范路径，禁止穿越。
3. 在隔离环境实际执行代表性脚本；记录真实命令、退出状态和产物读取校验。缺依赖的反例必须失败。
4. 数值、格式、引用或文件内容按用户目标断言，不能只检查文件存在。失败和未执行不得标记通过。
5. 用真实草稿工具输出完整JSON，核对全部文件摘要；同调用相同内容回放，不同内容冲突。
6. 成功交付附件后由有权管理员显式导入，普通用户与不可见产物拒绝；草稿生成不等于启用技能。
