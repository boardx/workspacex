# AGENTS.md — Phase 14 (agent-kernel-unification) 局部指令

> 阶段级 scoped 指令,补充根 AGENTS.md。只写本阶段特有的约束。

## 本阶段焦点
统一 agent-run 双内核为单一执行引擎（LangGraph/deep-agent-service），实现真流式传输、Plan Mode、分级权限、产出物版本化等 Claude Code 级别的通用 Agent 能力

## 权威来源
- 功能清单:本目录 `feature_list.json`(本阶段唯一权威)。
- 进度:本目录 `progress.md`。

## 规则继承
根 `AGENTS.md` 的所有硬约束在此继续生效(尤其"完成定义"与"干净收尾")。
