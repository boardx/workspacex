# AGENTS.md — Phase 09 Survey

本阶段只维护问卷模块。

## 必读
- `requirements/00-overview.md`
- `contracts/survey/domain.md`
- `feature_list.json`

## 约束
- 参考 boardx-dev-template 问卷系统方向，并以本仓 `/studio/survey`、`packages/contracts/src/survey.ts` 和 survey design deltas 为当前可追溯依据。
- 不新建第二套问卷系统、身份、权限或 Artifact 模型。
