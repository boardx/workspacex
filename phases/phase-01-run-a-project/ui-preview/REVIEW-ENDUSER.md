# 最终用户视角走查报告（REVIEW-ENDUSER）

> 2026-07-30。方法：`pnpm --filter web dev -p 3100` 起真实 dev server（清 `.next` 后重启，
> 首轮 500 是 stale `vendor-chunks` 缓存，非产品缺陷，重建后全绿）。
> 逐路由 HTTP 拉取渲染 HTML，抽取 `href` / `data-testid` / `<h1>` / `<button>` 重建每屏的
> 可达跳转，再回源码核对。**未用浏览器截图**（无该工具）——见「本审计盲区」。
>
> **一句话结论**：单屏几乎都做得很好、很密、七态齐全；但**拼起来走不通**。
> 被送去签核的「十一个域」几乎全是**只能靠手敲 URL 才能到的孤岛**，
> 与用户真正能点到的（左栏导航指向的）**另一套更旧的骨架屏**并存。
> 产品主线「工作坊的会前/现场/会后」三段**没有一段能从导航自然走到**。

---

## 走查前提：存在两套并行的界面世界

| | 用户能点到的（左栏 rail 指向） | 送签核的「十一域」（无任何入口，只能敲 URL） |
|---|---|---|
| 项目 | `/projects` → `/projects/[id]`（surface hub） | `/project`（带 会前/现场/会后 tab 的工作台）|
| 蓝本 | —（无） | `/tpl`（7 屏：list/designer/apply/prep/workflow/promote/versions）|
| 访谈 | `/studio/interview`（访谈 Studio） | `/itv`（访谈模板库）＋ `/rec`（现场录音转写）|
| 画布 | `/projects/[id]/canvas`（现场画布） | `/canvas`（画布模板库）|
| 研究 | `/studio/research`（上下文包） | `/project?tab=research`（项目内投影）|
| Skill | `/admin/skill`（Skill 库） | `/skill`（Skill 库与市场）|
| Agent | `/admin/agent`（Agent 管理） | `/preview/agent-runtime`（三层权限编辑器）|
| 成员/邀请 | `/admin/members`（成员与配额） | `/org-admin/preview`（参与者与邀请）|

**证据**：全仓 grep `href=`/`push(` 指向 `/tpl` `/skill` `/rec` `/itv` `/canvas` `/project`(单数)
`/org-admin` `/preview/agent-runtime` `/chat/preset` 的内部跳转 **= 0 条**
（`apps/web/app` + `apps/web/components`，排除 node_modules）。
左栏 `lib/navigation.ts:35-70` 只连 `/chat /projects /studio/* /brain /tasks /admin`。

⇒ 送审的域屏，用户**永远点不到**。评审签的是 A，用户用的是 B。

---

## 路径 1 · 办一场工作坊（产品主线）

| 步 | 我想做什么 | 我该点哪 | 实际发生了什么 | 卡住了吗 |
|---|---|---|---|---|
| 1 | 开一场三小时定题工作坊 | 左栏「项目」→ `/projects` →「新建项目」按钮（`projects-new`） | `onCreate={() => window.alert("演示：新建项目向导…")}`（`components/projects/projects-screen.tsx:95`）——**弹个 alert 就没了** | **卡死**：入口有，通向一个 alert 死胡同 |
| 2 | 套一个蓝本 | 应从「新建项目」进套用向导 | 套用向导 `ApplyWizardScreen` **确实存在**，但只挂在 `/tpl?screen=apply`，靠调试用的屏切换器切，导航到不了。且 `projects-screen.tsx:79` 还留着**过期注释**「蓝本设计器尚未建」（`/tpl` designer 早已建成） | **卡死**：屏在、无入口 |
| 3 | 会前：定主题/分组/派任务/发问卷 → 怎么知道准备好了 | — | 会前筹备屏 `/tpl?screen=prep`（`ProjectPrepScreen`）**和** `/project?tab=prep`（`tab-prep.tsx`）**各一套**，两处都孤立。准备度阈值配色在 README S-18 有，但屏不可达 | **卡死**：两套 prep，都点不到 |
| 4 | 现场：切环节/看四组并行/AI 副驾/录音转写 → 我在哪主持 | surface hub 的「现场大屏」 | `/projects/[id]` 的 `stage` surface **`href: null`**，渲染成禁用块「尚未建（原型档案主持台为移动端形态，桌面大屏未抽取）」（`app/projects/[projectId]/page.tsx:43-44`）。真正的主持台 `/project?tab=live`（`tab-live.tsx`：下一环节/＋5分钟/四组网格）孤立不可达 | **卡死**：能主持的屏点不到；能点到的是 null |
| 4b | AI 副驾/录音转写 | `/chat` + `/projects/[id]/canvas` | **这一段通**：`/chat` 有批准卡、工具调用、转录卡、改派，且连到 `/admin/agent` 与 `/projects/p1/canvas`。现场画布做得很实 | 未卡（唯一顺的一段）|
| 5 | 会后：产出汇总/结论签署/待办/导出 → 结论从哪来、谁签 | — | `/project?tab=results`（`tab-results.tsx`）做得很好：结论、发布结论（危险动作+二次确认+版本绑定）、**候选决策来自转写、逐条签署**、审计。但**孤立不可达** | **卡死**：签署屏点不到 |

**路径 1 结论**：走到第 4b（现场对话/画布）能通，其余四步（新建/套蓝本/会前/主持/签署）
**全部因缺入口而走不通**。屏几乎都做好了，就是没接线。这是最贵的一类返工。

---

## 路径 2 · 做一次研究项目

| 步 | 我想做什么 | 我该点哪 | 实际发生了什么 | 卡住了吗 |
|---|---|---|---|---|
| 1 | 新建研究项目、与工作坊区分 | 左栏「项目」→ 新建 | 数据模型无「项目类型」：`ProjectStatus` 只有 running/preparing/draft/delivered，`ProjectFilterKey` 只有 all/client/internal/priority/archived（`lib/mock/projects.ts:13,125`）。**没有「研究项目」这个种类**，新建仍是工作坊式「选蓝本」alert | **卡死**：研究项目无独立入口，与工作坊不可区分 |
| 2 | 拆研究计划 | — | 全仓无「研究项目 → 拆研究计划」的独立屏。最接近的 `研究计划参数` 是**访谈**的 `research-design.tsx`（UC-6.2 访谈大纲）和 `rec/retention-panel.tsx`（数据保留），**语义是访谈研究设计，不是 Q-12 说的研究项目计划** | **卡死**：缺屏 |
| 3 | 深度研究、收洞察 | 左栏「研究」→ `/studio/research` | **通**：上下文包（读入 24 / 丢弃 9）、丢弃清单 7 类原因过滤器。这是可达的 | 未卡 |
| 4 | 四种项目角色不适用研究项目，怎么加人 | — | `/project` 工作台**无条件**渲染 facilitator/groupLead/member/observer 四视角切换（`project-role-switcher`）。研究场景无专属角色模型。**人类 Q-12 的裁决未在 UI 落地** | **卡住**：角色模型与裁决冲突 |

**路径 2 结论**：只有第 3 步可达。研究项目作为**独立容器**在产品里不存在——它被做成了
「项目里的一个 tab 投影」（`tab-research.tsx:8` 自述「本屏是研究/访谈/问卷 Studio 在本项目下的投影」），
与 Q-12「研究项目 = 拆研究计划 + 深度研究」的独立工作流对不上。

---

## 路径 3 · 做一次用户洞察（访谈）

| 步 | 我想做什么 | 我该点哪 | 实际发生了什么 | 卡住了吗 |
|---|---|---|---|---|
| 1 | 建访谈模板 → 定报告模板 → 套模板新建 → 质性/画像推演 → 出洞察 | 左栏「访谈」→ `/studio/interview` | `/studio/interview`（访谈 Studio）可达；但访谈**模板库**在 `/itv`（H1「访谈模板·5」），**现场录音转写**在 `/rec`（H1「第3组访谈·储能采购决策」），二者都孤立。三个访谈相关屏（`/studio/interview`+`/itv`+`/rec`）分属三条路由、无互链 | **部分卡**：模板库/现场屏点不到 |
| 2 | 访谈没有引导师/组长，角色是什么 | — | `/rec` 现场屏无 facilitator/groupLead 角色切换（正确）；但 `research-design.tsx:83` 用「你不是**研究员**」做拒绝——访谈侧用「研究员/受访者」轴（README S-03 的 `?view=`），与项目侧 `?as=` 四角色**正交并存**，两套角色轴同时存在 | 未硬卡，但角色本体分裂 |
| 3 | 洞察怎么进项目、独立容器还是挂项目下 | — | `tab-research.tsx` 表明洞察是「项目下的投影」；但 `/itv` `/rec` `/studio/interview` 又像独立容器。**「用户洞察」是独立还是挂项目下，界面给了互相矛盾的答案** | 概念卡：归属不明 |

**路径 3 结论**：能出洞察的屏零散存在，但「模板→套用→现场→洞察→进项目」这条链**没有一条连续可点的路**。

---

## 路径 4 · 导入一个社区 skill 并发布

| 步 | 我想做什么 | 我该点哪 | 实际发生了什么 | 卡住了吗 |
|---|---|---|---|---|
| 1 | 后台→Skill 库→从 Codex 社区挑一个 | `/skill` 或 `/admin/skill` 的「导入契约」 | `/skill` 的「导入契约（社区 · **置灰**）」被禁用：`title="社区导入 phase-1 不实现（D-06），入口置灰"`（`skill-library.tsx:69-71`）。**社区导入本 phase 明确不做** | **卡死（设计裁决）**：路径 4 的前提在 phase-01 不可走 |
| 2 | 六道关、第4关查重阻断三个出口 | — | Skill 发布/治理链存在（`skill-promotion.tsx:53`「五态机（出口 N）」、版本与停用 `skill-versioning.tsx`）。但「六道关 + 查重阻断三出口」的**导入侧**因社区导入置灰而无从触发 | 卡：治理链在，导入端封 |
| 3 | 发布后怎么被用起来、谁能用 | `/skill` 绑定/可见性 | **通**：`skill-binding.tsx` 有「可绑定池（已启用 ∩ 可见性覆盖你）」、引导师只读浏览可绑定 skill。发布→绑定语义清晰 | 未卡 |

**路径 4 结论**：发布与绑定端做得好；「从社区导入」这条题面因 D-06 被有意推迟，**用户故事本身在 phase-01 不可走**。
这是 scope 决定不是 bug，但需人类确认题面与 phase 范围的错位。

---

## 路径 5 · 我是组员 / 观察者

| 步 | 我想做什么 | 我该点哪 | 实际发生了什么 | 卡住了吗 |
|---|---|---|---|---|
| 1 | 收到分组链接、免注册进场 | `/join` → 「获取」→ `/group` | **通**：`/join`（KICKOFF）→ `/group`（第2组·采购比选，便签/语音转便签）。链路清晰 | 未卡 |
| 2 | 我能做/不能做什么，被限是消失还是变灰 | `/group` | `/group` 是组员唯一屏，无外链（合理）。被限内容以**消失**为主：`/chat?as=observer` 下输入区 `showComposer=!readOnly`（`chat-main.tsx:35`）——**整块不渲染**，批准卡/转录卡被 filter 掉（`chat-main.tsx:30-32`）；顶部加「只读」徽标（`chat-observer-tag`）。有徽标兜底，但**输入区无任何说明地消失** | 未硬卡；「消失」为主，说明不足 |
| 3 | 观察者界面是否显著更少 | `/chat?as=observer` | **是**：观察者按钮从「批准执行/改/停止录音/分享/发送」缩到「侧栏/看洞察」，明显更少。但**左栏 rail 与线程列表全套 chrome 仍在**——观察者仍看到完整导航骨架 | 未卡；减得不够彻底 |

**路径 5 结论**：五条路径里**最顺的一条**。免注册进场、观察者收敛都做了。
唯一瑕疵：限制以「消失」实现且输入区无说明；观察者仍见全套导航外壳。

---

## 断链清单（单列）

| # | 从哪到哪断了 | 缺什么 | 严重度 |
|---|---|---|---|
| 1 | 左栏导航 → 工作坊会前/现场/会后全生命周期（`/project` 工作台） | **缺入口**：`/project` 及其 prep/live/results/results 签署屏无任何链接，只能敲 URL | **致命** |
| 2 | `/projects` 新建项目 → 套蓝本向导 | **入口通向错的地方**：按钮 = `window.alert`；真向导在 `/tpl?screen=apply` 无链接 | **致命** |
| 3 | surface hub → 现场主持大屏 | **缺屏**：`stage` surface `href:null`「尚未建」；桌面主持大屏未抽取 | **致命** |
| 4 | 研究项目 → 拆研究计划 | **缺屏 + 缺项目类型**：无研究项目种类、无研究计划编制屏 | 高 |
| 5 | surface hub → 项目内 chat/访谈/问卷/任务 | **入口通向错的地方**：全部指向 `/chat` `/studio/*` `/tasks` 等**全局**路由，丢项目作用域（`app/projects/[projectId]/page.tsx:36-41`） | 高 |
| 6 | 后台 Skill → 从社区导入 | **有意封禁**：D-06 社区导入 phase-1 置灰 | 中（scope 决定） |
| 7 | 任意 → `/asset-governance` | **404**：资产治理域在建 | 中（已知在建） |

**最致命的一处**：#1。产品被人类明确定义为「工作坊的会前/当中/之后 AI 增强」，
而这条主线的**每一段屏都做好了却全部孤立**——用户从导航自然走，一段都到不了。
这不是缺功能，是**缺接线**，且没有任何门控能发现它（门控只验单屏七态，不验跨屏可达性）。

---

## 我在真实使用中最困惑的五件事（最不可替代的产出）

1. **同一个「项目」为什么有两套完全不同的屏？** 我点导航进的是 `/projects/[id]`
   （一堆 surface 卡片的 hub），可评审在看的是 `/project`（带会前/现场/会后 tab 的工作台）。
   作为用户我根本不知道哪个才是「我的项目」——而且两个都自称项目工作台。

2. **「新建项目」点了弹个 alert 就没了。** 我要办工作坊，第一步就撞墙。
   到底怎么开始一场工作坊？我找不到任何一条能走完的路。

3. **我要主持现场，「现场大屏」是灰的（尚未建）。** 那我三小时的工作坊靠什么切环节、
   看四组进度？能切环节的屏（`/project?tab=live`）我又点不到。主持这件核心事**无处落脚**。

4. **访谈到底是不是项目？研究到底是不是项目？** `/itv` `/rec` `/studio/interview`
   像三个独立入口，`tab-research`/`tab-results` 又说它们是「项目下的投影」。
   同一个东西一会儿独立一会儿从属，我建不清心智模型。

5. **左栏导航和这十一个域几乎不相干。** 导航连的是 `/studio/interview`、`/admin/skill`、
   `/admin/agent`、`/admin/members`；而做得更好更全的是 `/itv`、`/skill`、
   `/preview/agent-runtime`、`/org-admin/preview`。我在用的是「旧的一半」，
   好的那一半藏在 URL 里。**这会让签核签的和用户用的不是同一个产品。**

---

## 「两个入口做同一件事」清单

| # | 同一件事 | 入口 A（导航可达） | 入口 B（孤岛，送审） |
|---|---|---|---|
| 1 | 打开一个项目 | `/projects/[id]`（surface hub） | `/project`（tab 工作台） |
| 2 | 会前筹备 | `/project?tab=prep` | `/tpl?screen=prep` |
| 3 | Skill 库 | `/admin/skill` | `/skill` |
| 4 | 访谈工作台 | `/studio/interview` | `/itv`（模板）＋`/rec`（现场） |
| 5 | Agent 管理 | `/admin/agent` | `/preview/agent-runtime` |
| 6 | 成员/邀请 | `/admin/members` | `/org-admin/preview` |
| 7 | 研究 | `/studio/research` | `/project?tab=research` |
| 8 | 画布 | `/projects/[id]/canvas`（现场板） | `/canvas`（模板库）※语义不同但同名易混 |
| 9 | 对话 | `/chat` | `/chat/preset`、`/chat/landing` |

**至少 7 处是真·同事两做**（第 8 语义不同、第 9 是变体）。根因是 v1 骨架屏与 v2 域屏并存
（`ui-preview/` 下 `*-v2` 目录、git 历史「查出两套 skill 界面并存」）——**两代原型没有收敛**。

---

## 我认为顺畅的地方（单列）

1. **`/chat` 现场对话**：批准卡二次确认、工具调用展开、转录卡、改派、连到 `/admin/agent`
   与项目画布——AI 四种在场方式（线程同事/画布协作者/后台 worker/项目主持人）都在，做得实。
2. **`/projects/[id]/files` 文件浏览器**：206 个 testid，版本历史、含机密徽标、级联删除、
   摄取进度阶梯条——信息密度真实，七态齐。
3. **路径 5 免注册进场**：`/join → /group` 链路干净；观察者收敛（输入区消失+只读徽标）到位。
4. **`/tpl` 蓝本设计器**：四档时长（半天/一天/两天/三天）、混合线下、双语，配置目录完成度，
   作为**单屏**是全仓最完整的之一。
5. **`/project?tab=results` 签署**：候选决策来自转写、逐条签署、发布结论危险动作二次确认——
   会后语义设计得好。
   ⚠ 这五处「顺畅」全部是**单屏内**顺畅；一旦跨屏就断。全通和全红一样可疑——这里正是「单屏都对、拼不起来」。

---

## 本审计盲区（如实说）

1. **未用浏览器真实点击/截图**：环境无 `preview_start` / 截图工具，我用 HTTP 拉 HTML +
   抽 `href`/`testid`/`button` + 回源码核对来重建可达性。**客户端 `onClick` 里的 `router.push`
   跳转**若不体现为 `<a href>` 我可能漏判——但我对九个孤岛域做了 `push(`/`href` 双向 grep（=0），
   故「无入口」结论可靠；具体交互态（hover/展开后的二级跳转）未逐一手点。
2. **七态未逐格核**：README 声称 108 格；我只抽查了 observer 态与几处 dep-failed/denied 文案，
   未复核全部 108 格截图与实屏是否一致（那是 UIUX 角色的活）。
3. **`/asset-governance` 在建**：404，未走。
4. **路径 4 的六道关/查重三出口**因社区导入置灰无法触发，只核到发布/绑定端，导入端未走通。
5. **首轮 500**（`/`、`/admin/[module]`、`/projects/[id]/*`）是 stale `.next` 缓存，
   清缓存重建后全部 200——已排除为环境问题，非产品缺陷，但**若 CI 不清缓存会误报**。

---

## 摘要（回答launching agent 的五问）

1. **五条路径各走到第几步**：路径1 只有第4b通、其余4步全卡（新建/套蓝本/会前/主持/签署无入口）；
   路径2 只第3步通；路径3 只出洞察屏可达、链不连；路径4 因社区导入置灰第1步即卡、发布绑定端通；
   路径5 全程通（最顺）。
2. **断链 7 处，最致命 = #1**：工作坊会前/现场/会后全生命周期屏全部做好却全部孤立，
   从导航一段都到不了；无门控能发现（门控只验单屏七态，不验跨屏可达）。
3. **最困惑五件事**：见上（两套项目屏 / 新建=alert / 主持台是灰的 / 访谈研究归属矛盾 /
   导航连旧屏、好屏藏 URL 里）。
4. **两个入口做同一件事：至少 7 处**（项目/会前/skill/访谈/agent/成员/研究），根因 v1 与 v2 未收敛。
5. **实际点了 34 个路由**（全部 HTTP 命中 + 源码核对；`/asset-governance` 404）；
   盲区 = 无真实浏览器截图、七态未逐格、社区导入端未走通。
