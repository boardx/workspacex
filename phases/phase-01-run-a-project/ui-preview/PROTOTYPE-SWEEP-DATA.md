# 原型全面通读 · JS 数据区（字节 16,600,000 – 17,050,600）

> 权威原型：`phases/requirements/WorkspaceX Standalone.html`（17,050,600 字节）。
> 本文覆盖 **16.6M 到文件尾**（450,600 字节 = 文件的最后 2.6%）。界面区 15.0–16.6M 由另一 agent 负责。
> **只读产物**：本轮未修改任何代码 / mock / feature_list / 契约文件，未 commit。
>
> 方法：`tail -c +16600001` 切片 → 反转义（`/`/`\n`/`\"`）→ 5,098 逻辑行。
> 所有偏移 = 切片内 `grep -bo` 命中 + 16,600,000，可用
> `tail -c +<offset-100> "WorkspaceX Standalone.html" | head -c 800` 复核。

## 0. 区间的两半

| 段 | 字节 | 反转义后行 | 内容 |
|---|---|---|---|
| A | 16,600,000 – 16,753,190 | 1 – 1,040 | **HTML 模板尾部**：项目蓝本后 5 个面板 + **六种资产共用的三步新建向导**（来源 / 配置 / 治理与发布）+ 原型导航面板 + agent 私聊抽屉 + 全屏对话 + 3 个弹层 |
| B | 16,753,190 – 17,050,600 | 1,041 – 5,098 | **JS 数据区**：112 个 `const NAME = [ / {` 声明、`Component.state` 初始值、`renderVals()` 里 ~180 个内联数据字面量 |

**A 段不在左栏导航上，点原型点不到**——它是 `<sc-if value="{{ isBpCaps }}">` / `{{ anIsMcp }}` 这类条件模板，必须先把 `adminTab` 切到对应资产、再打开向导才会渲染。本轮四条最严重的问题里有三条出自 A 段。

---

## 1. 数据结构索引（穷举）

### 1.1 A 段 · HTML 条件模板里的数据表（无变量名，按标题索引）

| 标题 | 偏移 | 它是什么 | 字段全集 | 我们有吗 | 判定 |
|---|---|---|---|---|---|
| 分组打印素材 | 16,600,000（区间起点即在此表中） | 4 件印刷品（A0 画布 / A3 组长手册 / A5 洞察卡 / A4 进组指引）+ 便签级 OCR 回填规则 | 名称·份数·版面预览 4 区·同构模板·说明 | `tpl.ts` PRINT 面板 | 已覆盖 |
| **组内能力** | **16,608,657**（表头 16,609,206；首行 16,609,949） | **7 项现场小组能力 + 每项默认开/关** | 能力 / 组员用它做什么 / 默认 | `tpl.ts:303 CAPS_PANEL.caps`（7 项） | **值不一致**（见 §2-①） |
| 产出回流 | 16,613,263 | 3 条「能力产出落到哪」 | cap → to | `tpl.ts CAPS_PANEL.reflow`（3 条逐字一致） | 已覆盖 |
| **可见性** | **16,614,368**（组长行 16,615,622；引导师行 16,616,083） | **4 个角色 × 看得到 / 看不到** | 角色 / 看得到 / 看不到 | `tpl.ts CAPS_PANEL.visibility`（**只 3 行**） | **值不一致 + 缺行**（见 §2-②） |
| **Agent 编排** | **16,616,979**（表头 16,617,589；「可主动发言」列 16,617,701） | **4 个在场 agent × 在场环节 × 可主动发言** | Agent / 职责 / 在场环节 / 可主动发言 | `agent-runtime.ts AGENT_PANEL.agents`（4 行） | **值不一致**（见 §2-③） |
| **介入尺度** | **16,621,137**（阈值 16,621,403；自动投票 16,623,022） | 触发阈值 + 3 条开关（2 开 1 关） | 阈值 / 变更史 / 3 个勾选项 | `AGENT_PANEL.intervention` | 部分（阈值与史逐字对，第 3 条呈现为文案而非开关） |
| 不可放开的硬约束 | 16,623,295 | 4 条 ×（AI 不写决策 / 不替组长提交 / 互调留痕 / 个人层永不进上下文 16,624,880） | 4 条文本 | `AGENT_PANEL.hardLimits` | 已覆盖 |
| Skill 绑定 | 16,625,255 | 11 个 skill 绑到**环节**（1 个已降级待替换） | Skill / 环节 / 作用 / 状态 | `tpl.ts` SKILL 面板 | 已覆盖 |
| 输出物 | 16,631,136 | 6 件产出 + 回流规则 + 结项检查 4 条 | 产出 / 来自 / 生成方式 / 去向 | `tpl.ts` OUT 面板 | 已覆盖 |
| 报告模板 | 16,637,112 | 客户版 18 页 6 节骨架 + 内部复盘版 6 页 + 4 条写作硬约束 | 序号 / 节名 / 谁写 | `tpl.ts` REP 面板 | 已覆盖 |
| **向导 · 第 1 步「从哪里来」** | **16,646,528** | 4 种资产来源（空白 16,646,989 / 社区导入 16,647,523 / 复制现有 16,648,056 / **从实际用法沉淀 16,648,779**）+ 3 个沉淀候选（14× / 6× / 9×）+ 社区源 4 个（16,651,285） | 名称 / 说明 / 代价标 / 候选证据 / 源类型·可用数·同步状态 | 无 | **未使用** |
| **向导 · 落地检查六道关** | **16,655,113** | Skill 导入的 6 关状态机（解析 / 安全扫描 / 依赖权限 / 查重 / 沙箱试跑 / 发布范围）+ 查重分歧三选一 + 原文改写对照 | 序号 / 关名 / 状态(通过·提示·阻断·进行中) / 明细 / 出口动作 | 无 | **未使用** |
| **向导 · 组装 Agent 四栏** | **16,665,755** | 人格与职责 / 能力（16,670,704 MCP 逐工具 + 16,672,390 可读知识层三层）/ 模型与路由（16,673,566）/ 边界与试跑（16,677,239） | 名称·头像·职责·系统提示·发言时机(16,668,617) / Skill 白名单·MCP 逐工具·知识层 / 主·备·机密三模型+路由规则+temperature+上下文预算 / 硬约束·配额上限·沙箱结果 | 部分（`admin.ts` 有 agent 列表与 MCP 服务器行） | **部分**（四栏向导本体缺） |
| **向导 · 接入 MCP 服务器** | **16,680,674** | 连接 4 字段 + 握手探测 + **14 天隔离期** + **逐工具授权 9 个·默认全关·已开 3**（16,684,167） | 名称·端点·传输(SSE/stdio/HTTP)·认证 / 工具·副作用·开放给 / 副作用工具最宽只到「需人工确认每次」 | `admin.ts MCP_SERVERS` 只有**服务器级** authScope | **未使用**（逐工具层） |
| **向导 · 接入模型** | **16,688,762** | 端点凭据 + **能力探测 12 条探针**（16,692,174，10/12 通过）+ 成本 + **三级降级阈值 组织85%/团队90%/个人95%**（16,696,279）+ 路由规则 3 条 | 供应方·模型ID·端点·凭据·数据出境 / 6 项探测结果 / 输入输出单价 / 三级阈值 / 从上往下匹配的路由表 | 无 | **未使用** |
| 向导 · 和 AI 一起搭画布模板 | 16,700,790（围栏 16,708,535） | 对话式建模板 4 轮 + 预览 6 区 + 围栏语法自动生成 + **坐标不写回** | 对话轮次 / 分区名·引导提问 / ```canvas 围栏 | `canvas.ts` 有 mermaid 围栏与双用途 | **部分**（```canvas 围栏与「坐标不写回」缺） |
| **向导 · 第 3 步治理与发布** | **16,709,463** | 六种资产**完全一样**的三件事：可见范围 3 档 / 责任与复核（**复核周期 6 / 12✓ / 24 月**，16,713,252；30 天无人复核降级）/ 发布前检查 3 条 + 发布方式（直接 vs **灰度 5 人** 16,717,473） | 可见范围·联签要求 / 负责人·复核周期·到期行为 / 检查项·禁用条件 / 发布方式 | 无 | **未使用** |
| 原型导航面板 | 16,722,000 附近（明细见 §4） | 10 个入口 + 「原型 ▸」折叠 | 入口名 | `lib/navigation.ts` | 已覆盖 |

### 1.2 B 段 · 112 个 `const` 声明（按偏移，穷举）

**图例**：判定 = `已覆盖` / `部分` / `未使用` / `值不一致` / `默认值反了` / `派生量`（原型内部渲染中间量，不是产品事实）

| # | 变量 | 偏移 | 它是什么 | 字段全集 | 我们有吗 | 判定 |
|---|---|---|---|---|---|---|
| 1 | `AN_META` | 16,753,190 | 6 种资产的新建向导标题/副标/名词 | key(agents·skills·models·mcp·canvasadmin·blueprint) → [title, sub, noun] | 无 | **未使用** |
| 2 | `AG_FILES` | 16,755,259 | Agent 仓库 5 个真实文件（AGENT.md / system.md / reflection.md / tools.json / regression.jsonl）含全文 | path, size, body | `skill.ts` 有 Skill 侧同构物；**Agent 侧无** | **未使用** |
| 3 | `AG_TREE` | 16,757,416 | Agent 文件树（4 目录 + 5 文件 + depth） | t(f/d), i, name, depth | 无 | **未使用** |
| 4 | `MODELS` | 16,757,856 | **18 台模型**（10 闭源 API + 8 开源自托管） | g, id, vendor, tags, ctx, price, st(on/test/off) | `admin.ts MODELS` 18 行 | **值不一致**（见 §2-⑤） |
| 5 | `MD_ST` | 16,761,013 | 模型三态色板 | on/test/off → [label, fg, bg, bd] | `MODEL_STATUS_LABEL` 三态一致 | 已覆盖 |
| 6 | `MD_PARAMS_API` | 16,761,226 | 闭源模型 **10 个参数默认值** | k, v, hint | 无 | **未使用**（见 §2 默认表） |
| 7 | `MD_PARAMS_OPEN` | 16,762,193 | 自托管模型 **10 个参数默认值** | k, v, hint | 无 | **未使用**（见 §2 默认表） |
| 8 | `DEF_PARTS` | 16,763,126 | 本轮参与者默认 3 项（Ava ＋3 / skill 假设拆解 / 画布假设树） | id, label | `chat.ts COMPOSER_STATUS` | 已覆盖 |
| 9 | `DEF_CTX` | 16,763,304 | 默认上下文 3 项（项目图谱 64 / 现场转录 近12分钟 / 组织大脑 仅已验证） | id, label, meta | `COMPOSER_STATUS.context = "已引用 3 项上下文"`（计数对，三项明细无） | 部分 |
| 10 | `CMD_TABS` | 16,763,642 | 命令面板两模式 × 三标签 | part[agent,skill,canvas] / ctx[brain,project,file] | 无 | **未使用** |
| 11 | `CMD_TAB_LABEL` | 16,763,747 | 6 个标签的前缀符号（@ / / #） | key → label | 无 | **未使用** |
| 12 | `CMD_ITEMS` | 16,764,046 | 命令面板 **6 组 × 4–5 条候选** | icon, iconBg, iconFg, name, desc, meta, id | 无 | **未使用** |
| 13 | `CMD_HINT` | 16,768,659 | 6 组各自的规则提示（过期条目标注 / 跨项目标「未沉淀」/ 机密强制本地 / 同消息最多挂两个 skill / 围栏写回） | key → 一句话 | 无 | **未使用** |
| 14 | `MD_CAPS` | 16,769,369 | 18 台模型的 4 项能力（视觉·工具·限流·失败回退） | id → [vision, tool, ratelimit, fallback] | 无 | **未使用** |
| 15 | `MD_CONN` | 16,771,528 | 10 台闭源模型的端点·凭据·区域合规 | id → [endpoint, cred, region] | 无 | **未使用** |
| 16 | `MD_CONN_API` | 16,773,190 | 闭源接入 4 字段（含网关 org-gateway.internal:8443） | k, v | 无 | **未使用** |
| 17 | `MD_CONN_OPEN` | 16,773,498 | 自托管 4 字段（vLLM 0.9 / 2×A100 / 权重 SHA 校验） | k, v | 无 | **未使用** |
| 18 | `MD_TESTS` | 16,773,802 | 模型启用前 **五项测试** | k, r(PASS), v | 无 | **未使用** |
| 19 | `SK_FILES` | 16,774,166 | Skill 仓库 5 个真实文件含全文 | path, size, body | `skill.ts` | 已覆盖 |
| 20 | `SK_TREE` | 16,776,606 | Skill 文件树 | t, i, name, depth | `skill.ts` | 已覆盖 |
| 21 | `AD_META` | 16,777,476 | 后台 **9 个标签**的标题/副标/CTA（含计数：14 agent / 86 skill / 18 模型 / 7 蓝本 / 22 画布模板 / 6 MCP / 48 成员） | key → [title, sub, cta] | `admin.ts` 导航 + 各屏 | 已覆盖 |
| 22 | `TH_META` | 16,778,559 | 三条线程的标题/副标/团队数 | a/b/c → [title, sub, team] | 无（我们只画单线程） | **未使用** |
| 23 | `BR_META` | 16,778,980 | 大脑 **7 个标签** | key → [title, sub] | `brain.ts` | 已覆盖 |
| 24 | `ST_META` | 16,780,094 | 4 个 Studio（itv/res/svy/proto）的元信息 | ws, owner, name, desc, newLabel, stage, task, slot, ctx, goal, expect | `studio.ts` / `itv.ts` / `research.ts` / `survey.ts` | 部分（`goal`/`expect`/`slot` 三字段无对应） |
| 25 | `ST_MODE` | 16,782,357 | 产出三态（草稿 / 实时关联 / **固定快照**默认） | key → [label, hint] | `canvas.ts` I-11 固定快照 | 已覆盖 |
| 26 | `WS_GRP` | 16,782,672 | 项目 **21 个 wsPhase 分成 4 组**（res 6 / prep 4 / live 8 / after 5） | res/prep/live/after → phase[] | `project.ts` 四段 | 部分（阶段名集合不同，见 §3-6） |
| 27 | `P_META` | 16,784,092 | 原型 Studio **六阶段** | ctx/dir/build/cmp/audit/ship → label | `studio.ts PROTOTYPE_STEPS`（**五步且缺版本比较**） | **值不一致**（见 §2-④） |
| 28 | `PD_META` | 16,784,665 | 原型预览三档设备（1280/768/375） | desk/tab/mob → label | 无 | **未使用** |
| 29 | `PS_META` | 16,784,920 | 原型四态（默认/Loading/Empty/Error） | 4 key → label | 无 | **未使用** |
| 30 | `EX_CTA` | 16,785,207 | 原型四种导出（单文件 HTML / 12 张 PNG / 说明 PDF / 交接包 ZIP） | html/png/pdf/zip → label | 无 | **未使用** |
| 31 | `IVG_MAP` | 16,786,120 | 访谈 **13 个 view → 5 组**（design/people/virtual/rec/insight） | view → group | `itv.ts ITV_SCREENS` 8 屏 | **部分**（见 §3-7） |
| 32 | `TK_META` | 16,786,793 | 任务 **7 个标签** | key → [title, sub] | `tasks.ts` | 已覆盖 |
| 33 | `BP_SECS` | 16,788,261 | 蓝本设计器 **16 个面板 key** | 字符串数组 | `tpl.ts` 16 项 | 已覆盖 |
| 34 | `CV_DOCS` | 16,788,713 | 画布 4 篇文档 | flow/persona/bmc/journey | `canvas.ts` | 已覆盖 |
| 35 | `SYNCED` | 16,788,777 | 同步态①「已同步」 | [label, fg, bg, bd] | `projects.ts` 有「已同步」 | 部分 |
| 36 | `AHEAD` | 16,788,849 | 同步态②「画布领先 · 未写回」 | 同上 | 无 | **未使用** |
| 37 | `BEHIND` | 16,788,936 | 同步态③「文档领先 · 需重新渲染」 | 同上 | 无 | **未使用** |
| 38 | `CONFLICT` | 16,789,030 | 同步态④「冲突 · 两侧同时改」 | 同上 | 无 | **未使用**（见 §3-8） |
| 39 | `CV_META` | 16,789,120 | 4 篇画布各自标题/围栏语法/**同步态** | title, syntax, sync | 无（我们只有 claims 三态，是另一个维度） | **未使用** |
| 40 | `TPL_ZONES` | 16,789,587 | 用户画像模板 **6 分区** + 便签上限 + 必填 + 填充率 | name, prompt, max, required, fill | `canvas.ts TEMPLATE_ZONES` 6 区 | **值不一致**（见 §2-⑥） |
| 41 | `TPL_SPECS` | 16,790,773 | 三张画布模板的完整规格（persona / bmc 12 格 / journey 5 阶段） | key, title, fields, cols, rows, zones, notes | `canvas.ts` 有模板库行，无 fields/cols/rows/notes | **部分** |
| 42 | `FLOW_NODES` | 16,793,047 | 假设树 6 节点（含 diamond 判定节点） | id, label, shape, x, y, w, h | 无（我们无假设树画布数据） | **未使用** |
| 43 | `FLOW_EDGES` | 16,793,687 | 假设树 5 条边（标签「致命」「是」） | from, to, label | 无 | **未使用** |
| 44 | `AG_DATA` | 16,793,946 | **两天档议程：4 半场 × 14 环节**，每环节 8 字段 | id, day, win, theme, items[id,name,out,dur,fac,grp,cv,sk,tag] | `project.ts` 有议程环节但无 fac/grp/cv/sk 四列 | **部分**（见 §3-3） |
| 45 | `AG_TAG` | 16,797,251 | 环节 5 种标签色（需人签字 / 隔夜任务 / AI 主讲 / 分组 / 投票） | label → [fg, bd, bg] | 无 | **未使用** |
| 46 | `WAG_DATA` | 16,797,984 | 半天档议程：2 半场 × 7 环节（可拖拽重排的那份） | id, day, start, theme, items[id,name,dur,tag] | `project.ts` 提到「假设风暴 → 假设树画布」 | 部分 |
| 47 | `lines`(persona md) | 16,799,910 | 用户画像导出的 Markdown 骨架 | 字符串数组 | `canvas.ts CANVAS_MARKDOWN_SYNC.mermaid` | 部分（结构不同） |
| 48 | `nodes` | 16,802,625 | fabric 节点索引表 | — | — | 派生量 |
| 49 | `NB` | 16,802,662 | 假设树节点两种告警色（致命 / 待验证） | 致命/待验证 → [bg, fg] | 无 | **未使用** |
| 50 | `parts` | 16,803,600 | fabric Group 子元素 | — | — | 派生量 |
| 51 | `cursor` | 16,814,449 | 便签排布游标 | — | — | 派生量 |
| 52 | `base` | 16,815,189 | fabric 形状基样式 | — | — | 派生量 |
| 53 | `rec` | 16,816,940 | 连线记录 | — | — | 派生量 |
| 54 | `p1` | 16,817,350 | 连线端点 | — | — | 派生量 |
| 55 | `lines` | 16,825,738 | mermaid flowchart 导出行 | — | 无 | **未使用**（导出格式） |
| 56 | `lines` | 16,826,492 | ```canvas 围栏导出行 | — | 无 | **未使用**（见 §3-9） |
| 57 | `fields` | 16,826,555 | 围栏 fields IR | — | 无 | **未使用** |
| 58 | `sections` | 16,826,783 | 围栏 sections IR | — | 无 | **未使用** |
| 59 | `s`(renderVals) | 16,838,089 | state ＋ props 合并 | screen/transcript/wsPhase 三处 props 兜底 | — | 派生量（**默认值来源**，见 §2 表） |
| 60 | `GRP_BASE` | 16,838,400 | **4 个小组**：组长·成员·状态·场景 ＋ **访谈对象 targets** ＋ **counts 8 类计数** | key,no,name,scene,sceneFull,lead,members,state,tone,targets[name,dept,contact,bg,mode],counts[svy,ux,chat,itv,dr,canvas,vote,ev] | `tpl.ts`/`project.ts` 有 4 组与访谈对象 | **部分**（counts 8 类 与 tone 三档缺） |
| 61 | `TONE` | 16,841,904 | 小组三态色（ok/warn/bad） | tone → {stFg,stBd,stBg} | 无 | **未使用** |
| 62 | `curGrp` | 16,842,451 | 当前组 | — | — | 派生量 |
| 63 | `LIVE_FNS` | 16,842,514 | 现场小组左栏 **5 项能力**（chat/itv/dr/svy/kg） | key, ab, name, meta, hot, mod, newLabel | `project.ts` 组内功能 | **部分**（与 §2-① 的 7 项能力表不同集，原型自身也不一致） |
| 64 | `FN_BODY` | 16,843,398 | **7 项能力各自的示例会话 + 状态 + chips + actions**（chat/itv/dr/ux/canvas/vote/ev） | desc,status,chips,ph,lines[t,who,whoFg,text],actions[label,bg,fg,bd] | 无 | **未使用**（且原型自己也只用了 `desc/status/lines`，`chips`/`actions` 被 `rawFn` 清空——原型内部死数据） |
| 65 | `POOL_ALL` | 16,849,704 | 11 人待分配人员池 | 姓名数组 | `tpl.ts` 有分组名单 | 部分 |
| 66 | `usedNames` | 16,850,083 | 已占用姓名 | — | — | 派生量 |
| 67 | `MODES` | 16,850,483 | 访谈对象三态循环（待预约 / 已完成 / 观察） | 字符串数组 | `tpl.ts` 有「待预约」 | 部分 |
| 68 | `FN_ST` | 16,854,090 | 5 项能力的状态文案 | key → label | 无 | **未使用** |
| 69 | `rawFn` | 16,854,230 | 当前能力（**把 FN_BODY 的 chips/actions 清空**） | — | — | 派生量 |
| 70 | `curFn` | 16,854,455 | 按角色过滤后的能力（`hidePrivate` 抹掉含「私聊」的 chip/action，`obs` 抹掉全部 action） | — | `agent-runtime.ts` 观察者只读 | 部分 |
| 71 | `titles` | 16,855,628 | 4 个 nav 占位标题 | nav → label | `navigation.ts` | 已覆盖 |
| 72 | `DEF`(访谈四段) | 16,860,406 | 新建访谈默认 **4 段提纲**（5/20/20/10 分钟 + goal + 2 条开场问法） | t, m, goal, qs[] | `itv.ts INTERVIEW_TEMPLATES` | 部分（分段名与分钟一致性未逐条核；`goal` 字段无） |
| 73 | `fields`(idScene) | 16,861,570 | 访谈场景三要素（谁 / 在什么处境 / 要做什么决定）含默认值 | k, label, ph, def | `itv.ts` 上下文三要素 | 已覆盖 |
| 74 | `DEF_SEGS` | 16,865,526 | 访谈**模板**默认 4 段（无 goal，与 #72 是两份） | t, m | `itv.ts` 模板编辑器 | 部分 |
| 75 | `ALL_TAGS` | 16,866,034 | 模板三标签（客户 / 内部 / 高优先级） | 数组 | `itv.ts`/`tpl.ts` | 已覆盖 |
| 76 | `COLS` | 16,882,271 | 项目看板 **4 列**（待办/进行中/待复核/已完成） | k, name, dot | `project.ts` | 已覆盖 |
| 77 | `BASE`(看板卡) | 16,882,575 | 8 张看板卡 | id,col,title,who,ab,src,due,overdue | `project.ts` | 部分（卡片语料不同） |
| 78 | `FILT` | 16,884,399 | 看板 4 个筛选（全部/我的/现场/逾期与今天） | k, label | 无 | **未使用** |
| 79 | `SRC`(看板色) | 16,884,797 | **7 种任务来源**色（决策树/现场/会前任务/报告缺料/研究/转写/手动） | label → [fg,bd,bg] | `project.ts` 有「会前任务」 | **部分**（7 类来源枚举缺） |
| 80 | `SRC`(报告取材) | 16,887,331 | 报告 **7 类取材源**（对话20/访谈9场/研究4份/画布58便签/投票3轮/转写4路/决策3条），默认选中 5 | 字符串数组 | 无 | **未使用** |
| 81 | `TONE_L` | 16,887,652 | 报告三种口径（客户董事会3页 / 内部团队6页 / 一页纸），默认 board | key → label | `tpl.ts` 有「客户董事会」 | 部分 |
| 82 | `BASE`(报告列表) | 16,889,830 | 3 份报告（v3草稿/已完成/已发布）+ 增删改名 | id,title,aud,st,time,meta | 无 | **未使用** |
| 83 | `ST`(报告态) | 16,890,724 | 报告 4 态色（v3草稿/已完成/已发布/空白） | label → [fg,bd,bg] | 无 | **未使用** |
| 84 | `THREADS` | 16,896,990 | **研究阶段 6 条对话线程**（主线/研究/访谈/问卷/合规/测算，含 6 种头像色与 6 种状态） | key,ab,name,time,status,desc,last,lines,actions,ph | `chat.ts` 有线程但语料/条数不同 | **部分** |
| 85 | `AB` | 16,901,830 | 6 种线程头像色（CH/DR/IT/SV/WD/LG） | ab → [bg,fg] | 无 | **未使用** |
| 86 | `KIND_L` | 16,904,064 | 研究四类型（桌面/竞品/监管/客户侧核实），默认 desk | key → label | 无 | **未使用** |
| 87 | `DEPTH_L` | 16,904,190 | **研究三深度**（快速8分3路 / 标准20分6路 / 深挖45分12路），默认 std | key → label | 无 | **未使用** |
| 88 | `G` | 16,919,049 | 5 个作用域 key（g1–g4 + res） | 数组 | — | 派生量 |
| 89 | `BASE`(分组对话) | 16,919,233 | **4 模块 × 5 作用域的对话/访谈/研究/问卷卡片矩阵**（约 30 张卡） | id,title,vis,who,time,last,skills,ctx | `chat.ts` 有分组对话 | **部分** |
| 90 | `VIS` | 16,925,519 | **15 种可见性/状态标签色**（团队可见/私有/组员私有/本组共享/组长/需介入/已完成/已就绪/已回收/转写中/运行中/回收中/待预约/待判定/草稿） | label → [fg,bd,bg] | `chat.ts` 有部分 | **部分**（枚举不全） |
| 91 | `NEW_ITEM` | 16,926,454 | 四模块新建项的默认值（新对话默认「本组共享」/ 新访谈默认「待预约」…） | fn → {title,vis,last,skills,ctx} | 无 | **未使用**（含默认值） |
| 92 | `base` | 16,928,442 | 新建项在 res 作用域的覆写（vis→团队可见） | — | — | 派生量 |
| 93 | `all` | 16,929,166 | 扁平化全部卡片 | — | — | 派生量 |
| 94 | **`GK`** | **16,931,609** | **4 个小组各自的「本组图谱」**：本组场景 / 已确认结论 / 待决岔口 / 建议下一步 / 2 条 facts / 2 条 nexts | scene,done,doneMeta,fork,forkSub,next,facts[[text,src]],nexts[[text,action]] | `canvas.ts GROUP_TREE`，**注释写「[未探明] 档案只探明全场，此为原创设计」** | **未使用 + 注释事实错误**（见 §3-1） |
| 95 | `defs`(gk 四节点) | 16,934,283 | 本组小树 4 节点骨架（root/done/fork/ghost） | t,s,st,m,k | 无 | **未使用** |
| 96 | `K`(gk 色) | 16,934,756 | 本组小树 4 种节点样式 | kind → 9 个样式字段 | 无 | **未使用** |
| 97 | `KIND_META` | 16,936,378 | 新增节点三类（已定/待决/依据） | kind → [label, styleKey] | 无 | **未使用** |
| 98 | `SRC`(gk 依据) | 16,937,479 | 节点依据 **5 个来源**（本组对话/访谈转写/研究报告/问卷结果/现场观察），默认选「本组对话」 | 数组 | 无 | **未使用**（含默认值） |
| 99 | `ST`(9 方法环节) | 16,947,698 | **推演流水线 9 个方法环节**（PESTEL/SWOT/画像/旅程/HMW/方案/MVP/UI原型/PRD） | 数组 | `canvas.ts METHOD_STAGES` 9 项**逐字一致** | 已覆盖 |
| 100 | `OUT` | 16,947,863 | 9 个环节各自的产出物 | 数组 | 无 | **未使用** |
| 101 | `LANES` | 16,948,119 | **推演流水线 4 场景 × 9 格**（含 subs 子步骤） | name, subs, row[9 码] | `canvas.ts PIPELINE_SCENARIOS/GRID` | **值不一致**（见 §2-⑦） |
| 102 | `META`(格态) | 16,948,697 | **5 种格状态**：d 已完成 / c 正在讨论 / **b 证据冲突** / **s 停滞8分钟** / n 图谱建议 | code → 9 个样式字段 + note | `canvas.ts MethodCellStatus` 把 b 与 s **合并成「冲突停滞」** | **值不一致** |
| 103 | `nodes` | 16,949,990 | 流水线节点渲染 | — | — | 派生量 |
| 104 | `paths` | 16,950,017 | 流水线连线 | — | — | 派生量 |
| 105 | `cross` | 16,951,250 | **2 条跨场景互证连线**（紫虚线） | [[laneA,colA,laneB,colB]] | 无 | **未使用** |
| 106 | **`ND`** | **16,952,387** | **决策树 9 节点**（主决策 + 3 路径 + 2 岔口 + 商业模式 + 2 个图谱推演节点） | id,lv,r,title,sub,st,meta,kind(cur/lead/open/conf/dead/ghost) | 无 | **未使用**（见 §3-2） |
| 107 | `EG` | 16,954,137 | 决策树 8 条边（4 种线型 d/s/x/g） | [from,to,label,style] | 无 | **未使用** |
| 108 | `K`(树色) | 16,954,600 | 决策树 **6 种节点态**样式 | kind → 8 个样式字段 | 无 | **未使用** |
| 109 | `ES` | 16,955,877 | 决策树 4 种边样式 | code → [stroke,w,dash] | 无 | **未使用** |
| 110 | `pos` | 16,956,043 | 节点坐标索引 | — | — | 派生量 |
| 111 | `paths` | 16,956,578 | 树连线 | — | — | 派生量 |
| 112 | **`AG`** | **17,044,825** | **6 个可单独私聊的 agent**（ava/atlas/scout/ledger/warden/echo） | key,ab,abBg,abFg,name,desc,st,stFg,model,skills,lines,quick,ph | `agent-runtime.ts AGENT_CHAT_ROSTER` 6 人**逐字一致** | **已覆盖**（上一轮已修） |

### 1.3 B 段 · `renderVals()` 里的内联数据字面量（穷举，按偏移）

| 名 | 偏移 | 它是什么 | 我们有吗 | 判定 |
|---|---|---|---|---|
| `idStats` | 16,863,321 | 访谈计划 4 项统计（22 场 / 1–3 人 / 60 分 / **保留 180 天·禁止用于训练**） | `rec.ts` 有留存与「禁止用于训练」 | 部分 |
| `liveTs` | 16,864,128 | 现场转录 6 行（含正在识别的尾行） | `chat.ts TRANSCRIPT_ENTRIES`（4 行，语料不同） | 部分 |
| `repChat` | 16,892,000 附近 | 报告对话 4 轮 | 无 | **未使用** |
| `repQuick` | 16,892,200 附近 | 报告 4 个快捷（一页纸 / 内部版 / 客户版 / 补风险节） | 无 | **未使用** |
| `repSections` | 16,892,384 | **报告 4 节**（结论 / 为什么是交付确定性 / 并网时长真实口径 / 未解问题）含 paras + srcs + 重写按钮 | 无 | **未使用** |
| `repGap` | 16,894,566 | 缺料说明（缺 2 块 → 补齐后可升 v4） | 无 | **未使用** |
| `kbCols` | 16,886,377 | 看板列渲染（含跨列移动回写来源） | `project.ts` | 部分 |
| `dashFuncs` / `dashAgents` / `dashSkills` / `dashSkillNote` | 16,909,534 起 | **组长 7 项 / 组员 5 项**功能；组长 4 / 组员 3 个 agent；组长 8 / 组员 6 个 skill；两套说明 | `entry.ts` 有「向引导师举手」；**申请延时 / GapFinder / Returner / 口语化改写 全无** | **部分** |
| `roleThreads` / `roleVisNote` / `roleCtx` / `roleMsgs` / `roleQuick` / `roleComposerHint` | 16,915,182 起 | 组长/组员两套：3 条线程 + 可见性说明 + 5 项上下文（组员含「逐字稿原文 · 未授权」灰条）+ 3 条示例消息（RETURNER / GAPFINDER）+ 4 个快捷 + 输入提示 | 无 | **未使用** |
| `signGroups` | 16,946,817 | 签到：每组 n/m + 每人「已到/未到/组长」标 | `org-admin.ts`/`tpl.ts` 有「未到」 | 部分 |
| `kgTabs` | 16,951,703 | 图谱 **三视图**：决策树（默认）/ 推演流水线 / 事实关系 | `canvas.ts` 只有后两个 | **部分**（见 §3-2） |
| `kgTreeLegend` | 16,935,000 附近 / 3935 行 | 决策树图例 4 项 | 无 | **未使用** |
| `trForks` | 16,957,767 | **3 个待决岔口**，每个带「为什么重要」+ 2–3 个选项 + 证据数 | 无 | **未使用** |
| `trMoves` | 16,958,834 | **5 个「下一步可做的动作」**，每个带目标/成本/派给谁/影响（关掉4节点·补2证据…）/挂哪个节点 | 无 | **未使用** |
| `kgScenes` | 16,960,839 | 场景侧栏（全部场景 **硬编码 39%** + 4 场景各自 pct = d 格数/9） | `canvas.ts` 注释写「进度 14/36 格」 | **值不一致**（见 §2-⑦） |
| `kgLegend` | 16,977,000 附近 / 3977 行 | 流水线图例 4 项 | 无 | **未使用** |
| `kgEdges` | 16,962,507 | **事实关系 5 条边**（from / rel / to / 状态 / 来源链） | `canvas.ts FACT_NODES` 是节点表不是边表 | **部分** |
| `kgByGroup` | 16,963,619 | 按组统计（假设 n / 证据 m / 节点数） | 无 | **未使用** |
| `liveCtx` | 16,964,000 附近 | 主持台 7 项上下文（按角色过滤：非 fac 抹掉「逐字稿」「第 3 组」，obs 再抹「转录」） | `project.ts` 部分 | 部分 |
| `liveMsgs` | 16,964,934 | 主持台 **4 条 AI 插话**（FACILITATOR 超时建议 / GROUPWATCH 组况告警 / 人类问 / FACILITATOR 收敛判断）+ 每条 2–3 个 action；**非 fac 角色 action 全清空** | 无 | **未使用** |
| `liveVotes` | 16,967,699 | 3 条投票结果（7/5/4 票） | `project.ts` 有「愿意接受排他条款」 | 部分 |
| `liveSkills` | 16,968,051 | 主持台 6 个 skill | 无 | **未使用** |
| `liveAgents` | 16,968,612 | 主持台 4 个 agent（FC/GW/SC/EC）+ 各自 why + skills | `agent-runtime.ts` 有主持台 | 部分 |
| `newChatPresets` | 16,971,031 | 「从预设开始」4 项 | `chat.ts NEW_CHAT_MODAL.startFrom` | 已覆盖 |
| `newChatSkills` | 16,971,400 附近 | 预设技能 5 项（3 亮 2 灰） | `chat.ts presetSkills`（3 项） | 部分 |
| `newChatVis` | 16,836,811 | 三种角色下的可见性说明（预设 / 组员 / 组长 / 引导师） | `chat.ts NEW_CHAT_MODAL.visNote` | 已覆盖 |
| `chatFilters` | 16,972,000 附近 | 对话筛选 6 项（全部14 / 四组 / 我的1） | 无 | **未使用** |
| `facChats` | 16,972,742 | 引导师视角 6 条跨组对话（含 vis 五种） | `chat.ts` 部分 | 部分 |
| **`chatPresets`** | **16,974,576** | **4 条预设**（name/note/skills/**to**/**used**） | `chat.ts PRESETS` 4 条**逐字一致** | **已覆盖**（v2 已修） |
| `presetChips` | 16,975,296 | 预设名 chips 4 项 | `chat.ts` | 已覆盖 |
| `leadOwnChats` | 16,975,600 附近 | 组长自己的 2 条对话 | 无 | **未使用** |
| `leadMemberChats` | 16,976,000 附近 | 组长能看的 5 条组员对话 | 无 | **未使用** |
| `myChats` | 16,976,474 | 组员自己的 2 条对话 | 无 | **未使用** |
| `memberPresets` | 16,976,943 | 组员「点开即用」3 项 | 无 | **未使用** |
| `inviteRole/inviteCode/inviteRoles/inviteTtls/inviteGroups` | 16,977,383 / 16,977,896 | **邀请 4 种身份**（组员默认 / 组长 / 观察者只读 / **协同引导师**）× **3 种有效期**（24h 默认 / 7天 / 一次性）+ 每组入口 URL + 重置全部链接 | `identity.ts` 有「协同引导师」「一次性」 | **部分**（默认值与四×三矩阵缺，见 §2 表） |
| `wsTheme` / `wsBgText` | 16,978,300 附近 | 主题与背景默认文案 + AI 重写按钮 | `project.ts` 定题 | 部分 |
| `grpN` / `grpInc` / `grpDec` | 16,979,000 附近 | 分组数默认 **4**，范围 **2–8** | 无（未见上下限） | **部分** |
| `liveTabs` / `stageNav` / `stageTabs` | 16,980,000 – 16,983,000 | 主持台 tab（全场 + 4 组）/ 看板·知识图谱·分组签到 / 四组实时进展·推演模板 | `project.ts` 部分 | 部分 |
| `grpSteps` | 16,984,846 | 小组 **5 条提交检查**（3 done / 2 未） | 无 | **未使用** |
| `grpNotes` | 16,985,200 附近 | 小组 2 条提示 | 无 | **未使用** |
| `grpOutputs` | 16,985,727 | 小组 **4 项产出**（画布 v3 / 证据包 / 访谈摘录 / 汇报稿草稿） | 无 | **未使用** |
| `mdGroups` / `mdConn` / `mdParams` / `mdCaps` / `mdTests` / `mdUsers` | 16,993,000 – 16,996,000 | 模型详情屏全部 6 块（含「五项全通过后才出现在 agent/skill 的模型选择里」的门控语义） | 无 | **未使用** |
| `nrKinds`/`nrDepths`/`nrSources`/`nrOuts`/`nrGrpOpts`/`nrNodeOpts`/`nrPreview` | 16,904,900 – 16,906,400 | 新建深度研究弹层全部字段 + 预览句 + **4 个决策节点选项（含"不挂节点，只存进洞察库"）** | 无 | **未使用** |
| `gnKinds`/`gnParents`/`gnSrcs`/`gnHint` | 16,937,670 起 | 「往本组图谱加节点」弹层（16,748,233）：三类型 + 挂到哪 + 5 来源 + 「结论类没挂依据会被标待确认」 | 无 | **未使用** |
| `nvStep*`/`nvKind*`/`idCtx`/`idScene`/`idSegs` | 16,860,000 – 16,864,000 | 新建访谈四步向导 + **三种对象（真人默认 / 数字专家 / 用户画像）** | `itv.ts` 三步向导 | **部分**（四步 vs 三步；三种对象类型缺） |
| `fsSideTabs`/`fsSources`/`chatFull.tools`/`chatFull.runs`/`chatFull.outs` | 16,940,000 – 16,943,000 | 全屏对话右栏（来源/执行/产出三档 + 3 条 tool-call + 3 条 run + 3 项产出） | `chat.ts RIGHT_TABS` 五档 | 部分 |
| `isTkToday` 等 | 16,959,000 附近 | 任务 4 个标签合并成一屏 | `tasks.ts` | 已覆盖 |

**统计**：112 个 `const` + ~55 个内联字面量 + 18 张 A 段 HTML 表 = **185 个数据结构**。
判定分布：**已覆盖 34 · 部分 51 · 未使用 76 · 值不一致 8 · 默认值反了 3 · 派生量 13**。

---

## 2. 默认值对照表（本区最易错的一类）

> 下面每一行都是「屏在、控件在、只有默认值不同」——**看截图看不出来**。

### ① 组内能力 7 项默认开/关（原型 16,609,949 起 7 行）

| 能力 | 原型默认（偏移） | 我们的默认 | 一致？ |
|---|---|---|---|
| 与 AI 的对话 | **开 · 必留**（16,609,949） | `tpl.ts:308` `"开 · 必留"` | ✅ |
| 用户访谈 | 开（16,610,4xx） | `tpl.ts:309` `"开"` | ✅ |
| 深度研究 | 开（16,610,9xx） | `tpl.ts:310` `"开"` | ✅ |
| 用户研究 | 关 · 两天档才开（16,611,3xx） | `tpl.ts:311` `"关 · 两天档才开"` | ✅ |
| **画布便签** | **开 · 必留**（16,611,824） | `tpl.ts:312` `"开"` | ❌ **丢了「必留」** |
| 组内投票 | 开（16,612,3xx） | `tpl.ts:313` `"开"` | ✅ |
| 证据检索 | 开（16,612,8xx） | `tpl.ts:314` `"开"` | ✅ |

### ② 项目级 AI 权限三开关

| 参数 | 原型默认（偏移） | 我们的默认（文件:行） | 一致？ |
|---|---|---|---|
| Facilitator 主动提议收敛 | **开**（Agent 编排表「✓ 默认开」16,617,701；`aiProactive: p.aiProactive ?? true` 17,011,894） | `agent-runtime.ts:299` `defaultOn: false` | ❌ **反了** |
| 实时转录与语音转便签 | **开**（`state = { … recording: true … }` 16,798,890 起；Echo 全程在场 16,617,9xx） | `agent-runtime.ts:300` `defaultOn: false` | ❌ **反了** |
| AI 直接在小组画布落笔 | **开**（介入尺度「✓ 画布上可直接落笔（改动带 AI 署名）」16,621,137 区） | `agent-runtime.ts:301` `defaultOn: false` | ❌ **反了** |
| AI 自动发起投票 | **关**（同区，未勾选，16,623,022） | 我们无此开关（第 3 条被换成「画布落笔」） | ⚠ 缺项 |

> 我们的注释写 `O-23：默认全关，收紧优先`。**若 O-23 确实要覆盖原型，需要在签核里显式记录「与原型相反且为什么」**；现状是注释里既引用原型偏移、又给出相反默认值，两处事实自相矛盾。

### ③ 其余带默认值的参数（原型 → 我们）

| 参数 | 原型默认（偏移） | 我们的默认 | 一致？ |
|---|---|---|---|
| 产出关联模式 | **固定快照** snap（`stMode: "snap"` 16,798,890 区；`ST_MODE` 16,782,357） | `canvas.ts` I-11 固定快照 | ✅ |
| 项目角色 | **引导师** fac（`roleNow = s.role \|\| "fac"` 16,842,158） | `identity.ts` 默认引导师 | ✅ |
| 项目阶段 | **会后** over（`p.workshopPhase \|\| "over"` 16,838,089 区） | `project.ts` 默认「现场」 | ❌ 不一致 |
| 转录侧栏 | **开**（`p.transcriptOpen ?? true`，同区） | `chat.ts` 默认开 | ✅ |
| 侧栏自动跟随 | **跟随开**（`sideFollow === false` 才固定，16,772,000 区文案 16,738,988） | 无此开关 | ⚠ 缺 |
| 推理过程折叠 | **收起** `reasoning: false` | `chat.ts` 收起 | ✅ |
| 分组数 | **4**，可调 **2–8**（`grpN` 16,979,000 区） | `tpl.ts` 4 组，**无上下限** | ⚠ 部分 |
| 画布模板列数 | **3 列**（`tplCols \|\| 3`） | `canvas.ts:230` `"3 列"` | ✅ |
| 画布模板必填区 | **3 / 6 区必填**（用户描述·目标和需求·痛点和挑战，16,789,587） | `canvas.ts` 2 / 6 必填（痛点·目标） | ❌ 少一区（见 §2-⑥） |
| 便签上限 | 痛点 **10** / 行为偏好 **8** / 动机 **6**（同上） | 8 / 6 / 5 | ❌ **三处都低** |
| 邀请身份 | **组员**（`inviteRole \|\| "member"` 16,977,383） | 无默认 | ⚠ 缺 |
| 邀请链接有效期 | **24 小时**（`inviteTtl \|\| "24h"` 16,977,896） | `identity.ts` 有三档但无默认 | ⚠ 部分 |
| 研究类型 | **桌面研究** desk（`nrKind \|\| "desk"` 16,904,064） | 无 | ❌ 缺 |
| 研究深度 | **标准 20 分 6 路** std（`nrDepth \|\| "std"` 16,904,190） | 无 | ❌ 缺 |
| 研究来源 | **官方与监管 ＋ 行业报告**（5 选 2，16,905,xxx） | 无 | ❌ 缺 |
| 研究交付形式 | **研究简报**（4 选 1） | 无 | ❌ 缺 |
| 图谱节点类型 | **结论 · 已定** done（`gnKind \|\| "done"` 16,937,670） | 无 | ❌ 缺 |
| 图谱节点依据来源 | **本组对话**（5 选 1，16,937,479） | 无 | ❌ 缺 |
| 新建对话可见性 | **本组共享**（`NEW_ITEM.chat.vis` 16,926,454） | 无 | ❌ 缺 |
| 新建访谈对象状态 | **待预约** | `tpl.ts` 有「待预约」 | ✅ |
| 报告取材源 | **7 选 5**（去掉「投票 3 轮」「转写 4 路」，16,887,331） | 无 | ❌ 缺 |
| 报告口径 | **客户董事会 · 结论先行 · 3 页** board（16,887,652） | 无默认 | ⚠ 部分 |
| 看板筛选 | **全部** all（16,884,399） | 无筛选器 | ❌ 缺 |
| 图谱视图 | **决策树** tree（`kgView \|\| "tree"` 16,951,703） | 我们无决策树，默认落「事实关系」 | ❌ **默认视图不存在** |
| 图谱场景过滤 | **全部场景** all（16,960,839，pct 硬编码 39%） | `canvas.ts` 注释 14/36 = 38.9% | ⚠ 见 §2-⑦ |
| 原型阶段 | **阶段 3 页面与调整** build（`pStage \|\| "build"` 16,784,092） | `studio.ts` 无阶段默认 | ⚠ 部分 |
| 原型预览设备 | **1280 桌面** desk（16,784,665） | 无 | ❌ 缺 |
| 原型预览状态 | **默认状态** default（16,784,920） | 无 | ❌ 缺 |
| 原型导出格式 | **单文件 HTML** html（16,785,207） | 无 | ❌ 缺 |
| 新建原型保真度 | **可点击原型**（三档中档） | 无 | ❌ 缺 |
| 新建原型类型/视图/配色 | app / desk / p1 | 无 | ❌ 缺 |
| 访谈对象类型 | **真人** real（`nvKind \|\| "real"`，另两档：数字专家 / 用户画像） | `itv.ts` 有真人/数字人混排，无「用户画像」第三档默认 | ⚠ 部分 |
| 访谈视图 | **现场记录** live（`itvView \|\| "live"` 16,786,120） | `itv.ts` 默认 library | ❌ 不一致 |
| Studio | **原型** proto（`studio: "proto"` 16,798,890 区） | 无全局默认 | ⚠ 缺 |
| 后台标签 | **数据总览** dashboard | `admin.ts` dashboard | ✅ |
| 大脑标签 | **三层关系总览** tiers | `brain.ts` tiers | ✅ |
| 任务标签 | **我的今天** today | `tasks.ts` today | ✅ |
| 任务抽屉标签 | **执行** run | 无 | ⚠ 缺 |
| 成员标签 | **配额** quota | `admin.ts` | ✅ |
| 画布文档 | **假设树** flow | `canvas.ts` | ✅ |
| 右栏标签 | **原始转录** raw | `chat.ts RIGHT_TABS[0]` | ✅ |
| 问卷标签 | **题目** q | `survey.ts` | ✅ |
| 画布模板视图 | **卡片** card | `canvas.ts` | ✅ |
| 围栏导出格式 | **canvas**（另两档 md / mermaid） | `canvas.ts` 只有 mermaid | ⚠ 部分 |
| 推演链视图 | **图** graph（另一档 tpl） | `brain.ts` | ✅ |
| 右轨标签 | **转录** transcript（另一档 agent「2 个在跑」） | `chat.ts` | ✅ |
| **模型参数（闭源 10 个）** | temperature **0.3** / top_p 0.95 / max_output_tokens 8192 / thinking.budget_tokens **10000** / tool_choice auto / response_format json_schema / prompt_cache **on · 5min** / service_tier standard / stop_sequences [] / seed —（16,761,226） | 无 | ❌ **10 个全缺** |
| **模型参数（自托管 10 个）** | temperature 0.3 / top_p·top_k 0.9·40 / repetition_penalty 1.05 / max_model_len 128000 / quantization **FP8** / tensor_parallel_size 2 / gpu_memory_utilization **0.90** / max_num_seqs 64 / enable_prefix_caching **true** / lora_modules consulting-tone-v4（16,762,193） | 无 | ❌ **10 个全缺** |
| Agent 组装 temperature | **0.6** / 上下文预算 **80k** / 单次上限 120k / 单日 2M（16,673,566 区） | 无 | ❌ 缺 |
| Agent 发言时机 | **被 @ 时** ＋ **出现"我们决定"时主动插话**（两项已选）；「每轮都发言」未选（16,668,617） | 无 | ❌ 缺 |
| MCP 逐工具授权 | **默认全关**，示例已开 3 / 9（16,684,167） | `admin.ts` 只有服务器级 authScope | ❌ **粒度缺** |
| MCP 第三方隔离期 | **14 天**（16,680,674 区） | 有 isolated 态，**无 14 天** | ⚠ 部分 |
| MCP 传输 | SSE（三档 SSE/stdio/HTTP 的默认） | 无 | ❌ 缺 |
| 资产复核周期 | **12 个月**（6/12✓/24 三档，16,713,252；30 天无人复核降级为仅负责人可见） | `brain.ts` 知识条目有 12 个月 ttl；**资产侧无** | ⚠ 部分 |
| 资产可见范围 | **指定团队**（三档中的第一档已选） | 无 | ❌ 缺 |
| 资产发布方式 | 两选：直接发布 / **灰度发布 · 5 人**（推荐灰度，16,717,473） | 无 | ❌ 缺 |
| 模型三级降级阈值 | **组织 85% / 团队 90% / 个人 95%**，达阈值**降级不硬停**（16,696,279） | `tpl.ts:455` 只有 90%；**85/95 两级缺** | ❌ **部分反了** |
| 收敛提议触发阈值 | **重复 5 次**（曾为 3 次，被 9 位主持人反馈后上调，16,621,403） | `agent-runtime.ts AGENT_PANEL.intervention` 5 次 | ✅ |
| 议程环节默认时长 | AG_DATA 14 环节各自 dur（20/30/50/35/60/60/30/20/25/75/40/40/50/30） | `project.ts` 有议程但时长未逐条核 | ⚠ 未核 |
| 访谈默认分段时长 | 5 / 20 / 20 / 10 分（合 55 分，16,860,406）；模板侧同（16,865,526） | `itv.ts` 有模板 | ⚠ 未逐条核 |
| 访谈数据保留 | **180 天 · 禁止用于训练**（16,863,321） | `rec.ts` 有留存与禁训 | ✅ |

**默认值不一致合计：18 处 ❌ + 14 处 ⚠**（其中 **6 处是「值明确相反」**：三个 AI 开关 + 图谱默认视图 + 项目阶段默认 + 访谈默认视图）。

### ④ 原型 Studio 阶段数（`P_META` 16,784,092）

| | 原型 | 我们（`studio.ts:10`） |
|---|---|---|
| 阶段 | 6：设计上下文 / 设计方向 / 页面与自然语言调整 / **版本比较** / **设计审计** / 确认与交付 | 5：设计方向 / 信息架构 / 页面与调整 / 交互与状态 / 交付 |
| 版本 | 有 `isPCmp` **阶段 4 版本比较** | `studio.ts:5` 注释写「**只保留最新版本 … 不做版本并排比较**」 |

**判定：值不一致（且我们的硬规则与原型直接相反）**。

### ⑤ MODELS 18 台（`MODELS` 16,757,856）

| 维度 | 原型 | 我们（`admin.ts:273`） |
|---|---|---|
| 总数 / 分组 | 18（10 闭源 + 8 自托管） | 18（10 + 8）✅ |
| 状态分布 | **on 9 / test 3 / off 6** | enabled 13 / disabled 2 / untested 3 |
| 型号 | gemini-**3**-pro/flash · mistral-large-3 · doubao-pro-4 · claude-haiku-**4.5** · deepseek-**v4** · qwen**3.6-35b-a3b** · glm-**5.2** · kimi-k2 · gemma-4-31b · gpt-oss-20b · whisper-large-v3-**zh** | gemini-**2.5**-pro/flash · **moonshot-k2** · claude-haiku-**4.6** · deepseek-**v3.2** · qwen3-**32b/72b** · glm-**4.5-9b** · mixtral-8x22b · deepseek-r1-70b · bge-m3-embed |
| 计价单位 | `￥18/M`（每百万 token） | `￥0.090 / 1k`（每千 token） |
| 上下文 | `200k ctx` / `2M ctx` / `1M ctx` | `500k` / `400k` / `1M` |

**判定：值不一致**（18 台里只有 5 个型号名重合：claude-opus-4.6 / claude-sonnet-4.6 / gpt-5.2 / gpt-5.2-mini / grok-4 / llama-4-70b）。

### ⑥ 画像模板 6 分区（`TPL_ZONES` 16,789,587 + `TPL_SPECS.persona` 16,790,773）

| 序 | 原型分区 | 上限 | 必填 | 填充率 | 我们（`canvas.ts:222`） |
|---|---|---|---|---|---|
| 1 | **用户描述** | 6 | ✅ | 82% | **我们没有这一区** |
| 2 | 目标和需求 | 8 | ✅ | 88% | 02 目标和需求 8 ✅ 88% |
| 3 | 行为与偏好 | **8** | ✗ | 61% | 03 行为与偏好 **6** ✗ 61% |
| 4 | 痛点和挑战 | **10** | ✅ | 96% | 01 痛点和挑战 **8** ✅ 96% |
| 5 | 动机 | **6** | ✗ | **44%** | 05 动机 **5** ✗ **null** |
| 6 | 影响因素 | 6 | ✗ | 17% | 04 影响因素 6 ✗ 17% |
| — | （原型无） | | | | 06 **关键场景** 5 ✗ null ← 我们多造的 |

**判定：值不一致**（缺一区、多一区、顺序不同、3 个上限偏低、1 个填充率丢失）。

### ⑦ 推演流水线 4 场景（`LANES` 16,948,119 / `META` 16,948,697 / `kgScenes` 16,960,839）

| 维度 | 原型 | 我们（`canvas.ts:405`） |
|---|---|---|
| 场景名 | 业主首次评估 / 采购比选 / 并网审批 / 运营期归因（各带 subs 三子步） | 德国自用 / 法国 EMC / 北欧工商业 / 南欧安装商（**无 subs**） |
| 格状态 | **5 种**：已完成 / 正在讨论 / **证据冲突** / **停滞 8 分钟** / 图谱建议下一步 | **5 种但把「证据冲突」与「停滞」合并成「冲突停滞」**，多一个「未开始」 |
| 已完成格数 | LANES 行里数出 **19**；侧栏总进度却硬编码 **39%**（≈14/36）——**原型自身不一致** | 注释写 14/36，`PIPELINE_GRID` 实际数出 **12** ——**我们自身也不一致** |
| 跨场景互证 | 2 条紫虚线（`cross` 16,951,250） | 无 |

**判定：值不一致**。

---

## 3. 问题清单

### 3-1 · `canvas.ts` 注释断言「组级图谱是原创设计」——原型里有整整一屏 【重做】

| | |
|---|---|
| 原型证据 | `GK` **16,931,609**：4 个小组各一份「本组图谱」，字段 `scene/done/doneMeta/fork/forkSub/next/facts[2]/nexts[2]`。渲染出 4 节点小树（`defs` 16,934,283、样式 `K` 16,934,756）、「本组已确认的事实」列表（`gkFacts` 16,939,653）、「图谱建议的下一步 + 一键派出」（`gkNext`）。配套弹层「**往本组图谱加一个节点**」在 **16,748,233**（三种节点类型 16,937,670 / 挂到哪个上游 / **5 个依据来源** 16,937,479 / 提示「结论类没挂依据，引导师那边会标成待确认」），以及汇入按钮 `gkPush` **16,931,490**：「本组已确认的 4 个节点已汇入全场图谱，等引导师批量确认」。 |
| 我们现在是什么样 | `apps/web/lib/mock/canvas.ts:429` `GROUP_TREE`，注释逐字写：`/** 本组小树（[未探明] 组级图谱屏——档案只探明全场，此为原创设计）*/`。3 个节点，无 fork/next/facts/依据来源/汇入流程。`feature_list.json` 124 个 feature 里「本组图谱」命中 **0**。 |
| 建议修法 | 删掉「[未探明]…原创设计」这条断言（它是错的，会让签核人以为没有原型依据）；按 `GK` 补 4 组数据 + 4 节点骨架 + 加节点弹层 + `gkPush` 汇入语义；给「结论类无依据 ⇒ 标待确认」写一条门控。 |

### 3-2 · 图谱**默认视图「决策树」整块缺失** 【重做】

| | |
|---|---|
| 原型证据 | `kgTabs` **16,951,703** 三视图：**决策树（默认，`s.kgView \|\| "tree"`）** / 推演流水线 / 事实关系。决策树含：`ND` **16,952,387** 9 节点（主决策 + 收购/合资/自建三路径 + 2 岔口 + 商业模式 + 2 个「图谱推演」ghost 节点）、`EG` **16,954,137** 8 条边（4 种线型：普通/满足硬约束/已否决/图谱建议）、`K` **16,954,600** **6 种节点态**（在议 cur / 领先 lead / 待验证 open / 冲突 conf / **已否决 dead** / **图谱建议 ghost**）、`trForks` **16,957,767** 3 个待决岔口（每个带「为什么重要」+ 选项 + 证据数）、`trMoves` **16,958,834** 5 个可派出的下一步（带成本/派给谁/**影响：关掉 4 节点·补 2 证据·定优先级**/挂哪个节点）。 |
| 我们现在是什么样 | `canvas.ts` 只有 `FACT_NODES`（事实关系）与 `PIPELINE_GRID`（推演流水线）。全仓 grep「决策树」在 `apps/web/lib`+`components` **0 命中**；`feature_list.json` **0 命中**。原型的**默认视图**在我们这里不存在。 |
| 建议修法 | 新增决策树视图为图谱默认屏；节点六态（尤其 **dead 已否决** 与 **ghost 图谱建议** 两态是产品差异化）+ 边四型 + 岔口面板 + 「下一步可派出」面板（这是「图谱驱动下一步」这条产品主张的唯一落地处）。 |

### 3-3 · 议程环节表缺「引导师做什么 / 组员做什么 / 绑哪张画布 / 挂哪个 skill」四列 【重做】

| | |
|---|---|
| 原型证据 | `AG_DATA` **16,793,946**：两天档 4 半场 14 环节，每环节字段 `name, out(产出), dur, **fac**(引导师做什么), **grp**(组员做什么), **cv**(绑定画布模板 key), **sk**(挂载 skill), tag`。示例：环节 i3「HMW 发散」`fac:"巡组、处理停滞" grp:"唯一提交人 / 写便签" cv:"hmw" sk:"语音转便签"`。`AG_TAG` **16,797,251** 5 种环节标签（需人签字 / 隔夜任务 / AI 主讲 / 分组 / 投票）。半天档另一份 `WAG_DATA` **16,797,984**（7 环节，可拖拽跨半场移动 + 「跨半场移动后必须重算时间窗」的 dirty 提示）。 |
| 我们现在是什么样 | `project.ts` / `tpl.ts` 有议程环节与三角色矩阵，但 grep「洞察简报（AI 主讲）」「隔夜任务派发」**0 命中**；`AG_TAG` 五标签 0 命中。 |
| 建议修法 | 把 `fac`/`grp`/`cv`/`sk` 四列补进议程环节字段（`agenda_segment`），五标签做成枚举；补 `agDirtyMsg` 那条「跨半场移动后必须重算——上午多塞一节午餐就得推后，这类冲突现场才发现最贵」的门控。 |

### 3-4 · 六种资产**共用的三步新建向导**整块缺失 【重做】

| | |
|---|---|
| 原型证据 | `AN_META` **16,753,190** 定义 6 种资产（agents/skills/models/mcp/canvasadmin/blueprint）共用同一向导（1 来源 → 2 配置 → 3 治理与发布）。第 1 步 **16,646,528**：4 种来源 + **「从实际用法沉淀」检测到 3 个候选**（14× 同一段提示被复制 / 6× 手搓同结构画布 / 9× 临时授予同组 MCP 工具）+ 社区源 4 个（1,842 / 312 / 96 / 903 条，含一个「失败·凭据过期」）。第 2 步按资产分五套：Skill 走**落地检查六道关** **16,655,113**（解析 / 安全扫描 / 依赖权限 / **查重 68% 阻断 + 三选一** / 沙箱试跑 / 发布范围）；Agent 走**四栏组装** **16,665,755**；MCP 走**逐工具授权** **16,684,167**；模型走**12 条能力探针** **16,692,174**；画布走**对话式共建** **16,700,790**。第 3 步 **16,709,463**：三资产完全一样的可见范围 / 责任与复核 / 发布前检查 + 灰度。 |
| 我们现在是什么样 | `feature_list.json` 里「落地检查」「六道关」「查重」「能力探测」「探针」「逐工具」「灰度」（除 F68 的 skill 灰度）、「隔离期」全部 **0 命中**。`components/admin` 有各资产列表屏，无向导。 |
| 建议修法 | 这是一整条能力域（资产治理），建议单开契约束：一个向导壳 + 五套第 2 步 + 一套第 3 步；`AN_META` 的 6×(来源, 配置, 治理) 矩阵就是覆盖矩阵。**六道关每一关的「阻断 vs 提示」语义是硬门控，不能只当装饰**。 |

### 3-5 · 三个项目级 AI 开关默认值与原型相反 【重做】

见 §2-②。原型三项均为**开**（`aiProactive ?? true` 17,011,894 / `recording: true` / 介入尺度两个 ✓），我们三项均 `defaultOn: false`。且我们的注释同时引用原型偏移与相反默认值——**同一事实两处、且互相打架**，正是 AGENTS.md 点名的第六次漂移形态。
建议：要么改成与原型一致，要么在 `agent-runtime` 契约束的 `design-signoff.md` 里把 O-23 写成「显式覆盖原型 + 覆盖理由 + 人类签字」，并把偏移引用改成「原型为开，我们故意关」。

### 3-6 · 可见性表少「组长」一行，且「引导师看不到组员私聊」写反 【重做】

| | |
|---|---|
| 原型证据 | 可见性表 **16,614,368**，4 行：组员「本组全部 + 自己的私聊 / 别组内容」；**组长「本组全部 + 组员私聊 / 别组私聊」（16,615,622）**；**引导师「所有组 + 所有对话 / **—**」（16,616,083）**；观察者「已发布产出与脱敏聚合 / 原始转写与私聊」。JS 侧同源：`chatVisNote` **16,930,040** `fac: "引导师可见本组全部，包括组员私聊"`；`hidePrivate` **16,854,389** 只对 `member`/`obs` 生效（**fac 与 lead 都能看私聊**）；`canSeeCross` **16,877,593** = `fac \|\| obs`。 |
| 我们现在是什么样 | `tpl.ts CAPS_PANEL.visibility` **3 行**：组员 / 引导师 / 观察者。缺组长行；且引导师那行写 `cannot: "组员私聊"` —— **与原型的「—」相反**。 |
| 建议修法 | 补组长行；把引导师 `cannot` 改成 `—`；`canSeeCross = fac \|\| obs`（观察者能看跨组脱敏聚合，这一点容易漏）。 |

### 3-7 · Agent 编排表的 agent 名与在场环节被改写 【修补】

| | |
|---|---|
| 原型证据 | Agent 编排表 **16,616,979**：FC Facilitator（全程 / **✓ 默认开**）、**AV Ava**（**02 · 03 · 06** / ✓）、**EC Echo**（全程 / **静默**）、SC Scout（**按需召唤** / 仅被召唤时）。 |
| 我们现在是什么样 | `agent-runtime.ts AGENT_PANEL.agents`：Facilitator（全程）、**Analyst**（**03 / 04 / 05**）、**Scribe**（全程）、Scout（**02 / 06**）。四个 agent 里两个被改名、两个在场环节被改。 |
| 建议修法 | 改回 Ava / Echo（与 `AG` 花名册、`liveAgents`、`CMD_ITEMS.agent` 同名，否则四处 agent 名对不上），环节改回 Ava=02·03·06、Scout=按需召唤。 |

### 3-8 · 画布 ↔ Markdown 的**四种同步态**只落了一种 【修补】

| | |
|---|---|
| 原型证据 | `SYNCED` 16,788,777「已同步」/ `AHEAD` 16,788,849「**画布领先 · 未写回**」/ `BEHIND` 16,788,936「**文档领先 · 需重新渲染**」/ `CONFLICT` 16,789,030「**冲突 · 两侧同时改**」；`CV_META` 16,789,120 把 4 篇画布分别钉在 4 个不同同步态上（flow=已同步 / persona=画布领先 / bmc=**冲突** / journey=文档领先），并有 `cvConflict` 分支渲染冲突条。 |
| 我们现在是什么样 | `canvas.ts CANVAS_MARKDOWN_SYNC` 只有 `lastSync: "3 秒前"` + 三句说明；四态枚举与 bmc 的冲突态 0 命中。我们的 `NodeStatus`（已确认/冲突/待确认）是**图谱声明**的三态，与画布↔文档同步态是**不同维度**，不能顶替。 |
| 建议修法 | 补四态枚举 + 每篇画布的当前态 + 冲突态的出口（原型有 `cvConflict` 分支）。这直接关系到「正式产出只能绑固定快照」那条硬规则的可见性。 |

### 3-9 · ```canvas 围栏与「坐标不写回」缺失 【修补】

| | |
|---|---|
| 原型证据 | `updateExport()` **16,826,492**：非 flow 类模板导出 ```canvas 围栏 = `模板: <key>` + 字段行 + `## 分区` + `- 便签`，并同时产出 IR JSON `{type:"template", template, fields, sections}`；flow 类导出 ```mermaid + IR `{type:"flowchart", direction, nodes, edges}`。向导侧文案 **16,708,535**：「AI 输出这段围栏，前端渲染成可编辑画布；画布里的改动写回同一段 Markdown。**坐标不写回**——只回写内容与分区归属。」 |
| 我们现在是什么样 | `canvas.ts` 只有 ```mermaid 围栏样例；```canvas 围栏、IR 双产物、「坐标不写回」三项 0 命中。 |
| 建议修法 | 补 ```canvas 围栏格式（它是「AI 输出即可渲染画布」这条主张的接口契约）+ IR 两型 + 「坐标不写回」作为契约级约束写进 `canvas` 束的 API 契约。 |

### 3-10 · 模型详情屏（参数 / 能力 / 连接 / 五项测试 / 降级）整块缺失 【重做】

见 §1.2 #6/#7/#14/#15/#16/#17/#18 与 §2-③。尤其：
- **「五项全通过后，这个模型才会出现在 agent 与 skill 的模型选择里；任何参数改动都会要求重跑测试」**（16,993,000 区）——这是一条**状态机门控**，我们完全没有；
- **三级降级阈值 组织 85% / 团队 90% / 个人 95%**（16,696,279），我们只落了 90% 一级；
- 20 个模型参数默认值全缺。

### 3-11 · 主持台 AI 插话与组长/组员两套工作台语料整块缺失 【修补】

`liveMsgs` **16,964,934**（4 条，含 GROUPWATCH「第 3 组近 8 分钟只有组长在说话、举手 2 分钟未响应」这类**可操作告警**，且**非 fac 角色 action 全部清空**）、`liveSkills` 16,968,051、`dashFuncs/dashAgents/dashSkills` **16,909,534**（组长 7/4/8 vs 组员 5/3/6 两套）、`roleMsgs` **16,915,182**（RETURNER 解读退回意见 / GAPFINDER 缺口检测两个专用 agent）、`grpSteps` 16,984,846（5 条提交检查）、`grpOutputs` 16,985,727（4 项产出）。我们只有 `entry.ts` 里的「向引导师举手」。
建议：这批是「同一界面四种角色」这条主张的**具体证据**，签核第 ① 件要看的正是它；按角色两套补齐，并把「非 fac 的 action 清空」做成门控而非文案。

### 3-12 · 报告生成（4 节 + 7 类取材 + 3 种口径 + 报告列表 CRUD）缺失 【修补】

`SRC` 16,887,331（7 类取材，默认 5）、`TONE_L` 16,887,652（3 口径）、`BASE`/`ST` 16,889,830/16,890,724（3 份报告 + 4 态 + 增删改名）、`repSections` **16,892,384**（4 节，每节带 `st` 复核状态 + paras + **srcs 出处**+ 单节重写）、`repGap` 16,894,566（缺料说明与升版条件）、`repQuick` 4 个一键改写。我们全无。

### 3-13 · 深度研究新建弹层（类型/深度/来源/交付/挂节点）缺失 【修补】

`KIND_L` 16,904,064 / `DEPTH_L` **16,904,190**（8分3路 / 20分6路 / 45分12路交叉验证）/ `nrSources` 5 选 / `nrOuts` 4 选 / `nrNodeOpts` **4 个决策节点选项含「不挂节点，只存进洞察库」** / `nrPreview` 拼出的执行预览句。这是「研究结果回流到决策节点并标置信度」这条链路的入口，我们 0 命中。

### 3-14 · MCP **逐工具**授权粒度缺失 【重做】

原型 **16,684,167**：9 个工具逐个开关、**默认全关**、示例已开 3；每工具带 `副作用`（只读 / 对外发送 / 写入外部）与 `开放给`（全体成员 / 仅项目负责人 / 需人工确认每次 / 未开放）；硬规则 **16,703** 区：「带副作用的工具永远不能设为『全体成员自动调用』，最宽只能到『需人工确认每次』」。
我们 `admin.ts MCP_SERVERS` 只有服务器级 `authScope`——原型开篇那句「**整台服务器一刀切授权是绝大多数越权的来源**」（16,680,674 区）正是在反对我们现在的做法。

### 3-15 · 命令面板（@ Agent / Skill / 画布模板 · 知识库 / 项目 / 文件）缺失 【修补】

`CMD_TABS` 16,763,642 / `CMD_TAB_LABEL` 16,763,747 / `CMD_ITEMS` **16,764,046**（6 组共 26 条候选）/ `CMD_HINT` **16,768,659**（6 条规则，含「同一条消息里最多挂两个 skill」「带机密标记的文件会强制切到本地模型」「跨项目引用会标『未沉淀』」「过期条目会被标注」）。我们只有 `COMPOSER_STATUS` 一行摘要。`CMD_HINT` 那 6 条里至少 3 条是**硬约束**，不是提示文案。

### 3-16 · 原型 Studio 的设备三档 / 状态四态 / 导出四格 缺失 【记录】

`PD_META` 16,784,665 / `PS_META` 16,784,920 / `EX_CTA` 16,785,207 / `npFid` 三档保真度。加上 §2-④ 的六阶段。

### 3-17 · 访谈 13 view / 5 组 与我们 8 屏对不上 【记录】

`IVG_MAP` **16,786,120** 13 view → 5 组（design[plan,outline] / people[people,ready] / **virtual[vexp,vrun,vadopt]** / rec[live,proc,verify] / insight[debrief,matrix,report]）。我们 `ITV_SCREENS` 8 屏，缺 `outline`(提纲独立一屏) / `ready`(受访者就绪) / `proc`(转写加工) / `verify`(核验) / `debrief`(访谈后即时汇报) 五个 view，virtual 三步压成一屏。默认 view 也不同（原型 `live`，我们 `library`）。

### 3-18 · 21 个 wsPhase 与我们的四段划分不同 【记录】

`WS_GRP` **16,782,672**：res[rov,conv,survey,itv,research,lab] / prep[scope,agenda,mat,hw] / live[live,board,groups,chats,tpl,uxl,drl,kg] / after[after,deliv,actions,rep,audit] —— 共 **21 个子页**（另有 setup/todo/graph/new/over 5 个不在四组内 = 26 个 wsPhase）。我们 `project.ts` 四段但子页集合不同（缺 `lab` 实验室 / `uxl` / `drl` / `audit` 审计 / `conv` 研究对话 / `tpl` 推演模板）。

### 3-19 · 现场 5 项能力 vs 蓝本 7 项能力：**原型自身不一致**，我们只跟了一半 【记录】

`LIVE_FNS` **16,842,514** 现场左栏 5 项（chat / itv / dr / **svy** / **kg**）；蓝本 `组内能力` 表 **16,608,657** 7 项（chat / itv / dr / **ux** / **canvas** / **vote** / **ev**）；`FN_BODY` **16,843,398** 恰好是后者 7 个 key。也就是说 **原型的蓝本表与现场左栏是两套不同集合**，且 `FN_BODY` 的 `chips`/`actions` 被 `rawFn`（16,854,230）清空——是原型自己的死数据。
建议：这不是我们的错，但**必须在签核里问人类哪一套是权威**，否则蓝本里配的 7 项开关在现场只有 3 项能对上。

### 3-20 · 其余「原型有、我们完全没有」的小结构 【记录】

`TH_META` 三线程 / `AB` 六线程头像色 / `VIS` 15 种可见性标签色 / `NEW_ITEM` 四模块新建默认值 / `FILT` 看板筛选 / `SRC` 7 类任务来源 / `TONE` 小组三态 / `FN_ST` / `OUT` 9 环节产出 / `cross` 跨场景互证 / `kgByGroup` / `chatFilters` / `leadOwnChats` / `leadMemberChats` / `myChats` / `memberPresets` / `AG_FILES`+`AG_TREE`（Agent 仓库文件视图，Skill 侧有、Agent 侧无）/ `FLOW_NODES`+`FLOW_EDGES`+`NB`（假设树画布数据）/ `MD_*` 六件（见 3-10）/ `AN_META`（见 3-4）。

---

## 4. 方法与局限

### 覆盖率

| 段 | 字节 | 我读到什么程度 |
|---|---|---|
| A（16,600,000–16,753,190，150 KB HTML） | 100% 的**文本内容** | 用 `re.sub(r'<[^>]+>',' \| ',…)` 剥掉标签后得 416 行 23 KB，逐行读完。**CSS/内联样式/`style-hover` 未读**——所以「视觉细节层」的差异本文没有覆盖。 |
| B（16,753,190–17,050,600，297 KB JS） | ~85% | 逐行读完 5,098 行中的 1,041–1,840 与 2,600–5,098（数据 + `renderVals`）。**1,840–2,600（约 760 行 fabric.js 画布渲染代码）只做了两件事**：① `grep` 出全部 `const … = [ / {` 声明并逐个归类（#47–#58）；② 抽读 2,320–2,372（`updateExport()`，产出 §3-9）+ `grep -oE '"[^"]*[一-龥][^"]*"'` 拿到该段全部中文串（25 条，只有便签/节点/形状/连线这类交互提示，无产品事实）。 |

### 已知盲区（不隐瞒）

1. **fabric 渲染段 1,840–2,600 未逐行读**。上述两次交叉验证让我有信心那里没有产品级数据结构，但**不能保证没有藏在计算里的业务规则**（例如便签落区判定 `nearestZone`、连线锚点算法）。
2. **CSS / 内联样式未比对**。原型是 inline-style 密度极高的写法，字号档位、间距、色板的差异本文一律未查——那属于 `uiux-standards.md` 的门控范围。
3. **`sc-if` 条件表达式未穷举**。A 段是模板，某些面板只在特定 `state` 组合下渲染；我按 `{{ isBpXxx }}` / `{{ anIsXxx }}` 定位了 18 张表，但**若某张表的 `sc-if` 条件我没识别出来，它就不在索引里**。
4. **逐条数值未全核**。`AG_DATA` 14 个环节时长、`DEF`/`DEF_SEGS` 的分段分钟、`MD_CAPS` 18×4 = 72 个能力格、`MODELS` 18×7 = 126 个字段值，我核了抽样与结构，**未逐格比对**。§2 表里凡标 ⚠「未核」的都是这一类。
5. **只比对了 mock 与 feature_list/契约的文本命中**。`grep` 命中即算「有」——**有可能命中的是注释而非实现**（例如 §3-1 就是靠读注释才发现断言错误）。反过来，**用不同措辞实现的功能可能被我误判为「未使用」**；我对每条 `重做` 级结论都做了第二次确认（查 `feature_list.json` + 查 `components/`），`修补`/`记录` 级只做了一次。
6. **本文未查 `ui-preview/*-v2` 的截图本身**，只查了 mock 与组件源码。若某功能已画在截图里但没进 mock，会被我判成「未使用」。

### 与另一 agent 的边界

15.0–16.6M（界面区）由另一 agent 负责。**两处交界需要合并时注意**：
- 我的 A 段（16.6–16.75M）是蓝本 + 资产向导的**后半**；蓝本前 11 个面板在 16.6M 以下，属对方区间。`tpl.ts` 的 16 个面板要两份报告合起来才完整。
- `chatPresets`（16,974,576）在我区间，但配套的**预设编辑器弹层**（`newChatTitle` 16,836,655 等）也在我区间；而**预设的消费端**（组员「点开即用」区）文案在 15,574,080，属对方区间。
