# 待你拍板的全部事项（2026-07-31）—— 每条两个 action

> 设计类裁决的**候选与推荐**在 `RULINGS-NEEDED-2026-07-31.md` 里，这里只收敛成 A/B 供拍板。
> 运维/流程类是本文新增的。**空着不裁 = 选 A**（除非那条注明「必须你裁」）。

## 一、发布（回答「是否已发布到服务器」）

**现状：一个字节都没有部署。** `backend-gates.yml` 的 `deploy` job 条件是
`if: startsWith(github.ref, 'refs/tags/v')` —— **只有打标签才部署**，而仓库里从未打过 `v*` 标签
（唯一的标签是 `backup/pre-harness-template-20260724`）。自建 runner 是活的、门控在跑，
但 `sudo /usr/local/bin/workspacex-deploy` 从未被调用过一次。

**D-1｜要不要现在发一版**

| | action | 后果 |
|---|---|---|
| **A**（推荐） | **暂不发**。等 wave-1 结束、有第一条端到端可见的用户路径再打 `v0.1.0` | 现在 `main` 上 15 个 feature 绝大多数是**领域层/契约层**——没有 HTTP 路由、没有前端接线的居多（F55/F61/F69/F48 都明说了「只到应用层端口」）。部署上去打开浏览器基本看不到新东西 |
| B | **现在打 `v0.1.0` 发一版** | 好处是把 deploy 这条路径**验一次**（它从未跑过，很可能有坑，越晚发现越贵）。代价是发一个用户看不出差别的版本 |

⚠ 无论选哪个，**deploy 路径从未被执行过**这件事本身是风险。若选 A，建议至少手动
`workflow_dispatch` 跑一次 backend-gates 确认 gates job 在 runner 上是绿的（它现在是）。

## 二、设计裁决（挡住 feature 开工）

**D-2｜`research` Q-8：研究是几层** —— 挡 **F145–F148**

| | action |
|---|---|
| **A**（推荐） | **两层**：研究（计划）→ 研究问题，每个问题一段 Scout 对话。依据：原型 `res.goal` 逐字「5 个研究问题各有 ≥3 条独立来源」与详情屏三计数「研究问题 5」互相印证，**这是原型唯一自洽的读法** |
| B | **一层**：三词同义统一叫「研究」。最简，但「5 个研究问题」无处安放 |

（三层无任何原型出处，属发明，我不列为选项。）
⚠ 选 A 需一并指明：`uc-24-1` 创建的是**研究（计划）**还是**研究问题**。

**D-3｜`asset-governance` Q-0：确认或推翻我做的裁决** —— 关系到**已合入 `main` 的 F132** 及另外 11 个 feature

我按你 07-30 的授权裁了「方案 C（拆）」：浏览编辑 + 治理发布 + 复核降级 + 后台外壳留 phase-1；
外部社区导入 + 仓库导入 + 六道落地关 + 沙箱试跑留 phase-2。**D-06 一个字不用改。**

| | action |
|---|---|
| **A**（推荐） | **确认方案 C**，我把结论落进 `23-asset/OPEN-QUESTIONS.md` 的 Q-0 裁决行（现在是空白，⚠ 缺口 15） |
| B | **推翻**，F132…F143 十二个 feature 全部作废重来，F132 需从 `main` 回滚 |

**D-4｜缺口 17：试跑台留 phase-1 还是 phase-2**（裁决文与落地方向相反）

| | action |
|---|---|
| A | 按裁决文：**留 phase-1**。代价：会触碰**已签核**的 `agent-runtime` 束的配额面 |
| **B**（推荐，＝落地现状） | **整块移 phase-2**。代价：`agent-runtime` 的 F55–F60 失去一个可视化入口，不影响可实现性 |

**D-5｜「综合 Studio」是什么** —— 不挡 feature，但**已签核的 `interview` 束里有个通往它的出口**

`apps/web/lib/mock/itv.ts:888` 的 `INSIGHT_REPORT_EXPORTS` 含「送入综合 Studio」，
而**任何 phase 都不存在这个 Studio**。研究域的候选洞察也往同一个地方送。

| | action |
|---|---|
| **A**（推荐） | 认定它 ＝ phase-02 的 `09-kg` / `10-report`，把两处出口指过去 |
| B | 认定它是**未立项的新模块**，登记进 phase-02 requirements，phase-01 两处出口保持「已声明未实现」 |

（第三种是撤掉 `interview` 那个出口，但那会改已签核束的语义，除非你明确要，我不推荐。）

**D-6｜其余 26 条待裁要不要现在裁**

| | action |
|---|---|
| **A**（推荐） | **只裁 D-2…D-5 + `asset` Q-7/Q-11 + `research` Q-10/Q-12/Q-7**（共 9 条），够解锁 wave-1。其余 20 条随对应 feature 开工时再问 |
| B | 一次裁完 31 条。好处是后面不再打断，代价是你现在要连续读很久 |

## 三、签核状态

**D-7｜三个束「签了字但正文说别签」怎么收**

`project` / `asset-governance` / `research` 的正文逐字写着「请不要把 `status` 改成 `confirmed`」。
逐条对账见 `SIGNOFF-RECONCILIATION-2026-07-31.md`：**`project` 那两条阻塞都已解除（正文过期）**，
另两个各有仍然成立的（`asset` 的 Q-1b/Q-7/Q-11/Q-12，`research` 的 Q-8）。

| | action |
|---|---|
| **A**（推荐） | **保持签核有效**，我在三份正文各加一个 2026-07-31 更正块（留痕原文，不删）说明哪些阻塞已解除、哪些还在。开发照常 |
| B | 把 `asset-governance` / `research` 退回 `pending`，等 D-2/D-3 裁完再签。代价：这两个束的 17 个 feature 全部停工 |

**D-8｜补一道门控：签了的文件正文若自称不可签核就判红**

现在 `assertDesignSignedOff` 只读 frontmatter，正文写什么都不看。

| | action |
|---|---|
| **A**（推荐） | 补。含反证：删掉自述要能变绿、加回去要能变红、空文件不许平凡通过 |
| B | 不补，靠人读 |

**D-9｜`agent-runtime` 束的 `covers` 事后补入了 F129–F131**（2026-07-30），
文件自己标着「需人类重新确认第 ②③ 件」

| | action |
|---|---|
| A | 你重新过一遍那三条的 UI/用例/契约后确认 |
| **B**（推荐） | 先按已签核继续（F129–F131 都在 wave-2 之后，不急），等真要开工前你再确认 |

## 四、工程/流程

**D-10｜`@repo/api` 测试套件连跑不确定失败（issue #74）**

实测：空闲机器、全新库、同一命令连跑六次 = **绿 / 红49 / 红61 / 红22 / 绿 / 绿**。
它让 pre-push 和 `harness verify` 都不可信，今晚已导致三个 agent 考虑或使用 `--no-verify`，
并且**藏掉了一条真红**（`binding-three-modes.test.ts` 的悬空 promise）。

| | action |
|---|---|
| **A**（推荐） | **先修它再继续 wave-1**。理由：它是所有门控的地基；带着它往下做，后面每一条「绿」都要打折 |
| B | 带病继续，靠 GitHub CI 兜底（CI 也在同一条船上，只是核少、暂时没炸） |

**D-11｜自建 runner 上 backend-gates 端口冲突**

PR #72 合并后那次 backend-gates 红在 `EADDRINUSE :::33210` —— 两次运行在**同一台自建 runner**
上重叠，固定端口撞车。不是代码问题，但会让 `main` 上的门控随机变红。

| | action |
|---|---|
| **A**（推荐） | 给 workflow 加 `concurrency: { group: backend-gates, cancel-in-progress: false }`，串行化 |
| B | 端口改成按 run id 派生 |

**D-12｜并发 agent 上限**

今晚实测：13 个并发时 load 102、104 个 vitest 进程、一个 agent 被 OOM kill、三个被迫考虑绕门控。
降到 5 个之后一切正常。

| | action |
|---|---|
| **A**（推荐） | **上限 6**，宁可慢 |
| B | 加机器（这是唯一真正提速的办法） |

**D-13｜F52 的「凭据端点不泄露」测试放哪**

它是 `fs` + 正则**扫描渲染源码**（不是 DOM 断言），能在 api 环境跑。放 api 则验收命令真的覆盖那个泄漏
（那是个**真缺陷**：`mcp-policy-screen.tsx` 把内部地址渲染给了只读角色）；代价是一个 api 测试知道 web 的目录结构。

| | action |
|---|---|
| **A**（推荐） | 留 api。耦合写在文件头，换来「验收通过」和「端点没泄漏」是同一件事 |
| B | 移到 web，F52 的验收命令不再覆盖那个泄漏（需我改 `feature_list.json` 并留痕） |

**D-14｜`KNOWN_CONTRACT_GAPS.OA3`：邀请链接 7 天无单一事实源**

| | action |
|---|---|
| A | 把 `orgInviteLinkDays` 加进 `AUTH_POLICY`（跨 phase-00 契约） |
| **B**（推荐） | 裁定「域常量即单一事实源」，现状不动 |

## 五、我不需要你裁、已经在做的

- issue **#75** `findBindings` 取最后一行而非最严的（授权判定不确定，影响三种对象）
- issue **#76** `hookTimeout` 未设
- 迁移号集中分配（已到 `0023`）、`verify-rls.sh` ratchet 只许实测（已写进纪律 21）
- worker 临时文件带 owner 前缀（纪律 20）
