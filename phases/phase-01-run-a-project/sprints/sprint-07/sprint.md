# Sprint 01/07 — L3 context-pack 检索召回接线进 chat——文件式检索（F155，delta context-engine-l3-file-based）

- **所属阶段**: Phase 01 (run-a-project)
- **创建于**: 2026-08-14 03:37:15

## 本 sprint 目标
L3 context-pack 检索召回接线进 chat——文件式检索（F155，delta context-engine-l3-file-based）

## 领取的 feature(引用自阶段权威清单,按 id)
- F155 (P1, chat) — L3 context-pack 检索召回接线进 chat（ContextAssemblyPort 内侧调既有引擎 + 权限约束检索 + 注入伪消息，ModelCallPort 不动）

> 实际工作集见同目录 `active-features.json`(脚本派生,只读,勿手改)。
> 修改功能归属:改阶段 `feature_list.json` 里对应 feature 的 `sprint` 字段,再重跑
> `pnpm harness new-sprint`(或 refresh)重新派生。

## 完成标准
- 上述每个 feature 经 `pnpm harness verify --sprint 01/07` 门控为 `passing`。
- `session-handoff.md` 与 `progress.md` 已更新。
