# Sprint 01/20 — F26 工作流编排三用例接上真实 Postgres（#1680 gap-fill）

- **所属阶段**: Phase 01 (run-a-project)
- **创建于**: 2026-08-20 22:48:42

## 本 sprint 目标
F26 工作流编排三用例接上真实 Postgres（#1680 gap-fill）

## 领取的 feature(引用自阶段权威清单,按 id)
- I1680 (P1, tpl) — gap-fill：F26 三个用例（saveAsOrgTemplate/switchWorkflowTemplate/getWorkflowOrchestration）接上真实 Postgres + 控制器 + DI（#1680）

> 实际工作集见同目录 `active-features.json`(脚本派生,只读,勿手改)。
> 修改功能归属:改阶段 `feature_list.json` 里对应 feature 的 `sprint` 字段,再重跑
> `pnpm harness new-sprint`(或 refresh)重新派生。

## 完成标准
- 上述每个 feature 经 `pnpm harness verify --sprint 01/20` 门控为 `passing`。
- `session-handoff.md` 与 `progress.md` 已更新。
