# F24 —— 项目筹备页外壳（四子标签+计数）+ 定题单点继承

范围：`apps/api/src/domain/templates/project-prep.ts`,
`apps/api/src/domain/templates/project-topic.ts`,
`apps/api/src/application/templates/{get-project-prep,project-prep-ports,
save-and-sync-topic,save-and-sync-topic-ports}.ts` —— 契约
`templates.getProjectPrep` / `templates.saveAndSyncTopic` 已在
`packages/contracts/src/templates.ts` 中签核存在，本 feature 只补齐其
domain/application 实现与验证。

依 issue #74：Postgres 依赖测试本地跳过，只跑纯 vitest + typecheck。

## 必需验证（两条，均 exit 0）

$ pnpm --filter api exec vitest run tests/tpl/prep-tabs-counts.test.ts

 RUN  v2.1.9 /private/tmp/f24-worktree/apps/api

 ✓ tests/tpl/prep-tabs-counts.test.ts (7 tests) 5ms

 Test Files  1 passed (1)
      Tests  7 passed (7)
   Duration  381ms

[exit 0]

$ pnpm --filter api exec vitest run tests/tpl/single-source-theme-inheritance.test.ts

 RUN  v2.1.9 /private/tmp/f24-worktree/apps/api

 ✓ tests/tpl/single-source-theme-inheritance.test.ts (7 tests) 5ms

 Test Files  1 passed (1)
      Tests  7 passed (7)
   Duration  410ms

[exit 0]

## 附加验证（回归 + 单源门控，非本 feature 必需项，但用于证明未引入新失败）

$ pnpm --filter api exec vitest run tests/tpl/
 Test Files  24 passed (24)
      Tests  246 passed (246)
[exit 0]

$ pnpm --filter api exec vitest run tests/contract-single-source.test.ts
 Test Files  1 passed (1)
      Tests  4 passed (4)
[exit 0]

$ node apps/api/scripts/lint-permission-paths.mjs
✅ lint-permission-paths: every tenant-table read goes through the guarded read path
scanned=573 tenant-tables=83 allowlisted=27
[exit 0]
（allowlist 计数未变化——本 feature 未新增需要豁免的租户表读取路径）

$ node apps/api/scripts/lint-naming-single-source.mjs
✅ lint-naming-single-source: 0 处败选名（stepId / step_id / agenda_stage / stage.<action>）
scanned-roots=4
[exit 0]

$ pnpm --filter api exec vitest run tests/kernel/permission-propagation-six-paths.test.ts
 Test Files  1 passed (1)
      Tests  29 passed (29)
[exit 0]

## typecheck 说明（未新增失败，但也未能取得干净的 exit 0）

`pnpm --filter api run typecheck` 在本仓当前 `origin/main`（`ac639298`）上，
与本 feature 的改动完全无关地，本就因 `packages/fabric-markdown/src/{fabric-objects,
mermaid-parser}.ts` 缺 DOM lib 类型而失败（`Cannot find name 'document'` 等）。
已用一个不含本 feature 任何改动的干净 `origin/main` worktree 复现同一失败，确认是
预置基础状态问题，不是本 feature 引入。用
`pnpm --filter api exec tsc --noEmit -p . 2>&1 | grep -i "project-prep\|project-topic\|save-and-sync-topic\|get-project-prep"`
核对：本 feature 新增/修改的 6 个源文件与 2 个测试文件，无一条 tsc 报错与之相关。

## 实现要点

- `getProjectPrep`：`domain/templates/project-prep.ts::planProjectPrepTabs` 纯函数把
  四类真实计数（`groupCount` / `agendaSegmentCount` / `materialsCount` /
  `preTasksDone`+`preTasksTotal`）拼成契约要求的 `{key, label, count}[]`；
  label 里的数字与 `count` 同源，V6「计数与实际写入数据一致、改数据后同步刷新」
  由此保证。V15 空态：全 0 输入不做特殊分支，四个标签仍然全部出现。
  用例 `application/templates/get-project-prep.ts` 只做「查角色 → 查计数 → 拼标签」，
  「不存在」与「越权可见」两者统一映射 `NO_PROJECT_ROLE`（同 `get-project-overview.ts`
  既有判例，不重新发明区分）。
- `saveAndSyncTopic`：单点继承落成「一张表、一次 upsert-by-projectId」而不是
  「三份拷贝 + 一致性校验」——`ProjectTopicRepository.saveAndSync` 端口要求实现
  以 `projectId` 为唯一键（唯一约束，不是先查后写），并发冲突由仓储发现
  「更新 0 行」抛 `TopicRevisionConflictError`，翻译成契约码 `VERSION_CHANGED`。
  `syncedTo` 三处（grouping / agenda / ai-context）读到的是同一次 upsert 之后
  同一个 `topicId`，不是三份独立写入。
  AI 生成来源校验 `isAiSourcesSufficient` 在 domain 层再判一次（不仅依赖 zod
  `.min(1)`，覆盖用例被直接调用、绕过 HTTP 校验的路径）——空来源即
  `AI_SOURCES_INSUFFICIENT`，不落库（防御性判断在写入之前）。
  权限：只有 `facilitator`（引导师）能保存，`groupLead`/`member`/`observer`
  一律 `ROLE_INSUFFICIENT`，对应原型「引导师操作」与组员视角写操作按钮置灰
  （`uc-2-2-prep-member.png`，V14）——这是本 feature 唯一的角色判断口径判断，
  已在文件头注写明依据，供后续与 F25（分组编排）对齐组长权限时参考。

## 已知范围边界（非本 feature 缺口，供后续 feature 承接）

- 四类真实计数的**存储实现**（分组 / 材料 / 会前任务各自的领域模型与仓储）不在本
  feature 范围内——`ProjectPrepRepository` 只是端口，真实实现随 F25-F28 各自落地
  时补齐；本 feature 验证的是「端口→标签」这条纯函数链路的正确性，与 F23 的
  `apply-blueprint-init.ts` 同一惯例（六类写入计划也是先有 planner，仓储随后续
  feature 补齐）。
- 本 feature 未新建 HTTP controller / infrastructure 仓储实现 / apps/web 组件——
  两条 verification 命令都是 `apps/api` 纯 vitest 单测，未要求 web 侧组件测试
  （与 F23 不同，F23 的 verification 里有 `apps/web` 测试，F24 没有）；
  `contracts/design-signoff.md` 已确认整束签核，`ui.md` 未给出本 feature 专属的
  `data-testid`（只有束级四张截图索引），故 UI 落地留给后续在真实项目工作台子屏
  组建时一并处理，不属于本 issue 的两条 verification 覆盖范围。
