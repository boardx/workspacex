# F174 六屏设计 QA

## 结论

通过。实现沿用已签 `guided-deep-research/ui-preview/01-home.png` 至 `06-report.png`
的信息架构、品牌 token、全局导航、步骤条、卡片/表单密度和主操作位置；没有把旧
`?screen=detail` 当作研究主入口。

2026-08-14 增量 QA：用户新增的创建研究命名弹窗没有独立参考图，因此以已签首页的
现有 token、表单密度和相邻「新建访谈/转录」模式为视觉真值。真实浏览器截图为
`07-create-research-dialog.jpg`（1019 × 930 像素，Codex in-app Browser 桌面视口，1x CSS 密度）。
验证了名称必填、标签可选、回车添加、进入 brief、最终 API 创建和历史卡片恢复；控制台无错误。

参考图是高密度裁切图，当前实现截图按相同 CSS 视口采集：

- 首页、资料研究、研究报告：1920 × 1080
- 主题确认、研究方向、报告大纲：1280 × 720

每个 `*-comparison.png` 左侧为签核图，右侧为本地真实浏览器实现。

## 逐屏核对

| 屏幕 | 结果 | 核对重点 |
| --- | --- | --- |
| 01 首页 | 通过 | 默认进入引导式研究；历史研究真实加载；未完成项显示“继续研究” |
| 02 主题 | 通过 | 步骤条、主题/目标/范围表单、右侧研究问题摘要与主按钮位置一致 |
| 03 方向 | 通过 | 可编辑、启用、删除、添加和重新生成；卡片密度与签核稿一致 |
| 04 大纲 | 通过 | 可编辑章节、问题标签、计划摘要和开始研究主操作保持一致 |
| 05 搜索 | 通过 | 进度、当前查询、章节任务与最新来源同时可见 |
| 06 报告 | 通过 | 目录、正文、来源引用三栏结构，保留返回和导出入口 |

## 本轮发现并修复

1. 引导式研究不再渲染 Research Studio 二级左栏；旧 `?screen=` 页面仍保留菜单。
2. 浏览器端此前没有 `/research` 同源代理，历史列表和检查点请求落到 Next 404；
   现已同时代理裸路径和深路径，并由反证测试锁定。
3. 搜索摘要和报告三栏结构补上稳定视觉契约锚点，防止后续接真实 Web Search/报告数据时
   把已签布局替换掉。
4. 创建研究先弹出命名与可选标签，保存到真实会话并在历史卡片显示；弹窗居中、遮罩、字号、
   绿色主操作与现有设计系统一致，无 P0/P1/P2 差异。

## 证据

- `01-home-comparison.png`
- `02-brief-comparison.png`
- `03-directions-comparison.png`
- `04-outline-comparison.png`
- `05-search-comparison.png`
- `06-report-comparison.png`
- `07-create-research-dialog.jpg`

## 增量对比与最终结果

- 字体：复用 Noto Sans SC 与现有 `text-11/12/13/20` 层级；名称和标签信息层级清楚。
- 间距：`max-w-md`、7 单位内边距、5 单位字段间距与相邻创建弹窗一致。
- 颜色：完全使用 `card / border / muted / primary / ring` 语义 token，无硬编码颜色。
- 图片与图标：该弹窗无内容图片；关闭按钮使用项目既有 Lucide 图标，没有伪造资产。
- 文案：明确写出「标签（可选）」和最多 5 个；主操作为「进入研究」。
- 聚焦区域：弹窗本身即完整聚焦区域，无需额外裁切；背景只用于确认模态层级和首页上下文。
- 比较历史：首轮 lint 发现标签移除 hover 缺 transition（P2），已补 `transition-colors duration-200`；
  修复后 lint-design 通过，最终截图如上。

final result: passed
