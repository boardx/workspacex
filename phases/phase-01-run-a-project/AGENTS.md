# AGENTS.md — Phase 01 (能跑完一场项目) 局部指令

> 阶段级 scoped 指令,补充根 AGENTS.md。只写本阶段特有的约束。

## 本阶段焦点
从进场到产出的完整闭环：身份与邀请、项目蓝本、Skill/Agent 能力平面、模型与 MCP、实时转录、访谈、推演画布、对话、项目文件

## 权威来源
- 功能清单:本目录 `feature_list.json`(本阶段唯一权威)。
- 进度:本目录 `progress.md`。

## 规则继承
根 `AGENTS.md` 的所有硬约束在此继续生效(尤其"完成定义"与"干净收尾")。
