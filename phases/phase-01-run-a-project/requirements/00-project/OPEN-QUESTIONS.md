# 待人类裁决 — 「项目」域的 12 个问题（其中 1 条已降级为确认动作）

> 2026-07-30。同目录 `DERIVED-FROM-CONTRACTS.md` 列的是**已签核契约已经定死的部分**；
> 这份列的是**剩下的、我推不出来的部分**。
>
> 按本仓纪律，每条都给**研究后的候选选项 + 优缺点与风险 + 推荐**，不抛开放性问题。
> ⚠ 但**推荐不是答案**：本仓发生过「有人编了一个数值制造出已算过的假象」
> （`phases/phase-00-shared-kernel/design-coherence.md:200`），
> 所以 `uc-00-*.md` 里凡涉及本文的地方一律写「待人类裁决 → 第 N 条」，**不填推荐值**。
>
> **我们查过**（每条下方注明）：`DECISIONS-FINAL.md`(D-01…D-41 + D-03a)、`DECISIONS-OPEN.md`(O-01…O-40)、
> `DECISIONS-UI-ROUND.md`(D-U1…D-U12)、`DECISIONS-CONTRACT-ROUND.md`、`COMPLIANCE-INQUIRY.md`、
> phase-00 六个束的 `domain.md`/`usecases.md`/`design-coherence.md`、
> phase-01 九束 `contracts/*/`、`ui-preview/project/PROTOTYPE-ANSWERS.md`、
> phase-01 的 49 份 UC 全文。若某条其实已有出处，请直接指出来——比重新裁决省事。

**优先级建议**：🔴 Q-1 / Q-2 / **Q-12** 阻塞开工（项目实体的形状取决于它们）；
🟠 Q-4 / Q-5 / Q-8 / **Q-11** 阻塞定稿；🟡 其余可与实现并行澄清。
**Q-3 已从 🔴 待裁决降级为「确认既有裁决」**——见下方总表「已有答案」列与 [Q-3](#q3) 正文。

> **2026-07-30 追加 Q-11 / Q-12**：来自并行的 `itv`（访谈）UI 原型工作
> （`phases/phase-01-run-a-project/ui-preview/itv/README.md`，44 张截图）。
> 两条都不是访谈域能自己回答的——它们改的是**项目实体本身的形状**，故收进本文。

> ## ⚠ 2026-07-30 二次改造：先穷尽仓库，再问人
>
> 本文第一版有两处**没有先穷尽仓库里已有的答案**就把问题抛给人类，两处都出了事：
>
> 1. **Q-3 根本不是待裁决。** `phases/requirements/DECISIONS-FINAL.md:31` 的 **D-03a**
>    （2026-07-28 定稿、自称全局权威）已经把字段名定为 `agenda_segment` / `agenda_segment_id`，
>    而本文第一版只引了更早的 D-03，**推荐了相反的名字** `agenda_stage`。
>    ⇒ 已改为「确认既有裁决」，可裁空间收窄为唯一真正开放的那一条。
> 2. **Q-12 的四个候选全都不对。** `project` 域 UI 原型核验后发现运行态用的是
>    「**项目挂到另一个项目**」——项目自引用的可选父项目
>    （`ui-preview/project/PROTOTYPE-ANSWERS.md` Q-12）。
>    ⇒ 已补为候选 **E**，并因此**改了推荐**。
>
> 为防第三次，总表新增「**已有答案**」列，12 条逐条重查
> `DECISIONS-FINAL.md` / `DECISIONS-OPEN.md` / `DECISIONS-UI-ROUND.md` /
> `DECISIONS-CONTRACT-ROUND.md` / `PROTOTYPE-ANSWERS.md` / 九束 `contracts/*/`。
> 取值口径：
> `已定（出处）` = 有人类裁决，**降级为确认动作，不要重裁** ·
> `原型已答（出处）` = 运行态原型给了形状，但仍需人类确认它是不是数据模型 ·
> `部分` = 有些子问已定、有些没有（正文逐条标） ·
> `未覆盖` = 全仓查不到出处。

---

# 裁决总表（打勾用）

> **这份文件是需求输入，不是权威。** 权威永远是
> `phases/phase-01-run-a-project/feature_list.json`。本表里的「推荐」是研究结论，
> **不是答案**；没有人类签名的格子一律视为未裁决。
>
> **裁决后的下一步**：把每条的裁决写进下方对应的《裁决》块（勾选 + 签名 + ISO 日期）→
> 由 **requirement-author** 智能体读本目录的 `uc-00-1/2/3` 与本文，
> 把 `[待裁] → Q-N` 的留白替换成裁决值并更新 `feature_list.json`（它是唯一有权改清单的角色）→
> 涉及**已签核束**的裁决（Q-3 的唯一开放子问、Q-10 方案 A）另需回到对应束的 `design-signoff.md` 走签核，
> **agent 不得代签**。
>
> **总表不复制论证**。每格只给结论与影响面，点编号跳到正文看候选与优缺点。

**排序说明**：本表**不按编号排，按上游程度排**——排在前面的问题，其答案会**改变后面问题的形状**
（少一个候选、或多一个必须回答的子问题）。所以请自上而下答：
Q-12 决定项目实体上有没有「类型 / 父子」维度，Q-11 决定项目是不是顶层容器，
这两条一变，Q-1（创建路径）/ Q-2（环节实体）/ Q-6（列表）的候选集合都要重画；
中段（Q-6 / Q-9 / Q-7）是纯下游，答案由前面的裁决基本决定；
**「已有答案」的排在最后——它们只需确认，不需要重裁**。

| # | 一句话问题 | **已有答案** | 我的推荐 | 影响面（哪几个束 / feature 等这个答案） | 裁决 |
|---|---|---|---|---|---|
| [**Q-12**](#q12) 🔴 | 「研究项目」与「业务项目」是两种项目，还是项目上的一个维度？ | **原型已答（新形状）** — `ui-preview/project/PROTOTYPE-ANSWERS.md` Q-12：运行态用的是「**项目挂到另一个项目**」，四个原候选都没画到 ⇒ 已补候选 **E** | **E**（项目自引用的可选父项目 `parent_project_id`）—— **推荐已改**，原推荐 A（`kind` 维度）撤回，理由见正文 | 束：`interview`（`contracts/interview/domain.md` 是九束里**唯一**出现「研究项目」的文件）。feature：**F80**（访谈范围模型：`project_id` 可空 + 两种权限投影 + 服务端过滤）、**F81**（范围切换器）。另**改变 Q-1 / Q-2 / Q-6 的候选集** | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-11**](#q11) 🟠 | 项目与 Studio 是从属关系还是平级？ | **原型已答** — `PROTOTYPE-ANSWERS.md` Q-11：导航里 Studio 与项目**平级**、项目内又可调用 ⇒ 支持候选 ②，与三处已签契约同向 | **②** Studio 一等、可选关联项目（`projectId` 可空 + 事后 `bindToProjectStep`） | 束：`interview`（`ui.md` / `design-signoff.md` / `coverage.md` 均以 Studio 为一等）、`files`（`domain.md` / `design-signoff.md` 的产出归属）、`canvas`（`ui.md`）。feature：**F80 / F81**，导航归属波及 **F90**。与 **Q-10 是同一个洞** | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-2**](#q2) 🔴 | 「议程环节」是不是独立实体？它的状态机是什么？收不收某类产出？ | **部分** — ② 有原型证据（`PROTOTYPE-ANSWERS.md` Q-2：状态机 + 「三视角唯一驱动源」运行态就是这么演的）＋ **O-02 裁决 ②**（`DECISIONS-OPEN.md:78-105`：蓝本 ⊃ 工作流模板，环节实例来源已定）；① 与 ③ **未覆盖** | ① **A** 独立表 + 外键；② **B** 四态 + `mergedInto`；③ `acceptedSources` 默认全接受，**但必须配反证测试** | 束：**六束**已把环节当既有挂载点——`agent-runtime`（I-36 载入触发源）、`canvas`（I-32 白名单）、`files`（N-4）、`templates`、`skills`、`interview`。feature：**F05 F16 F19 F26 F27 F31 F63 F64 F81 F102** 已在环节上排工，而**环节实体本身在 `feature_list.json` 里没有任何 feature** | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-1**](#q1) 🔴 | 项目怎么创建？只能套蓝本，还是可以空白新建？ | **原型已答（仅产品面）** — `PROTOTYPE-ANSWERS.md` Q-1：「**从空白开始**」在运行态是一等入口，与套蓝本并列 ⇒ 「能不能空白建」已答；「**一条还是两条创建事务**」原型看不出，仍**未覆盖** | **C**（一条创建路径 + 蓝本可选参数 `blueprintVersionId?`） | 束：`templates`（全束以 uc-2-2 为主体）。feature：**F23**（套用蓝本新建：六类初始化事务写入）、**F22**（新建三入口）、**F24**（项目筹备页外壳） | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-10**](#q10) 🟡 | 「不属于任何项目」的内容，怎么被读回来？ | **未覆盖** — 全仓无裁决。原型只证明「个人层未升级内容一等可见」（`PROTOTYPE-ANSWERS.md` Q-10），**读取路径是契约内部张力，UI 看不见** | **A** 放开 `readContent.in.projectId` 为 `nullable`，且作为**契约缺陷报告**提给 artifact / identity 两束签核人，不由实现者顺手改 | 束：phase-00 的 `artifact` / `identity`（**改的是已签核束**）。phase-01 feature：**F80 F81**。与 **Q-11 连带 1 是同一个洞，请一起裁** | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-4**](#q4) 🟠 | 项目成员怎么进来？两层角色的四种交叉各是什么行为？ | **部分** — ① **原型已答**（`PROTOTYPE-ANSWERS.md` Q-4：直接指派 + 链接进场**两入口并存**）；四格交叉的第 4 格另有相关已定裁决 **O-04**（`DECISIONS-OPEN.md:133-156`：管理员读项目层内容 = **放行 + 留痕**，且**不区分**是否持项目角色）与 **O-03**（协同引导师 = 引导师多实例）；② ③ 仍**未覆盖** | ① **B** 两个入口但**共用同一个 application 用例**；② `lead` 对自建未加入的项目持**管理权、不持内容读取权**；③ **不缓存跨请求判定** | 束：`org-admin`（成员/角色面）、`chat`（F108 两层交集判定）、`agent-runtime`（可见性）。feature：**F04 F10 F11 F12 F15 F58 F108**。⚠ **「项目成员名单本身怎么形成」在清单里没有 feature** | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-5**](#q5) 🟠 | 项目有没有「结束/归档/关闭」？结束后里面的东西怎么办？ | **部分** — 原型证明「**已归档**」标签在项目列表里存在（`PROTOTYPE-ANSWERS.md` Q-5），但**行为一律未演示**；仓库里的归档裁决 **O-10 / O-18 / O-11** 全是**画布模板 / 蓝本 / skill** 的归档，**不是项目**；连带四问 **未覆盖** | **B** 两态 `active / archived`，`archived` = 只读，**归档不删除任何内容** | 束：**九束都没有把项目归档写进契约**（各束出现的「归档」全是蓝本 / 模板 / skill / 对话的归档，不是项目）。feature：**清单里没有项目生命周期 feature**。真实缺口：`packages/contracts/src/context-pack.ts:147` 的 `QueryContext.projectIds` 不按项目状态过滤 | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-8**](#q8) 🟠 | `STEP_CLOSED` / `STEP_REJECTS_ARTIFACT_TYPE` 的判据由谁给？何时补？ | **未覆盖** — 无裁决；原型空白（后端失败码，UI 无体现）。纯下游于 Q-2 | 1. **是**本域交付责任；2. **加**外键（新增 `0016-*`，不改已 passing 的 `0008`）；3. 补齐时**必须同时交两条反证** | 束：`interview/usecases.md` 是九束里唯一引用 `STEP_CLOSED` 的；判据来源是 phase-00 已签的 `artifact` 束。feature：**无 feature 承接**，而 **F81** 的挂载路径会直接撞上它 | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-6**](#q6) 🟡 | 项目列表 / 项目主页要展示什么？ | **部分** — ② **原型已答**（`PROTOTYPE-ANSWERS.md` Q-6：概览内容在运行态极明确，见 `uc-00-2-overview-default.png`）；① 「两段式返回」是响应体设计，**未覆盖**；准备度口径仍是 uc-2-2 的 `[待确认]` | ① **B** 两段式返回（「我在里面」与「我管着它」**分开**，不是一个混合数组加布尔）；② 概览只放**已有出处**的东西，**准备度不在本域定义** | 束：phase-00 `web-kernel`（顶栏两层身份条）。feature：**无**——`uc-00-2:195` 明写「Q-6 裁决前不要为项目列表生成 feature」。纯下游：吃 Q-4② / Q-5 / Q-11 / Q-12 的裁决 | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-9**](#q9) 🟡 | 「删除项目」提供吗？ | **未覆盖** — 有**先例未推广**：N-5 只裁了「不提供删除组织」（phase-00 `design-coherence.md:209`）；原型未出现删除项目入口，是**弱旁证不是证据** | **不提供**，并照抄 N-5：把 `DELETE /projects/*` 加进 `no-forbidden-routes.test.ts` 的禁止清单 | 束：九束均无删除项目。feature：**无**。纯下游：其成立性依赖 Q-5 选 B（归档承接「不再用了」） | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-7**](#q7) 🟡 | 一个人能同时在多少项目里？项目能不能跨组织？ | **部分（主体已定）** — 跨组织由 **I-1** 答死（`.../identity/domain.md:84`，外键层不可表达）；多组织切换由 **O-12 裁决①** 定死（`DECISIONS-OPEN.md:328-354`：一账号 + 顶部切换器 + 切换清空项目上下文 + 一人可归属多组织 + 团队单一归属）；容量由 **O-34 裁决**（`DECISIONS-OPEN.md:906-929`：组织并发项目 20 等，标注未实测）。剩 ①人均上限 与 ③停用组织下项目的呈现 **未覆盖** | 跨组织**已由 I-1 答死**（不可表达，非待定项）。剩余三问：① **不设**人均上限；② 若有跨组织诉求走**导出/复制**语义；③ 组织被停用时其项目**显示且标注只读**，不消失 | 束：`org-admin`（组织生命周期与切换器）。feature：**F95** 边缘相关；主体已被 **O-34**（容量基线）与 **I-1**（同组织约束）覆盖，本条只剩三个局部问 | ☐ 采纳推荐　☐ 选其它（写编号 ____） |
| [**Q-3**](#q3) ✅ | 「环节」这个字段到底叫什么？ | **已定** — `phases/requirements/DECISIONS-FINAL.md:31` **D-03a**（2026-07-28 定稿，**自称全局权威**）：议程环节 = `agenda_segment` / `agenda_segment_id`，事件 `agenda_segment.switched` / `.state_changed` | **不是待裁决，是确认动作。** 唯一真正开放的子问：phase-00 已签核并已落库的 `stepId` / `step_id` 与已实现的闭集动作词 `stage.*` **要不要改名对齐**（那是**修订已签核束**，D-03a 没有说这件事） | 束：`templates`（**仍写 `agenda_stage` 的只剩这 2 个文件**：`domain.md:25` 两名并列、`ui.md:123`）；已按 D-03a 落地的 **14 个文件**见正文清单；`interview`（`domain.md:33` / `usecases.md:94` 仍写 `stepId`，那是**契约层原名**不是漂移）。feature：**F26 F31 F33 F35 F41 F57 F64** | ☐ 确认 D-03a 有效（`agenda_segment_id`）　☐ 另裁 `stepId`/`stage.*` 改不改：____ |

## 连带效应速查（某条的答案会锁死另一条）

⚠ 这些耦合原本散在各条正文里，最容易漏。每对都是**双向**的。

| 一侧 | 另一侧 | 耦合形状 |
|---|---|---|
| **Q-12** 选 A/B（有 `kind`） | **Q-1** | 必须补答「两类项目是否都套蓝本」；Q-12 若选 D（无类型），Q-1 不受影响 |
| **Q-12** 选 A/B | **Q-2** | 必须补答「研究项目有没有议程环节」→ 决定环节实体要不要对某类项目**可选** |
| **Q-12** 选 A/B | **Q-6①** | 列表要不要按类型分区，与「两段式返回」是同一处响应体设计 |
| **Q-12** 选 A/B | **Q-11** | **Q-11 的候选 ③ 直接出局**（③ 依赖「研究项目」是独立第三方实体，即 Q-12 的 C） |
| **Q-11** 选 ①（Studio 从属项目） | **Q-12** | 「研究项目」失去载体，Q-12 的范围切换器档位无处安放（且 ① 本身与三处已签文本冲突） |
| **Q-11** 选 ②（推荐） | **Q-10** | **强制 Q-10 必须在 A / B 之间选一个**（`projectId=null` 的产出必须有读取路径）；Q-10 的 C 只在 Q-11 选 ① 时成立 |
| **Q-11** | **Q-6②** | 「项目概览要不要展示尚未绑定的产出」是同一个问题的两个提法 |
| **Q-12** 选 **E**（父子项目） | **Q-2** / **Q-6①** / **Q-11** | 新增三个必答子问：⑴ **议程环节挂在哪一级**（父还是子，还是两级都有）；⑵ 列表要不要按父子层级折叠（与「两段式返回」是同一处响应体设计）；⑶ Q-11 的候选 ③ **同样出局**——「研究项目」在 E 下不是第三方实体而是**树上的一个位置** |
| **Q-12** 选 **E** | **Q-5** / **Q-9** | 父项目归档 / 删除时子项目怎么办，是 E 独有的新问题（A/B/C/D 都没有）。⚠ `projects` 上现有的 `ON DELETE CASCADE` 邻接面会因自引用外键**多一条级联路径** |
| **Q-2①** 选 A（独立表） | ~~**Q-3**~~ | ~~命名必须先定~~ **已由 D-03a 定死为 `agenda_segment_id`**，本耦合失效；Q-2① 若选 B（不建表），受影响的只是「这个名字有没有一张表用它」 |
| **Q-2①** 选 B（不建表） | **Q-8** | 两个失败码**永远不可评估**，Q-8 只剩「承认它空转」一个答案 |
| **Q-2②** 的状态集合 | **Q-5 连带 2** | 归档时「进行中的环节」怎么办，必须能被状态集合表达出来 |
| **Q-1 连带 3**（创建者是否自动获项目角色） | **Q-4 待答②**（`lead` 对自建未加入项目） | **是同一条边的两端**，两处若给不同答案，「管理员不是超级用户」就破了 |
| **Q-5** 选 A（不做生命周期） | **Q-9** | Q-9 的「不提供删除」失去替代方案，「项目不再用了」无处安放 |
| **Q-5** 选 B（归档） | **Q-6 A5 / Q-7③** | 归档项目与停用组织下的项目，是**同一种只读呈现**，两处必须一致 |
| **Q-4②** | **Q-6①** | `lead` 创建但未加入的项目，要不要出现在他自己的列表里 |

---

<a id="q1"></a>
# 🔴 Q-1 项目怎么创建？只能套蓝本，还是可以空白新建？

> **推荐：C（一条创建路径 + 蓝本可选参数）** —— 创建事务只有一处（identity.ts:602-605 的教训），
> 且「没有蓝本」是能被表达出来的正常值而非一个假蓝本。
> 🔗 上游：**Q-12**（若项目有类型，须补答两类项目是否都套蓝本）。

## 已有答案（2026-07-30 重查）
**原型已答（仅产品面）** — `ui-preview/project/PROTOTYPE-ANSWERS.md` Q-1：新建项目视图的
「1 选蓝本」九宫格里，六张蓝本卡**之外**有两张虚线卡，逐字为
「**从空白开始 · 自己排环节，没有骨架约束**」与「复制一场已办的」。
⇒ 「能不能空白新建」在运行态**已经是一等入口**，不必再问产品。
⚠ 但该文件同时明确：「UI 有空白入口 **≠** 后端是两条创建路径」——
**一条还是两条创建事务，原型看不出，仍待裁**（即下面的 A/B/C 三选一）。
决策文件侧（`DECISIONS-FINAL.md` / `DECISIONS-OPEN.md` / `DECISIONS-UI-ROUND.md`）**未覆盖**本条。

## 现状
- 契约里**只有蓝本一条路**：项目唯一的出生路径是
  `phases/phase-01-run-a-project/requirements/02-tpl/uc-2-2-套用蓝本新建项目.md`，
  即套蓝本的副产品。
- 「谁能创建」已有答案：组织角色 `lead`（项目负责人）——
  `phases/phase-00-shared-kernel/requirements/00-core/uc-0-3-角色本体与两层权限模型.md:99`
  逐字写着「创建与管理项目」。
- 空白新建**已被提出但标记为未确认**：uc-2-2 的 `A4`（`:174-175`）
  ——「**[设计]** 不套蓝本直接新建空项目：允许，但项目筹备页四子标签均为空态，
  不得伪造默认结构；此后可否补套蓝本见 R10 [待确认]」。
  `[设计]` 按该文件口径 `:11` 的定义就是「**需人类确认**」。
- 同文件 `:418` 另留一条 `[待确认]`：「空项目能否事后补套蓝本——若能，已有内容如何与默认值合并」。

## 候选

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **A** | **只能套蓝本**（含一个官方「空白蓝本」） | 创建路径唯一；六类初始化的事务写入（uc-2-2 E2「要么全成功要么整体回滚」）只需实现一次；项目必有 `blueprint_version_id`，`来自后台 vN` 的快照语义处处成立 | 「空白蓝本」是一个语义上的空壳，会诱导实现者给它塞默认值——那正是 uc-2-2 `V15` 要禁的「不生成示例分组或示例议程」；且后台需要维护一个谁都不该编辑的特殊蓝本 |
| **B** | **两条路径**：套蓝本 / 空白新建 | 贴合原型 A4；空项目是真实存在的低准备度形态（uc-2-2 `A2`/`:170-171` 举了原型实例「准备度 15% · 未邀请」） | 两条创建路径 = 两处要各自不出错。phase-00 已因同型问题写下纪律：`getLocalOrg` 明确**不创建**任何东西，「若这里也能创建，那就有了两条创建路径，而 I-2 要靠两处各自不出错」（`packages/contracts/src/identity.ts:602-605`） |
| **C** | **一条创建路径 + 蓝本可选参数**（`createProject(blueprintVersionId?: string \| null)`） | 只有一处创建事务；空白 = 传 null，六类初始化跳过而非写空值；与 D-17「artifact 的 projectId 可空」是同一种表达法 | 需要明确「未套蓝本」在项目上的表示（null 还是哨兵值）；补套蓝本仍需单独回答 |

## 我的推荐
**C**。理由不是"折中"，是它同时满足两条已有纪律：创建路径唯一（identity.ts:602-605 的教训），
且"没有蓝本"是一个能被表达出来的**正常值**而不是一个假蓝本
（同 `Artifact.projectId` 可空的处理法，`packages/contracts/src/artifact.ts:126-130`）。

## 连带必须一起答的
1. 空项目能否**事后补套蓝本**？若能，已有内容如何合并（覆盖 / 只填空缺 / 拒绝）？
   （uc-2-2 `:418` 已列为 `[待确认]`，本域不重复提问，只标记它阻塞 `uc-00-1`）
2. 组织角色 `lead` 之外，`admin` 能否创建项目？
   ⚠ 注意 `admin` 已被 D-18 定为「不是超级用户」
   （`phases/phase-01-run-a-project/requirements/01-auth/uc-1-4-角色可见性判定.md:131`），
   「能创建」与「能读内容」是两件事，但把它们混在一起是常见错误。
3. 创建者是否自动获得项目角色？uc-1-4 `:132` 说项目负责人「通常同时持有引导师，
   但两者是**独立授予**的」——「通常」不是规则。

### 裁决
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q2"></a>
# 🔴 Q-2 「议程环节（step）」是什么实体？它有没有状态机？

> **推荐：① A 独立表 + 外键；② B 四态 + `mergedInto`；③ `acceptedSources` 默认全接受（须配反证）**
> —— 不建表则两个已签失败码永远不可评估，那正是本仓九次「全绿但空转」的形状。
> 🔗 上游：**Q-12**（研究项目有没有议程环节；若选候选 **E**，还要答**环节挂在父级还是子级**）。
> 🔗 下游：**Q-8**（选 B 则该条只剩「承认空转」）、**Q-5 连带 2**。
> 🔗 ~~Q-3~~ **命名已由 D-03a 定死为 `agenda_segment_id`**，本条选 A 建表时直接用它，不必等命名。

## 已有答案（2026-07-30 重查）
**部分。**
- **待答 ② 的前提（状态机存在、且是三视角首屏唯一驱动源）有原型证据**：
  `PROTOTYPE-ANSWERS.md` Q-2 —— 议程屏每个环节有编号/标题/时长/绑定模板、有「当前」态、
  顶部恒显「环节 3 / 7」，主持台有「下一环节」「＋5 分钟」，议程屏文案逐字
  「组长切换环节状态后，三种视角的首屏立刻跟着换」。**运行态就是这么演示的。**
- **环节实例的来源已定**：**O-02**（`phases/requirements/DECISIONS-OPEN.md:78-105`，裁决 **②**）
  —— 蓝本 ⊃ 工作流模板，**两层结构**；「蓝本含全部档位、项目套用后只实例化所选档位的环节」。
  ⚠ 该条自己写着「`agenda_segment_id` 的持有方随之改变（D-03a 的字段契约悬空）」
  ⇒ **O-02 决定了环节实例由谁持有，但没决定它是不是一张表**。
- **待答 ① ③ 未覆盖**：`PROTOTYPE-ANSWERS.md` Q-2 逐字——①「是存储决策，原型看不出」；
  ③「原型的环节卡只显示『绑哪张模板』，**没有任何『本环节只接受哪几类来源』的白名单控件**」。

## 现状（这是本域最硬的债）
- `packages/contracts/src/artifact.ts:205` 把 `stepId: z.string()` 当**给定输入**。
- `apps/api/migrations/0008-f06-binding-modes.sql:23-31` 逐字记下：
  **phase-00 没有 `steps` 表**，`step_id` 无外键，
  且 `STEP_CLOSED` / `STEP_REJECTS_ARTIFACT_TYPE` 两个已签核失败码
  「**not evaluable by anything in this repository today**」。
- **「环节可被推进」已经签核**：`stage.advance`「推进环节」在闭集动作词表里
  （`apps/api/src/domain/identity/project-role-matrix.ts:26`），
  源自 UC-0.3 R5（`.../uc-0-3-…md:110`）。
- **已有两条规则以「环节状态」为前提**：
  ① 临时提权「**环节 3 结束自动失效**…失效必须由服务端在**环节状态机变更**时主动收回」
  （`.../01-auth/uc-1-4-角色可见性判定.md:97-100`）；
  ② 「组长切换环节状态后，三种视角的首屏立刻跟着换」，且环节状态是
  「**三视角首屏的唯一驱动源**」（`.../02-tpl/uc-2-2-…md:286-287`）。
- 环节实例的来源已有裁决 **O-02**（`.../uc-2-2-…md:380-395`）：
  蓝本 ⊃ 工作流模板；**套用时是引用而非内联**，项目侧议程环节实例各持
  `agenda_segment_id` 指回蓝本侧的环节定义。

⇒ 所以问题**不是**「环节要不要有状态」（那已定），而是下面三件：

## 待答 ①：环节实例是不是一张独立表？

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **A** | 独立 `agenda_segments` 表（表名按 D-03a，原写 `agenda_stages` 系笔误已改），`artifact_bindings.step_id` 加外键 | `STEP_CLOSED`/`STEP_REJECTS_ARTIFACT_TYPE` 立刻可判；孤儿绑定不可能存在；文件浏览器按环节分组有真实来源 | 要给 0008 补一次外键迁移（已有数据的 `step_id` 必须先有对应行）；phase-00 的绑定测试需要建环节夹具 |
| **B** | 不建表，环节只存在于蓝本快照 JSON 里 | 无迁移；套用蓝本时不必再展开一层 | 两个失败码**永远不可达**——`0008-f06:29-31` 已经说了「a check that cannot fail is worse than an absent one, because it reads as coverage」。这正是本仓九次「全绿但空转」的形状 |

**推荐 A**，且**推荐把外键补上**：不补的话，`STEP_CLOSED` 会以「实现时再说」的形式一直空转。

## 待答 ②：状态集合与迁移规则

已知的约束点：需要能表达「进行中」「已结束」（`环节 3 结束`）、
需要能表达「被提前结束 / 被跳过 / 被合并」（uc-1-4 `:99` 逐字列出这三种），
需要能表达 `STEP_CLOSED`（绑定被拒）。

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **A** | 三态 `pending → active → closed`，另加 `skipped`（终态） | 最小闭集；`STEP_CLOSED` 判据显然（`closed`/`skipped`）；「唯一驱动源」= 恰好一个 `active` | 「合并」表达不了（uc-1-4:99 明确要求合并也触发失效）——需要额外记「被并入哪个环节」 |
| **B** | 四态 + `mergedInto` 外键（`pending/active/closed/skipped`，`skipped` 与 `closed` 均可带 `mergedInto`） | 覆盖 uc-1-4 列出的全部三种非正常结束 | 状态与关系混在一张表 |
| **C** | 不建状态机，只记 `startedAt/endedAt` 时间戳 | 简单 | ⚠ **与 uc-1-4:98 正面冲突**：那里逐字写「失效条件是**流程节点**…**不是时间点**」。**不建议** |

**推荐 B**。并建议把「同一项目内 `active` 的环节至多一个」写成**数据库层的部分唯一索引**
而不是应用层规则——「唯一驱动源」这句话只有这样才是可断言的。

## 待答 ③：环节收不收某类产出（`STEP_REJECTS_ARTIFACT_TYPE` 的判据）
uc-2-2 的六类初始化第 2 类写「每环节**材料要求**与推演模板」（`:73`），
第 6 类写「6 件输出物的**验收口径**」（`:77`）——两者都可能是判据，但没有一处说明
「哪些 `ArtifactSource`（七类，`packages/contracts/src/artifact.ts:27-35`）被某环节接受」。

**推荐**：环节持有一个 `acceptedSources: ArtifactSource[]`，**默认全接受**（空数组 = 不限制），
由蓝本的「材料要求」投影而来。⚠ 但**默认全接受 = 这道门默认不生效**，
所以必须同时要求「至少有一个环节配置了非空白名单」的反证测试，否则又是一条空转的门。

### 裁决
（本条有三个待答，请分别勾选）

**待答 ① 环节实例是不是一张独立表**
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

**待答 ② 状态集合与迁移规则**
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

**待答 ③ 环节收不收某类产出**
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q3"></a>
# ✅ Q-3 「环节」这个字段叫什么 —— **确认既有裁决，不是待裁决**

> **本条已于 2026-07-30 降级。** 第一版把它列为 🔴 待裁决并推荐了 `agenda_stage`——
> 那是**错的**：`DECISIONS-FINAL.md` 的 **D-03a** 早在 2026-07-28 就把名字定成了
> `agenda_segment`，而第一版只引了更早的 D-03。
> ⇒ 本条现在需要的是**一次确认**，加上**一个真正开放的子问**（见第三节）。

## 一、既有裁决的原文与出处

出处：`phases/requirements/DECISIONS-FINAL.md:31`，表格 **D-03a** 行，
标题逐字为「**字段名消歧（2026-07-28 定稿，全局权威）**」。原文要点逐字：

> 三个概念必须是**三个不同字段**，**不得共用 `stage`/`phase`/`segment` 一个名**：
> · 议程环节 → **`agenda_segment` / `agenda_segment_id`**，基数 7/11/14/19/18…，实例级可增删。
>   引用方：04-agent（载入触发源）、03-skill UC-3.2、07-canvas、11-board、09-kg
> · 16 项设计配置 → **`design_facet` / `design_facet_id`**，**恒 16**，不随档位变。
>   引用方：02-tpl、04-agent（第 12 项 Agent 编排）、03-skill（第 13 项 Skill 绑定）、13-deliv（第 14 项 输出物）
> · 9 个方法环节 → **`method_stage` / `method_stage_id`**，**恒 9**（PESTEL→PRD）。引用方：09-kg、07-canvas
> **事件契约**：agent 载入规则的触发条件**只能引用 `agenda_segment_id`**，
> 禁止写入 `design_facet_id` 或 `method_stage_id`；
> 对外事件为 `agenda_segment.switched` / `agenda_segment.state_changed`

它已被 `phases/phase-01-run-a-project/requirements/00-overview.md:117`
收为「本阶段必须遵守的术语约定」。

⇒ **`agenda_stage` 这个名字在 D-03a 之后不再有效。** D-03 只说了「不得裸用『环节』二字」
（动机是与 9 个方法环节、16 项设计配置区分），**具体用哪个词由 D-03a 定**，两者不冲突，是先后。

## 二、落地现状：14 个文件已按 D-03a 写，2 个文件仍写旧名

**已按 `agenda_segment` 落地的 14 个文件**（`grep -rl agenda_segment phases/phase-01-run-a-project/contracts/`）：

```
agent-runtime/design-signoff.md   agent-runtime/domain.md   agent-runtime/usecases.md
canvas/design-signoff.md          canvas/domain.md          canvas/usecases.md
files/coverage.md                 files/domain.md           files/ui.md            files/usecases.md
skills/coverage.md                skills/design-signoff.md
templates/domain.md               templates/usecases.md
```

其中 `files/domain.md:101` 的 **N-4 已经有 information_schema 机械断言**——
也就是说这个名字**已经被门控守着了**，不是纸面约定。

**仍写旧名 `agenda_stage` 的只剩 2 个文件**（同一个束）：

| 文件 | 行 | 现状 | 该怎么改 |
|---|---|---|---|
| `contracts/templates/domain.md` | `:25` | 表格里 `agenda_stage_*` / `agenda_segment_id` **两名并列** | 删掉 `agenda_stage_*`，只留 `agenda_segment_id`。⚠ 这行同时并列了类型名 `AgendaStage` / `AgendaSegment`，也要收敛 |
| `contracts/templates/ui.md` | `:123` | 逐字写「我们自己的表述与字段名一律写全「议程环节」/ `agenda_stage`」 | 把 `agenda_stage` 换成 `agenda_segment` |

⚠ **改这 2 处是「按已有裁决收敛」，不是新裁决**，但它们属于 `templates` 束的签核材料，
故仍应由该束签核人在签 `templates` 时一并确认——本文不代改。

**另有两处写 `stepId`，但那不是漂移**：`contracts/interview/domain.md:33`、
`contracts/interview/usecases.md:94`。`stepId` 是 **phase-00 已签核契约的原字段名**
（`packages/contracts/src/artifact.ts:205`），interview 束在引用那个契约时照抄原名是正确的。
它是否要改名，正是下面那条唯一开放的子问。

## 三、唯一真正开放的一条：phase-00 的 `stepId` / `stage.*` 改不改

D-03a **没有说这件事**——它规范的是 phase-01 的需求与契约用词，
而 phase-00 已签核并已落库的两个名字写在 D-03a 之前：

| 名字 | 出处 | 性质 |
|---|---|---|
| `stepId` / `step_id` | `packages/contracts/src/artifact.ts:205`；`apps/api/migrations/0008-f06-binding-modes.sql:47` | **已签核契约 + 已落库** |
| `stage.*`（`stage.advance` / `broadcast` / `timer` / `group` / `bulkConfirm` 5 条） | `apps/api/src/domain/identity/project-role-matrix.ts:26-30` | **已实现的闭集动作词** |

### 候选

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **①** | **改名对齐**：`step_id` → `agenda_segment_id`，`stage.*` → `agendaSegment.*` | 全仓一个词，D-03a 的「不得共用一个名」在**代码层**也成立；`agenda_segment` 已有机械门控（N-4），改完可以一条 grep 断言旧名不再出现 | ⚠ **这是修订已签核束的动作**（phase-00 `artifact` + `identity`），不是实现动作，必须由那两束的签核人签。代价：新增一支列改名迁移（**不改已 passing 的 `0008`**）、`project-role-matrix.ts` 的闭集动作词表连带改、phase-00 现有绑定测试的夹具跟着改 |
| **②** | **分层保留**：契约与存储层 `stepId` / `stage.*` 不动，需求 / UI / phase-01 新契约一律 `agenda_segment`，并在 `artifact.ts` 加一行注释钉死「`stepId` ≡ `agenda_segment_id`」 | 零迁移、零签核变更 | 仍是「一个概念两个名字」。`design-coherence.md:163-177` 处理 `IngestionRun.status` vs `state` 时明确说这类分歧「**会成为联调 bug**」，并立了纪律：改名的一侧连带改所有引用，**拒绝两个名字都留着**。若选 ②，等于对本例开一个例外，必须写明例外理由 |

**⚠ 无论选 ① 还是 ②，都请同时要求一条门控测试**（形如 `no-forbidden-routes.test.ts`，
见 phase-00 `design-coherence.md:209`）：
- 选 ① → 断言 `agenda_stage` **与** `step_id` / `stage.` 在全仓不再出现；
- 选 ② → 至少断言 `agenda_stage` 不再出现（那是**已经无效**的第三个名字），
  并断言 `stepId` 只出现在被列举的白名单文件里。

否则「第七次漂移」只是被推迟——`templates` 束现在那 2 处就是它已经开始的样子。

### 确认与裁决

**A. 确认 D-03a 有效**（若不勾，请说明为什么要推翻一份自称全局权威的定稿）
- [x] 确认：议程环节字段名 = `agenda_segment` / `agenda_segment_id`，事件 `agenda_segment.switched` / `.state_changed`
- [ ] 不确认，理由：______

**B. phase-00 的 `stepId` / `step_id` / `stage.*` 改不改**（**这是修订已签核束**）
- [x] ① 改名对齐
- [ ] ② 分层保留（须写明为什么本例可以有两个名字）
- [ ] 其它：______

**C. `templates` 束那 2 处旧名的收敛**（按已有裁决执行，非新裁决）
- [ ] 确认由 `templates` 束签核人在签该束时一并改掉

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q4"></a>
# 🟠 Q-4 项目成员怎么进来？两层角色如何交互？

> **推荐：① B 两个入口但共用同一个 application 用例；② `lead` 对自建未加入的项目持管理权、
> 不持内容读取权；③ 不缓存跨请求的判定结论。**
> 🔗 与 **Q-1 连带 3**（创建者是否自动获得项目角色）是同一条边的两端，两处答案必须一致。

## 已有答案（2026-07-30 重查）
**部分。**
- **待答 ① 已由原型答**：`PROTOTYPE-ANSWERS.md` Q-4 —— 「参与者与邀请」屏**同时**给了
  ⑴ 直接指派（「邮箱或手机号，逗号分隔」＋「邀请」＋已在册名单）与
  ⑵ 链接进场（邀请链接 / 分组链接，身份四选）。⇒ **两入口并存**在运行态是事实（支持候选 B）。
- **四格交叉的第 4 格另有两条相关已定裁决**（本条第一版未引）：
  · **O-04**（`phases/requirements/DECISIONS-OPEN.md:133-156`，已裁）：管理员读**项目层**内容
    = **放行 + 写审计事件 + 对项目负责人可见**，**且不区分管理员是否持有该项目角色**
    ——「一律不可读」是稿本单方收紧，已撤回。⚠ 这条限定的是 `admin`，**不是 `lead`**，
    所以它不直接回答待答②，但**待答② 的答案不得与它相反**（否则「管理员不是超级用户」两处不一致）。
  · **O-03**（`DECISIONS-OPEN.md:106-132`，已裁）：协同引导师 = **引导师的多实例**，
    项目角色仍为四种 ⇒ 邀请屏的「身份四选」里那一档不新增角色。
- **待答 ② ③ 未覆盖**：`PROTOTYPE-ANSWERS.md` Q-4 逐字记「原型里操作者始终是**已加入的引导师**，
  这条边的两端从未被演示」。

## 现状
- 两层正交模型已签核：组织角色 4 种（`packages/contracts/src/identity.ts:170`）×
  项目角色 4 种（`:177`）；判定取**两层交集**，任一不过即拒
  （`.../uc-0-3-…md:58-59`）。
- `project_memberships` 主键 `(user_id, project_id)`（`0003-identity.sql:70`）
  ⇒ 一人一项目一角色。
- 已有的**入场**路径全在 01-auth，且都以「项目/分组已存在」为前提：
  `uc-1-3-生成与撤销邀请链接.md`（身份四选 + 主链接/分组链接）、
  `uc-1-2-用分组链接免注册进场.md`（持项目角色但**无组织角色**，
  见 `.../uc-1-4-…md:182-183`）、`uc-1-6-组织成员邀请与激活.md`。
- uc-2-2 的六类初始化第 5 类是「角色与进场：4 种角色的可见性、邀请链接与有效期默认值」（`:76`）。

⇒ **「怎么发邀请」有归属（01-auth），「项目成员名单本身怎么形成」没有。**

## 待答 ①：项目成员的授予入口在哪个模块？

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **A** | 全部走 01-auth 的邀请链接（唯一入口） | 入口唯一；免注册与注册用户共用一条路 | 项目负责人想直接把一位已有组织成员加进项目，也得走发链接——原型「参与者与邀请」屏是有直接名单操作的（uc-1-4 `:168`） |
| **B** | 两个入口：**组织内成员直接指派** + **组织外/免注册走链接** | 贴合原型；两类人本来性质就不同（一类有组织角色，一类没有，见 uc-1-4 `:182-183`） | 两条写路径要各自做权限判定与审计 |

**推荐 B**，但要求两条路径**共用同一个 application 层用例**（差别只在身份来源），
理由同 `readContent` 为什么不给个人层另开接口（`design-coherence.md:394`：
「另开一个个人层读取接口意味着 I-8 要写两遍，漏掉的那一遍不会有任何东西报警」）。

## 待答 ②：组织角色与项目角色的四种交叉情形，各自是什么行为？

| 情形 | 已有出处 | 待答 |
|---|---|---|
| 有组织角色 + 有项目角色 | 交集判定（uc-0-3 `:58`） | ✅ 已定 |
| 有组织角色 + **无**项目角色 | `E1/E3`：项目内资源一律不可见，**管理员亦然**（uc-0-3 `:84`、uc-1-4 `:111`） | ✅ 已定。但**「能不能看到这个项目存在」**未定 → 见 Q-6 |
| **无**组织角色 + 有项目角色 | 免注册现场组员（uc-1-4 `:182-183`）：「其组织层判定按『本项目授权范围』处理，**不得访问任何组织级资源**」 | ✅ 已定 |
| 组织角色 `lead` 对**自己创建但未加入**的项目 | 无 | 🔴 **待答**：他能管理成员/设置吗？能读内容吗（D-18 说不能）？ |

**推荐**：把最后一行明确成「`lead` 对其创建的项目持有**管理权**（成员、设置、生命周期）
但**不持内容读取权**」，并像 D-18 那样把它写成硬约束 + 审计。
理由：这与 `admin` 的边界是同一条规则的同一侧，两处若给出不同答案，「管理员不是超级用户」
就变成了「除了项目负责人以外的管理员不是超级用户」。

## 待答 ③：项目角色变更立即生效的**实现位置**
uc-0-3 `:71`（A3）、uc-1-4 `:106`（A4）都写「立即生效，不等会话过期」，
但没说是靠不缓存判定、还是靠缓存失效广播。
⚠ 这与 `AUTH_SERVICE_UNAVAILABLE`「一律拒绝，**不得降级放行**」
（`packages/contracts/src/identity.ts:199`）相接。**推荐**：不缓存跨请求的判定结论，
只缓存单请求内的（`authorizeBatch` 存在的意义正是让这条可承受，见 `identity.ts:392-405`）。

### 裁决
（本条有三个待答，请分别勾选）

**待答 ① 项目成员的授予入口在哪个模块**
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

**待答 ② `lead` 对自己创建但未加入的项目**
- [x] 采纳推荐
- [ ] 其它：______

**待答 ③ 角色变更立即生效的实现位置**
- [x] 采纳推荐
- [ ] 其它：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q5"></a>
# 🟠 Q-5 项目有没有「结束 / 归档 / 关闭」？结束后里面的东西怎么办？

> **推荐：B（两态 `active / archived`，`archived` = 只读，归档不删除任何内容）** ——
> 它是唯一一个能在 O-39 缺失的情况下仍然定稿的方案，且完全不碰 I-11 / X-4。
> 🔗 上游：**Q-2②**（归档时进行中的环节怎么办，须能被状态集合表达）。
> 🔗 下游：**Q-9**（选 A 则删除失去替代方案）、**Q-6 A5 / Q-7③**（同一种只读呈现）。

## 已有答案（2026-07-30 重查）
**部分。**
- **原型已答「这个状态存在」**：`PROTOTYPE-ANSWERS.md` Q-5 —— 项目列表卡片上有
  「**已归档**」标签（与「内部」并列的 tag）。⇒ 倾向支持候选 B。
- ⚠ **但仓库里所有「归档」裁决都不是项目的归档**，别误读为已定：
  · **O-10**（`DECISIONS-OPEN.md:279-302`，已裁）= **画布模板**归档；
  · **O-18**（`:498-526`，已裁）= **蓝本**的删除/回滚/归档/可见性；
  · **O-11**（`:303-327`，已裁）= **skill** 四态，`已归档` 是 `已停用` 的终态子类。
  三条都可作**形状参照**（尤其 O-18① 「已套用过的不可删除，只可归档」与本条推荐同向），
  但**没有一条说的是项目**。
- **连带四问全部未覆盖**：`PROTOTYPE-ANSWERS.md` Q-5 逐字记「原型只把『已归档』当一个展示标签，
  **没有演示归档后的任何行为**」。

## 现状
- `projects` 表**没有任何状态列**（`apps/api/migrations/0003-identity.sql:34-38`）。
- 对比：`organizations` 在 F22 里被加了 `status ∈ {active, disabled}`
  （`0014-f22-org-lifecycle.sql:50,68-69`），且冻结是做在 PG 的 **RESTRICTIVE 策略**里
  （`:165-184`），`projects` 是被冻结的表之一。
  ⇒ **项目已经有一种只读形态，但它来自组织层。**
- ⚠ **这条与两条已签不变量正面相接**：
  - `artifact` **I-11**：固定快照**不可删、不可改、不可降级**（`.../artifact/domain.md:102`）。
  - **X-4** 裁决（`design-coherence.md:101-113`）：**撤回删除是不可变原则的唯一豁口**，
    只有合规撤回能删快照，且必须同时作用于 S3 与 PG，下游**标失效而非静默消失**。
  - 且 X-4 依赖 **O-39 法定留存清单**，那份清单**不存在**
    （`COMPLIANCE-INQUIRY.md` Q-1；代码 `legalHoldCategories: {known:false}`，**取值抛错而非放行**）。

⇒ **任何「项目结束后清理/归档内容」的方案，在 O-39 给出之前都没有判据。**

## 候选

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **A** | **不做**项目生命周期（phase-01 内项目永远 active） | 不新增与 I-11/X-4 的接触面；phase-01 的目标是「跑完一场」，不是「归档一场」 | 「跑完」之后是什么状态答不上来；环节全 closed 的项目与刚建的项目在数据上无法区分；项目列表会无限增长 |
| **B** | **两态 `active / archived`**，`archived` = **只读**，内容一字不动（复用 F22 那套 RESTRICTIVE 策略的形状） | 与已落地的组织冻结同构，实现可复用且可断言；**完全不碰 I-11/X-4**（归档不删任何东西）；项目列表可分区 | 需要定义「归档能不能撤销」；需要定义归档时进行中的环节怎么办 |
| **C** | 三态 `active / archived / deleted`（含内容清理） | 覆盖数据留存诉求 | 🔴 **现在无法定稿**：`deleted` 撞 I-11，其豁口判据 O-39 缺失。选它等于把 phase-01 卡在一个外部输入上 |

## 我的推荐
**B**，且**明确写「归档不删除任何内容」**。理由：
① 它是唯一一个能在 O-39 缺失的情况下仍然定稿的方案；
② 冻结做在数据库策略层已有先例并已 passing（F22），复用形状比新造安全；
③ 「归档 ≠ 删除」这句话本身就是 X-4 豁口边界的正确一侧——豁口只留给**合规撤回**，
不该被"项目结束"这种日常动作借道。

## 连带必须一起答的
1. 归档**可逆**吗？（F22 的组织冻结是**不可逆**的：`0014-f22-org-lifecycle.sql:37`
   注释写「冻结它，`status` 就再也改不回 `'active'`」。项目若照抄这一点，代价大得多。）
2. 归档时**进行中的环节**怎么办（自动 closed？拒绝归档？）——与 Q-2 待答②耦合。
3. 归档项目里的 artifact 还能被**其它项目**下游引用吗？
   （I-14 只管"必须是 pinned"，不管"来源项目是否归档"。）
4. Context Pack 会不会召回归档项目的内容？
   （`packages/contracts/src/context-pack.ts:147` 的 `QueryContext.projectIds` 是数组，
   没有任何地方说它要按项目状态过滤。**这是一个真实的检索面缺口。**）

### 裁决
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

连带四问（选 B 时必答）：1 归档可逆？______　2 进行中的环节怎么办？______
3 归档项目的 artifact 还能被下游引用？______　4 Context Pack 召回归档内容吗？______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q6"></a>
# 🟡 Q-6 项目列表 / 项目主页要展示什么？

> **推荐：① B 两段式返回（响应体把「我在里面」与「我管着它」分开，不是混合数组加布尔）；
> ② 概览只放已有出处的东西，准备度百分比不在本域定义。**
> 🔗 纯下游：吃 **Q-4②**（`lead` 自建未加入的项目是否入列）、**Q-5**（归档项目怎么显示）、
> **Q-11**（要不要展示尚未绑定的产出）、**Q-12**（要不要按类型分区）四条的裁决。

## 已有答案（2026-07-30 重查）
**部分。**
- **待答 ②（项目主页显示什么）原型已答，且已有真实截图**：`PROTOTYPE-ANSWERS.md` Q-6 与
  `ui-preview/project/uc-00-2-overview-default.png` —— 项目在研究的问题（Serif 大标题）·
  状态条（`进行中 · 环节 3/7 · 就绪检查 3/3 ✓ · 12 人在场 · 09:00 开始`）·
  当前环节三角色分工卡 · 待办 3 项 · 最新动态时间线。
  ⚠ 原型作者**刻意没在概览用「准备度 %」**（口径是 uc-2-2 的 `[待确认]`），改用口径明确的
  「就绪检查 3/3」——本条下方推荐②与之一致，**不要在概览里编一个分母**。
- **待答 ①（`GET /projects` 返回谁的 + 是否两段式）未覆盖**：响应体设计，UI 天然看不出，
  决策文件亦无出处。

## 现状
- **已签核并已实现的板块只有一个**：`GET /projects/:projectId/backflow`
  （`packages/contracts/src/artifact.ts:333-339`），返回 `BackflowEntry[]`，
  字段与徽标是闭集（`:214-224`），草稿不出现，空态返回 `[]` 且**不生成伪数据**
  （`.../artifact/usecases.md:97`）。
- **没有** `GET /projects` —— 契约里不存在项目列表路由（我在
  `packages/contracts/src/*.ts` 全文搜过，`/projects` 只出现于 backflow 一处）。
- 顶栏形状已签核：两层身份条「顾问 · 能源组 ｜ 本项目：组长 · 第 2 组」
  （`.../web-kernel/usecases.md:127-128`），且**非项目页不得出现项目层 testid**
  （`web-kernel/domain.md:107`，有机械门控）。
- 项目内一级标签已在原型抽取里定过：`概览 ｜ 研究洞察 ｜ 项目筹备 ｜ 现场协作 ｜ 成果沉淀 ｜ 待办 ｜ 设置`
  （`.../02-tpl/uc-2-2-…md:311-312`）。
- 项目列表行的内容也有原型证据：`准备度 68% · 9/12 已确认 · 3 组 · 蓝本 商业模式共创 v3`
  （uc-2-2 `:337-338`），**但准备度的计算口径已被登记为 `[待确认]`**（uc-2-2 `:419-420`）。

## 待答 ①：`GET /projects` 返回谁的项目？

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **A** | 只返回**我持有项目角色**的项目 | 与 D-7/I-11 一致（无角色 = 项目内资源不可见）；不泄露组织内项目的存在性 | 组织角色 `lead` 创建后未加入的项目会从他自己的列表里消失（与 Q-4 待答②耦合） |
| **B** | 返回**我有项目角色的** + **我作为 `lead`/`admin` 可管理的**（两段，明确标注） | 管理视角可用；分段展示使「我在里面」与「我管着它」不混淆 | 需要明确：出现在列表里 ≠ 能进去看内容（D-18 边界） |

**推荐 B**，且**要求响应体把两段分开**（不是一个混合数组加 `canManage` 布尔）。
理由与 D-8（`projectLayer` 两种 null 不许合并，`identity.ts:260-272`）同型：
「我在这个项目里」与「我管着这个项目」是两种不同的东西，压成一个列表会让前端猜。

## 待答 ②：项目主页（`概览`）显示什么？
**推荐**只把**已有出处**的东西放进 phase-01 的项目概览：
当前议程环节（Q-2 定了才有）、四类项目角色的人数、回流列表（已签核）、
蓝本名与版本（uc-2-2 `:337`）。
⚠ **准备度百分比不要在本域定义**——它的口径是 uc-2-2 `:419-420` 已登记的 `[待确认]`，
在别处编一个分母就是第二份事实。

### 裁决
**待答 ① `GET /projects` 返回谁的项目**
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

**待答 ② 项目主页（概览）显示什么**
- [x] 采纳推荐
- [ ] 其它：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q7"></a>
# 🟡 Q-7 一个人能同时在多少项目里？项目能不能跨组织？

> **推荐：跨组织已由 I-1 答死（不可表达，非待定项）。剩余三问 —— ① 不设人均上限；
> ② 若确有跨组织诉求走导出/复制语义而非共享；③ 组织被停用时其项目显示且标注只读，不消失。**
> 🔗 ③ 与 **Q-5**（归档项目怎么显示）是同一种只读呈现，两处必须一致。

## 已有答案（2026-07-30 重查）
**部分，且主体已定 —— 本条只剩两个局部问。**
- **跨组织：已定**（I-1，见下方现状），非待定项。
- **多组织切换模型：已定** —— **O-12**（`phases/requirements/DECISIONS-OPEN.md:328-354`，已裁）：
  「① 一个账号 + 顶部组织切换器，切换时**清空全部项目级上下文**，权限按当前组织重新求值；
  **一个人可创建/归属多个组织**；**团队为单一归属**（组织内一人一队）。」
  ⇒ 下方待答 ② 的前半（「跨组织协作的诉求存在吗」）在**账号层面**已有答案：
  一人可跨组织，但**项目**不可跨组织。两者不矛盾，是不同层。
- **容量：已定但标注未实测** —— **O-34**（`:906-929`，已裁）：单项目在场 60 人、
  **组织并发项目 20**、单场音频 8 小时、单项目材料 5 GB、历史 3 年 × 200 项目，
  且逐字写「无本项目实测依据，需产品确认；确认前各 UC 不写性能类 AC，不阻塞开工」。
- **未覆盖的只剩两条**：① 人均项目数上限；③ 组织被停用时其项目在列表里的呈现。
  `PROTOTYPE-ANSWERS.md` Q-7 逐字记「一个人所属项目数上限在原型里没有任何体现——空白」。

## 现状
- **跨组织：已经答了，且是外键层面的**——`projects.org_id` 是单列非空外键
  （`apps/api/migrations/0003-identity.sql:35`），
  `acl_bindings` 不变量 **I-1**「subject 与 object 必属同一组织」
  （`.../identity/domain.md:84`，有 SQL 约束 + 跨组织插入失败断言）。
  ⇒ **项目跨组织在当前模型里不可表达。** 这不是待定项。
- **多组织归属已落地**（F22）：一账号多组织 + 顶部切换器（O-12），
  切换时**必须清空当前项目与当前环节**（`packages/contracts/src/identity.ts:516-521`）。
- **一人多项目**：`project_memberships` 主键 `(user_id, project_id)` 允许任意多行；
  uc-0-3 `:71` 明写「同一人可在多个项目持有不同项目角色」。**上限无出处。**
- 容量基线有一条**已裁决但标注未实测**的记录 **O-34**（`.../uc-2-2-…md:426`）：
  单项目在场 60 人、**组织并发项目 20**、单场音频 8 小时、单项目材料 5 GB、
  历史 3 年 × 200 项目；且逐字写着「⚠ **无本项目实测依据，需产品确认**；
  确认前各 UC **不写性能类 AC**，不阻塞开工」。

## 待答
1. **要不要给"一人所属项目数"设上限？**
   - **推荐：不设。** 没有任何已知业务约束要求它，而一个编出来的上限
     会变成 `thresholds.ts` 里又一个 `{known:false}`。
     容量问题由 O-34 的组织并发项目数（20）承接，那是**组织**维度不是**人**维度。
2. **跨组织协作的诉求存在吗？** 若存在，请明说——因为满足它要改 I-1，
   那是内核级改动，不是项目域能吸收的。
   - **推荐**：若确有诉求，走**导出/复制**语义而非共享语义——
     phase-00 已有现成先例：`exportToOrganization` 是**单向 + 复制而非迁移 + 必须先预览确认**
     （`packages/contracts/src/identity.ts:703-720`）。
3. **组织被停用（F22）时，其项目对成员显示成什么？**
   已知：数据库层冻成只读（`0014-f22-org-lifecycle.sql:165-184`）。
   未知：项目列表里是消失、还是显示为只读。
   - **推荐：显示且标注只读**。消失读作「数据没了」，那是 F16 处理"隐藏 vs 禁用"时
     已经立过的规矩（`packages/contracts/src/identity.ts:139-141`：
     「藏起来读作『产品做不到这件事』，而真相是『这个组织不允许』」）。

### 裁决
**① 人均项目数上限**
- [x] 采纳推荐（不设）
- [ ] 其它：______

**② 跨组织协作诉求**
- [x] 采纳推荐（无诉求；若有则走导出/复制语义）
- [ ] 其它：______

**③ 组织被停用时其项目的呈现**
- [x] 采纳推荐（显示且标注只读）
- [ ] 其它：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q8"></a>
# 🟠 Q-8 `STEP_CLOSED` / `STEP_REJECTS_ARTIFACT_TYPE` 的判据由谁给？何时补上？

> **推荐：1. 是本域交付责任；2. 给 `artifact_bindings.step_id` 加外键（新增 `0016-*`，
> 不改已 passing 的 `0008`）；3. 补齐时必须同时交两条反证断言。**
> 🔗 纯下游：若 **Q-2①** 选 B（不建环节表），本条只剩「承认这两个失败码空转」一个答案。

## 已有答案（2026-07-30 重查）
**未覆盖。** 决策文件（`DECISIONS-FINAL` / `-OPEN` / `-UI-ROUND` / `-CONTRACT-ROUND`）无任何相关裁决；
`PROTOTYPE-ANSWERS.md` Q-8 逐字「纯后端失败码，运行态 UI 无对应提示。空白」，
并指出它与 Q-2③ 同源（原型的环节没有 `acceptedSources` 白名单控件，所以拒绝判据也无处显现）。
⇒ 本条是**纯下游**：Q-2① 定了才有落点。

## 现状
两个失败码在已签核契约里（`packages/contracts/src/artifact.ts:112-113`，
`.../artifact/usecases.md:20-21`），
而 `0008-f06-binding-modes.sql:26-31` 明确记着它们**今天不可评估**，
并且拒绝用「一个永远说 open 的可空查表」去假装覆盖：

> a check that cannot fail is worse than an absent one, because it reads as coverage.

⚠ 这正是本仓记录过九次的「全绿但空转」形状（用户记忆条目：workspacex 门控反证纪律）。

## 待答
1. 补齐它们是不是**本域（00-project）的交付责任**？
   - **推荐：是。** 它们的判据全部落在环节实体上（Q-2），别的域都拿不到。
2. 补齐时**要不要给 `artifact_bindings.step_id` 加外键**？
   - **推荐：加**。不加则「绑到一个不存在的环节」仍然合法，`STEP_CLOSED` 只能靠应用层自觉。
   - ⚠ 代价诚实说明：这要动一个 **phase-00 已 passing 的迁移的邻接面**（新增 0016 而非改 0008），
     且 phase-00 现有绑定测试需要建环节夹具。
3. 补齐后**必须同时交一份反证**：构造一个 closed 环节去绑定，断言拿到 `STEP_CLOSED`；
   构造一个白名单不含该来源的环节，断言拿到 `STEP_REJECTS_ARTIFACT_TYPE`。
   两条断言若都跑不红过，说明门仍然是空的。

### 裁决
- [x] 采纳推荐（三条一并采纳）
- [ ] 选 __ （候选编号）
- [ ] 其它：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q9"></a>
# 🟡 Q-9 「删除项目」提供吗？

> **推荐：不提供，并照抄 N-5 —— 把 `DELETE /projects/*` 加进 `no-forbidden-routes.test.ts`
> 的禁止清单**（「没有」这件事本身没人会去验）。
> 🔗 纯下游：其成立性依赖 **Q-5** 选 B（由归档承接「这个项目不再用了」）。

## 已有答案（2026-07-30 重查）
**未覆盖 —— 但有一条「先例未推广」。** N-5 只裁了「不提供删除**组织**」（见下方现状），
从未有人把它推广到项目。`PROTOTYPE-ANSWERS.md` Q-9 逐字：项目卡片右下有「⋯ 更多」与「看产出」，
但**未在运行态里找到「删除项目」入口**——「这既不证明『提供』也不证明『不提供』，只是空白」，
作者只把它记为「不提供删除」的**弱旁证**。⚠ 不要把「原型没画」读成「已裁不做」
（`DECISIONS-FINAL.md:115` 的通用规则第 2 条：**不得把「未探明」写成「原型没做」**）。

## 现状
- **有现成先例**：N-5 已裁决**不提供「删除组织」API**
  （`design-coherence.md:209`），并补了 `no-forbidden-routes.test.ts`
  断言路由表里确实没有它——理由逐字是「**『没有』这件事本身没人会去验**，
  某天有人为别的需求加上，不会有任何东西报警」。
- 但 `projects` 上有真实的级联：`groups`、`project_memberships`、`acl_bindings`、
  `artifact_bindings` 均 `ON DELETE CASCADE` 到 `projects`
  （`0003-identity.sql:43,62`；`0008-f06:44`）。
  ⇒ **如果谁加上删除，级联会静默清掉绑定行。**
  ⚠ 而 `0008-f06:191` 的 BEFORE DELETE 触发器已经预见到这一点：
  「Deleting the parent artifact or project reaches this row by cascade and is refused for the same reason.」

## 候选 / 推荐
**推荐：不提供，并照抄 N-5 的做法**——把 `DELETE /projects/*` 加进
`no-forbidden-routes.test.ts` 的禁止清单。
理由：① 与组织的处置一致；② 数据库已经有一个触发器在替我们挡这条路，
但那是**防御**不是**规范**，规范要写在路由表的断言里；
③ 归档（Q-5 方案 B）已经覆盖了"这个项目不再用了"的真实诉求。

⚠ 若人类选择「提供删除」，那么它必须与 X-4 的豁口边界一起裁决
（`design-coherence.md:101-113`），因为删项目会连带触及快照——
而快照的删除权目前**只授予合规撤回**。

### 裁决
- [x] 采纳推荐（不提供删除 + 加进 `no-forbidden-routes.test.ts`）
- [ ] 其它：______（若选「提供删除」，须与 X-4 豁口边界一并裁决）

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q10"></a>
# 🟡 Q-10 「不属于任何项目」的内容，归属与生命周期是什么？

> **推荐：A（把 `readContent.in.projectId` 放开为 `nullable`），并作为契约缺陷报告提给
> artifact / identity 两束的签核人，而不是由实现者顺手改掉。**
> 🔗 与 **Q-11 连带 1 是同一个洞，请两条一起裁**：Q-11 选 ② 会强制本条在 A / B 之间二选一，
> 本条的 C 只在 Q-11 选 ①（不建议）时才成立。

## 已有答案（2026-07-30 重查）
**未覆盖。** 决策文件无出处。原型只答了半边：`PROTOTYPE-ANSWERS.md` Q-10 ——
项目知识图谱（`isSetGraph`）的**个人层带**里，「**我的笔记 · 未升到项目**」与「我的偏好 · 已开启」
是一等可见节点，组织层还有「复用自中盛项目」的跨项目节点。
⇒ 「无项目 / 未升级的个人层内容**存在且要能被看见**」有原型证据；
但「`readContent.in.projectId` 非空导致这类内容没有合法读取路径」是**契约内部的紧张关系**，
原型作者逐字写「UI 层看不见——修不修契约是 Q-10 的核心，原型不能答」。

## 现状（一条已签核契约内部的紧张关系）
- `Artifact.projectId` **可空**：`packages/contracts/src/artifact.ts:126-130`
  「Studio 可独立发起、不依赖项目（A1）」；
  `saveDraft.in.projectId` 亦为 `nullable`（`:255`）；
  Context Pack 同形（`context-pack.ts:253`，用例 `.../context-pack/usecases.md:43`）。
- 但唯一的内容读取面 `readContent` 的 `in.projectId` 是 **`z.string()`，非空**
  （`packages/contracts/src/identity.ts:443-450`），
  而 `design-coherence.md:394` 强调它是**唯一**读取面、个人层与项目层共用这一道门。

⇒ 按字面：**一份 `projectId = null` 的 artifact，没有合法路径被读回来。**

## 候选

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **A** | 把 `readContent.in.projectId` 放开为 `nullable` | 一处小改；与 `authorize.in.projectId?`（`identity.ts:382`）一致 | 改的是**已签核束**；且要同时回答「没有项目时，两层交集的项目层怎么判」——答案应是 `projectLayer = null`（I-11），即只走组织层 + 个人层 |
| **B** | 规定「无项目内容只存在于**个人层**」，读取走 `layer` 分流而非 `projectId` | 不改签核契约；与 `ContentLayer`（`identity.ts:210`）的既有语义吻合 | 那就等于说 Studio 独立产出**永远是私人的**，而 A1 的原话只说「不依赖项目」，没说「私人」 |
| **C** | 要求 artifact 必有项目（取消可空） | 模型最简 | ❌ 与 `artifact.ts:126-130`、`context-pack/usecases.md:43` 两处已签核文本直接冲突。**不建议** |

## 我的推荐
**A**，并把它作为一条**契约缺陷报告**提给 artifact / identity 两束的签核人，
而不是由实现者顺手改掉。理由：本仓已经有两次「实现时发现契约表达不了它自己声明的状态」
的处理先例，两次都是**报上去改契约**而不是在实现里绕
（`PermissionDecision.orgLayer.role` 放开为可空：`identity.ts:246-256`；
`CapabilityListing.endpoint` 补字段：`identity.ts:287-300`）。

⚠ 另附：这也牵动 Q-5——若项目可归档，那么「归档时把内容留在项目里」与
「内容本来就可以没有项目」是两种不同的收尾语义，请一并确认。

### 裁决
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q11"></a>
# 🟠 Q-11 项目与 Studio 是什么从属关系？

> **推荐：②（Studio 一等，可选关联项目）** —— 理由不是偏好，是三处已签核文本
> （artifact 字段可空 / saveDraft 前置 / context-pack 范围）**只在 ② 下成立**。
> 🔗 上游：**Q-12**（若 Q-12 选 A/B/**E**，本条候选 ③ 直接出局——E 下「研究项目」是树上的位置，不是第三方实体）。
> 🔗 本条**原型已答**：`ui-preview/project/PROTOTYPE-ANSWERS.md` Q-11 逐字给出两条 `[原型]` 证据
> （Studio 与项目在左侧导航**平级分段**；项目内又能「＋ 发起访谈 / 发起研究」并回流），
> 与三处已签契约同向 ⇒ **候选 ② 的形状已被运行态证实，本条降为「确认 + 答连带三问」。**
> 🔗 下游：**Q-10**（同一个洞）、**Q-6②**（要不要展示尚未绑定的产出）。

> 来源：并行的 `itv` UI 原型 agent。它对着 UC-6.0 得出「**访谈 Studio 是一等对象、独立发起、
> 不是项目的下级**」，并据此把导航重构为 `/studio/interview`。
> 这与「项目是顶层容器、一切挂在项目下」的隐含假设不一致，需要在**项目实体**这一侧定音。

## 现状（先分清"已规定"和"未规定"，详见 `DERIVED-FROM-CONTRACTS.md` D-17b）

**已签核规定了的**（不可改）：
- `Artifact.projectId` **可空**，注释逐字写「**Studio 可独立发起、不依赖项目**（A1）」
  （`packages/contracts/src/artifact.ts:126`；`.../artifact/domain.md:22`）。
- `saveDraft` 前置条件：「用户在**四个 Studio 之一**有可保存内容（**无项目归属也可**，A1）」
  （`.../artifact/usecases.md:42`）。
- 「不属于任何项目」在 Context Pack 里是**一等范围**，项目层为空只装组织层+个人层
  （`.../context-pack/usecases.md:43`）。
- 事后挂载的路径已签核**且已实现**：`bindToProjectStep`
  （`packages/contracts/src/artifact.ts:293-313`，F06 passing）。
- `ArtifactSource` 里 `"prototype-run" // 原型 Studio 产出`（`artifact.ts:31`）
  ——**Studio 是产出的来源，不是产出的容器**。
- 原型侧同向：UC-6.0 副标题原文「**访谈 Studio · 独立发起，不依赖项目**」，
  范围切换器里「**不属于任何项目**」是一等选项而非兜底
  （`.../06-itv/uc-6-0-访谈-studio-列表与范围.md:12,14,55,138-139`）；
  底部原话「产出留在研究项目 …；**需要时再挂到某个项目环节**」（`:76`）。

**未规定的**：`Studio` 在 `packages/contracts` 里**不是任何实体**——
全文只在 4 处注释出现，没有字段、没有路由、没有枚举
（`artifact.ts:11,31,126`；`context-pack.ts:11`）。
⇒ **「Studio 的地位」从未被规定，不是被否定。**

## 候选

| | 方案 | 优点 | 缺点 / 风险 |
|---|---|---|---|
| **①** | **Studio 从属项目**（必须先有项目才能开 Studio） | 导航简单；一切内容都有项目归属，权限判定只有一条路径 | ❌ **与已签契约直接冲突**：`projectId` 可空、`saveDraft` 明写「无项目归属也可」、`context-pack` 有「不属于任何项目」范围。选它等于回退三处已签核文本。**不建议** |
| **②** | **Studio 一等，可选关联项目**（`projectId` 可空 + 事后 `bindToProjectStep`） | **与已签契约逐条吻合**，零回退；原型证据同向；F06 已实现的挂载路径正好是"事后归属"的那条路 | 需要回答两件事：⑴ 无项目产出的**权限**靠什么判（项目层为空时只剩组织层+个人层）；⑵ 项目**列表/概览**要不要显示"尚未归属"的产出 |
| **③** | **两者平级，由第三方概念（如"研究项目"）关联** | 能同时表达"访谈属于研究项目、也能挂到业务项目环节" | 引入第三个顶层实体，权限模型从两层交集变成两层 + 第三方归属；⚠ 与 `.../identity/domain.md:54`「三者最终都归结为 `acl_bindings`，**不另建三套权限表**」相冲；且它与 Q-12 强耦合，不宜单独裁决 |

## 我的推荐
**②**。理由不是偏好，是**它已经被签核了**：三处已签文本（artifact 字段、saveDraft 前置、
context-pack 范围）都只在 ② 下成立，而 ① 需要推翻它们、③ 需要新增一层。

## 连带必须一起答的
1. **无项目归属的 artifact 怎么被读回来？** ⚠ 这是一个**真实的契约缺陷**：
   唯一的内容读取面 `readContent.in.projectId` 是 `z.string()` **非空**
   （`packages/contracts/src/identity.ts:443-450`）——按字面，`projectId = null` 的产出
   没有合法读取路径。**这与 Q-10 是同一个洞**，请两条一起裁。
2. **Studio 产出进项目的路径唯一吗？** 已签的路径是 `bindToProjectStep`（三模式，
   只有 `pinned` 可被下游引用，I-14）。**推荐明确写"唯一"**，
   并像 N-5 那样加一条断言「不存在第二条挂载路由」
   （`design-coherence.md:209` 的做法）——否则各 Studio 会各造一条。
3. **项目概览要不要展示"本项目相关但尚未绑定"的产出？** 我倾向**不展示**：
   未绑定就是未归属，展示它等于制造一个没有绑定行支撑的第二份"项目内容"清单。
   但这是产品判断 → 与 Q-6② 一并答。

### 裁决
- [x] 采纳推荐
- [ ] 选 __ （候选编号）
- [ ] 其它：______

连带三问：1 无项目归属的 artifact 怎么读回（与 Q-10 同裁）：______
2 Studio 产出进项目的路径唯一吗：______　3 概览是否展示未绑定产出（与 Q-6② 同裁）：______

裁决人：yanbin shen   日期（ISO）：2026-07-30 09:19:24+08:00

---

<a id="q12"></a>
# 🔴 Q-12 「研究项目」与「业务项目」是两种项目，还是项目上的一个维度？

> **推荐：E（项目自引用的可选父项目 `parent_project_id`）。**
> ⚠ **推荐已于 2026-07-30 改过**：原推荐 A（`projects.kind`）**撤回**。
> 原因不是偏好变了，是**候选集变了**——运行态原型给出的第五种形状
> （`ui-preview/project/PROTOTYPE-ANSWERS.md` Q-12）不在原来的 A/B/C/D 里，
> 而它用**更少的机件**解释了**更多的证据**。撤回理由见下方「推荐为什么改了」。
> 🔗 **本文最上游的一条**：它的答案会改变 **Q-1**（各级项目是否都套蓝本）、
> **Q-2**（环节挂在哪一级 / 研究项目有没有议程环节）、**Q-6①**（列表是否按层级折叠）、
> **Q-11**（选 A/B/E 则 Q-11 的候选 ③ 出局）四条的候选集合。

> 来源：同上。原型 agent 报告「一场访谈能否**同时**归属研究项目 + 业务项目」在两处措辞里打架，
> **数据模型是一个还是两个可空外键，UC 没定**，它只好两级并存渲染并明确标注「无法自洽」。
> 它同时指出这是 **F80「访谈范围模型：project_id 可空 + 两种权限投影 + 服务端过滤」**（3 点）的地基。

## 现状
- **已登记为 `[待确认]`**：`.../06-itv/uc-6-0-访谈-studio-列表与范围.md:216-218` 逐字——
  「一场访谈能否**同时**归属研究项目与业务项目（原型两处措辞不同：访谈列表的范围是
  『研究项目 · 采购决策如何形成』，而行内显示的所属是『欧洲进入策略』——
  这暗示**两级归属并存**，需确认数据模型是一个外键还是两个）」。
  ⇒ 本文**不重复提问**，而是把它**升级**：因为它决定的不只是访谈的字段，
  是**项目实体上要不要有"项目类型"这个维度**。
- **契约侧完全没有**：`projects` 表只有 `id / org_id / name`
  （`apps/api/migrations/0003-identity.sql:34-38`），
  `packages/contracts` 里不存在任何 `ProjectKind` / `projectType` 之类的东西。
  对比：组织**有** `kind`，且它被写成「**一等字段，不是特例分支**」
  （`packages/contracts/src/identity.ts:23`、`.../identity/domain.md:19,22-23`）。
- uc-6-0 的核心数据对象里，**「研究项目」与「项目」是并列列出的两个东西**
  （`.../uc-6-0-…md:30`：「访谈场次（`project_id` 可空）、研究项目、项目、项目环节…」）。

## 🔴 2026-07-30 新增证据：运行态原型用的是**第五种形状**

出处：`phases/phase-01-run-a-project/ui-preview/project/PROTOTYPE-ANSWERS.md` Q-12
（`project` 域 UI-prototyper 对着运行态原型 `WorkspaceX Standalone.html` 的 `wsDetailView` 逐条核验）。
逐字两条：

- `[原型]` **运行态里没有任何「项目类型 / kind」维度**：没有「研究项目 vs 业务项目」切换器，
  项目列表卡片上的「内部 / 已归档」是**标签(tag)不是类型分区**。
- `[原型]` 新建项目「2 主题与时长」步骤有一个字段逐字写着
  > 「**挂到哪个项目（决定能读哪些洞察与图谱）：远洋新能源 · 欧洲市场进入 ▾**」

  而正在新建的项目名叫「**欧洲进入策略 · 定题项目**」。
  ⇒ 运行态出现的是「**一个项目挂到另一个（更大的）项目下**」的父子归属，
  「定题项目」是子、「欧洲市场进入」是父。

⚠ 关键在于它**同时解释了 itv agent 的观察**：itv 看到「访谈范围 = 研究项目（采购决策如何形成），
行内所属 = 业务项目（欧洲进入策略）」——在 project 原型里这两级正是**父项目 ⊃ 子项目**。
也就是说，「研究项目 / 业务项目」不是两种**类型**，而是同一棵树上的两个**位置**。

## 候选

| | 方案 | 数据形状 | 优点 | 缺点 / 风险 |
|---|---|---|---|---|
| **A** | **一种项目 + `kind` 维度**（`projects.kind ∈ {research, delivery}`），访谈**只有一个** `project_id` | 一个枚举列 + 一个可空外键 | 与组织 `kind` 是**同一种手法**，而那条已经被写成纪律：「共用同一张表、同一套 ACL/RLS 机制。**不做成两条代码路径**——第二套路径必然长期失修」（`.../identity/domain.md:22-23`）；权限、列表、环节全部只有一套 | ⚠ **原型证据不支持**：运行态里**找不到任何 kind 维度**；且「同时归属两者」表达不了 |
| **B** | **一种项目 + 两个可空外键**（访谈持 `research_project_id` 与 `delivery_project_id`） | 两个可空外键 | 能表达"两级并存"，正是 itv 侧渲染出来的样子 | ⚠ 两套可见性投影要各自正确（F80 标题里的「两种权限投影」就是它）；且**两级之间的关系无处表达**——「研究项目 A 是业务项目 B 的子项目」这件事在 B 里说不出来，只能靠每条访谈重复声明两个外键 |
| **C** | **两个独立实体**（`research_projects` 与 `projects` 两张表） | 两张表 | 概念最清晰 | ❌ 权限模型要复制一份到新表（RLS、acl_bindings 的 `object_kind` 闭集要加值、四种项目角色要不要也适用…）。`acl_bindings.object_kind` 是 `('project','artifact','segment')` 闭集（`0003-identity.sql:80`；`identity.ts:383`），加第四种是**修订已签束**。**不建议** |
| **D** | **不引入类型**，「研究项目」只是命名习惯 | 无变化 | 零成本 | 与 uc-6-0 的两处措辞、范围切换器的档位对不上；等于宣布两个原型 agent 观察到的差异是幻觉——**证据不支持** |
| **E** ⭐ | **项目自引用的可选父项目**：`projects` 加一列 `parent_project_id text NULL REFERENCES projects(id)`；访谈仍**只有一个** `project_id`（指向它实际所在的那一级），"另一级"由这条边推导 | 一个可空自引用外键 | ① **原型逐字就是这个**（「挂到哪个项目」）；② **一个机件解释两侧证据**：itv 的「两级归属」= 树上的父与子，无需 `kind`、无需第二个外键、无需第二张表；③ **不碰 `acl_bindings.object_kind` 闭集**（仍是 `project`）；④ 两级复用**同一张表、同一套 RLS、同一套四种项目角色**，正好满足 `identity/domain.md:22-23` 那条「不做成两条代码路径」的纪律；⑤ 「研究项目」不再需要作为概念存在，`interview/domain.md` 里唯一那处「研究项目」可以收敛成「父项目」 | ⚠ **三个全新问题，A/B/C/D 都没有**：<br>⑴ **权限沿父子边怎么走**——原型那句括注「**决定能读哪些洞察与图谱**」意味着**子项目从父项目继承读范围**，而 I-13 / I-7 逐字规定可见性沿链路**只收紧不放宽**（`.../artifact/domain.md:104`、`.../identity/domain.md:90`）。**这是一处真实冲突，必须显式裁决**（见下方三件套的第 1 条）；<br>⑵ **环形与深度**：自引用外键需要防环（`A→B→A`）与限深，本仓无先例；<br>⑶ **同组织约束**：父子必须同 `org_id`，否则 I-1「subject 与 object 必属同一组织」被自引用边绕过（需一条 CHECK 或触发器，形状可照抄 I-1 的断言法）。<br>另：`projects` 上现有的 `ON DELETE CASCADE` 邻接面会**多一条级联路径**，与 Q-5 / Q-9 连带 |

## 推荐为什么改了（原推荐 A 撤回）

**新推荐：E。** 三条理由，都不是偏好：

1. **原推荐 A 是从 uc-6-0 的两处措辞**反推**出来的，而现在有了直接证据，且直接证据说的是别的。**
   A 与 D 都是在「只有 itv 的文字线索」时构造的候选；运行态原型是更强的证据层级
   （本仓四标记法里 `[原型]` 高于 `[设计]`），而它**明确否证了 kind 维度的存在**。
   继续推荐 A 就是拿一个反推结论压过一条直接观察。
2. **E 用更少的机件解释更多的证据。** A 解释不了「挂到哪个项目」这个字段；
   B 解释不了「父子关系本身」；E 一条可空自引用外键同时解释 project 侧与 itv 侧两处观察。
3. **E 与 A 共享 A 的全部优点，却不引入 A 的缺点。** A 之所以被推荐，理由是
   `identity/domain.md:22-23` 那条「共用一张表 + 一套 RLS，不做两条代码路径」——
   **E 同样共用一张表和一套 RLS**（父与子都是 `projects` 行、都走 `acl_bindings`、
   都用同一套四种项目角色），所以那条纪律 E 也满足。而 A 额外背上了一个
   **在运行态里找不到的枚举列**。

⚠ **但 E 不是免费的，且它带来一处与已签不变量的真实冲突**（上表风险 ⑴）。
所以推荐 E 的同时，我**不推荐**照原型那句括注去实现「子继承父的读范围」——
见下方必须一并裁决的第 1 条。若人类判定「父子边必须放宽读范围」，
那么 E 与 I-13 二者必有一个要改，**那是签核动作，不是实现动作**。

## ⚠ 无论选哪个，请一并裁决三件事（否则 F80 仍然做不下去）

1. **两级归属的可见性是交集、并集，还是「父子边不传播权限」？**
   - **推荐：父子边不传播读权限**——即 `parent_project_id` 只是**组织/导航归属**，
     不是权限边。子项目要用父项目的洞察，走**已有的**跨项目引用路径
     （引用必须指向 `pinned` 快照，I-14；`.../artifact/domain.md:105`）。
   - 理由：I-7 逐字写「取**所有来源中最严格**的一档（不是最宽松，也不是并集）」
     （`.../identity/domain.md:90`），I-13 写可见性沿数据链路传播且**只收紧不放宽**。
     一条「让子项目能读父项目内容」的边**天生是放宽**，与两条已签不变量同时冲突。
   - ⚠ **但原型那句括注「决定能读哪些洞察与图谱」字面上要的正是放宽。**
     这不是我能合并的：**要么承认原型那句是 UI 措辞（推荐），要么修订 I-7 / I-13（签核动作）。**
     请明确选一边，不要留着。
2. **议程环节挂在哪一级？**（父、子、还是两级都可有）
   ——它决定 Q-2 的环节实体是不是要对「有子项目的项目」可选，
   也决定 `bindToProjectStep` 的 `(projectId, stepId)` 里那个 `projectId` 指哪一级。
3. **项目列表要不要按父子层级折叠？** → 与 Q-6① 的两段式返回一并答。
   （E 下这变成「树 vs 平铺」，与 A 下的「按类型分区」不是同一个问题。）

**选 E 时另加两问**（上表风险 ⑵⑶，不答则无法建列）：
4. **父子层级深度上限**是多少（1 层？无限层 + 防环？）
5. **父子必须同组织**这条要不要落成 DB 约束（推荐要，形状照抄 I-1 的跨组织插入失败断言）

## 波及面（提醒人类：这条便宜不了）
「项目上多一个维度」（无论是 `kind` 还是 `parent_project_id`）会同时波及：
项目列表（Q-6）、权限判定（两级归属的投影）、环节定义（Q-2）、
创建路径（Q-1，各级项目是否都套蓝本）、`bindToProjectStep` 的目标项目校验，
**选 E 时还额外波及** `groups`（小组挂哪一级）与 `projects` 的级联删除路径（Q-5 / Q-9）。
⇒ 这就是我把它标为 🔴 阻塞开工的原因：它比 Q-1 更靠上游。

### 裁决
- [ ] 采纳推荐（**E**）
- [x] 选 C （候选编号 A / B / C / D）

**裁决理由（yanbin shen，2026-07-30，逐字记录）：**

> 研究项目和项目是完全不同的两种项目，没有必要把他们放在一个表单里面。
> 目前项目指的是可以套用工作坊模板/项目模板的项目；研究项目只是针对一个研究主题，
> 需要拆解出来一个研究计划，然后做深度研究的项目，他们的过程和目的完全不同。
> 还有一个是用户洞察，也是一种独立的项目。

⚠ **三种而不是两种。** 本条裁决把候选 C 从「两张表」扩为**三类独立实体**：
`项目`（套用工作坊/项目模板）· `研究项目`（研究计划 → 深度研究）· `用户洞察`。

### 本条裁决的连带工作（必须一并处理，否则 C 落不了地）

1. **`acl_bindings.object_kind` 闭集要加值。** 现为 `('project','artifact','segment')`
   （`0003-identity.sql:80`、`identity.ts:383`）。三类实体意味着要加 **2 个值**而不是 1 个。
   ⚠ **这是修订已签核的 phase-00 `identity` 束**，属签核动作，不是实现细节。
2. **四种项目角色（facilitator/groupLead/member/observer）是否适用于研究项目与用户洞察？**
   若适用 → `project_memberships` 的形状要跟着变；若不适用 → 各自的成员模型要单独定义。
   **未裁决。**
3. **RLS 与租户隔离要复制到新表**，且 `verify-rls.sh` 从 catalog 推导租户表，
   新表会被自动纳入——这是好事，但意味着三张表都要带 `org_id` 且策略一致。
4. **`Artifact.projectId` 指向哪一类？** phase-00 已签契约里它是单一外键。
   三类实体意味着要么三个可空外键，要么一个多态引用（后者与 `object_kind` 闭集同型）。
   **未裁决，且它影响 phase-00 已 passing 的 F04/F06/F07。**
5. **访谈挂在哪一类**——人类同日指出访谈的 UI 与原型完全不同，
   `itv` 域正在重做（`ui-preview/itv-v2/`），会顺带核实这一点。

### 人类同日追加的两条（2026-07-30，逐字记录）—— 它们答掉了上面的连带 2

> 在访谈里是没有这几种角色的，引导师、组长什么的是不必要的。
> 我们现在的项目主要的就是工作坊的管理，工作坊模板，然后工作坊的执行过程中的
> 之前、当中和之后的 AI 增强。

**⇒ 连带 2 已答：四种项目角色（facilitator / groupLead / member / observer）
是「工作坊」的角色，不适用于研究项目与用户洞察。** 后两类各自的成员模型需单独定义（仍未裁）。

**⇒ 「项目」的定位收窄为「工作坊」**：工作坊模板 + 执行过程**会前 / 现场 / 会后**三段的 AI 增强。
这条同时收窄了 `project` 束 `domain.md` 的范围，也解释了为什么议程环节、分组、
现场协作这些概念全部围绕工作坊——它们本来就是工作坊的机件，不是「一切项目」的通用机件。

⚠ **既有材料里有两处与这条不符，需处置**：
- `ui-preview/itv/`（v1，44 张）把项目的四角色套到了访谈上——已由 `itv-v2` 推翻，
  错误逐条记在 `ui-preview/itv-v2/V1-WAS-WRONG.md`。
- 人类提供的原型截图里，访谈屏的视角切换器仍渲染着「引导师 / 组长」。
  **以人类原话为准**（这两个角色不该存在），原型该处按遗留处理。
- [ ] **原型那句「挂到哪个项目」只是 UI 措辞，后端另有模型**：______
- [ ] 其它：______

必须一并裁决的三件：1 父子/两级归属的权限语义（交集 / 并集 / **不传播**）：不传播
2 议程环节挂在哪一级：项目
3 项目列表按层级折叠还是平铺（与 Q-6① 同裁）：平铺

选 E 时另加：4 层级深度上限：______　5 父子同组织落成 DB 约束？：______

裁决人：yab'bin   日期（ISO）：______

---

# 附：本文没有重复提问的、已登记在别处的待确认项

以下问题**属于项目域但已有登记处**，本文不重复提，只标记它们阻塞本域哪个 UC：

| 事项 | 登记处 | 阻塞 |
|---|---|---|
| 空项目能否事后补套蓝本、已有内容如何合并 | `.../02-tpl/uc-2-2-…md:418` | `uc-00-1` R4 |
| 准备度百分比的计算口径 | `.../02-tpl/uc-2-2-…md:419-420` | `uc-00-2` R3 |
| 中途换工作流模板的处置策略（A5） | `.../02-tpl/uc-2-2-…md:396-397` | `uc-00-1` R4 |
| 矩阵格的内容粒度（一格一条待办还是多条） | `.../02-tpl/uc-2-2-…md:416-417` | 不阻塞本域 |
| O-39 法定留存清单 | `phases/requirements/COMPLIANCE-INQUIRY.md` Q-1 | Q-5 的方案 C |
| 容量基线（组织并发项目 20 等）需产品确认 | `.../02-tpl/uc-2-2-…md:426`（O-34） | Q-7 |
| 一场访谈能否同时归属研究项目与业务项目 | `.../06-itv/uc-6-0-…md:216-218` | **已升级为 Q-12**（它改的是项目实体，不只是访谈字段） |
| 访谈范围切换器的完整档位 | `.../06-itv/uc-6-0-…md:214-215` | Q-12 的下游，本域不重复提 |
