# 契约束 `asset-governance` — 支撑材料②：UC 覆盖证明

> **这一件回答的问题**：前面三件定的接口，**真的够跑通业务吗？**
> 领域模型再漂亮、API 再整齐，只要有一条 UC 的验收线索找不到对应接口，业务就是跑不通的。
>
> 覆盖 feature：**F132 … F143**（12 个 / 61 点）。
> ⚠ **这一行是派生视图，不是权威。** 权威是 `design-signoff.md` frontmatter 的
> `covers:`（ADR-023 决策三）。
>
> 依据 UC：`23-asset/uc-23-1` … `uc-23-8`（**8 份**）。
> R12 验收线索合计 **108 条**（机械重数 2026-07-31）：
> `uc-23-1: 14 · uc-23-2: 15 · uc-23-3: 15 · uc-23-4: 14 · uc-23-5: 14 · uc-23-6: 13 · uc-23-7: 12 · uc-23-8: 11`。
> ⚠ 本文上一版头部写的是 **111**，与逐份重数不符（14+15+15+14+14+13+12+11 = **108**）。
> 三处旧文里的「111 条」是同一个错数的派生 —— 已改，登记为 ⚠ **缺口 14**（口径修正，非待裁）。

## 怎么读这张表

**两个方向都要查，缺一个方向就是白查**（`contract-design.md` 硬规则）：
- **UC → API**：某条验收线索找不到对应 API ⇒ **接口不够，业务跑不通**（第一~四节）
- **API → UC**：某个 API 操作没有任何 UC 要它 ⇒ **接口是多余的，或有 UC 没写**（第六节）

「状态」列是**缺口的名字**，编号见第七节。
⚠ **本文没有为了消红给任何一行填假落点**（纪律第 10 条）：
55 条随 D-06 留 phase-2 的线索**逐行具名为「⚠ 缺口 P2」**，
而不是编一个本 phase 根本不存在的端口。

---

## 🔴 本文的范围由 Q-0 的裁决定死

`requirements/23-asset/DECISION-Q0.md`（2026-07-30，**方案 C：拆**）：

| 能力 | 归属 |
|---|---|
| 资产目录浏览与编辑（`uc-23-3`）· 治理与发布（`uc-23-4`）· 复核到期与降级（`uc-23-6`）· 后台外壳与左栏 IA（`uc-23-8`） | **phase-1**（本束覆盖，53 条 R12） |
| 外部社区导入（`uc-23-1`）· 落地检查六道关（`uc-23-2`）· 沙箱试跑（`uc-23-5`）· 社区源（`uc-23-7`） | **phase-2**（随 D-06，55 条 R12） |

该裁决的「连带处置」第 2 条逐字要求：
> `coverage.md` 保持「覆盖缺口清单」形态，不做假的覆盖表 ——
> 那 **55 条**依赖 phase-2 的 R12 线索**显式标为「随 D-06 留 phase-2」**。

**本文照办**：第五节把这 55 条逐行列出并具名，一条都不沉默、一条都不编端口。

⚠ **一处必须让人类看见的不一致**（⚠ **缺口 15**）：
`DECISION-Q0.md` 的裁决人是 **main coordinator**（依据人类 2026-07-30「做决定做调整，
不需要我参加决策」的授权），而 `requirements/23-asset/OPEN-QUESTIONS.md` 里
Q-0 的复选框**仍未勾选、裁决行仍是空白、无署名无时间戳**。
feature（F132…F143）与本文都已按 C 落地。
⇒ **两处对同一件事的记载不一致**，而 OPEN-QUESTIONS 自称「本文件是裁决原文的唯一所在」。
**收敛哪一份是签核动作，agent 不改它**。

---

## 一、`uc-23-8` 后台外壳与左栏 IA（R12 共 11 条）· **phase-1**

| V | 一句话 | API 操作 | 前端消费点 | 状态 |
|---|---|---|---|---|
| V1 | 组织头部与额度条逐字正确 | `ListAdminNav().org` | `apps/web/app/admin/page.tsx` + `ORG_HEADER` | ✅ **已建成且正确**（本束 108 条里唯一一条） |
| V2 | 左栏资产项集合 ＝ `AssetKind` 六值（**双向**） | `ListAdminNav()` | `AdminNav` 组件 | ⚠ **缺口 16**：现状 **4/6**，缺画布模板与项目蓝本（F132） |
| V3 | **反证**：任一侧删一项 ⇒ 门控报红 | —（门控，非端口；F132 的验收命令本身） | — | ⚠ **缺口 16**：门控尚未建 |
| V4 | 每一项 `href` 均非 404 | `ListAdminNav()` | `app/admin/[module]` | ⚠ **缺口 16**：`/admin/blueprint` 现为 404 —— **人类那条投诉的直接对象** |
| V5 | 计数是派生值 | `ListAdminNav().items[].count` | 同上 | ⚠ **未建**：现为 mock 常量 |
| V6 | 0 条也显示 | 同上 | 同上 | ⚠ **未建** |
| V7 | 取不到显 `—` 且其余照常 | 同上 | 同上 | ⚠ **未建** |
| V8 | 非管理员访问 `/admin` 被拒 | phase-00 `identity`（**消费**，不在本束声明） | 未建 | ⚠ **未建** |
| V9 | 「我的本地」(F16) 改左栏后行为不变 | —（回归断言） | `/admin/local` | ⚠ **缺口 11**（Q-11：左栏 IA 与人类截图不一致），**回归风险** |
| V10 | 六个计数不产生六次串行请求 | `ListAdminNav()` **单次返回** | — | ⚠ **未建** |
| V11 | 额度阈值全仓恰 1 处定义 | —（`agent-runtime` 拥有该阈值） | — | ⚠ **待补**机械断言（本束**不得**有第二份阈值定义） |

⇒ **V2 / V3 / V4 是本束存在的全部理由**（人类问「为什么在管理后台看不到项目蓝本」）。

---

## 二、`uc-23-4` 治理与发布（R12 共 14 条）· **phase-1**

| V | 一句话 | API 操作 | 状态 |
|---|---|---|---|
| V1 | 第 3 步五块布局与文案逐字 | `GetAssetGovernance` | ⚠ **未建** |
| V2 | 可见范围三选一，人数随所选团队实时推算 | `Get/SetAssetGovernance` | ⚠ **未建** |
| V3 | 选「全组织」出现联签提示且发布走联签路径 | `SetAssetGovernance` + `PublishAsset` | ⚠ **缺口 1**：联签流程 `[待确认]`，**原型只有七个字** |
| V4 | 「仅自己 · 私有草稿」四处 0 命中 | `SetAssetGovernance` + phase-00 `identity`（I-12 / I-26） | 端口有；**判定函数是消费的**（跨束） |
| V5 | 「谁能改它」集合生效于编辑器只读 | `SetAssetGovernance` + `ASSET_NOT_EDITABLE` | 端口有；⚠ **未建**界面 |
| V6 | 复核周期三档封闭，构造第四档被拒 | `SetAssetGovernance`（I-11） | 端口有 |
| V7 | **三条独立禁用条件**（未填 / 有红项 / 两者） | `PublishAsset` 三个错误码 | 端口有 |
| V8 | 试跑 `!` ⇒ 发布前检查多一条红项 | `RunPreflightChecks.sourceRef`（I-22） | ⚠ **缺口 P2**：上游 `RunTrial` 在 `uc-23-5`，**随 D-06 留 phase-2** |
| V9 | `[去处理]` 跳转到产生该红项的位置 | `RunPreflightChecks.sourceRef` | 端口有 |
| V10 | **六类的治理字段完全相同** | `SetAssetGovernance` | 🔴 ⚠ **缺口 2**（Q-1b 未裁）；代码侧现状 **3/6** |
| V11 | 六道关有阻断时直调发布被拒 | `PublishAsset` → `GATE_NOT_PASSED` | ⚠ **缺口 P2**：上游六道关在 `uc-23-2`，**随 D-06 留 phase-2** ⇒ 本 phase 该码**无可达触发路径** |
| V12 | `reviewDueAt = publishedAt + reviewCycle` | `PublishAsset` | 端口有 |
| V13 | 发布写审计（谁/什么资产/什么范围/什么时间） | `PublishAsset` | 端口有 |
| V14 | 灰度语义（5 人后第 6 个人看到什么） | `PublishAsset(mode: staged)` | 🔴 ⚠ **缺口 3**（Q-3 未裁）：端口保留参数但**未定义行为**；**UC 自己写着「未裁前不写验收」** |

---

## 三、`uc-23-6` 复核到期与降级（R12 共 13 条）· **phase-1**

| V | 一句话 | API 操作 | 状态 |
|---|---|---|---|
| V1 | 到期转 `待复核` | `ScanReviewClocks` | 端口有 |
| V2 | `待复核` 仍出现在列表/检索/绑定面板 | `GetReviewClock` + I-13 | 端口有 |
| V3 | 调用一个 `待复核` 资产时调用方看到提示 | ❌ **无端口** | ⚠ **缺口 4**：提示出现在哪、给谁看，**原型 0 命中** |
| V4 | 满 30 天降级为 `{负责人}`，其他人四处 0 命中 | `ScanReviewClocks` + I-14 | 端口有 |
| V5 | 降级走**同一条**可见性写入路径（无第二条） | I-14 断言 | 端口有 |
| V6 | 降级后负责人仍能看到并打开 | `ScanReviewClocks` + I-14 | 端口有 |
| V7 | 复核重置计时并留下复核记录 | `ReviewAsset` | 🔴 ⚠ **缺口 5**：`conclusion` 的取值集合**空缺**（`uc-23-6` R7-8） |
| V8 | **零调用资产也降级**（反证惰性判定） | `ScanReviewClocks`（**主动扫描**，I-15） | 端口有；**命名即契约** |
| V9 | 自动转态写审计，执行者是系统不是人 | `ScanReviewClocks` | 端口有 |
| V10 | 「最近一次扫描时间」可观测，停摆时变旧 | `ScanReviewClocks` | 端口有 |
| V11 | 复核周期三档封闭 | `SetAssetGovernance`（I-11） | 端口有 |
| V12 | 与组织大脑到期规则**是同一段代码**（全仓恰 1 处定义） | —（I-27 机械断言） | 🔴 ⚠ **缺口 6**：落点依赖 **Q-7**（阈值归谁） |
| V13 | 负责人被停用时的行为 | `ASSET_DOWNGRADED_OWNER_MISSING` | ⚠ **缺口 7**：兜底接管人 `[待确认]`；**UC 自己写着「未裁前不写验收，但必须有一条」** |

---

## 四、`uc-23-3` 资产目录的浏览与编辑（R12 共 15 条）· **phase-1**

| V | 一句话 | API 操作 | 状态 |
|---|---|---|---|
| V1 | Skill 文件树逐字等于 `SK_TREE`（5 文件 3 目录） | `GetAssetDirectory` | 端口有 |
| V2 | Agent 文件树逐字等于 `AG_TREE` | `GetAssetDirectory` | 端口有 |
| V3 | 徽标按扩展名派生，`.txt` 走 fallback 不报错 | `ReadAssetFile.badge` | 端口有 |
| V4 | 目录行不显示大小，文件行显示 | `GetAssetDirectory` | 端口有 |
| V5 | 单选高亮，同一时刻至多一行 | `ReadAssetFile`（前端态） | 端口有 |
| V6 | `编辑` / `预览` 双模，预览渲染当前内容（含未保存） | `ReadAssetFile`（前端态） | 端口有 |
| V7 | 改动后出现 `有未保存改动`，保存后消失 | `WriteAssetFile.dirty` | 端口有 |
| V8 | 有未保存改动时刷新/离开被拦截 | `WriteAssetFile.dirty` | ⚠ **缺口 8**：拦截**原型未画** |
| V9 | 删除 `SKILL.md` 被拒 | `ROOT_FILE_UNDELETABLE`（I-7） | 端口有 |
| V10 | frontmatter 非法 YAML ⇒ 保存被拒且**库里内容未变** | `CONTRACT_VALIDATION_FAILED`（**复用**） | 端口有 |
| V11 | **发布后运行时读到的文件集合 ＝ 编辑器树集合（双向）** | I-6 | 端口有 |
| V12 | 未保存改动**不**影响线上调用（两侧同时断言） | I-8 | 端口有 |
| V13 | 不在可改集合内 ⇒ 编辑器只读，无 `[保存]` | `ASSET_NOT_EDITABLE` | 端口有 |
| V14 | 可见范围外直接访问 URL ⇒ **404 不是 403** | phase-00 `identity`（消费） | 端口有 |
| V15 | 保存后重扫安全（注入样本被第 02 关拦下） | ❌ **I-25 未成为端口副作用** | 🔴 ⚠ **缺口 9**：**已知可绕过的洞**；且第 02 关本体 ⚠ **缺口 P2**（随 D-06 留 phase-2）⇒ **本 phase 这条无法端到端验** |

⚠ **整组的适用范围本身未知**：原型只对 skill / agent 给了目录形态，
其余四类 `[待确认]` ⇒ **这组端口对 2/6 类有依据、对 4/6 类是推测**（⚠ **缺口 10**）。

---

## 五、随 D-06 留 phase-2 的 55 条（**逐行具名，不编端口**）

> Q-0 裁 C ⇒ 这四份 UC 整体移到 phase-2。
> `DECISION-Q0.md` 连带处置第 2 条逐字：**「显式标为『随 D-06 留 phase-2』」**。
> ⚠ 它们的「API 操作」列填 **—（本 phase 无端口，随 D-06 留 phase-2）** 是**诚实的落点**，
> 不是占位符：本 phase 确实不该有这些端口。每行同时带具名缺口 **⚠ 缺口 P2**。
> ⚠ **不许**因为「表里有空格不好看」就给它们编一个 `usecases.md` 第二节的端口名——
> 那 10 个端口本身就是「裁决前不得实现」的（`usecases.md` 第二节标题逐字）。

### 5.1 `uc-23-1` 选择资产来源并起草导入（14 条）

| V | 一句话 | API 操作 | 状态 |
|---|---|---|---|
| V1 | 六个资产标签页主 CTA 文案与 `AD_META` 逐字一致 | —（本 phase 无端口，随 D-06 留 phase-2） | ⚠ **缺口 P2** |
| V2 | 向导步骤条 `1 来源 / 2 配置 / 3 治理与发布`，当前在 1 | 同上 | ⚠ **缺口 P2** |
| V3 | 第 1 步四条来源路径的角标与文案逐字 | 同上 | ⚠ **缺口 P2** |
| V4 | 选「从社区导入」出现地址框 / 上传入口 / 源目录表 | 同上 | ⚠ **缺口 P2** |
| V5 | 源目录表只列已连接源，同步失败的不可选带 `[修复]` | 同上 | ⚠ **缺口 P2** |
| V6 | 按内容识别资产类型，识别不出时出 E1 态**而非默认成某一类** | 同上 | ⚠ **缺口 P2** |
| V7 | 在 Skill 页导入 `mcp.json` ⇒ 显式提示且**未静默创建** | 同上 | ⚠ **缺口 P2** |
| V8 | 社区导入的草稿必经六道关，用法沉淀的不经过 | 同上 | ⚠ **缺口 P2**（六道关本体亦在 phase-2） |
| V9 | 「复制现有并改」的草稿第 3 步三项预填且可回指原资产 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 12**（`DetectUsageCrystallizationCandidates` 的去向需子裁决） |
| V10 | 「从实际用法沉淀」候选条数与检测一致且跨三种类型 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 12**：⚠ **它不涉外部源也不涉沙箱**，C 之下去向未定 |
| V11 | 任意时刻存草稿退出后重进，停同一步、内容不丢 | 同上 | ⚠ **缺口 P2** |
| V12 | 草稿在**任何**他人的搜索/列表里 0 命中 | 同上 | ⚠ **缺口 P2**（判定面与 V4·23-4 同源，phase-1 已覆盖那一半） |
| V13 | 无维护权限账号**看不到**主 CTA（不是点了 403） | 同上 | ⚠ **缺口 P2** |
| V14 | 源同步后「已同步 N」变化而资产库条目数**不变** | 同上 | ⚠ **缺口 P2** |

### 5.2 `uc-23-2` 落地检查六道关与三级结论（15 条）

| V | 一句话 | API 操作 | 状态 |
|---|---|---|---|
| V1 | 恰好六关，序号 01–06 与关名逐字 | —（本 phase 无端口，随 D-06 留 phase-2） | ⚠ **缺口 P2** |
| V2 | 三级结论恰 `通过/提示/阻断`，第四值被拒 | 同上 | ⚠ **缺口 P2** |
| V3 | 进度分子 ＝ 通过关数、分母恒 6 且与逐关自洽 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 13**（Q-4：原型分子分母不自洽） |
| V4 | 有 `阻断` 时第 3 步不可达（改路由也进不去） | 同上 | ⚠ **缺口 P2** |
| V5 | `提示` 不阻断 | 同上 | ⚠ **缺口 P2** |
| V6 | 第 04 关并排面板（左带调用次数与满意度、右标无历史） | 同上 | ⚠ **缺口 P2** |
| V7 | 三个出口的后置（`分别保留` 要填两个场景 / `放弃导入` 后离开待处理） | 同上 | ⚠ **缺口 P2** |
| V8 | 第 03 关 diff 内容取自草稿实际内容（不是写死示例） | 同上 | ⚠ **缺口 P2** |
| V9 | 采纳改写后草稿的 `tools:` 真的变了（编辑器里可见） | 同上 | ⚠ **缺口 P2**；⚠ 它的**验收落点在 phase-1 的 `uc-23-3`**，⇒ 跨 phase 依赖 |
| V10 | 未处置的 `提示` 在第 3 步显示为红色未完成项 | 同上 | ⚠ **缺口 P2**；⚠ 落点在 phase-1 的 `RunPreflightChecks`，⇒ 跨 phase 依赖 |
| V11 | 安全扫描 `reject` 维护者本人不能解除，安全评审人可以 | 同上 | ⚠ **缺口 P2** |
| V12 | 方法论审核人裁决第 02 关 ⇒ `REVIEWER_FUNCTION_MISMATCH` | 同上 | ⚠ **缺口 P2** |
| V13 | 提交人 ＝ 裁决人 ⇒ `SELF_REVIEW_FORBIDDEN` | 同上 | ⚠ **缺口 P2** |
| V14 | 02 关未通过时 05 关不启动 | 同上 | ⚠ **缺口 P2** |
| V15 | 每关结论可取 `{判定时间, 判定依据}` 且重放同结论 | 同上 | ⚠ **缺口 P2** |

### 5.3 `uc-23-5` 试跑台与回归用例（14 条）

> ⚠ `DECISION-Q0.md` 对本份 UC 的处置**与另外三份不同**：
> 「**试跑台 phase-1 保留，但第 05 关沙箱试跑不做**」——两者同名不同物
> （试跑台跑的是 **LLM 调用**，第 05 关跑的是**外来资产的脚本**）。
> ⚠ 但**本束现在没有为试跑台生成任何 feature**（F132…F143 的 `spec_ref` 全在 23-3/4/6/8），
> 也没有端口。⇒ 这 14 条**现在整体落在 phase-2 一侧**，
> 与裁决文里「phase-1 保留试跑台」**对不上** ⇒ ⚠ **缺口 17**（必须让人类看见）。

| V | 一句话 | API 操作 | 状态 |
|---|---|---|---|
| V1 | 左栏三块与三条场景名副文案逐字 | —（本 phase 无端口） | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V2 | 顶部提示按资产类型分两版（Agent / Skill） | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V3 | 改提示不保存 ⇒ 试跑反映改动**且线上不受影响**（两侧同断） | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V4 | 执行轨迹逐条点名实际装载的文件（删一个文件轨迹里消失） | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V5 | 自动校验项随资产内容变化 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V6 | 「空材料压力测试」⇒ 输出**不编造结论** | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V7 | 「只有反面证据」⇒ 反对意见被保留 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V8 | 越权工具调用 ⇒ 轨迹显示「拦截 N 次」 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V9 | 成本条三项且该次消耗**计入组织配额** | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17**（配额面板属 `agent-runtime`，**已签核**） |
| V10 | `[存为回归用例]` ⇒ `evals/regression.jsonl` 真的多一行 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V11 | `[跑全部用例]` 跑**全部**条目且结果逐条可见 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V12 | `!` 级发现 ⇒ 发布前检查出现红项；标为可接受后消失**且留痕** | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17**；⚠ 下游落点 `RunPreflightChecks` 在 phase-1（V8·23-4 同一条边） |
| V13 | 不在可改集合内 ⇒ 无 `▶ 试跑` 入口 | 同上 | ⚠ **缺口 P2** + ⚠ **缺口 17** |
| V14 | 试跑用到的项目材料试跑者无权限时的行为 | 同上 | ⚠ **缺口 P2** + ⚠ **待确认**（R5 该项未裁，UC 自己写着「未裁前不写」） |

### 5.4 `uc-23-7` 社区源的连接与同步（12 条）

| V | 一句话 | API 操作 | 状态 |
|---|---|---|---|
| V1 | 源目录表表头四列，区标题人数与实际源数一致 | —（本 phase 无端口，随 D-06 留 phase-2） | ⚠ **缺口 P2** |
| V2 | 每行显示该源的资产类型集合，四源互不相同 | 同上 | ⚠ **缺口 P2** |
| V3 | 同步后计数更新而**资产库条目数不变** | 同上 | ⚠ **缺口 P2** |
| V4 | 清单里混入不属于该源声明类型的条目 ⇒ 被拒并留痕 | 同上 | ⚠ **缺口 P2** |
| V5 | 凭据过期的源在导入向导里**不可选**且带 `[修复]` | 同上 | ⚠ **缺口 P2** |
| V6 | 「新建 Skill」页只出现提供 Skill 的源 | 同上 | ⚠ **缺口 P2** |
| V7 | 修复凭据后恢复可选且下次同步成功 | 同上 | ⚠ **缺口 P2** |
| V8 | 凭据在**任何**响应与日志里 0 命中明文 | 同上 | ⚠ **缺口 P2**；⚠ 这是一条**安全线**，移走时要确保 phase-2 的占位登记里保留它 |
| V9 | 非平台组管理员看得到表、看不到 `[＋ 添加源]` / `[修复]` | 同上 | ⚠ **缺口 P2** |
| V10 | 上游条目下架 ⇒ 已导入的同名资产**不受影响** | 同上 | ⚠ **缺口 P2** |
| V11 | 上游清单解析失败 ⇒ 保留上次成功清单并显式报错 | 同上 | ⚠ **缺口 P2** |
| V12 | 「可用 N」与「已同步 M」口径可区分（不混用同一标签） | 同上 | ⚠ **缺口 P2** |

---

## 六、反方向：API → UC（**这个方向现在就能查，而且查出东西了**）

> 「某个 API 操作没有任何 UC 要它」= 接口多余，或有 UC 没写。

| 已建成的东西 | 有 UC 要它吗 | 处置 |
|---|---|---|
| `OVERVIEW_METRICS` / `OVERVIEW_ANOMALIES` / `OVERVIEW_ACTIVITY`（`apps/web/lib/mock/admin.ts`） | ❌ **phase-01 无任何 UC 要它**。它的 `ucRefs` 指向 `17-gov/uc-17-1 · 17-7`，而 `17-gov` 在 **phase-03** | 🔴 **X-I / Q-12**：屏在 phase-01、UC 在 phase-03。**这是一块悬空代码**，登记为可见缺口 |
| `ADMIN_NAV` 的 `我的本地`（F16） | ✅ `00-core/uc-0-5`（**phase-00**） | 合法，但它使 phase-01 的左栏 IA 与人类截图不一致 ⇒ **Q-11** |
| `ADMIN_NAV` 的 `成员配额` / `反馈` | ⚠ `ucRefs` 写的是 `17-gov/uc-17-5 · 17-6`（**phase-03**）；`org-admin` 束覆盖的是 `01-auth/*` | ⚠ **同一种悬空**，登记进一致性复核 |
| `McpAuthScope`（`lead\|team\|all`） | ✅ `agent-runtime`（**已签核**） | ⚠ 与本束 `visibility` 是两套语义 ⇒ **X-C / Q-1b** |
| `/tpl` 路由（蓝本设计器） | ✅ `templates`（**已签核**） | ⚠ 不在 `/admin` 下 ⇒ **X-J / Q-11**，本束**不可单方搬** |

⇒ **反向检查发现 3 处悬空**（总览 / 成员配额 / 反馈的 UC 依据全在 phase-03）。
这是本束建立过程中**最有价值的副产品**：它们与「后台外壳没人认领」是同一个病，
只是发现得更晚。**登记在这里，而不是顺手改掉。**

⚠ 另一方向的空：`usecases.md` **第二节那 10 个端口**（`CreateDraftFromSource` /
`DetectAssetKind` / `RunLandingChecks` / `ResolveDedupConflict` / `DisposeHint` /
`RunTrial` / `SaveRegressionCase` / `RunAllRegressionCases` /
`List/Add/Sync/RepairMarketSource` / `DetectUsageCrystallizationCandidates`）
**在 Q-0 裁 C 之后全部落在 phase-2 一侧**，本 phase**不实现**。
它们已在 `packages/contracts/src/asset-governance.ts` 的
`DEFERRED_TO_PHASE_2` 里逐条登记 —— **有名字、可见、会被问到**。
⚠ 那个常量的注释逐字：「`operations` 里出现其中任何一个，都表示有人绕过了 Q-0/D-06」。

---

## 七、缺口清单（**每一条都有名字，会在每次 `doctor` 里出现**）

| # | 缺口 | 卡在哪 | 影响的行 |
|---|---|---|---|
| **缺口 P2** | **随 D-06 留 phase-2** —— 本 phase 无端口，且**不该有** | **Q-0 裁 C**（`DECISION-Q0.md`，已裁） | 第五节全部 **55 行** |
| **缺口 1** | 全组织联签流程 `[待确认]`，原型只有七个字 | 待确认 | 23-4 / V3 |
| **缺口 2** | 「六类共用治理字段」的适用范围未定（代码侧现状 3/6） | **Q-1b** | 23-4 / V10 |
| **缺口 3** | 灰度语义未定 | **Q-3** | 23-4 / V14（UC 自己写着「未裁前不写验收」） |
| **缺口 4** | 「调用 `待复核` 资产时的提示」出现在哪、给谁看 | 原型 **0 命中** | 23-6 / V3 |
| **缺口 5** | 复核 `conclusion` 的取值集合空缺 | `uc-23-6` R7-8 | 23-6 / V7 |
| **缺口 6** | 30 天与三档周期的定义落点归谁 | **Q-7** | 23-6 / V12 |
| **缺口 7** | 负责人被停用时的兜底接管人 | `[待确认]` | 23-6 / V13 |
| **缺口 8** | 离开拦截**原型未画** | 原型缺屏 | 23-3 / V8 |
| **缺口 9** | 🔴 保存后重扫安全未成为端口副作用（**已知可绕过的洞**） | I-25 未落端口；且第 02 关本体在 phase-2 | 23-3 / V15 |
| **缺口 10** | `uc-23-3` 一组端口对 **4/6 类资产是推测**（原型只给 2 类目录形态） | 原型缺屏 | 第四节整组 |
| **缺口 11** | 左栏 IA 与人类截图不一致（`我的本地` 等） | **Q-11** | 23-8 / V9 |
| **缺口 12** | `DetectUsageCrystallizationCandidates` 与 `uc-23-1` 三条非外部路径的去向 | **Q-0 方案 C 的子裁决**（`DECISION-Q0.md` 已指出但**未定**） | 23-1 / V9 · V10 |
| **缺口 13** | 六道关进度分子分母在原型里不自洽 | **Q-4** | 23-2 / V3 |
| **缺口 14** | 本文旧版头部的「111 条」是错数（实为 108） | 口径修正（**非待裁**） | 已改 |
| **缺口 15** | 🔴 **Q-0 的裁决记在 `DECISION-Q0.md`，而 `OPEN-QUESTIONS.md` 的裁决行仍空白** | 两处记载不一致；OPEN-QUESTIONS 自称「裁决原文的唯一所在」 | 全束（**收敛哪一份是签核动作**） |
| **缺口 16** | 左栏现状 4/6，缺画布模板与项目蓝本；双向门控未建 | 实现缺口（F132 覆盖） | 23-8 / V2 · V3 · V4 |
| **缺口 17** | 🔴 **`DECISION-Q0.md` 说「试跑台 phase-1 保留」，而本束没有为它生成任何 feature、也没有端口** | 裁决与落地不一致 | `uc-23-5` 全部 14 行 |

---

## 八、这份覆盖证明现在**不够**的地方（签核时请读这一节）

1. **108 条 R12 线索里，只有 1 条现在是成立的**（23-8 / V1，组织头部）。
2. **55 条随 D-06 留 phase-2**（第五节），这是 **Q-0 裁 C 的直接结果，是判断而非遗漏**；
   它们逐行具名，**没有一行被编上一个本 phase 不存在的端口**。
3. **反向检查查出的 3 处悬空不属于本束能解决的范围**——它们需要阶段一致性复核
   （`design-coherence.md` 第二~六节目前留白待填）。
4. **`uc-23-3` 一组端口的适用范围对 4/6 类资产是推测**（缺口 10）。
5. 🔴 **两条「裁决与落地对不上」必须让人类看见**：缺口 15（Q-0 裁决记在哪）
   与缺口 17（试跑台该不该留 phase-1）。**收敛它们是签核动作，agent 不改。**
