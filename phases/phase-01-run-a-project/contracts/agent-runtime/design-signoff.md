---
bundle: agent-runtime
phase: "01"
covers: [F48, F49, F50, F51, F52, F53, F54, F55, F56, F57, F58, F59, F60]   # 束↔feature 映射的权威（ADR-023 决策三）；改它等于改评审范围
status: pending           # pending | confirmed —— ⚠ 只能由人类改，agent 不许动
confirmed_by:             # 确认人（姓名/邮箱）
confirmed_at:             # ISO 8601，且不得晚于签核当下
---

# 契约束 `agent-runtime` 设计签核

覆盖 feature：**F48–F60**（13 件 / 53 点）—— 派生视图，权威是上面 frontmatter 的 `covers:`
依据 UC（**九份**）：

| 模块 | UC | R12 条数 |
|---|---:|---:|
| `20-model` | `uc-20-1 接入并测试一个模型` | 12（V8–V12 编号重复两轮，合并计数） |
| `20-model` | `uc-20-2 模型启用与可选范围` | 15（V0 / V1a / V1b / V2–V13；表中 V1a+V1b 合并为一行） |
| `20-model` | `uc-20-3 机密数据的模型路由` | 18 |
| `21-mcp` | `uc-21-1 注册 MCP 服务器与授权范围` | 19 |
| `21-mcp` | `uc-21-2 MCP 安全策略与放行评审` | 18（含 V2a / V6a） |
| `04-agent` | `uc-4-1 注册一个 Agent` | 21 |
| `04-agent` | `uc-4-2 按状态载入 Agent` | 19 |
| `04-agent` | `uc-4-3 与单个 Agent 私聊` | 16 |
| `04-agent` | `uc-4-4 Agent 行为审计` | 23 |
| | **合计** | **161** |

## 为什么 model + mcp + agent 是**一个**束而不是三个

判据是 `contract-design.md` §一：**这些东西的不变量互相依赖吗**。这里是互相依赖的，且是硬依赖：

- **三层权限求交**（`有效权限 = MCP授权范围 ∩ agent工具白名单 ∩ 任务权限包`，O-23）
  的第 ① 层住在 `21-mcp`、第 ② 层住在 `04-agent`。**拆开签，agent 束签的时候会发现
  MCP 束的授权范围模型不够用**——正是 ADR/执行书点名要避免的那种情况。
- **「只有测试通过并已启用的模型才出现在 agent 的模型选择里」** 是 agent 侧的硬闸门，
  但判定数据全在 `20-model`；组合模型（O-22③）更是「模型池一条记录 ⇔ agent 侧恒为单个
  `model_id`」的**同一条不变量的两面**。
- **机密硬路由**（UC-20.3）的开关住在 21-mcp（开关三）、执行体住在 20-model、
  展示与批准住在 agent 的批准卡、留痕住在 agent 的 tool-call 审计。**一条控制、四处落点。**
- **tool-call 审计**（UC-4.4）的命名空间 `mcp:<服务器>.<工具>` 由 21-mcp 定义，
  「没有留痕就没有调用」这条硬前置同时约束 MCP 调用与模型调用。

⇒ 三者的不变量绑在一起，必须同束。

## 五件产出物

| # | 文件 | 内容 |
|---|---|---|
| ① UI | `ui.md` | 9 块屏的落点与真实 `data-testid`；**截图待补** |
| ② 用例 | `usecases.md` | 48 个用例端口 + 统一失败枚举 **38 个错误码** + 端口表 |
| ③ API 契约 | `packages/contracts/src/agent-runtime.ts` | **尚未创建**——本轮只是骨架 |
| 支撑 | `domain.md` | 26 个实体/值对象 + **47 条不变量**（其中 8 条跨束） |
| 支撑 | `coverage.md` | 161 条 R12 逐条映射 + **19 个缺口** + 反向检查 |

---

## ① UI —— 人看到的界面对不对

→ 详见本束 `ui.md`。

现状一句话：**后台三屏（`/admin/agent` `/admin/model` `/admin/mcp`）已用真实组件建成并带稳定
`data-testid`；对话屏的 AI 团队面板 / 批准卡 / tool-call 明细也已建成（归 `chat` 束宿主）；
但 `phases/phase-01-run-a-project/ui-preview/` 下没有任何截图文件**，只有三份 markdown。

### 签核前请重点确认

- [ ] **截图缺失 ⇒ 第 ① 件目前不具备签核条件。**
      `ui.md` 已给出 9 个约定文件名（`ui-preview/<slug>.png`），需 ui-prototyper 补齐后再签这一节。
- [ ] **后台三屏「可选范围」被降级为只读文字**（`ui-preview/README.md` **S-13**）——
      D-07 要求「可选范围过滤」，实现只做了顶部筛选器 + 一行只读文字，**没做逐模型多选编辑器**。
      这是范围上的收缩，请确认它是 phase-1 的正确边界，还是要补回编辑器。
- [ ] **18 台模型的型号与定价全是编的**（S-13）。UC-20.1 R6 明写「不得把 18 个模型硬编码进
      代码或种子数据」。请确认这些示例只作为**组织配置数据**存在，且删掉后界面走空态而非崩。
- [ ] **原型里新造了第 7 个 agent「Forge」**（S-13），把总览的「自建 agent 访问客户 CRM
      被拦 7 次」与 Agent 页的「越权申请待确认」串成同一条线索——**原型是两处独立提及，
      没说是同一个 agent**。请确认这条线索是否成立（它影响 F53 越权拦截证据的归属）。
- [ ] **五块屏尚未建**：组织级审计检索屏（D-34，原型中不存在，需从零补画）、
      项目级审计与反馈时间线、蓝本第 12 项「Agent 编排」配置面板、
      项目设置「AI 权限」三开关、MCP 放行评审面板。见 `ui.md` 的「本束需要哪几块屏」。

---

## ② 用例 —— 用例接口与失败模式穷举对不对

→ 详见本束 `usecases.md`（48 个用例，统一失败枚举 38 码）。

### 签核前请重点确认

- [ ] **失败模式穷举了吗**——已有原型是 happy path 演示，**零错误态屏**
      （UC-20.3 R8 逐字写着「调用链只有『64 命中 / 复用 1 份 / 运行中』三态，无失败态」）。
      重点看这七类是否都覆盖：并发、越权、依赖失败、幂等重放、部分成功、超时、撤回中。
- [ ] **`NO_LOCAL_MODEL_AVAILABLE` 是不是真的「明确失败」而不是任何形式的回落**——
      UC-20.3 R9 明写「本用例不允许任何形式的功能降级」。这一条错了等于数据泄露。
- [ ] **`PROVENANCE_WRITE_FAILED` ⇒ 调用失败** 这条硬前置（UC-4.4 E2 / R7）
      是不是所有工具调用路径都遵守，还是只有涉客户数据的那条。
      两份 UC 说法不同：UC-4.4 说全部 tool-call，UC-21.2 E5 只说涉客户数据。**需你定口径。**
- [ ] **拒绝语义是否泄露存在性**：可见性范围外的 agent「接口不得返回其存在性」（UC-4.1 R5），
      因此必须是 `AGENT_NOT_FOUND` 而非 `FORBIDDEN`——与 phase-00 `artifact` 束
      对草稿的 404-非-403 处理同源，请确认口径一致。
- [ ] **超限排队不是拒绝**（O-22②）：`CONCURRENCY_LIMIT` 不是错误码而是一个**排队响应**，
      带位次与预计等待。请确认这个建模（错误 vs 正常响应）是你要的。

---

## ③ API 契约 —— 对外形状与错误码对不对

**本束有对外 HTTP 面。** 第 ③ 件的落点是：

```
packages/contracts/src/agent-runtime.ts     ← zod 单一事实源
```

⚠ **该文件尚未创建。本轮产出的是签核骨架，不含代码。**
它是**签核通过后、开工时的第一件产出**——`covers` 里第一个被 claim 的 feature
（依赖序上应是 F48 或 F52）必须先落这份 zod 契约，再写实现，因为：

- 前端 mock 必须从它生成（`contract-design.md` 硬规则 2：不许手写 mock）；
- 响应体也要被它校验（硬规则 6），**特别覆盖拒绝路径**——本束的价值一大半在拒绝路径上；
- 新增文件时同时在 `packages/contracts/src/index.ts` 导出。

⚠ 本束体量大（48 个操作），若在实现期发现单文件超 2000 行，按 AGENTS.md 的文件规模约束
**按 model / mcp / agent 三个域切分为 `agent-runtime/{model,mcp,agent}.ts` 并由
`agent-runtime.ts` 汇总导出**——但**束仍是一个**，签核面不因文件切分而分裂。

### 签核前请重点确认

- [ ] **错误码语义在三个域间是否一致**：同一种失败（依赖不可用 / 越权 / 并发冲突）
      在 model / mcp / agent 三侧必须是同一个码。这正是阶段一致性复核要查的「错误语义是否一致」，
      本束因为跨三模块，**在束内就已经有这个风险**。
- [ ] **`routeModelCall` 是不是唯一执行点**——UC-20.3 R10 要求「任何模块自行调用模型
      都必须经过同一个路由执行点，不得旁路」。这条要靠**架构测试**（依赖规则）保证，
      不是 zod 能表达的。请确认这条架构测试有人写。
- [ ] **`listSelectableModels` 只有一份实现**（UC-20.2 R7），三个消费点
      （agent 下拉 / skill 下拉 / 蓝本模型策略）复用。请确认不会各写各的。
- [ ] **凭据字段根本不在响应 schema 里**（不是「返回后再过滤」）——
      UC-20.1 AC3「凭据永不回显、永不出现在接口响应、日志与审计明文中」。

---

## 本束与哪些束有交叉约束（留给阶段一致性复核）

| # | 交叉点 | 涉及 | 为什么不能在本束单独定 |
|---|---|---|---|
| **X-1** | **三层权限求交的第 ③ 层**（任务权限包 R1/R2/R3） | `org-admin` 束（角色本体）· 11-board / 00-core | 第 ①② 层在本束，第 ③ 层不在。三层是**一个**判定函数，分两处实现就是第 N 次「同一事实两处声明」 |
| **X-2** | **角色本体**：安全评审人 / 方法论审核人 / 能力维护者 / 项目负责人 | `org-admin` 束 | O-21 把审核职能拆成两种，且要求「同一自然人可兼任、同一事项上只能行使一种」——这是角色模型的约束，本束只能消费 |
| **X-3** | **机密硬路由口径 D-U1**（下方单列，最重要） | `chat` 束（批准卡）· 17-gov（机密标记采集） | 见下 |
| **X-4** | **tool-call 审计写入失败即调用失败** ＋ `provenance_events` 查询面 | phase-00 `artifact` 束缺口①（provenance 查询面跨束）· 17-gov | phase-00 已经把「统一一个 provenance 查询面」提进一致性复核。本束是它**最大的写入方与查询方**，不能另建一套 |
| **X-5** | **agent run 四件套**（plan / tool calls / checkpoint / Context Pack） | phase-00 `context-pack` 束 | AC1「当时的输入」就是那次 run 的 Context Pack，必须可重放；Context Pack 的模型在 phase-00 |
| **X-6** | **上下文只经 Context API，无旁路** | phase-00 `context-pack` / `api-kernel` 束 | 旁路取数没有 Context Pack 记录 ⇒ UC-4.4 AC1 审计断链。要靠架构测试拦，归 `api-kernel` 的门控面 |
| **X-7** | **机密标记 `artifact.is_confidential` 的传播**（Artifact → Version → Segment → Context Pack） | phase-00 `artifact` + `context-pack` 束 · 17-gov（采集） | O-17 裁定采集归 17-gov、粒度材料级；本束**只消费**，但传播链一断路由就判错 |
| **X-8** | **`agenda_segment` / `design_facet` / `method_stage` 三名不得共用** | 02-tpl 束 · 07-canvas · 11-board · 09-kg | D-03a 已定稿。载入规则的触发条件**只能引用 `agenda_segment_id`**，这是本束对外的核心契约 |
| **X-9** | **降级运行标注 `降级运行 · sonnet`** 与批准卡宿主 | `chat` 束 | 展示在 chat，判定在本束 |
| **X-10** | **skill 版本快照绑定** | `skill` 束（03-skill / UC-3.4） | agent 的 skill 挂载必须记录到版本粒度，否则 UC-4.4「当时跑的是哪一版」无解 |

### ⚠ X-3 单列：机密数据硬路由 D-U1 的**字面矛盾尚未解决**

`ui-preview/README.md` 的 **S-01**（列在「🔴 必须先定的」第一条，也是「建议优先核对的 5 处」第一条）
明写这条**待人类裁决**，而当前仓库里存在**两个互相矛盾的口径**：

| 出处 | 口径 |
|---|---|
| 已建成的 UI mock（`apps/web/lib/mock/chat.ts` 的 `modelPolicyViolation`） | **机密只路由本地，云端模型可并存承接非机密部分**；只在「有机密但无任何本地模型」时报违规 |
| `feature_list.json` F51 标题与 `user_visible_behavior` | **含机密 ⇒ 整轮所有模型调用走本地，云端本轮不可用**（明写「不是机密走本地、云端承接非机密的分流」） |
| UC-20.3 R3 第 4 步（O-22③ + O-17 联合裁决） | 支持「整轮」：粒度是**材料级**，phase-1 **不存在分段路由**；**含闭源成员的组合在上下文含任一机密材料时整条不可用** |

原型自身就是矛盾的：批准卡同时印着「gpt-5.2 ＋ **本地** qwen3-32b」与「含机密，**仅本地模型**」。

⇒ **签核时必须裁决这一条**，它直接决定后端 gateway 的拦截规则。
本束按 UC-20.3 的裁决（**整轮全本地**）写契约，见 `domain.md` **I-10**、`usecases.md` 的
`routeModelCall`，并在 `domain.md` 的「待人类裁决」一节留了口子。
**若你的本意是 UI mock 那一版，`modelPolicyViolation` 与二次确认文案、I-10 的断言、
F51 的验收全部要改。**

---

## 确认动作

人类逐节核对上面三件（① UI / ② 用例 / ③ API 契约）后，把 frontmatter 的 `status`
改为 `confirmed`，填 `confirmed_by` / `confirmed_at`（ISO 8601，**不得晚于签核当下**）。

⚠ **这是人的动作，agent 不得代劳。** 该字段受 `.github/CODEOWNERS` 与 CI 保护（ADR-023 决策五）。
在此之前 `new-sprint` 与 `claim` 都会拒绝把 F48–F60 开进 sprint。

⚠ 另需满足 ADR-023 决策四：`phases/phase-01-run-a-project/design-coherence.md` 的
`covers_bundles:` 必须包含 `agent-runtime`，且**声明的束集合 ⊇ 本阶段全部束**——
否则本束签了也放行不了。
