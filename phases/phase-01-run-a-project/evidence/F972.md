# F972 —— 证据

`plan-control` 契约束第 ③ 件（`packages/contracts/src/plan-control.ts`）+ append-only 账本表
（`chat_plan_ledgers` / `chat_plan_orphan_constraints`）+ RLS。issue #2155。

## 验证命令与结果

### `pnpm --filter @repo/contracts exec vitest run tests/plan-control/plan-control-schema-single-source.test.ts`

```
 ✓ tests/plan-control/plan-control-schema-single-source.test.ts (31 tests) 5ms

 Test Files  1 passed (1)
      Tests  31 passed (31)
```

### `pnpm --filter api exec vitest run tests/plan-control/ledger-append-only-unique.test.ts`

（真实隔离 Postgres：`WORKSPACEX_DB=wsx_f972_plan_control`，攻击面覆盖 app_rw 与属主两个角色）

```
[db-isolation] db=wsx_f972_plan_control capacity=100 current=6 required=32
 ✓ tests/plan-control/ledger-append-only-unique.test.ts (12 tests) 832ms

 Test Files  1 passed (1)
      Tests  12 passed (12)
[db-isolation] db=wsx_f972_plan_control peak_connections=2
```

## 门控证据

- `node .harness/scripts/lint-third-artifact.mjs phase-01-run-a-project`：
  `plan-control` 行由 `✗ 第 ③ 件缺失 R12 0 行` 变为
  `✓ 形态 A  R12 10 行  packages/contracts/src/plan-control.ts`。
  （同一次输出里 `agent-interrupts` 仍红——那是另一条并行线 F212 的范围，不属本 feature。）
- `pnpm exec tsx .harness/scripts/verify-uc-coverage.ts 01`：仅剩的 2 项失败
  （`project/coverage.md` 漏 4 条 R12、23 个孤儿 feature）经 `git stash` 前后对比逐字相同，
  与本次改动无关，非本 feature 引入。
- `pnpm --filter @repo/contracts exec tsc --noEmit -p .`：干净，无输出。
- `pnpm --filter api exec tsc --noEmit`：仅剩 `packages/fabric-markdown` 的 DOM-lib 幽灵错误
  （已知基线噪音，dist 未构建导致，与 `plan-control.ts` 无关，grep 确认零命中）。

## 范围边界（按任务指示遵守）

- 未改动 `packages/contracts/src/agent-interrupts.ts`、`packages/contracts/src/chat.ts`、`apps/web`。
- `mutateThread.op` 未扩（`chat.ts:545` 封闭枚举原样保留），`plan-control` 走独立操作集
  （`design-signoff.md` 3.1 已裁决 A）。
- `RunControlAction` 无 `restore-checkpoint`；`UC-11` 未实现，不留占位错误码。
- XC-59：`derivePlanPhase` 与 `PLAN_APPROVAL_TOOL_WHITELIST` 只认 `call_skill`，表驱动反证覆盖
  仅有 `agent-interrupts` 三个新工具名待决中断、无 `call_skill` 待审批时 `PlanPhase !== "approving"`。
- XC-60：`PlanAppliedTo` 为 `"ledger-only" | "ledger-and-engine"`；`PLAN_CONSTRAINT_BLANK` 维持不变。
