# 需求分析师视角审计（2026-07-30）—— 结论与证据

> 完整正文由 requirements-analyst agent 返回在会话里（它按「只读」约束未落盘）。
> 本文件是 main coordinator 摘录的**结论与可复现证据**。
> ⚠ 该 agent 自陈盲区：原型全量通读未完成、未逐条把 124 个 feature 与原型对读、
> 未打开截图确认、未跑 `TURBO_FORCE=1` 全量。**它的「缺失」清单同样承担
> 「定点抽取」的风险**——本轮已九次因此误判。

## 🔴 最严重：两个已签核束的验收全部失效（V-1 / V-2）

`pnpm --filter web vitest run <任意路径>` **恒 exit 0**。已复现：

```
真实路径 exit=0
垃圾路径 exit=0        ← 指向不存在的测试文件，判通过
```

机制：`apps/web/package.json` 有 `test` 脚本、**没有 `vitest` 脚本**；
pnpm 在「无匹配脚本」时退出 0。`verify.ts:83` 逐条 `sh(cmd)` 按退出码判定 ⇒ **门控读成通过**。

⇒ **21 个 feature / 84 点今天就能整套全绿，零实现**：F48–F68，
即**已签核的 `agent-runtime`（F48–F60）与 `skills`（F61–F68）两束的全部**。

⚠ `validate-fl` 的 `PLACEHOLDER` 只查 `echo` / `test -ef` / `ls` / `true` / `cat` 五种
（`validate-fl.ts:33-39`），**这一种它看不见**。「全绿但空转」第十次同形。

## 🔴 V-3 / V-4：219 条 verification 从仓库根跑不起来，且**结果不确定**

仓库根无 `tests/`、无 `vitest.config.ts`。而 filter 是**子串匹配**，实测：

```
$ pnpm vitest run tests/kernel/rbac-two-layer.test.ts
Test Files  26 failed | 1 passed (27)
```

命中了 `.claude/worktrees/agent-*/apps/api/tests/**` 的多份仓库副本。
⇒ **同一条 verification 的结果取决于当下有几个 agent worktree。**

正确形式仓里已有且经证明：phase-00 全部 22 个 passing 用的是
`pnpm --filter api exec vitest run …`，缺文件时 exit 1。
phase-01 只有 `tests/project/`（F116–F128，28 条）沿用了它。

**V-9：全 124 条里只有 6 个 feature 的 verification 完全可信**
（F117/F119/F120/F121/F126/F127，全在 `project` 束），应作为唯一范本。

## 🔴 X-1：F58 伪造出处（最危险的单条）

`DECISIONS-OPEN.md` 的 **O-23 裁决行逐字只有一句**：
> 裁决：合成规则 = 取交集（收紧优先），且不可被下层放宽

「三个 AI 权限开关**默认全关**」只出现在**推荐段**，裁决行未提。
而 F58 的 `notes` 写着「默认全关为 **O-23**」——**伪造出处**。

且**原型说反**（按 `#17171A`+旋钮右=开、`#DEDCD3`+左=关 判读）：
`15.2532M` 设置磁贴逐字「AI 权限 / 3 项 · 1 项已关」⇒ **2 项开**；
`15.2744M` 主动提议收敛=开、`15.2748M` 语音转便签=开、`15.2146M` AI 可在小组画布落笔=开。

且**同文件自相矛盾**：`uc-4-2:103` 引 D-10「默认直接落笔」vs `:120`「默认全关」，相隔 17 行。

⇒ 正确结论：**默认值无出处**。而 F58 的 `ai-permission-default-off.test.ts`
现在不存在也 exit 0（V-1），将来会按一个从未被裁决、且被原型与 D-10 两次否定的方向写成绿灯。

## 🔴 M-1…M-4：MCP 四件同时错（全在已签的 `agent-runtime` 束）

原型 `16.6843M` 面板逐字 `逐工具授权 · 9 个 | 默认全关 | 已开 3`，
`16.6847M` 表头 `工具 | 副作用 | 开放给`，实值含 `对外发送→需人工确认每次`、`写入外部→未开放`；
`16.6883M` 逐字「带副作用的工具永远不能设为『全体成员自动调用』，最宽只能到『需人工确认每次』」；
`16.6838M` 逐字「第三方源默认进入 **14 天隔离期**：只有平台组可调用，全量留痕，期满复核后才对成员开放」。

对照 `uc-21-1:226`：`auth_scope` 三取值（仅项目负责人/仅某团队/全体成员）。
`逐工具`/`副作用`/`需人工确认每次`/`未开放` 在 `contracts/agent-runtime/**` 与
`requirements/21-mcp/**` **全部 0 命中**；`隔离期`/`14 天`/`平台组` 在 FL 中 0 命中。

⇒ **粒度错**（服务器级 vs 逐工具）· **枚举缺两值** · **`副作用` 域字段不存在** ·
**封顶不变式不存在** · **14 天隔离期整块缺失**。O-20 很可能是在没看过这张表时把模型收窄成三值的。

## 🔴 M-5：约 45 点已裁范围凭空消失

`DECISIONS-PENDING:326`（D-20，选择 A）研究 Studio 新模块 ~21 点；
`:341`（D-21，选择 A）原型 Studio ~24 点。`DECISIONS-FINAL:97/98` 权威重述。

实核：`grep -rnE '(^|[^A-Za-z0-9_-])D-2[01]([^0-9]|$)'` 在两个 phase 的 requirements、
契约、apps、docs、.harness **零输出**。两个 phase 都没有 studio 模块。

## 🔴 A-1：`uc-8-5` 同文件两句相反（O-04 反转残留）

`:43`（R3 主流程第 1 步，实现者最先读的位置）：「管理员不是超级用户——无项目角色即不获读取权」
`:131`：「**已撤回**。按 D-18：管理员对项目层对话内容可读、必留痕，**不区分**是否持项目角色」

⇒ 照 `:43` 写测试会断言 **403**，方向相反，**而它有出处，测试会绿**。

## ⚠ 它纠正了我先前的一个错误陈述

我此前向人类报告「O-02/O-03/O-04/O-12/O-32 从未被引用过」。
**不成立**——实测引用数分别 **42 / 107 / 45 / 110 / 77**，全部大量引用。
O-04 的真实问题不是未引用，而是**反转后的旧口径残留**（A-1）。

## 从未被引用的裁决（7 条，实测）

| ID | 内容 | 危险度 |
|---|---|---|
| **D-20 / D-21** | 研究 Studio / 原型 Studio 立项，约 45 点 | 🔴 两个 phase 都没有该模块 |
| **D-U7 / TODO-1** | 人时折算三数值（折算系数/最小样本量/token 单价）待产品给 | 🔴 唯一防线是 `mock/tasks.ts:244-246` 三个裸布尔，**未进 `THRESHOLDS`** ⇒ 不受三条机械保证保护 |
| **Q-3**（合规） | 撤回 SLA 是否要印在同意书上 | 🔴 `consent-form.tsx:127` **已渲染** `{s.sla}` ⇒ 已对外承诺、合规未确认 |
| **P-3** | `01-auth` 在 phase-00 做完立刻插 | 🔴 `roadmap.yaml` 无此记录，phase-01 仍 `not_started` |
| D-U9 / P-2 | 画布乐观交互 / 注释语言 | 🟡 |

## 结构性风险：裁决 ID 命名空间撞车

`D-10…D-27` 有**两套同形定义**（权威档案 vs `00-project/DERIVED-FROM-CONTRACTS.md` 的 `D-1…D-27`）。
实测 `D-20` 的 2 处「命中」全是撞名。**任何按 ID grep 的机械审计在该区间给假阳性。**

## 21 条项目束裁决的落地：0 干净 / 20 部分 / 1 未落地（U-8）

**落地面呈两半、方向相反**：
- **权威侧已更新**：`feature_list.json` F116–F128 + `packages/contracts/src/project.ts`
- **人类要签的那份材料整体停在裁决之前**：`contracts/project/` 四件 + 三份 UC，
  逐条写着「待裁 → U-N」/「[待裁] → Q-N」

⇒ **照现状签核 = 签一份写着「待裁」的文件。**

另：`agenda_stage` 旧名实际残留 **9 处**，而三份文件把残留量记成「2 处」且行号已失效。

## 它认为没问题的（逐项实测过，避免全是问题的偏置）

1. **`spec_ref` 三层校验真的三层都查**（文件 / `^##\s+R<n>` 锚点 / 裸模板），124/124 通过
2. `depends_on` 无悬挂、无环
3. **十一束 `covers:` 并集恰为 124**，无遗漏无重叠无幽灵
4. 字段类型卫生干净（`evidence` 全 string、无废弃的 `needs_ui_signoff`）
5. **19 条 grep 锚定的 testid 全部真实存在**（1991 个 testid 逐条比对）
6. **撤回链是真正的单一事实源**（`withdrawal-flow.ts` 三份副本已收敛 + 机械防复现）
7. **待定阈值注册表 + 反证套件是真的**，且**自带反证**（证明通用 `[threshold-ok]` 放行不了具体项）
8. Q-9 的门控真会红（`no-forbidden-routes.test.ts:70` 正则禁 `DELETE /projects`）
9. **`23-asset` 自陈最干净**：`open claw` 原型 0 命中就**拒绝发明这个源**
10. **「凭空发明」的 feature 数 = 0**。问题不是发明，是 X-1 那种**伪造出处**
11. **它推翻了我先前怀疑的三条缺失**：「项目成员名单无 feature」→ F125 有；
    「Q-5/Q-6/Q-8/Q-9 无承接」→ 分别有 F122/F124、F122/F123、F118/F120、F124
