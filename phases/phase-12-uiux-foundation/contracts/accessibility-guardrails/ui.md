# 契约束 `accessibility-guardrails` — 签核①：UI（界面落点）

> ## 🔴 自检（可机械核对）：**本文件引用 0 张截图，目录尚未产出。**
>
> 目录：`phases/phase-12-uiux-foundation/ui-preview/accessibility-guardrails/`
> **该目录现在不存在。**
>
> ⚠ **本束不引入新界面**——chat/profile/org-admin 是 phase-01 已上线页面，本束只做
> 键盘可达性修复与不可见标注（aria/alt），不改变默认视觉呈现。**人类 2026-08-23 已选定
> 方案 A**：为这几个既有页面补拍本阶段专属的「界面落点参考态」截图（当前默认状态，
> 用于确认签核①问的是不是正确页面，不是修复效果本身——修复前后对比图属于 F05-F08
> 落地时各自的验收产出）。
>
> ⇒ 在 ui-prototyper 产出前，`lint-ui-material.mjs` 会持续对本束报判定④「目录不存在 /
> 0 张 png」，这是正确的红。已在 `.harness/scripts/ui-material-map.json` 补上映射行。

## 该有哪些材料（界面落点参考态，非修复效果对比）

- chat：消息输入区 + 会话列表默认状态
- profile：资料编辑表单默认状态
- org-admin：成员列表 + 权限设置弹层默认状态（弹层需处于打开态，便于确认落点）

## 索引表（ui-prototyper 产出后填写）
| 状态 | 文件名 |
|---|---|
| ⚠ 未产出：chat 消息输入区 | — |
| ⚠ 未产出：profile 资料编辑表单 | — |
| ⚠ 未产出：org-admin 权限设置弹层 | — |

> 覆盖 feature 与依据见 `design-signoff.md`（权威）。
