# 原始需求（细化）— UC-21.2 MCP 安全策略与放行评审

> 所属：阶段一 · 能跑完一场项目 / **M21 MCP 服务器**（新增模块 21-mcp）
> 来源：**运行态原型** 后台 → AI 能力 → **MCP 服务器** 页面底部的**安全策略（开关）**：
> ```
> 安全策略（开关）：
>   ✓ 新服务器默认隔离，需人工评审后放行
>   ✓ 涉客户数据的工具调用全量留痕 180 天
>   ✓ 客户机密材料仅允许本地模型处理
>   ☐ 允许 agent 自行发现新 MCP 服务器（默认关）
> ```
> 配套证据：清单里的「欧盟法规库（**第三方·未审计**） mcp://eu-reg.vendor.eu 9 工具
> **待安全评审** ● **已隔离**」＝ 第一个开关的运行结果；
> 标题行「6 台 · 5 台已连接 · **1 台隔离待评审**」。
>
> ⚠ **证据错配更正（严重）**：此前把后台数据总览的「越权调用尝试 —— 一个自建 agent 尝试访问
> 未授权的 MCP『客户 CRM』，**已拦截 7 次**」当作**开关四（允许 agent 自行发现新 MCP）**的证据，
> 并把 AC4 建在其上——**这是错的**。
> 「客户 CRM」在清单里是 **已注册、`● 已连接`、授权范围「仅项目负责人」**的服务器；
> 该拦截证明的是**授权范围（UC-21.1）的执行**，与「发现**新**服务器」是两回事。
> 该证据**已归还 UC-21.1**；**开关四的执行证据在原型中不存在，属原型确认缺失**。

> 口径（四标记）：**[原型]** ＝ `WorkspaceX Standalone.html` **运行态实际存在**的行为（有界面可查，依据 scratchpad 的 proto-01~10 抽取档案）；**[Backlog]** ＝ `Backlog Use Case.html` 的**文档要求**（原型未必实现）；**[设计]** ＝ 依据前两者反推补全，**需人类确认**；**[待确认]** ＝ 不应由实现者自行决定。
> 元数据：优先级 **P0**；估点 **5**；建议迭代 **迭代 2 · 能挂能力**（须与 UC-21.1 同批交付）。
>
> **[人类已拍板 · D-07]** MCP 管理 phase-1 最小子集 ＝ 注册 ＋ 授权范围 ＋ **默认隔离**；
> **丢掉调用量统计**（被砍项写入 R6）。
> **[编号提示 · 待确认]** `UC-10.x` 与 phase-2 的 `10-report`（UC-21.1～10.4）冲突，
> 需人类裁决全局重编号方案；本文按协调方指定的编号书写。

## R1 概览（Use Case 名称 / Actor / 目标 / 系统边界）

- **Use Case ID / 名称**：UC-21.2 / MCP 安全策略与放行评审
- **Actor**：**组织管理员**（策略配置）、**安全评审人**（对隔离服务器行使放行/维持隔离）
- **目标**：[原型] 用**四个开关**把 MCP 这条「外部能力入口」管住——
  新东西默认不可用、涉客户数据的调用留够久、机密不出自托管、
  **AI 不能自己给自己找新工具**。
- **本用例结果**：组织持有一份**四开关**的安全策略配置；
  隔离中的服务器有明确的**评审 → 放行**路径；四条策略在运行时**被服务端强制执行**，
  违规尝试被拦截并计数。
- **系统边界**：四个安全开关的配置与生效、放行评审流程（评审人 / 结论 / 理由 / 留痕）、
  隔离态的运行时阻断、涉客户数据的留痕执行、机密路由的委派（UC-9.3）、
  agent 自行发现的禁止与拦截计数。
- **核心数据对象**：安全策略（四开关）、评审记录（评审人 / 时间 / 结论 / 理由 / 证据）、
  隔离状态、留痕策略（180 天）、拦截事件与计数、审计事件。

## R2 前置条件 / 触发条件

- **前置条件**：
  - MCP 服务器清单存在（UC-21.1）。
  - 当前用户是**组织管理员**（配置开关）或**安全评审人**（执行放行评审）。
  - [UC-9.2] 组织内至少有一个已启用的开源自托管模型——否则第三条开关
    「客户机密材料仅允许本地模型处理」会导致含机密的任务全部失败（这是**预期行为**，
    见 UC-9.3 E1，但需管理员知情）。
- **触发条件**：
  - [原型] 管理员在后台 → AI 能力 → MCP 服务器页面底部开关区调整策略；
  - [设计] 有服务器处于 `已隔离 · 待安全评审` 时，评审人进入评审。
    ⚠ 原型**只有两个静态字段**（授权范围 `待安全评审` ＋ 状态 `● 已隔离`）：
    **无评审角色、无评审入口、无评审屏**，评审流程整体为设计补全。
  - [设计] 任一策略被触发（隔离态被调用、agent 尝试自行发现）时的运行时拦截。

## R3 主流程（编号步骤，写清楚谁做什么、系统怎么响应）

### 开关一 · 新服务器默认隔离，需人工评审后放行（[原型] 默认 ✓ 开）

1. **系统处理**：任何新注册的 MCP 服务器（UC-21.1 R3 第 3 步）**自动置为 `已隔离`**，
   授权范围显示为 `待安全评审`。
   **系统响应**：隔离态下——
   - 其工具**不进入** agent 工具白名单可选池；
   - 已存在的白名单引用**失效**；
   - 任何调用尝试**被服务端拒绝**并计入越权拦截计数（UC-4.4）。
2. **安全评审人操作**：[设计] 对隔离服务器发起评审（**入口原型确认缺失**）。
   **系统响应**：[设计] 评审面板需呈现判断依据——
   服务器说明（[原型] 如「**第三方·未审计**」是隔离的直接理由）、端点（内网/外网）、
   工具清单与各工具的参数 schema、**是否涉客户数据**、**是否出组织**。
3. **安全评审人操作**：[设计] 给出结论——`放行` / `维持隔离` / `有条件放行`
   （**三种结论在原型档案 0 命中**，是本文提出的设计）。
   **系统响应**：
   - `放行` → 状态转 `已连接`，并**要求同时设定授权范围**
     （不允许「放行了但范围没定」的空档）；
   - `维持隔离` → 记录理由，服务器保持不可用；
   - [设计] `有条件放行` → 只放行**部分工具**（如只放只读工具，不放写操作工具）。
     **[待确认]** 是否支持工具级放行粒度。
   所有结论必须**填理由**并留痕，**不可删除**。
4. **系统处理**：放行后的工具进入白名单可选池；
   [设计] **已放行不等于自动加入任何白名单**——仍需能力维护者显式添加（UC-21.1 A1 同源）。
5. **组织管理员操作**（可选）：把某台已放行的服务器**重新隔离**。
   **系统响应**：立即生效，列出受影响的 agent 与进行中任务；进行中的调用被终止并明确失败。

### 开关二 · 涉客户数据的工具调用全量留痕 180 天（[原型] 默认 ✓ 开；**天数可配置性 [待确认]**）

6. **系统处理**：对标注为**涉客户数据**的工具（UC-21.1 R3 第 2 步的工具级标记），
   其每一次调用**全量留痕**——请求参数、返回摘要、调用者、agent、时间、所属项目。
   **系统响应**：[原型] 留存 **180 天**。
   **[待确认] 可配置性**：180 天是原型开关文案里的**一个取值**，
   原型未说明它是**硬编码默认值**还是**示例值 / 可配置项**。
   对照 `05-rec/uc-5-4` 的处理口径——「原型出现的 180 天是**示例项目取值，不构成默认值**」——
   本用例**不得把 180 天硬编码**；按 [D-14] 它应落在「留痕保留期」参数上并支持项目级覆盖，
   默认值与允许区间**需合规负责人给出**。
   [设计] 与 UC-4.4 的 tool-call 记录是**同一份数据的加严策略**（保留期更长、字段更全），
   不是另建一套。
   **[硬前置]** 承接 UC-4.4 R7：**留痕写入失败则该次调用必须失败**——
   「没有留痕就没有调用」。
7. **系统处理**：留痕内容按 [D-16] 脱敏策略展示——
   敏感参数默认脱敏，有权者展开原值且展开动作本身留痕。

### 开关三 · 客户机密材料仅允许本地模型处理（[原型] 默认 ✓ 开）

8. **系统处理**：该开关的**执行体是 UC-9.3**——本用例只负责**开关的存在、默认值与展示**。
   **系统响应**：含机密的上下文只能被开源自托管模型处理；
   闭源 API 调用被服务端阻断；含机密任务禁止降级。
   **[设计 · 建议]** 本开关**建议不允许关闭**（只读展示为常开）——
   它是本产品最强的一条数据边界，在原型中**五处独立出现**（见 UC-9.3 头部）。
   **[待确认]** 是否允许关闭，需人类确认。

### 开关四 · 允许 agent 自行发现新 MCP 服务器（[原型] **默认关 ☐**）

9. **系统处理**：默认关闭时，**agent 不得注册、发现或连接任何未经管理员登记的 MCP 服务器**。
   **系统响应**：[设计] 任何此类尝试**被拒绝并计入越权拦截计数**。
   ⚠ **执行证据 ＝ 原型确认缺失**：开关区四行已完整渲染，
   但原型**没有任何「agent 尝试发现新服务器被拦截」的记录或计数**。
   后台数据总览的「越权调用尝试 —— 自建 agent 访问未授权的 MCP『客户 CRM』，已拦截 7 次」
   **不是本开关的证据**——客户 CRM 是**已注册、已连接**、授权范围「仅项目负责人」的服务器，
   该拦截属于**授权范围的执行（UC-21.1）**，与「发现**新**服务器」是两回事。
   **[人类已拍板对应项]** 这条开关就是 **UC-4.1 R6「Agent 自主扩大权限」**这条
   「不包含」的**技术落地点**。
10. **组织管理员操作**（不建议）：打开该开关。
    **系统响应**：[设计] 必须给出**强警告**并说明后果
    （AI 将可自行接入未经评审的外部能力源）；
    即使打开，**新发现的服务器仍走开关一的默认隔离**——两条策略叠加，
    agent 发现 ≠ agent 可用。
    **[待确认]** phase-1 是否直接**不提供打开的能力**（只读展示为常关）。
    本文按「可展示、不可开」处理，需人类确认。

11. **系统收口**：校验 AC1「四个开关存在、默认值与原型一致、且在服务端被强制执行」、
    AC2「隔离态的服务器其工具不可被调用」、AC3「涉客户数据的调用全量留痕 180 天，
    留痕失败则调用失败」、AC4「agent 无法自行发现或接入新 MCP，尝试被拦截并计数」；
    策略变更与评审结论全量留痕。

## R4 备选流程与异常流程

- **备选流程**：
  - A1：[设计] 评审可**退回补充材料**——要求注册人补充服务器说明、供应商审计报告等，
    退回理由留痕。
  - A2：[设计] 评审结论可**设有效期**（如「放行 12 个月，到期需复审」），
    对照 [D-14] 的留存策略思路与知识晋升的有效期机制。
    **[待确认]** 是否需要。
  - A3：[设计] 无隔离服务器时，待评审列表展示真实空态
    （大意：当前没有待评审的服务器；**文案待定，非原型原文**），**不生成示例条目**。
    ⚠ 原型每屏都填满样例数据、**零空态**；且原型**没有「评审队列」这个对象**——
    只有清单标题行的「1 台隔离待评审」这一个计数，本文已删去「评审队列」一词。
  - A4：[设计] 策略开关的每次变更都应在**后台数据总览**留下一条可见记录
    （策略变更是高影响动作）。
- **异常流程**：
  - E1：[设计] 评审人同时是注册人时**不得自审自批**（与 UC-3.1 A4 同源）；
    组织内无第二名评审人时明确阻断并提示。
  - E2：[设计] 放行时未设定授权范围 → 拒绝放行（不允许「已连接但范围未定」）。
  - E3：[设计 · 关键] 关闭开关一（新服务器默认隔离）时——
    **必须强警告**并说明「此后新注册的 MCP 将立即可用，不经评审」；
    **[待确认]** 是否允许关闭。本文建议**不允许关闭**。
  - E4：[设计] 关闭开关二（涉客户数据留痕）时——
    **[待确认]** 建议不允许关闭（与 [D-14] 的合规留存要求冲突）。
  - E5：[设计] 留痕存储不可用时，涉客户数据的工具调用**一律失败**（硬前置）。
  - E6：[设计] 已放行的服务器被供应商变更端点或工具集时，
    [设计] 触发**重新评审**（签名变更需重新确认，UC-21.1 E4）。
  - E7：[设计] 请求超时或依赖不可用时保留已填内容，明确失败并可安全重试。
  - E8：[设计] 权限在过程中被撤回时立即终止后续写操作；已完成步骤按审计规则保留。

## R5 权限与可见性

- **组织管理员**：可查看与调整安全策略开关（受 R7 的「哪些不可关」约束）；
  可重新隔离已放行的服务器；可查看全部评审记录与拦截计数。
- **安全评审人**：对隔离服务器行使 `放行` / `维持隔离` / `有条件放行`；
  必须填理由；**不得自审自批**。
  **[待确认]** 该角色与 UC-3.1 的**审核人**是否同一职能——
  本文按**两个独立职能**（安全评审 vs 方法论审核）书写，可由同一人兼任。
- **能力维护者**：可**只读**安全策略（用于理解「为什么这个工具用不了」）；
  不可改开关、不可评审。
- **审核人**（UC-3.1）：审核 agent 发布时可见相关服务器的评审状态，
  用于裁决工具白名单里的「越权申请」。
- **引导师 / 组长 / 组员 / 观察者**：不可见本屏；
  仅在调用被拒时看到可读的拒因（不泄露服务器端点等敏感信息）。
- **agent 自身**：**无任何权限**——不能改策略、不能发起评审、不能自行发现服务器。
- **[D-18] 管理员权力边界**在本用例不放宽：可见策略与元数据，
  不等于可读涉客户数据的留痕正文（留痕正文按脱敏与权限门控展示）。
- 未登录、链接失效或未列出的角色：默认无权限；接口不得返回越权数据。

## R6 后置条件 / 不包含

- **正常后置条件**：
  - 组织持有四开关的安全策略配置，默认值与原型一致（前三开、第四关）。
  - 隔离服务器有完整评审记录（评审人 / 时间 / 结论 / 理由），**不可删除**。
  - [原型验收] AC1a：**四个开关存在且默认值正确**（前三 ✓ 开、第四 ☐ 关）。
  - [设计验收] AC1b：四个开关**在服务端强制执行**（不是纯展示）——原型只渲染了勾选状态，
    执行行为无界面证据。
  - [原型验收] AC2a：**新服务器默认隔离**（开关一文案 ＋ 清单里的 `待安全评审 · ● 已隔离` 行）。
  - [设计验收] AC2b：隔离态的服务器其工具不可被任何 agent 调用，尝试被拒绝并计入拦截计数。
    ——**与 `uc-21-1` R7 同一条规则，统一标 `[设计]`**（原此处标 `[原型]` 与之矛盾，档案无据）。
  - [原型验收] AC3a：**涉客户数据的工具调用全量留痕 180 天**（开关二文案；
    **180 天的可配置性 [待确认]**，不得硬编码）。
  - [设计验收] AC3b：**留痕失败则调用失败**（「没有留痕就没有调用」，承接 UC-4.4 R7）。
  - [原型验收] AC4a：**开关「允许 agent 自行发现新 MCP 服务器」默认关 ☐**。
  - [设计验收] AC4b：**agent 无法自行发现或接入新 MCP 服务器**，尝试被拦截并计数。
    ⚠ **执行证据原型确认缺失**：此前援引的「已拦截 7 次」是**客户 CRM 授权范围**的拦截
    （UC-21.1），**不是发现新服务器**的拦截，不能作为本条的验收锚点。
  - [设计验收] AC5：放行必须同时设定授权范围；评审人 ≠ 注册人。
- **失败后置条件**：
  - 不产生「策略显示已开但实际未生效」的假象；策略生效状态可被验证。
- **不包含**（**[D-07] 被砍项**）：
  - **调用量统计** —— 原型的「工具调用量·近 7 天」区块**不在 phase-1 范围**；
    实现时留空或「—」，**不得造数**。
    **注意区分**：UC-4.4 的异常检测实时判据保留；面向展示的统计报表不做。
  - **服务器注册、工具发现、授权范围** —— 见 **UC-21.1**。
  - **机密路由的执行实现** —— 见 **UC-9.3**（本用例只提供开关三的存在与默认值）。
  - **涉客户数据留痕的存储与检索实现** —— 复用 **UC-4.4** 的 tool-call 审计，
    本用例只加严保留期与字段完整度，**不另建一套留痕系统**。
  - **[D-14] 五个留存参数的完整可配置化** —— 属 17-gov；
    本用例只承接「涉客户数据 180 天」这一条原型明写的策略。
  - **第三方供应商的安全审计流程本身**（合同、渗透测试、SOC2 核验）——属组织流程，
    系统只承载结论与证据附件。
  - **agent 自行发现能力的实现** —— 默认关且 phase-1 不实现。

## R7 业务规则（不随具体实现变化的硬性约束）

- **[原型] 四条安全策略与默认值**：
  | # | 策略 | 原型默认 | 执行体 |
  |---|---|---|---|
  | 1 | **新服务器默认隔离，需人工评审后放行** | ✓ 开 | 本用例 |
  | 2 | **涉客户数据的工具调用全量留痕 180 天** | ✓ 开 | 本用例 + UC-4.4 |
  | 3 | **客户机密材料仅允许本地模型处理** | ✓ 开 | **UC-9.3** |
  | 4 | **允许 agent 自行发现新 MCP 服务器** | **☐ 关** | 本用例 |
- **[设计] 开关必须是真控制而非展示**——每条都要有服务端执行点与可验证的拒绝行为。
- **[设计] 建议不可关闭的三条**：开关 1（默认隔离）、开关 2（涉客户数据留痕）、
  开关 3（机密仅本地）。**[待确认]** 需人类确认哪些可关。
- **[设计]** 开关 4 即便打开，**新发现的服务器仍走开关 1 的默认隔离**——
  两条策略叠加，**agent 发现 ≠ agent 可用**。
- **[设计]** 放行必须**同时设定授权范围**；评审人 ≠ 注册人；结论必须填理由且**不可删除**。
- **[设计 · 硬前置]** 涉客户数据的调用**留痕失败则调用失败**（承接 UC-4.4 R7）。
- **[设计]** 隔离态的服务器被调用**直接拒绝并计入越权拦截计数**
  （档案无据；与 `uc-21-1` R7 同一条规则，两处统一为 `[设计]`）。
- **[人类已拍板对应项]** 开关 4 是 UC-4.1 R6「**Agent 自主扩大权限**」这条不包含的落地点。
- 所有权限判断必须在服务端执行；隐藏入口只是界面表现，不构成安全控制。
- 策略变更、评审结论、放行、重新隔离都必须记录审计事件，并显示动作影响范围。

## R8 界面线索

- **前端入口**：[原型] 后台管理 → **AI 能力 → MCP 服务器** → 页面底部**安全策略（开关）**区：
  ```
  安全策略（开关）：
    ✓ 新服务器默认隔离，需人工评审后放行
    ✓ 涉客户数据的工具调用全量留痕 180 天
    ✓ 客户机密材料仅允许本地模型处理
    ☐ 允许 agent 自行发现新 MCP 服务器（默认关）
  ```
- **前端入口（评审）**：[原型] 清单中 `● 已隔离` 且授权范围为 `待安全评审` 的行
  （示例：「欧盟法规库（第三方·未审计）」）——**该行只有五个字段，行内无任何操作按钮**；
  标题行有「**1 台隔离待评审**」的计数。
  **[设计] 评审入口本身原型确认缺失**（原型无「评审队列」这个对象，也无 `[去评审]` 之类的按钮）。
- **前端入口（拦截结果）**：[原型] 后台 → **数据总览** →
  「`[中]` 越权调用尝试 —— 一个自建 agent 尝试访问未授权的 MCP「客户 CRM」，**已拦截 7 次**。」
  ⚠ **该条属 UC-21.1（授权范围）的执行证据**，不是本用例开关四的证据；
  本用例只在此说明「拦截计数的呈现位置在数据总览」，不据此推断开关四已实现。
- **推荐交互**：
  - [设计] 每个开关旁应有**一句「关掉会发生什么」**的后果说明；
    建议不可关的三条以**只读常开**呈现（带锁形图标与说明），而非可点但报错。
  - [设计] `已隔离` 行附「为什么被隔离」（如「第三方·未审计」）与 `[去评审]` 入口。
  - [设计] 评审面板并排呈现：服务器说明 / 端点（内外网）/ 工具清单（含涉客户数据、
    是否出组织标记）/ 结论三选 / 理由必填 / 授权范围设定（放行时必填）。
  - [设计] 开关 4 若提供打开能力，必须二次确认 + 强警告文案，
    并明确「即使打开，新服务器仍默认隔离」。
- **必须呈现的状态**：默认态、加载态、空态（无待评审服务器）、校验失败态（放行未设范围）、
  依赖失败态（留痕存储不可用）、无权限态、成功态；
  另加 `待评审`、`已放行`、`维持隔离` 三态。（七种必现状态按 D-36 统一规范实现。）
- **原型参考**：
  - `phases/requirements/WorkspaceX Standalone.html` — 后台 → AI 能力 → **MCP 服务器**（安全策略区）
  - 同上 — 后台 → **数据总览**（越权调用拦截计数）
- **UI sign-off**：
  - **安全策略四开关（含勾选状态与文案）、隔离行与「1 台隔离待评审」计数、
    数据总览的拦截告警均已存在，可直接签**。
  - **原型确认缺失 ×3**（已探明区内本该有却没有，**三者均需补画原型，不是补抽取**）：
    - **放行评审面板**（结论三选、理由填写、授权范围设定、证据附件）——
      **无入口、无按钮、无角色**；`已隔离` 行只有五个静态字段；
    - **开关的二次确认与「关掉会发生什么」的说明**——**开关区已完整渲染四行**，
      四行都只有一句文案 ＋ 勾选框，无任何确认或说明呈现；
    - **涉客户数据留痕的查看/检索入口**——MCP 页面上只有「工具调用量·近 7 天」三行统计，
      **无留痕检索入口**（若并入 UC-4.4 的审计屏，需在那边补画）。

## R9 非功能约束

- **性能/规模预期**：
  - [设计] 策略判定在调用路径上同步执行，增加时延应控制在**百毫秒级**。
  - [设计] 涉客户数据的全量留痕吞吐需能承受高频工具调用
    （原型近 7 天单工具 2,140 次量级），且**不可丢**。
- **安全/隐私/合规**：
  - **本用例是一组合规控制**，其失效等同权限失控；
    四条策略应各有**独立的集成测试与回归**，代码路径不得被功能开关绕过。
  - 留痕内容按 [D-16] 脱敏展示，展开需权限门控且展开动作留痕。
  - [D-14] 「涉客户数据 180 天」应与「留痕保留期 / 审计保留期」参数体系对齐；
    **[待确认]** 各参数默认值与允许区间仍需合规负责人给出。
  - 拦截计数进入 UC-4.4 与后台数据总览。
- **兼容与降级要求**：
  - 响应式 Web；后台以桌面端为主。
  - **本用例不允许功能降级**：策略服务不可用时，按**最严**处理
    （视为全部隔离、全部需留痕），而非放行。

## R10 已知约束 / 依赖

- **内部依赖**：
  - **UC-21.1**（服务器与工具模型、涉客户数据标记、隔离状态）；
  - **UC-9.3**（开关三的执行体）、**UC-9.2**（自托管模型可用性）；
  - **UC-4.4**（tool-call 留痕、越权拦截计数、数据总览告警）；
  - **UC-4.1**（开关四对应「Agent 自主扩大权限」的不包含）；
  - `uc-0-3` 角色本体（安全评审人）；00-core 审计模型。
- **被依赖**：UC-4.1（白名单可选池受隔离态约束）、UC-4.2（改派授权集）、
  UC-3.1（skill 数据范围声明）、17-gov（留存策略的上位体系）。
- **跨模块契约**：安全策略配置、评审记录、隔离状态、拦截计数必须使用统一标识；
  **策略的执行点必须唯一**，各模块不得各自实现一份判定。
- **[待确认 · 编号]** `UC-10.x` 与 phase-2 的 `10-report` 冲突，需全局重编号裁决。
- **[待确认 · 最关键]** **四个开关中哪些允许关闭**——
  本文建议开关 1/2/3 **不可关**、开关 4 **phase-1 不可开**，需人类确认。
- **[待确认]** **安全评审人**与 UC-3.1 的**审核人**是否同一职能。
- **[待确认]** 是否支持**工具级**的「有条件放行」（只放只读工具）。
- **[待确认]** 评审结论是否需要**有效期**与到期复审。
- **[待确认]** [D-14] 「涉客户数据 180 天」是否可配置、与五个留存参数的映射关系。
- **[待确认]** 「涉客户数据」的判定口径——按服务器整体标注还是按工具逐个标注
  （本文按**工具级**处理，见 UC-21.1 R3 第 2 步）。

## R11 切分提示（给 requirement-author）

- 建议拆分顺序：
  1. 安全策略配置模型（四开关 + 默认值 + 哪些不可关）
  2. **开关一：默认隔离的运行时阻断**（工具不进池、调用被拒、计入拦截）
  3. **放行评审流程**（评审人 ≠ 注册人、结论三选、理由必填、放行须设授权范围、不可删除）
  4. **开关四：agent 自行发现的禁止与拦截计数**（＋「即使打开也仍默认隔离」的叠加）
  5. **开关二：涉客户数据全量留痕 180 天**（复用 UC-4.4 的 tool-call 记录并加严）
     ＋「留痕失败则调用失败」
  6. 开关三的存在与默认值（执行委派给 UC-9.3）
  7. 重新隔离与受影响清单
  8. 策略变更留痕 + 数据总览可见
  9. 审计 + 自动化验收
- 第 2、4、5 项是安全内核，应各自成 feature 并有**绕过尝试被拒绝**的强验收。
- **优先级 / 先后依赖**：P0；5 点；迭代 2；依赖 UC-21.1、UC-9.3、UC-4.4。

## R12 AI Ready 验收线索

- V1（开关存在与默认值）：AC1 —— 新组织初始化后，四个开关的默认值为
  「开 / 开 / 开 / **关**」，与原型一致。
- V2（真控制）：AC1 —— 逐条验证开关是服务端控制而非展示：
  关掉某可关开关后对应拒绝行为消失；开启后恢复（对不可关的开关，验证其无法被关闭）。
- V3（默认隔离）：AC2 —— 新注册一台服务器：状态为 `已隔离`、授权范围 `待安全评审`；
  其工具不进白名单可选池；直接调用被拒并计入拦截计数。
- V4（评审流程）：AC5 —— 注册人尝试自审被拒；
  放行时未设授权范围被拒；放行后状态转 `已连接` 且工具进入可选池；
  结论与理由不可删除。
- V5（有条件放行）：只放行部分工具时，未放行的工具仍不可用（若该粒度被采纳）。
- V6（留痕）：AC3 —— 调用一个标注为涉客户数据的工具，
  留痕包含请求参数、返回摘要、调用者、agent、时间、项目，保留期 180 天可检索。
- V7（留痕硬前置）：AC3 —— 模拟留痕存储不可用，该次涉客户数据的调用**必须失败**。
- V8（agent 不能自扩权）：AC4 —— 模拟 agent 尝试注册/发现/连接未登记的 MCP，
  被拒绝并在后台数据总览的「越权调用尝试」中计数递增。
- V9（叠加策略）：即便开关 4 被打开（若允许），agent 发现的新服务器**仍为隔离态**，
  其工具依然不可用。
- V10（机密委派）：AC1 —— 开关三开启时，含机密的调用只走自托管模型（由 UC-9.3 执行）；
  验证该开关与 UC-9.3 的行为一致。
- V11（重新隔离）：把已放行的服务器重新隔离，列出受影响的 agent 与进行中任务，
  进行中的调用被终止并明确失败。
- V12（最严降级）：模拟策略服务不可用，系统按最严处理（视为全部隔离），**不放行**。
- V13（权限态）：逐一使用 组织管理员 / 安全评审人 / 能力维护者 / 审核人 / 引导师 /
  组员 / 观察者 / 未授权用户访问，可读数据与可执行动作严格符合 R5。
- V14（空态 · [设计]）：无待评审服务器时，待评审列表显示真实空态，不生成示例条目。
- V15（不造数态）：工具调用量区块为空或「—」，不出现编造的统计数字。
- V16（审计态）：策略变更、评审结论、放行、重新隔离可按操作者、时间、服务器检索。
