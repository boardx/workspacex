# 原始需求（细化）— UC-7.1 模板库与环节绑定

> 所属：阶段一 · 能跑完一场项目 / M7 可视化模板  
> 来源：`Backlog Use Case.html` 的 UC-7.1；UI 线索来自 `WorkspaceX Standalone.html`
> （后台「画布模板 22 个」屏、画布屏「本环节绑定的 skill」区）。  

> 口径（四标记）：**[原型]** ＝ `WorkspaceX Standalone.html` **运行态实际存在**的行为（有界面可查，依据 scratchpad 的 proto-01~10 抽取档案）；**[Backlog]** ＝ `Backlog Use Case.html` 的**文档要求**（原型未必实现）；**[设计]** ＝ 依据前两者反推补全，**需人类确认**；**[待确认]** ＝ 不应由实现者自行决定。
> 元数据：优先级 **P0**；估点 **8**；建议迭代 **迭代 4 · 能推演**。
>
> **[人类已拍板 2026-07-27 · D-08]** 图表交换格式为 **mermaid**，渲染层为 **fabric.js**，
> 指定复用既有库 **`fabric-markdown`**（已实现 13 种 mermaid 图 + 19 个《工作坊模板 A0》画布模板
> + 164 个单测）。本文的模板清单与发布流程按此口径书写。
>
> **[术语正名 · D-03]** 本文严格区分：**16 项设计配置**（蓝本设计器固定维度，其中第 9 项
> 「项目材料」、第 10 项「分组打印素材」、第 13 项「Skill 绑定」与本用例相关）·
> **N 个议程环节**（随时长档位/蓝本变化：半天 7 / 一天 11 / 两天 14 / 三天 19）·
> **9 个方法环节**（推演流水线横轴 PESTEL→PRD）。
> 本用例中凡未加限定的「环节」一律指 **议程环节**；模板与 skill 都绑在**议程环节**上。
> **注**：早期稿本使用的「蓝本 → 材料与模板」是**拼造名**，档案中不存在；已按上述三分正名。
>
> **[人类已拍板 2026-07-27 · D-11]** 原「AI 起草到**六成**」是不可实现、不可验收的模糊口径
> （且「六成」在原型档案中 **0 命中**，出自 `Backlog Use Case.html`），
> 改写为**两条结构化规则**：① 每个分区至少留一格空；② 每条草稿必须附原始引述；只提示不阻断。
> 详见 **UC-7.2**，本文不重复定义。

## R1 概览（Use Case 名称 / Actor / 目标 / 系统边界）

- **Use Case ID / 名称**：UC-7.1 / 模板库与环节绑定
- **Actor**：**引导师**、**组长**、**组员**
- **目标**：[原型] 方法论模板库（同理心地图、旅程图、假设树、HMW、商业模式画布…）绑定到
  **议程环节**，现场切环节时自动实例化给各组；AI 起草的留白规则见 **UC-7.2**（D-11 已改写，
  不再使用「六成」口径）。
- **本用例结果**：[Backlog] 用户通过“模板库与环节绑定”完成从“引导师在蓝本里给议程环节配模板与材料”到可追踪业务结果的闭环。
- **系统边界**：画布模板库（后台配置 + 发布流程）、mermaid 图表类型白名单、**议程环节**绑定、
  模板实例化、**议程环节**绑定的 skill、导出与图谱回流。
- **核心数据对象**：画布模板（`key` + 版本 + 类型 + 可见性范围 + 是否内置）、画布实例
  （记录它实例化时用的模板版本）、mermaid 图表类型白名单、**议程环节**-模板绑定关系、
  **议程环节**-skill 绑定关系、使用计数（被 N 场使用）。

## R2 前置条件 / 触发条件

- **前置条件**：
  - 用户已建立可校验的正式或临时身份，且拥有目标组织、项目、小组或资源的访问权。
  - 目标资源存在且未被删除/冻结；依赖能力“项目模板（环节绑定）、实时转录（素材来源）”可用，或界面能明确显示其不可用状态。
  - 若动作涉及个人数据、录音、原始材料或跨范围读取，相应授权与留存策略已生效。
- **触发条件**：
  - [设计] 引导师在蓝本设计器里为**议程环节**配模板与材料
    （对应 16 项设计配置的第 9 项「项目材料」、第 10 项「分组打印素材」、第 13 项「Skill 绑定」；
    项目侧对应子标签为「材料准备」）。
    **[待确认]** 这三项配置面板在档案里属**未探明**（proto-05/06/08 的未探明清单均写着
    「蓝本 16 项各配置面板」），本条入口为推断，需补抽取后确认。
  - 系统从 **推演模板** 进入本流程，并携带当前组织、项目、小组、角色和资源标识。

## R3 主流程（编号步骤，写清楚谁做什么、系统怎么响应）

### 主线 A：后台维护模板库（组织管理员）

1. **管理员操作**：[原型] 在后台「画布模板」屏浏览模板库（原型示数「22 个 · 19 已发布 · 2 草稿待审 ·
   1 已归档」），可按 `全部 / 已发布 / 草稿 / 已归档` 筛选，切换卡片/列表视图。
   **系统响应**：按可见性范围过滤（全体成员 / 仅某组），每行显示模板名、状态、结构摘要
   （如「信息栏＋6 分区＋便签」「12 格·来自 A0 模板集第 18 页」）、`key` 与版本（如 `persona v3`）、
   底层类型（`canvas` / `mermaid`）、可见性、**被 N 场使用**。
2. **管理员操作**：[原型] `[＋ 新建画布模板]`（「和 AI 聊出分区结构」），或从现成模板复制后改
   （原型示例：「高琳从 `user-journey` 复制后改的 5 阶段版」→ `journey-procurement v0.4`）。
   **系统响应**：新建的模板落 **草稿** 态，仅作者可见。
3. **管理员操作**：[原型] 走**三段发布流程**——`草稿（仅作者可见）` → `试跑（指定一个项目）` →
   `发布（全员可绑定）`。**系统响应**：[设计] `[试跑]` 要求选定唯一一个项目；
   [原型确认缺失] 「只有试跑过的模板才允许 `[发布]`」的阻断档案未证实（草稿行 `[发布][试跑]` 并列，见 R4/E1b）；
   [设计] 发布时必须指定**可见性**（全体成员 / 仅某组）。
4. **管理员操作**：[原型] 发布新版本。**系统响应**：**旧版自动归档**；
   [原型] **已经建出来的画布实例不被改动——它们记着自己用的是哪个版本**；
   归档只影响「还能不能用它新建」。
5. **管理员操作**：[原型] 配置**成员能用哪些图表类型**（mermaid 类型白名单，12 类开关）。
   **系统响应**：白名单只作用于**渲染**，不作用于**书写**——[原型]「关掉的类型顾问仍可在文档里写，
   只是不会渲染成画布对象——画布顶部会提示『有 N 条语法被忽略』」。

### 主线 B：把模板绑到**议程环节**（引导师）

6. **引导师操作**：[设计] 在蓝本设计器的模板选择面板里浏览可绑定的模板。
   **系统响应**：只列出**已发布**且当前用户可见性范围内的模板；草稿与已归档模板不可绑定。
   **[原型确认缺失]** 该选择面板的**分类维度**在档案中没有任何依据——
   早期稿本写的「按阶段浏览：共情 / 定义 / 构思 / 原型 / 验证」五个词**全档案 0 命中**，
   已删除。档案里唯一成体系的环节序列是 proto-08「设计思维标准五步」的环节链
   `对齐目标 → 现状共识 → 假设风暴 · hmw → 分组共创 · business-model → 收敛投票 → 行动项`，
   那是**议程环节链**，不是模板分类。分类维度需补画原型或由产品定义。
7. **引导师操作**：[设计] 给每个**议程环节**绑定模板与材料要求，并绑定**本议程环节可用的 skill**
   （[原型] 画布屏左栏「本环节绑定的 skill」：`提取假设 → 假设树`、`语音转便签`、`Markdown ⇄ 画布`；
   [原型] proto-08 环节链上直接印着 `03 假设风暴（当前） 45m · 模板 hmw ＋ 语音转便签`，
   即**议程环节 → 模板 + skill** 的绑定在原型中是写实的）。
   **系统响应**：校验 E1「同一议程环节最多绑两个模板」；记录绑定关系与所绑模板的**版本号**。
   **[待确认]** 「最多两个」这一上限在档案中未见，出自 `Backlog Use Case.html`。
8. **引导师操作**：[原型] 现场进入**议程环节**时自动推给对应分组。**系统响应**：为每个分组按绑定的模板版本
   **实例化**一张画布，实例上固化「模板 key + 版本」；[原型] 组员打开就是这张画布，不用自己找。
9. **系统收口**：校验 AC1「现场切**议程环节**，每组自动拿到对应模板与材料清单」与 AC2「画布实例记录其
   模板版本，模板改版不改动已建实例」；满足后记录操作者、时间、对象、结果与来源，并刷新相关列表、
   图谱、看板或通知状态。

## R4 备选流程与异常流程

- **备选流程**：
  - A1：[设计] 目标数据为空时展示空状态、原因和下一步入口，不生成示例数据冒充真实结果。系统不得丢失当前上下文，且需提示用户当前采用了哪条替代路径。
    （原型每屏均填满样例数据，**零空态**，本条无原型依据。）
  - A2：**[待确认 · D11]** **模板已归档后，已绑定该模板的议程环节能否继续实例化新画布？**
    早期稿本写「仍可正常实例化」，但 proto-03 原文是「归档只影响『**还能不能新建**』」，
    与之**直接冲突**。档案确定的部分只有：[原型] **已经建出来的画布不会被改动**、
    [原型] 已归档模板提供 `[恢复]` 出口。**「归档后已绑定该模板的议程环节可否新建实例」须由人类裁决，
    实现者不得自行决定。**
  - A3：[原型] **内置模板**（如「假设树 · flowchart · **内置不可删**」）**不可删除**，只能停用或改可见性。
- **异常流程**：
  - E1：[Backlog] 同一**议程环节**最多绑两个模板，避免摊薄时间。失败不得被呈现为成功，也不得产生无法追溯的半成品。
  - E1b：[原型确认缺失] 模板**未试跑就发布**时是否阻断，档案未证实。
    [原型] 画布模板的发布流程确为三段 `草稿(仅作者可见) → 试跑(指定一个项目) → 发布(全员可绑定)`，
    **但没有任何阻断态或提示文案**；草稿行的操作是 `[发布][试跑]` **并列**，界面上不强制先试跑。
    早期稿本引用的「试跑一场后才能发布」实际出自**项目蓝本**列表里「客户访谈复盘」一行的说明，
    **不是画布模板**，已停止在本处援引。阻断行为需补画原型。
  - E1c：[原型] 文档里写了**被白名单关闭**的 mermaid 类型时，不报错、不丢内容——保留源码，
    画布顶部提示「有 N 条语法被忽略」，并给出 `[源码]` 出口查看原文。
  - E2：[设计] 请求超时、网络中断或依赖不可用时，保留用户当前输入与最后成功数据，明确失败原因，并提供安全重试。失败不得被呈现为成功，也不得产生无法追溯的半成品。
  - E3：[设计] 同一资源被并发修改时不得静默覆盖；系统提示版本已变化，并允许刷新、对比或重新提交。失败不得被呈现为成功，也不得产生无法追溯的半成品。
  - E4：[设计] 权限在操作过程中被撤回时立即终止后续写操作；已完成步骤按审计规则保留，未提交输入留在本地草稿并提示重新授权。

## R5 权限与可见性

- **引导师**：可发起、查看并管理本 use case，范围限定在其有权访问的组织、项目或小组。
- **组长**：可在被授权范围内参与流程并查看与自身角色相关的结果。
- **组员**：可在被授权范围内参与流程并查看与自身角色相关的结果。
- **观察者**：默认不能发起或修改；仅当内容已发布且授权范围允许时只读查看脱敏结果。
- **项目负责人**（**组织角色**；原文档所称「项目经理」，按 `00-core/uc-0-3` 正名）：
  可查看项目级状态与审计摘要；**内容读取仍需其持有项目角色**；是否可代替业务 Actor
  执行写操作需由角色模板显式授权。
- **未登录、链接失效或未列出的角色**：默认无权限；接口不得返回越权数据，不能只依赖前端隐藏。

## R6 后置条件 / 不包含

- **正常后置条件**：
  - 形成与本 use case 对应的正式记录或明确状态，并可从来源页面再次打开。
  - 所有跨模块回流均携带来源资源、版本、操作者/触发者、时间和可见范围。
  - [原型验收] AC1a：现场切**议程环节**，每组自动拿到对应模板
    （[原型] 组员入口「引导师广播·环节 3/7」＋「组员打开就是这张画布，不用自己找」）。
  - [Backlog验收] AC1b：同时拿到**材料清单**——档案未见议程环节级材料清单的下发形态，属 Backlog 要求。
  - [原型验收] AC2：模板发布新版本后，已建出的画布实例内容与结构不变，且能查到它用的是哪个版本。
  - [原型验收] AC3：被白名单关闭的 mermaid 类型仍可书写、不渲染，画布顶部提示「有 N 条语法被忽略」。
- **失败后置条件**：
  - 不创建“看似成功”的正式记录；可重试内容保留为草稿，失败事件进入可观测日志。
- **不包含**：
  - **人类协作者的字符级 CRDT 与实时光标**（并发策略见 UC-7.3 的分粒度规则）；
    **AI 落笔位置指示保留**。
  - **AI 无角标、不可回滚的覆盖**（D-10：AI 默认可直接落笔，但必须带 AVA 角标且可一键回滚）。
  - mermaid 高级指令（`subgraph` / `classDef` / `style` / `click` / 主题）与图表类的像素级视觉还原
    ——[技术] `fabric-markdown` 明确列为非目标，本阶段一并排除。
  - 自定义模板的可视化编辑器（本阶段模板由后台声明式 `TemplateSpec` 维护）。
  - 不扩展到本 use case 未声明的跨组织、跨客户或公开发布能力。

## R7 业务规则（不随具体实现变化的硬性约束）

### 模板库与发布

- [Backlog] 同一**议程环节**最多绑两个模板，避免摊薄时间。
- [原型] **发布流程为三段**：`草稿（仅作者可见）` → `试跑（指定一个项目）` → `发布（全员可绑定）`。
  [原型确认缺失] 「**不可跳级 / 未试跑不得发布**」的阻断是**推断**——草稿行的操作是
  `[发布][试跑]` 并列，档案未见任何阻断。需补画原型或由产品裁定。
- [原型] **发布新版本时旧版自动归档，但已经建出来的画布不会被改动——它们记着自己用的是哪个版本**；
  [原型] **归档只影响「还能不能新建」**。
  **[待确认 · D11]** 「还能不能新建」是否覆盖「已绑定该模板的议程环节继续实例化」，
  档案未说明且与 A2 早期稿本冲突 —— 见 R4/A2，须人类裁决。
- [原型] 模板字段为一等约束：**内置模板不可删**、**可见性范围**（全体成员 / 仅某组，
  对齐 `00-core/uc-0-3` 的资源可见性字段）、**被 N 场使用**（使用计数必须真实统计，不得估算）。
- [原型] 画布实例必须**固化其模板 key 与版本号**，任何时候可回答「这张画布是哪个模板的哪一版建的」。

### mermaid 图表类型白名单（后台可配 12 类）

- [原型] 白名单取值：`flowchart` · `journey` · `mindmap` ｜ `quadrantChart` · `timeline` · `pie` ｜
  `classDiagram` · `erDiagram` · `sequenceDiagram` ｜ `gitGraph` · `gantt` · `xychart`。
- [原型] **白名单只关渲染、不关书写**：关掉的类型顾问仍可在文档里写，只是不会渲染成画布对象；
  画布顶部必须提示「**有 N 条语法被忽略**」，N 为被忽略的 mermaid 代码块条数。
- [设计] 被忽略的语法**原样保留在 Markdown 中**，不得删除、不得改写；白名单重新打开后应能正常渲染。

### 19 个《工作坊模板 A0》画布模板清单（解决原型作者留的 TODO）

> [原型] 原型作者留言：「附件 PDF 是转曲的，我只取到第 18 页『AI 商业模型画布』的完整字段，
> 其余 18 个模板的名称和格子请给我一份可复制的清单或原始文件。」
> **该 TODO 由 `fabric-markdown` 的既有实现闭合**——19 页已全部实现并通过单测，清单如下，
> `key` 即 ```` ```canvas ```` 围栏里的 `模板: <key>` 取值，**为跨模块契约的一部分，不得改名**。

| # | 模板名 | key |
|---|---|---|
| 1 | 用户画像 | `persona`（另有 ```` ```persona ```` 别名围栏） |
| 2 | PESTEL 分析 | `pestel` |
| 3 | SWOT 分析 | `swot` |
| 4 | 同理心地图 | `empathy` |
| 5 | 待完成工作画布 JTBD | `jtbd` |
| 6 | 用户旅程图 | `journey-map` |
| 7 | 价值主张画布 | `value-proposition` |
| 8 | 价值主张宣言 Ad-Lib | `adlib` |
| 9 | 商业模式画布 BMC | `bmc` |
| 10 | MVP 实验画布 | `mvp` |
| 11 | 戏剧结构金字塔 | `freytag` |
| 12 | 汉堡沟通模型 | `burger` |
| 13 | 三地平线模型 | `three-horizons` |
| 14 | HMW 问题陈述 | `hmw` |
| 15 | 黄金圈法则 | `golden-circle` |
| 16 | 三视角模型 | `three-lenses` |
| 17 | 故事板 | `storyboard` |
| 18 | AI 战略画布 | `ai-strategy` |
| 19 | AI 商业模型画布 | `ai-bmc` |

> **[待确认 · 命名冲突]** 上表 `key` 为 **`fabric-markdown` 源码实测值**
> （`src/diagrams/templates-{user,strategy,story}.ts` 的 `key:` 字段，19 个逐一核对通过）。
> 但**原型档案用的是另一套显示名**：proto-03「画布模板」屏出现
> `ai-business-model v1`、`burger-comm v2`、`user-journey`、`persona v3`、`journey-procurement v0.4`，
> 与库里的 `ai-bmc` / `burger` / `journey-map` / `persona` **不一致**。
> 两套命名孰为跨模块契约、是否需要别名映射层，**须人类裁决**，实现者不得自行取舍。

- [设计] 这 19 个模板**均为内置、不可删**；组织可改其可见性范围、可停用，但不得删除。
  （档案只对「假设树 · flowchart」一项写实了「**内置不可删**」，其余 18 项为推断。）
- [设计] 组织自建模板通过 `registerTemplate(spec)` 注册，走同一套发布流程；
  自建模板的 `key` 不得与上表冲突。
- AC1：现场切**议程环节**，每组自动拿到对应模板与材料清单。
- AC2：模板改版不改动已建实例；实例可回溯自己的模板版本。
- AC3：白名单关闭的类型可写不可渲染，且有「有 N 条语法被忽略」提示。

- 所有权限判断必须在服务端执行；隐藏入口只是界面表现，不构成安全控制。
- 任何覆盖、删除、撤销、签署、发布、写回或停用动作都必须记录审计事件，并显示动作影响范围。

## R8 界面线索

- **前端入口**：[原型] **后台管理 → 画布模板**（模板库与发布流程、mermaid 白名单开关）；
  [设计] 蓝本设计器 → 16 项设计配置里的第 9 项「项目材料」/ 第 10 项「分组打印素材」/
  第 13 项「Skill 绑定」（**议程环节**绑定的实际配置面，**[待确认]** 该面板属未探明，
  项目侧对应子标签为「材料准备」）；[原型] 画布屏左栏「本环节绑定的 skill」
  与「模板由后台配置 `[去后台 →]`」。
- **信息架构**：[原型] 画布编辑器、模板检查器与项目内嵌画布。
- **推荐交互**：
  - [原型] 后台模板列表按行呈现：模板名 · 状态 · 结构摘要 · `key vN` · 底层类型 · 可见性 · 被 N 场使用 ·
    行操作（`[编辑]` `[归档]`，草稿行为 `[发布]` `[试跑]`，已归档行为 `[恢复]`）。
  - [原型] 白名单开关区按 **12 类分 4 组、每组 3 个**呈现类型开关（档案排布：
    `flowchart · journey · mindmap ｜ quadrantChart · timeline · pie ｜
    classDiagram · erDiagram · sequenceDiagram ｜ gitGraph · gantt · xychart`），
    并在其下常驻说明
    「关掉的类型顾问仍可在文档里写，只是不会渲染成画布对象」。
  - [设计] 列表/卡片支持状态、版本或范围的快速辨识，主操作和危险操作分离。
  - [设计] 采用分步表单或右侧配置面板，必填、依赖与完成度持续可见。
  - [设计] 实时状态常驻显示，包含连接/录制/倒计时/同步状态与最后更新时间。
- **必须呈现的状态**：
  - 默认态、加载态、空态、校验失败态、依赖失败态、无权限态、成功态。
  - 涉及后台任务时显示排队/运行/需批准/完成/失败/已取消，并允许查看最近一次成功结果。
  - 涉及版本或发布时同时显示草稿状态、当前版本、来源版本和最后修改人。
- **原型参考**：
  - `/Users/shenyanbin/Documents/workspacex/phases/WorkspaceX Standalone.html`
  - `/Users/shenyanbin/Documents/workspacex/phases/Backlog Use Case.html`
- **UI sign-off**：实现前需由产品/设计确认入口、角色视图、危险动作和响应式布局。

## R9 非功能约束

- **性能/规模预期**：
  - [设计] 普通读取与本地校验应在 1 秒内给出可感知反馈；超过 1 秒显示加载状态，超过 10 秒的任务转为可离开页面的后台任务。
  - [设计] 列表、图谱、转写和审计记录必须分页或增量加载，禁止一次加载整个项目历史。
- **安全/隐私/合规**：
  - 最小权限、组织/项目/小组隔离、传输与静态加密、敏感字段脱敏、导出与 AI 读取留痕。
  - 原始材料、录音、转写、私聊、个人身份与客户数据按授权和留存策略处理。
- **兼容与降级要求**：
  - 响应式 Web；桌面端提供完整能力，移动端保证加入、查看、输入、投票/确认等现场关键动作。
  - WebSocket/实时通道不可用时降级为轮询并显示“非实时”；断网允许保留未提交输入，但不得伪装已同步。

## R10 已知约束 / 依赖

- **内部依赖**：[原型] 项目模板（环节绑定）、实时转录（素材来源）、`00-core/uc-0-3`（资源可见性范围字段）。
- **跨模块契约**：身份与权限、来源引用、版本、任务状态、审计事件和通知必须使用统一标识，避免同一对象在各模块成为不可关联的副本。
- **跨模块契约（本模块新增）**：**模板 `key`**（19 个 A0 模板 key 见 R7）与 **mermaid 图表类型标识**
  是画布、大脑、报告、任务四个模块共用的稳定标识，任何一侧不得私自改名或另起别名。

### 技术约束（[人类已拍板 D-08]，本模块四份 UC 共用）

- **数据链是硬约束，三段每段可查看可手改**：

  ```
  Markdown ⇄ mermaid 文本 ⇄ DiagramModel ⇄ Fabric 画布对象
  ```

  [原型] 说明页原文：「模板推理走 **Markdown → mermaid → 可编辑画布对象**三段，
  中间每一段都可查看和手改」；产物卡上直接印着 ```` ```mermaid → DiagramModel → fabric ````。
- **图表交换格式为 mermaid**：画布内容的权威表示是 Markdown 文档里的 ```` ```mermaid ````
  / ```` ```canvas ```` 围栏块，不是私有 JSON。任何导出、引用、写回图谱都以该文本为准。
- **渲染与编辑层为 fabric.js**（v7，center origin 约定）。
- **指定复用既有库 `fabric-markdown`**（本机路径 `projects/fabric-markdown`，`v0.1.0`）：
  已实现 **13 种 mermaid 图**（flowchart / sequenceDiagram / classDiagram / stateDiagram-v2 /
  erDiagram / mindmap / gitGraph / gantt / journey / timeline / pie / quadrantChart / xychart-beta）
  ＋ **19 个《工作坊模板 A0》画布模板** ＋ **164 个单测**（含 34 个序列化回归防护）。
  建议并入本仓 `packages/`。
- **[必须] 走一条 ADR 记录该复用决策与版本锁定**：需明确 ① 以源码并入 `packages/` 还是以依赖引入；
  ② 锁定的 `mermaid` 主版本（库依赖 `mermaid ^11.16.0`，其逻辑结构取自已弃用的
  `getDiagramFromText`，**必须锁主版本并保留纯 SVG 解析降级路径**）；③ `fabric ^7.4.0` 版本锁；
  ④ 上游改动的回流方式。
- **[设计] ReactFlow 降级为建议项**：原型说明页写「项目知识图谱把『问题 → 假设 → 证据 → 决策』
  显式画出来（生产实现用 ReactFlow）」，但**图谱侧没有任何业务规则依赖它**，
  因此本文将其记为 `[设计]` 的实现选型（见 09-kg），不作为硬约束；画布侧一律走 fabric.js。
- **[技术] 运行环境取舍**（来自库的已知取舍，直接约束验收策略）：
  - mermaid 解析必须在**真实浏览器**中运行（依赖 `getBBox` 做文本测量）；
  - **纯函数层（markdown 提取/替换、mermaid 序列化、模板引擎归区判定）可在 Node 中运行**，
    因此 round-trip 验收可脱离浏览器执行（见 R12）。
- [原型] 产品为响应式网页；完整离线模式、原生移动端、第三方 IM 与产品内自助计费在 backlog 中明确为范围外。
- **[待确认]** `fabric-markdown` 以源码并入 `packages/` 还是以 npm 依赖引入（影响 ADR 与 CI 策略）。
- **[待确认]** 单项目并发人数、组织数、材料量、音频时长和历史数据量尚未给出；容量与压测指标需在开发前定稿。
- **[待确认]** 通知渠道、消息模板、语言与时区策略未统一定义；本文仅承接原型明确写出的产品内/邮件行为。


### 原型缺口三态（B 类 · 本模块原缺此段，本次补齐）

> **未探明** ＝ 这轮没点进去看（补抽取即可）· **原型确认缺失** ＝ 已探明区内本该有却没有
> （需补画原型）· **原型待补** ＝ 点进去了、确认是空按钮/占位（需补接线）。

- **[原型确认缺失]**：① 模板选择面板的**分类维度**（原「按阶段浏览」五词全档案 0 命中）；
  ② **未试跑不得发布**的阻断态（草稿行 `[发布][试跑]` 并列，无阻断）；
  ③ 「同一议程环节最多绑两个模板」的上限提示。
- **[原型待补]**：`[＋ 新建画布模板]`（「和 AI 聊出分区结构」只有按钮与一句说明，无对话面板）；
  草稿行的 `[试跑]`（选项目的选择器未渲染）。
- **[未探明]**：蓝本 16 项中第 9「项目材料」/ 第 10「分组打印素材」/ 第 13「Skill 绑定」三个配置面板
  ——proto-05/06/08 的未探明清单均明确写着「蓝本 16 项各配置面板」，属**没点进去**，补抽取即可。

## R11 切分提示（给 requirement-author）

- 建议先拆“领域数据与权限 → 主流程服务 → 页面与状态 → 跨模块回流 → 审计/通知 → 自动化验收”。
- 单个 feature 应能在一次会话内实现并验证，不应把本 use case 的全部前后端与所有异常态塞进一个 feature。
- **建议切法（本 UC）**：① `fabric-markdown` 并入与 ADR 版本锁定 → ② 模板注册表与 19 个 A0 模板 key
  的契约测试 → ③ 模板发布状态机（草稿/试跑/发布/归档）→ ④ 实例固化模板版本 →
  ⑤ mermaid 类型白名单与「N 条语法被忽略」提示 → ⑥ **议程环节**绑定（含两模板上限）与 skill 绑定 → ⑦ 验收。
- **① 必须最先落地**：它是 07-canvas 四份 UC 的共同技术底座。
- **优先级 / 先后依赖**：P0；8 点；迭代 4 · 能推演；先满足“项目模板（环节绑定）、实时转录（素材来源）”。
- 对 P0 必做链路优先实现成功态、权限态与数据闭环，异常态不得延期到上线后。

## R12 AI Ready 验收线索

以下行为应可由自动化测试、接口断言或可复现人工步骤验证。
**[D-08 + D-35] 优先写不依赖浏览器与 `data-testid` 的 round-trip / 契约断言**——
`fabric-markdown` 的纯函数层可直接在 Node 里跑，绕开原型零 testid 的难题。

- **V0（契约态 · 优先级最高）**：模板注册表返回的 19 个 A0 模板 `key` 与 R7 表格逐字一致
  （集合相等断言）；缺一个或多一个都失败。
- V1（成功态）：AC1 —— 现场切**议程环节**，每组自动拿到对应模板与材料清单（接口断言：切议程环节后
  每个分组各产生一张画布实例，其 `template_key` / `template_version` 与环节绑定一致）。
- **V2（round-trip · Node 可跑）**：对每个 A0 模板取一份含 `字段: 值` + `## 段落` + `- 便利贴` 的
  Markdown 夹具 → 解析成 `DiagramModel` → 序列化回 Markdown，**结果与输入等价**
  （段落顺序、便签归属、字段值全部保持）。19 个模板逐个跑，任何一个不等价即失败。
- **V3（版本固化态）**：模板发布 `v2` 后，读取 `v1` 建出的画布实例，其结构与 `template_version`
  仍为 `v1`；把 `v1` 归档后，新建绑定的可选列表不含它，但已有实例仍可打开编辑。
- **V4（白名单态）**：关闭 `sequenceDiagram` 后提交一份含该类型的 Markdown ——
  接口返回的渲染结果不含该图对象，`ignored_syntax_count` 为 1，且**再次读取 Markdown 时该代码块
  原样存在**；重新打开白名单后同一文档能正常渲染出该图。
- V5（权限态）：逐一使用 组织管理员、引导师、组长、组员、观察者、未授权用户访问；
  标为「仅某组」的模板对非该组成员不可见（对齐 `uc-0-3` V3）。
- V6（空态）：模板库为空、**议程环节**未绑模板时显示真实空态与下一步，不生成示例模板。
- V7（校验失败态）：给同一**议程环节**绑第三个模板被拒绝（[Backlog] 上限规则）；未试跑的模板调用发布接口被拒绝（**[原型确认缺失]**，见 R4/E1b）。
- V8（依赖失败）：模拟网络/外部依赖失败，输入和最近成功数据保留，错误可解释且可重试。
- V9（审计态）：模板新建 / 试跑 / 发布 / 归档 / 恢复 / 白名单变更六类动作可按操作者、时间、对象检索；
  越权尝试也有安全审计记录。
