# 原始需求 — org-brain-knowledge-graph（Phase 18）

> 本文件夹是本阶段原始需求的家；**输入/上下文，不是权威**，权威是 `../feature_list.json`。
> 阅读顺序与各文件内容见 `00-overview.md`；**需要人类签什么**见 `05-signoff-plan.md`。

## 流水线
1. 人类过第 0 轮（`05-signoff-plan.md` S0-1…S0-8）。
2. ui-prototyper 出界面截图 → requirement-author 读本文件夹全部 `*.md` + UI → 生成 `../feature_list.json`。
3. 契约束 + 阶段一致性复核 → 人类签核 → claim 开工。
