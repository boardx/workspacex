# `viewer-role` — 不变量

1. **视角可见性判定只在服务端执行一次**：前端不得基于角色名在本地二次过滤数据；
   `getViewerOptions` 返回的就是可见集合的全集。（怎么断言：越权 `viewerId` 请求返回 403/`VIEWER_SCOPE_DENIED`，
   不是 200 + 空数据。）
2. **引导师是唯一能看到「主持台·全场」选项的角色**。（怎么断言：以组长/组员/观察者身份调用
   `getViewerOptions`，断言返回列表里不含 `kind: "stage"` 的全场项——除非 Q1 的观察者例外裁决生效。）
3. **观察者不可进入任何分组的原始转写/对话逐字稿**（Q1 裁决）。（怎么断言：以观察者身份请求
   分组内对话/访谈模块的详情接口，断言被拒或返回脱敏后的摘要，不含逐字稿字段。）
4. **组长/组员的视角天然锁定本组**，不存在"切到别组"这个合法操作路径。（怎么断言：`getViewerOptions`
   对组长/组员只返回一个 `kind: "group"` 条目，且其 `id` 等于该用户所属分组。）

## [待定] 未决项（不阻塞开工，需人类在签核时一并给答案）

- 观察者是「全场聚合只读」还是也可以「选进某个具体分组只读」——Q1 只回答了前者，
  `ui.md` 的视角/态矩阵已标出这一格是空的。
