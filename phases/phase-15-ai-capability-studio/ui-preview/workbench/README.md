# 浏览器验证记录

日期：2026-09-09，浏览器：Codex 内置浏览器，本地 Next 开发服务器，端口3027。

截图索引与验证边界统一记录在 `../../design-proposal/capability-studio/ui.md`，不在此重复状态。

自动验证：`pnpm --filter web exec vitest run tests/ui/capability-studio-preview.test.ts`，7/7通过。测试覆盖不可变发布、绑定分离、草稿/依赖使证据失效、失败禁止发布、上游/回退仅改草稿、路径与AI目标文件保护。

人工浏览器断言：主路径 r3 → 导入r4 → 保存r5 → 试跑 → 发布v2（仍绑定v1）→ 显式绑定v2；失败与依赖停用阻止发布。新建非法路径报错后可改正；重命名、删除确认、AI差异确认、失败返回编辑、上游合并与回退均运行过。窄屏读取 DOM 宽度390/390。没有连接生产服务。
