# 浏览器复验记录

北京时间01:04–01:10，实际 Codex in-app browser，localhost:3027。本轮 getState 返回可用浏览器，创建独立临时页后实际导航成功；此前 Mac 锁屏阻塞不再作为当前事实。基线提交967a372a0，加本轮未提交的私库连接原型及导入链接。以下为真实浏览器可见状态，非生产 API/数据库验收。

| 实际操作 | 观察结果 |
|---|---|
| 私库连接选择“草稿来源修复”，开始演示授权 | 返回目标与开始按钮锁定，只显示模拟回调；没有真实 GitHub 导航 |
| 模拟成功回调 | 两个仓库均未选，确认按钮 disabled |
| 明确选 private-research 并确认 | 仅该仓库出现在个人连接，版本1 |
| 重新授权后模拟拒绝 | 原连接ID、版本1和原仓库清单仍在 |
| 点击返回草稿来源修复示例 | 成功抵达 /preview/ai-capability-studio/workbench，编辑器可见；修复独立审查发现的错误根路径 |
| 工作台进入治理页，连接→发现差异→同配置再连接 | 差异面板仍有 legacy_search 移除/search签名变化与副作用收紧；search范围为每次确认、export_report未开放 |
| 打开模型配置，输入演示文本，模拟另一管理员保存 | 冲突面板出现，展示r1/r2及最新/本地比较表，提示敏感输入已清空。DOM value属性为null；实际输入property清空由9项动态canary测试验证，不能只凭属性null断言 |
| 在配置弹窗按 Escape | dialog计数变为0 |
| 工作台点击加载态→原生back→原生forward | 加载态URL与“加载中”一致；back恢复基础URL和文件编辑器；forward恢复?state=loading与加载文本 |
| 导入页点普通仓库适配链接 | 来源明确为固定独立fixture，许可未知、未选脚本，不复用前页输入 |
| 用 Space 勾选适配审阅，Enter 创建 | 仅生成SKILL.md和references/imported/README.md；analyze.py未导入。模板可编辑，明确不可发布/运行 |

已在会话内截取私库选择页与适配完成页真实截图，未将截图落盘，故本文件只作为操作记录，不伪造PNG路径。旧UI预览截图仅证明其原拍摄版本。尚需补充管理页完整视觉/响应式检查与新截图落盘，Y维持黄色。真实OAuth、上传、存储、试跑和发布没有在本轮调用。

01:13继续验证治理页：五个下拉分别选择“通过”并填写演示证据后，启用按钮可用；模拟凭据失败后按钮不可用。重新探测成功→启用→读取引用显示2个进行中调用→弹窗切换“允许跑完当前一轮”并填写理由→确认，页面显示只拒绝新调用。随后重新启用，旧引用快照与停用结果已清空，必须重新读取；没有把未知引用当0。此处仅验证演示动作，未写生产停用或审计。
