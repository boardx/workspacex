已按 PR #3239 当时 head `4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c` 做只读核对。结论：runtime policy 的独立类型方向正确，但现有签核操作尚未形成可实施字段 delta；尤其模型池 ID 与运行时 ID 仍是两套未映射事实，MCP 的 `credential: null` 也不能表达真正的 clear。

后续一致性修正：下表的provider/upstream新增要求只适用于single模型，不能强加给composite。组合模型的字段分支和实际执行目标不变量见composite-model-mapping.md；当前可执行草案显式限制单模型绑定，未宣称组合执行器已实现。

| 操作名 | 现字段 / 当前真实实现 | 拟增删字段及错误语义 | 现有可复用测试 | 尚缺证据 |
|---|---|---|---|---|
| `registerModel` | 入参已有 `kind/shape/vendor/.../credential/endpoint/members`；出参仅 `{modelId,status}`。[契约 L886-L912](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L886-L912)。真实写库只存展示和治理字段，`models` 表没有运行绑定或 revision。[migration L46-L80](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/migrations/0019-f48-model-pool.sql#L46-L80) | 入参加 `providerKey`、`upstreamModelId`；服务端生成 `configRevision`，出参增加它。保留 `modelId` 作为模型池稳定 ID，不改名；`vendor` 不能代替 provider。格式错误走 schema 400；provider 未部署/不可解析复用 `DEPENDENCY_UNAVAILABLE`，不得把池 ID直接当 upstream ID。 | `register-model-controller.test.ts`、`register-model-e2e.test.ts`、`credential-never-echoed.test.ts` 可扩展验证映射落库且不回显密钥。 | 没有映射列/仓储端口；没有“未知 provider 不落半截配置”测试；草案测试只验证独立 Zod shape。 |
| `configureModel` | 入参 `{modelId,patch,expectedVersion}`；patch 仅显示名、标签、合规、凭据、端点；出参 `{modelId,status,retestRequired}`。[契约 L920-L952](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L920-L952)。当前没有 application/controller/store 实现。 | patch 加可选 `providerKey/upstreamModelId`；保留 `expectedVersion` 字段，但明确其唯一语义是“客户端读到的 `configRevision`”。成功出参加新的 `configRevision`。revision 不符返回现有码 `VERSION_CHANGED`，且零写入；运行绑定、端点或凭据变化成功后返回 `retestRequired:true` 并回到现有 `待测试`。`RETEST_REQUIRED` 不应同时作为成功后的异常抛出。 | 可复用 register 的事务/秘密不回显测试，以及 `disable-model.ts` 已有 expectedVersion 比较模式。 | 无 configure 路由、CAS 写口、revision 存储、并发更新测试；也没有界定哪些 patch 必须递增 revision。 |
| `probeConnectivity` | 入参只有 `{modelId}`；出参 `{reachable,latencyMs,failureKind}`，现有码集无 `VERSION_CHANGED`。[契约 L960-L972](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L960-L972)。无实现。 | 入参加 `configRevision`；出参加 `modelConfigRef:{capabilityModelId,configRevision}`。拨号前 revision 不一致应新增复用现有码 `VERSION_CHANGED`；拨号后、记录前再次核对，避免配置在探活期间变化。`failureKind` 保留现有三值，不新增状态。 | 只能复用 provider gateway 的确定性 fake、timeout 测试模式。 | 没有 probe gateway/controller/持久化证据；也没有证明探测使用的是该 revision 对应的 provider、upstream、endpoint、credential。 |
| `recordAdmissionTest` | 入参 `{modelId,item,verdict,evidence}`；记录没有配置版本。[契约 L981-L993](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L981-L993)。已有 append-only application/PG repository，但未接 controller。[实现 L42-L55](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/src/application/model/record-admission-test.ts#L42-L55) | 入参和 `AdmissionTestRecord` 都加 `configRevision`。若不等于当前配置，复用并补签 `VERSION_CHANGED`，不追加记录。记录只能归属于一个精确 revision，不能在读取时回填“当前版本”。 | `admission-test-gate.test.ts`、`test-verdict-immutable.test.ts` 可扩展；现有测试已覆盖 append-only、后来判读覆盖较早判读及门禁阻塞项。 | 数据库记录无 revision；没有“旧配置五项全过、修改配置后不能沿用”的反证测试。 |
| `enableModel` | 入参已带 `{modelId,expectedVersion}`；出参 `{modelId,status}`。[契约 L1001-L1011](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L1001-L1011)。application 存在，但函数没有接收 expectedVersion，且无 controller/完整 ports。[实现 L88-L105](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/src/application/model/enable-model.ts#L88-L105) | 保留 `expectedVersion`，明确等于当前 `configRevision`；出参加 `configRevision`。先以 `VERSION_CHANGED` 拒绝陈旧请求；再只统计相同 revision 的五项记录。当前 revision 缺项仍用 `ADMISSION_TESTS_INCOMPLETE` 并返回具体阻塞项。 | `admission-test-gate.test.ts` 已覆盖五项门禁、组合模型及拒绝审计，可直接加入 revision 夹具。 | 当前实现完全忽略签核入参 `expectedVersion`；没有配置变化与 enable 并发测试。 |
| `listModelPool` | 出参含池 `modelId/status/vendor/.../credentialConfigured`，不含运行绑定和版本。[契约 L567-L582](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L567-L582)。该路由已接真实 PG。 | 管理行增加 `providerKey/upstreamModelId/configRevision`；不增加凭据、端点或新状态。 | `list-model-pool-e2e.test.ts`、`registry-fields.test.ts` 可验证字段完整性与秘密不回显。 | 仓储 SELECT 无这些列；无旧行映射/兼容缺省规则。 |
| `listSelectableModels` | 入参 `{consumer,purpose}`；候选仅五字段，服务端过滤已启用模型。[契约 L1098-L1103](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L1098-L1103)。application 有实现但无路由。 | 候选增加 `configRevision`；无需向选择器暴露 provider/upstream。用户仍选择稳定 `modelId`，提交时同时带所见 revision，最终映射由服务端完成。 | `selectable-set-equals-enabled.test.ts` 已覆盖状态、机密用途、组合成员和三个 consumer 的同集结果。 | 没有 HTTP/PG 证据；无“列表后配置变更，提交旧 revision 被拒绝”测试。 |
| `routeModelCall` | 入参 `requestedModelId`，出参仅 `selectedModelId/decisionId/degradedTo`。[契约 L1125-L1155](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L1125-L1155)。application 只路由池 ID；真实 run 仍直接持有 `modelProvider/modelId` 并调用 provider。[execute-run L1122](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/src/application/agent-run/execute-run.ts#L1122)，[provider dispatch L131-L139](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/src/infrastructure/agent-run/routing-model-call-port.ts#L131-L139) | 入参加与 `requestedModelId` 成对的 `requestedConfigRevision`；出参加完整 `modelBinding:{capabilityModelId,providerKey,upstreamModelId,configRevision}`，兼容期保留 `selectedModelId`。停用或 revision 失效用现有 `MODEL_NOT_SELECTABLE`；映射存在但 provider 不可执行用 `MODEL_DEPENDENCY_FAILED`；执行器最终仍可用现有 run 终态 `MODEL_PROVIDER_NOT_CONFIGURED`。 | `confidential-hard-route.test.ts`、`no-model-call-bypasses-router.test.ts`、`routing-model-call-port-*` 可扩展。 | route 无 controller/真实 PG reader；更关键的是 Agent run admission 尚未调用该映射，无法证明新 run 遵守模型停用。 |
| `registerMcpServer` | 初次注册入参要求非空 `credential`，并包含治理字段；操作未接线。[契约 L1206-L1237](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L1206-L1237) | 若统一采用 mutation，则删 `credential`、加 `credentialMutation`；初次创建时仅 `replace` 合法。`keep` 因不存在旧凭据应作为请求状态冲突拒绝；`clear` 是否允许匿名首次注册需要单独签核，不能由草案默认放开。 | 可复用 `quarantine-*`、管理员门及凭据不回显测试。 | 现有码集没有“keep 但无旧凭据”的准确错误；匿名首次注册是否允许也未签核。 |
| `discoverRemoteMcpTools` / reconnect | 当前入参 `{serverId,endpoint,credential:string|null}`，已接 controller/application/真实 HTTP MCP/PG。[契约 L1273-L1294](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L1273-L1294)。`null` 会以匿名方式探测，[composition L30-L38](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/src/infrastructure/mcp/discover-remote-mcp-tools-composition.ts#L30-L38)，但 PG 仅在非 null 时替换 secret，因此已有 secret 会被保留。[store L65-L84](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/src/infrastructure/mcp/pg-mcp-server-store.ts#L65-L84) | 删除 nullable `credential`，增加 `credentialMutation = keep \| replace{credential} \| clear`。`keep` 必须用现存凭据完成本次探测并保持 revision；`replace` 用新凭据探测成功后原子替换；`clear` 匿名探测成功后删除 secret。出参增加 `credentialConfigured`。失败不得改变原凭据。远端鉴权失败当前会折叠为 `MCP_SERVER_UNREACHABLE`；若产品要区分“凭据过期”，必须另行签核错误语义，不能假称现有实现已支持。 | `discover-remote-server.test.ts` 覆盖 admin/SSRF/成功；`http-mcp-gateway-real-protocol.test.ts` 覆盖协议；`pg-mcp-server-persistence-real-db.test.ts` 覆盖替换与状态保持。 | 无 keep 使用旧 secret 探测、clear 删除 secret、失败后原凭据保留、过期凭据分类、并发 reconnect 测试。当前 `null` 的“匿名探测但保留旧 secret”是实质歧义。 |
| `discoverMcpTools`（新增工具授权） | `McpDiscoveryResult` 已返回 `tools/added/removed/signatureChanged/tightenedByCapRecheck`。[契约 L683-L701](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/agent-runtime.ts#L683-L701)。真实实现对新工具使用现有 `未开放`；签名变化也重置为 `未开放`，且不写 whitelist。[实现 L138-L170](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/apps/api/src/application/mcp/discover-tools.ts#L138-L170) | 无需增加授权字段或状态；继续复用 `tools[].authScope` 与 `added[]`。reconnect 成功只能更新发现事实，不能自动扩大 server scope、tool scope 或 Agent whitelist。 | `new-tool-defaults-closed.test.ts` 覆盖首次发现、只读工具、管理员身份也不能绕过；`tool-scope-cap-recheck-on-side-effect-change.test.ts` 覆盖重新收紧。 | 缺真实 DB 端到端反证：已授权服务器 reconnect 新增工具后，旧 Agent whitelist 不包含新工具且 invoke 被拒绝。 |

补充核对：

- PR 草案已正确复用 `McpRunSnapshotRef`，没有创建第二套 MCP snapshot；现有冻结工具也已有内部 `credentialRevision`。[mcp-execution-snapshot L4-L8](https://github.com/boardx/workspacex/blob/4e6fbfda294a5511d86418a7c7f5f2ef04a01f4c/packages/contracts/src/mcp-execution-snapshot.ts#L4-L8)
- 草案的 `capabilityModelId` 是为消除歧义而设：当前模型池的 `modelId` 是池记录 ID，而 Agent/run 的 `modelId` 实际是直接传给 provider 的 upstream ID；二者目前没有解析层。
- 本次未执行外部模型、远程 MCP 或真实数据库测试；结论来自该 SHA 的契约、controller、application、repository、migration 与测试源码核对。未修改工作树、签核状态或 PR。
