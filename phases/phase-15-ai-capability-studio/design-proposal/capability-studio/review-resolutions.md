# 设计原型独立审查与修正

审查输入为 PR #3239 的 `50fe63022e6c141d1ce9a75bf23bc03c25d5ca2f`，审查来自三个云任务；以下记录主任务的修正与可证明边界。

## 页面交互

| 问题 | 修正 | 验证 |
|---|---|---|
| 历史版本行丢失目标版本 | 版本行把所选版本传入绑定弹窗，确认操作使用该版本 | 渲染组件先绑定v2，再从v1行确认，绑定回v1 |
| 编辑测试输入未使证据失效 | 当前测试输入归入预览状态，试跑保留输入快照；当前输入不同则禁止发布 | 渲染组件检查按钮禁用、过期提示和原始输入仍可读 |
| SPA后退可能丢失内存草稿 | 浏览器会话存储保存预览状态，恢复前经schema校验；页面链接保留丢弃确认 | 组件卸载/重挂可恢复未保存文件。原生浏览器后退/前进仍需解锁Mac后复验 |
| 快捷弹窗把单文件呈现为完整包 | 快捷示例限定仓库完整包；目录/单文件引导完整向导，向导明确选择范围 | blob URL不能出现完整包预览；契约夹具验证单文件只产生一个候选 |
| 修改来源清除部分结果 | 创建批次后锁定来源和候选，重试沿用原快照 | 渲染组件检查来源禁用，成功项保持第一次，失败项进入第二次 |

渲染组件5项、草稿状态8项、导入状态4项，共17项通过。额外进行三次受控变异：移除历史版本参数、移除输入快照比较、解除来源输入锁，分别触发对应组件断言失败；恢复后17项全过。没有仅以退出码或静态源码标记代替行为断言。

## 契约

- 导入与试跑异步失败码改为闭集，试跑复用现有 AgentRunError；消息脱敏仍由后续应用层映射与集成测试承担。
- 重试首次/后续attempt和前序ID相互约束，拒绝自引用；批次拒绝重复jobId、候选ID、跨preview和混合来源digest。
- 发布版本携带原草稿ID/revision，并与试跑证据的Skill、草稿及内容摘要交叉校验。
- 增加历史版本列表/读取操作；恢复结果同时返回草稿和来源版本摘要并检查血缘一致。请求与响应目标的关联仍须应用层/HTTP测试证明。
- 单模型执行绑定显式带shape=single；当前执行端口只有单个provider调用，组合模型不能冒充首成员。组合治理仍沿用现有契约，完整组合执行方案见composite-model-mapping.md，尚未签核或实现。

该轮契约定向14项通过（skill-development 9项 + capability-runtime-policy 5项），含未知错误码、重试谱系、重复jobId、发布证据不一致和恢复血缘反例。

## 第二轮复审（基线6617dceef）

契约review确认前五项关闭，新增成功导入来源血缘缺口：succeeded.result.sourcePin可为空或与job摘要不符。新增测试先失败（1失败/9通过），修正后skill-development 10项与runtime-policy 5项共15项通过。ImportJob现在要求成功结果携带与job一致的来源摘要；服务端实际内容真实性仍需后续集成验证。

UI复审确认历史绑定和输入失效修复，补充三项边界：单文件候选不得添加兄弟文件；终态批次需要显式开始下一批；会话恢复必须保留草稿来源。单文件反例先失败（1失败/3通过），修正夹具为仅SKILL.md。终态新增确认入口，保留本页历史后解锁新来源；运行中无此入口。导入来源纳入草稿状态schema与会话恢复。新增两项渲染回归证明结果保留和来源恢复。

另新增Model/MCP治理原型6项测试。最新整组UI为25项，最新契约为15项；浏览器复验仍未完成，原型历史仅页面内模拟，不能作为生产任务持久化证据。

## 当前外部条件

第三轮（基线23a48a0d3）独立契约review实际运行10/10并确认sourcePin关闭；UI确认单文件与来源恢复关闭，新增“打开新批次确认后又重试”的交错反例。新增组件断言先红（1失败/7通过），修复为确认期间禁止重试、确认区与提交同时检查busy。取消确认后仍可重试，来源继续锁定。新测试通过后，该组件文件为8项。

随后上传/AI修改/失败派生草稿契约新增8项，合计23项；不属于上述独立复审已接受的SHA，须另行review。

第四轮（基线c946a429）独立审实际运行23/23及contracts typecheck，确认AI补丁/上传标识/Agent pin兼容，发现空白创建响应可携带revision7和来源血缘。新增响应反例先失败，createSkillDraft改用NewBlankSkillDraft，要求revision1、无sourcePin、无历史版本血缘，修复后该测试文件5项通过。

后续Model管理delta补列表与选择器配置版本、路由请求ID/revision成对、实际路由目标绑定、引用清单与停用的配置引用；provider/upstream直接复用运行绑定schema，防止管理入口与运行时接受不同格式。该delta文件6项测试通过，仍待独立复审；不证明现有生产controller已经采用这些字段。

第五轮（管理delta基线a0126b62）独立审提出五项。映射格式与Model列表读取版本已在ef00ded9补齐；本轮补listMcpServers配置版本、拒绝空composite（新增反例先红）、modelEvidenceExchanges请求响应成对校验，及显式CapabilityAdminError闭集增量。probe/admission/enable的串模型或串revision响应均被成对校验拒绝。管理delta测试8项通过。

错误传输仍有明确实施前置：现有all-exceptions.filter.ts未采纳新错误闭集，当前生产接口也未采用新操作。正式接线必须同时将CapabilityAdminError接入reasonCode映射，并用HTTP测试证明两个新MCP错误可透出；不能只替换operation.in/out。类似地，模型适配边界必须解析modelEvidenceExchanges中的请求/响应对，不能只解析response.out后声称具备关联校验。

新增管理原型：单模型配置表单、冲突恢复和临时凭据清空；MCP原生凭据意图单选、替换输入、匿名确认和端点保留。连接configRevision与credentialRevision分开：keep凭据但变更端点增加前者，不轮换后者。管理状态/组件10项测试通过。未执行本轮真实浏览器验证，剩余控件见admin-ui-remaining.md。

2026-09-09浏览器工具明确返回Mac已锁屏且不能自动解锁。已向用户请求解锁，未绕过系统锁屏。旧截图仍是对应拍摄时刻的原型证据；新修正的浏览器历史导航和键盘验证不声明完成。代码、组件测试、契约审查和CI继续推进。

## 2026-09-09 23:16：准入证据与第六轮复审修正

新增独立连通性探测、三种人工准入判读、必填证据；配置变化保留旧输入供查看，必须明确以新版本重填。治理状态/组件12项通过，web与contracts类型检查通过。

独立审查确认1e618531上的五项契约问题关闭，又发现MCP串服务器响应；本轮补响应serverId和mcpDiscoveryExchange，校验服务器、每个工具与四类差异命名空间（包括零工具响应）。管理契约9项通过；生产adapter仍须在正式实施中采用exchange。

模型冲突现在并列显示最新配置与本地修改，明确勾选比较后才可重新应用。MCP两个结果模拟按钮共享提交前置，未确认clear或空replace都禁用。新代码待提交后的独立复审与浏览器验证。

#3241远端3975ff7b的fullstack-smoke失败日志证明GitHub403额度耗尽，已在23:03:49重置后重跑run34363590624的失败job，attempt2排队中；未判绿或合并。Mac锁屏与身份凭据未解除。Mermaid保持生产紫色、设计黄色、外部阻塞红色，并将设计工作量复估增加1.5人日，总46人日。

## 2026-09-10 第八轮：状态交错与覆盖缺口

独立契约审对bfc7d22c确认namespace finding关闭，独立33/33契约及types通过；云端无Docker，未独立执行API测试，主任务23/23隔离API证据不冒充双重复验。

UI审提出停用快照复用、MCP配置继承授权、旧差异残留、运行权限文案及失败返回丢上下文。新增反例先跑为3失败/10通过；修正后五个相关文件31/31。停用消费快照，重新启用清空旧结果；新端点/凭据配置成功后工具范围默认未开放，旧差异清空；失败保留旧配置。权限按钮明确为运行记录不可见。工作台收到fromRun仅匹配固定演示服务端引用并显示历史定位，不覆盖草稿或重放任务。

覆盖审发现的六组API/用户旅程缺口已拆入Mermaid子节点，权限矩阵与私库提案已有初稿，完整UI/UC/API尚不能签核。总估算调整以主Mermaid为准，不在此重复维护数字。
