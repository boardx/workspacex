# 设计原型独立审查与修正

审查输入为 PR #3239 的 `50fe63022e6c141d1ce9a75bf23bc03c25d5ca2f`，审查来自三个云任务；以下记录主任务的修正与可证明边界。

## 页面交互

| 问题 | 修正 | 验证 |
|---|---|---|
| 历史版本行丢失目标版本 | 版本行把所选版本传入绑定弹窗，确认操作使用该版本 | 渲染组件先绑定v2，再从v1行确认，绑定回v1 |
| 编辑测试输入未使证据失效 | 当前测试输入归入预览状态，试跑保留输入快照；当前输入不同则禁止发布 | 渲染组件检查按钮禁用、过期提示和原始输入仍可读 |
| SPA后退可能丢失内存草稿 | 浏览器会话存储保存预览状态，恢复前经schema校验；页面链接保留丢弃确认 | 组件卸载/重挂可恢复未保存文件。原生浏览器后退/前进仍需解锁Mac后复验 |
| 快捷弹窗把单文件呈现为完整包 | 快捷示例限定仓库完整包；目录/单文件引导完整向导，向导明确选择范围 | blob URL不能出现完整包预览；契约夹具验证单文件只产生一个候选 |
| 修改来源清除部分结果 | 创建批次后锁定来源和候选，重试沿用原快照 | 渲染组件检查来源禁用，成功项保持第一次，失败项进入第二次 |

渲染组件5项、草稿状态8项、导入状态4项，共17项通过。额外进行三次受控变异：移除历史版本参数、移除输入快照比较、解除来源输入锁，分别触发对应组件断言失败；恢复后17项全过。没有仅以退出码或静态源码标记代替行为断言。

## 契约

- 导入与试跑异步失败码改为闭集，试跑复用现有 AgentRunError；消息脱敏仍由后续应用层映射与集成测试承担。
- 重试首次/后续attempt和前序ID相互约束，拒绝自引用；批次拒绝重复jobId、候选ID、跨preview和混合来源digest。
- 发布版本携带原草稿ID/revision，并与试跑证据的Skill、草稿及内容摘要交叉校验。
- 增加历史版本列表/读取操作；恢复结果同时返回草稿和来源版本摘要并检查血缘一致。请求与响应目标的关联仍须应用层/HTTP测试证明。
- 单模型执行绑定显式带shape=single；当前执行端口只有单个provider调用，组合模型不能冒充首成员。组合治理仍沿用现有契约，完整组合执行方案见composite-model-mapping.md，尚未签核或实现。

契约定向14项通过，含未知错误码、重试谱系、重复jobId、发布证据不一致和恢复血缘反例。

## 当前外部条件

2026-09-09浏览器工具明确返回Mac已锁屏且不能自动解锁。已向用户请求解锁，未绕过系统锁屏。旧截图仍是对应拍摄时刻的原型证据；新修正的浏览器历史导航和键盘验证不声明完成。代码、组件测试、契约审查和CI继续推进。
