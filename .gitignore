node_modules/
# ⚠ 不带斜杠的这条是必须的：`node_modules/` 只匹配**目录**，不匹配同名的**符号链接**。
# 2026-08-09 实测事故：在 worktree 里用 `ln -s .../node_modules node_modules` 图省事，
# `git add -A` 把那个符号链接提交了进去，CI 的 pnpm install 当场秒级失败
# `ENOTDIR: not a directory, mkdir '.../node_modules'`（PR #842）。
node_modules
dist/
.turbo/
*.log
.DS_Store
.pnpm-store/
.harness/state/.cache/
# coordinator 互斥锁：单会话运行时态，不共享入库（见 .harness/scripts/lib/lock.ts）
.harness/state/coordinator-lock.json
# module-coordinator dual-write 记账（coord-service claim id 缓存，非权威，见
# .harness/scripts/lib/module-lock-state.ts）
.harness/state/module-lock-*.json
# dep-graph.md：pnpm harness dep-graph 每次运行都从 feature_list.json 重新生成，
# 提交静态快照只会过期误导，重跑一遍就是最新状态
.harness/state/dep-graph.md
# 同一条理由：.harness/templates/instances/ 下是**机器生成**的 TPL-EVD-001 证据实例
# （HMV2-066，每次 pnpm run lint:rewrite-coverage 都重写），不是人工撰写的模板实例
# ——区别于 .harness/templates/examples/ 那份手写的 dogfood 说明文档，后者要入库。
.harness/templates/instances/

# Next.js / 全栈骨架
.next/
# 构建损坏时把 .next 改名保留的现场（`.next.corrupt-<ts>/`）——`.next/` 盖不住它，
# 曾有一坨 117MB 的残骸在主 checkout 里躺了几天没人认领
.next.corrupt-*/
# ⚠ 通配，不要退回逐个枚举（2026-07-29）。
#   原先这里逐行列着 .next-prod-gate/ .next-ui-gate/ .next-e2e/ ——
#   每起一个新的预览/门控 build 目录就要记得加一行，而没人会记得。
#   phase-01 的 UI 先行原型起了 .next-oa 与 .next-tpl-preview 两个新目录，
#   两个都进了库（174 个文件，含 webpack cache 的 .pack.gz）。
#   一条通配规则顶掉一整类，比一份要人维护的清单可靠。
.next-*/
*.tsbuildinfo

# 本地环境与运行时态（派生，不入库）
.env
.env.local
**/.memory/

# Cloudflare 部署凭据（packages/coord-service）——只提交 .env.cloudflare.example
**/.env.cloudflare
.wrangler/

# 脚本派生的只读视图（由 new-sprint/claim 重生成，不入库；权威源是 feature_list.json）
**/active-features.json

# 旧系统代码：仅作实现参考，不入库（3 个完整旧代码库，5000+ 文件/含二进制）
phases/requirements/oldcode/
# Playwright 失败产物
**/test-results/
**/playwright-report/
.claude/worktrees/
.worktrees/

# evidence 日志必须入库（AGENTS.md 完成定义：没有证据 = 没有完成）
!phases/**/evidence/*.log

# 需求整改期的原始材料快照（3 个旧代码库 + 设计稿 + OCR 训练数据 + 音频，约 30MB
# 二进制）。它只在「原型 ↔ 需求」审计期间作对照用，权威来源是 phases/requirements/
# 下已整理的 99 份 UC，不入库。
phases/requirements-backup/

# 演示导出物（presentations 等），由工具生成，非事实源
output/
_fl-parts/
playwright-report/
test-results/
# #728 的对话主屏取证图（`pnpm run shots:chat-main` 的产物）。每轮迭代都会重抓，
# 属于过程物不是事实源；**通过 10 分门的那一组**才复制进
# phases/…/ui-preview/chat-main-ref/ 留档。
# ⚠ 刻意不放在 test-results/ 下 —— 那个目录被 Playwright 每次 run 清空。
.chat-shots/
.chat-behavior/
# F965 的「成果沉淀」tab 真栈取证图（`pnpm run shots:project-results` 的产物）。
# 同 `.chat-shots/` 的理由：过程物，每轮重抓，不是事实源。
.project-results-shots/
# F15 的 profile/org-admin 截图级保真度取证图（`profile-org-fidelity.spec.ts` 的产物）。
# 同 `.chat-shots/` 的理由：过程物，每轮重抓，不是事实源。
.profile-org-fidelity-shots/
# DA-19 CopilotRuntime 适配器 wire 级取证（`copilotkit-v2-runtime-adapter.spec.ts` 的
# 产物：请求/响应字节、SSE 帧、截图）。同 `.chat-shots/` 的理由：过程物，每轮重抓。
.copilotkit-v2-runtime-adapter/
# DA-19f useAgentContext 接线基座 wire 级取证（`copilotkit-v2-agent-context.spec.ts`
# 的产物：上行请求体、解析出的 context 字段）。同上，过程物，每轮重抓。
.copilotkit-v2-agent-context/
# DA-19d 人在环 wire 级取证（`copilotkit-v2-hitl.spec.ts` 的产物：SSE 帧、失败截图）。
# 同上，过程物，每轮重抓。
.copilotkit-v2-hitl/
# DA-13 活动文件工作台 wire 级取证（`copilotkit-v2-active-file-panel.spec.ts` 的产物：
# 原始/拼接后的 SSE 帧、渲染截图）。同上，过程物，每轮重抓。
.copilotkit-v2-active-file-panel/


# CI/CD 的本机配置：主机/端口/路径/并发度。**不含密码**（密码在 GitHub Secrets 与
# VM 上的 runner environment file 里），但含部署形态，仍然不进仓库。
# 模板见 .harness/config/local.env.example
.harness/config/local.env

# 私钥/证书一律不进仓库（VM SSH key、GitHub App 私钥、Cloudflare secrets 等）。
# ⚠ 这条 2026-08-05 之前一整天只活在某个工作区的未提交改动里，被一次
# `rebase --autostash` 卷走后才发现 —— 未提交的保护规则不是保护规则。
# 实际躺在这里的：.harness/config/devapp.pem（VM_SSH_KEY_PATH 指向它）。
*.pem
*.key
*.p12
*.pfx
.harness/state/DASHBOARD.md
apps/deep-agent-service/__pycache__/
apps/deep-agent-service/**/__pycache__/
apps/deep-agent-service/.venv/
apps/deep-agent-service/uv.lock.bak

# #1884：Monaco Editor 自托管静态资源，由 `apps/web/next.config.mjs` 在每次
# `next dev`/`next build` 启动时从 `node_modules/monaco-editor/min/vs` 复制而来
# （AMD loader 版），是构建产物不是事实源，不入库。
apps/web/public/monaco-editor/
