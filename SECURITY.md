# 安全策略

## 报告漏洞

**请不要用公开 issue 报告安全漏洞。** 公开 issue 会在修复发布前把问题暴露给所有人。

报告渠道（任选其一）：

- GitHub Security Advisory：仓库 Security 页 → Report a vulnerability
- 邮件：`security@boardx.us`（与站点 `/.well-known/security.txt` 公布的地址一致，由 `check-deploy.mjs` 机械核对）

报告里请尽量包含：受影响的版本或 commit、复现步骤、影响范围，以及你认为的严重程度。
能附一个最小复现用例最好。

## 我们的承诺

| 环节 | 时限 |
|---|---|
| 首次响应 | 3 个工作日内 |
| 初步定性（是否成立、严重程度） | 10 个工作日内 |
| 修复发布 | 严重与高危 30 天内；中低危随下一个常规版本 |
| 公开披露 | 修复发布后，或报告之日起 90 天，以先到者为准 |

我们会在 advisory 里署名报告者，除非你要求匿名。目前没有漏洞赏金。

## 受支持的版本

`main` 分支与最新发布版本接受安全修复。更早的版本不回溯，除非另行说明。

## 范围内

- 认证与授权绕过、越权访问他人组织或项目的数据
- 租户隔离失效（含数据库行级安全策略被绕过）
- 远程代码执行、注入、服务端请求伪造
- **Skill 沙箱逃逸**：沙箱执行模型生成的脚本，逃逸出容器或 Node 权限模型的边界
- **模型出网隔离失效**：`personal-local` 组织的请求离开本机
- 凭据泄露，含日志与错误信息中的泄露
- 供应链问题：依赖中的已知漏洞在本项目中可被触发

## 范围外

- 需要受害者已被攻陷的宿主机或已有管理员权限的攻击
- 社会工程、物理访问
- 自托管者自己的错误配置（如把数据库直接暴露在公网）
- 第三方 Skill 自身的缺陷，请报给该 Skill 的作者；若该 Skill 能借此逃逸沙箱，那属于范围内
- 没有实际影响的扫描器输出

## 自托管者须知

自托管时安全边界由你控制。至少做到：不要把数据库与对象存储暴露在公网；
为模型网关配置独立凭据并定期轮换；开启审计日志；及时升级。
