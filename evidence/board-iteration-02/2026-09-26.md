# Board Iteration 02 integration evidence

Validation was completed in the primary session against implementation SHA
`933d1e9ab` for issue #4242. Child agents did not start Docker or run end-to-end
acceptance.

## Durable Board library

The production `/studio/board` route now uses authenticated API data for Board
creation, rename, search, organization tag catalog management, AND tag filters,
server-side duplicate, archive, restore, and permanent deletion. Lifecycle and
tag mutations are revision-bound. Duplicate captures one authorized source
revision, remaps every object and internal reference, starts the target at a
fresh collaboration epoch, and persists an idempotency receipt.

```text
pnpm --filter @repo/whiteboard-core test
5 files, 36 tests passed

pnpm --filter api test:whiteboard-unit
10 files, 43 tests passed

pnpm --filter web exec vitest run \
  tests/whiteboard/whiteboard-library.test.tsx \
  tests/whiteboard/whiteboard-rewrite.test.ts \
  tests/whiteboard/live-whiteboard.test.ts \
  tests/whiteboard/collaborative-editor.test.tsx
4 files, 34 tests passed

pnpm --filter @repo/contracts test
93 files, 924 tests passed

pnpm --filter @repo/whiteboard-core typecheck
pnpm --filter @repo/contracts typecheck
pnpm --filter web typecheck
pnpm --filter api typecheck
pnpm --filter web lint
pnpm --filter api lint
all passed
```

The first sandboxed API unit invocation could not bind `127.0.0.1` and timed
out its two gateway tests. The required out-of-sandbox rerun passed all 43
tests; this was an execution-permission failure rather than a product failure.

## Primary-session full-stack acceptance

```text
pnpm exec tsx .harness/scripts/with-test-isolation.ts -- \
  pnpm --filter web exec playwright test \
  --config playwright.fullstack-smoke.config.ts \
  --no-deps --project=seeded-github-import \
  board-library-management.spec.ts

1 passed (1.5m)
```

The run started isolated PostgreSQL, Redis, MinIO, authenticated API, WebSocket
collaboration, Next.js, and Chromium. It exercised real organization tag
creation, AND filtering, search, an honest missing-thumbnail state, server-side
duplicate with retry replay and payload-conflict rejection, copied-content
isolation after the source changed, stale lifecycle rejection, and the UI paths
for duplicate, archive, restore, and permanent delete. Strict cleanup asserted
each Board and tag deletion and the isolation wrapper removed its Docker stack.

An earlier run revealed that the API search is intentionally substring-based:
`Copy-*` also matched `RetryCopy-*`. The acceptance now selects the exact Board
name from the returned candidates; the rerun above is the passing result.

## Independent review boundary

Earlier exact-SHA reviews approved frontend lifecycle behavior at `40ae5dd17`
and backend duplicate/content behavior at `4e4d34a01`, each with P0/P1/P2 =
0/0/0. Final review of `222aa119f` found that the new cursor codec required an
unwired production secret. The fix derives a domain-separated HMAC key from the
already provisioned, deployment-stable `MODEL_CREDENTIAL_KEY`, while preserving
an explicit `WHITEBOARD_CURSOR_SECRET` override. The production constructor
reproduction, cursor tests, and API typecheck pass; the corrected exact SHA must
be independently re-reviewed before the PR is considered review-complete.

This evidence does not authorize merge.
