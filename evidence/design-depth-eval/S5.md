# 深度评测 S5：5.0 / 10

> 代码版本 `05c91b73` · 14/29 条检查通过 · 由 `apps/web/e2e/parity-eval/score.mjs` 生成

| 维度 | 得分 | 通过 | 量什么 |
|---|---|---|---|
| V1 批注跨设备 | 1.00 | 3/3 | 批注存在服务端：换浏览器看得到、清存储刷新不丢 |
| V2 批注讨论 | 1.00 | 3/3 | 批注可回复、可标记解决、可重新打开 |
| V3 导出代码能交互 | 1.00 | 3/3 | 导出的 .tsx 在浏览器里跑：tabs、开关、勾选、底部导航都点得动 |
| V4 导出代码带图标 | 1.00 | 3/3 | 按钮、底部导航、图标列表的图标导出为 SVG |
| V5 查看代码 | 0.00 | 0/3 | 编辑器里直接看、复制这份代码，改了设计代码跟着变 |
| V6 真实图片 | 0.00 | 0/3 | 把占位图换成上传的真图：画布上显示、刷新还在、导出的代码带着它 |
| V7 演示模式 | 0.00 | 0/3 | 全屏演示、方向键翻页与页码、Esc 退出 |
| V8 变体进阶 | 0.00 | 0/3 | 方案旁摆着当前页、能提要求再出一组、能选出几个 |
| V9 代码规模 | 1.00 | 2/2 | 详情页与设计工作台组件都不超过 1500 行，后续功能有地方放 |
| V10 导出 PPTX | 0.00 | 0/3 | 幻灯片导出为可编辑的 .pptx：页数对、文字在 |
| **合计** | **5.0** | 14/29 | |

## 逐条

### V1 批注跨设备（3/3）

- ✅ `V1.c1` 写一条批注 ⇒ 交给服务端保存（请求带节点、页、原话）
- ✅ `V1.c2` 换一台电脑（新浏览器）打开同一个项目，看得到这条批注和它的图钉
- ✅ `V1.c3` 清掉浏览器存储再刷新，批注还在

### V2 批注讨论（3/3）

- ✅ `V2.c1` 回复一条批注：回复显示在它下面，并交给服务端
- ✅ `V2.c2` 标记解决：图钉消失、这条归到已解决
- ✅ `V2.c3` 已解决的可以重新打开，图钉回来

### V3 导出代码能交互（3/3）

- ✅ `V3.c1` 导出的组件里 tabs 点得动：点「规格」，它变成选中
- ✅ `V3.c2` 开关拨得动、勾选勾得上
- ✅ `V3.c3` 底部导航的项跳得到对应页，而且当前项跟着变

### V4 导出代码带图标（3/3）

- ✅ `V4.c1` 带图标的按钮，导出后按钮里有这个图标（SVG）
- ✅ `V4.c2` 底部导航每一项都有图标
- ✅ `V4.c3` 图标列表每一行前面有图标

### V5 查看代码（0/3）

- ❌ `V5.c1` 点「代码」：面板里是这个原型的 React 代码，含当前页的文案
  - TimeoutError: locator.click: Timeout 3000ms exceeded.
- ❌ `V5.c2` 一键复制：剪贴板里就是这份代码
  - TimeoutError: locator.click: Timeout 3000ms exceeded.
- ❌ `V5.c3` 在画布上改了字，面板里的代码跟着变
  - TimeoutError: locator.click: Timeout 3000ms exceeded.

### V6 真实图片（0/3）

- ❌ `V6.c1` 选中占位图、上传一张图片：画布上这块变成这张图
  - TimeoutError: locator.setInputFiles: Timeout 3000ms exceeded.
- ❌ `V6.c2` 刷新之后图片还在（写进了设计本身，不是只在这个页面上）
  - TimeoutError: locator.setInputFiles: Timeout 3000ms exceeded.
- ❌ `V6.c3` 导出的 React 代码里也带着这张图
  - TimeoutError: locator.setInputFiles: Timeout 3000ms exceeded.

### V7 演示模式（0/3）

- ❌ `V7.c1` 点「演示」：全屏只放第一页，不带编辑器的界面
  - TimeoutError: locator.click: Timeout 3000ms exceeded.
- ❌ `V7.c2` 方向键翻页，页码跟着走
  - TimeoutError: locator.click: Timeout 3000ms exceeded.
- ❌ `V7.c3` Esc 退出，回到编辑器
  - TimeoutError: locator.click: Timeout 3000ms exceeded.

### V8 变体进阶（0/3）

- ❌ `V8.c1` 方案旁边摆着「当前」这一页，方便对照
  - Error: expect(locator).toContainText(expected) failed
- ❌ `V8.c2` 写一句要求再出一组：请求带着这句话
  - TimeoutError: locator.fill: Timeout 3000ms exceeded.
- ❌ `V8.c3` 选要几个方案：选 2 个，就出 2 个
  - TimeoutError: locator.selectOption: Timeout 3000ms exceeded.

### V9 代码规模（2/2）

- ✅ `V9.c1` 详情页 detail-screen.tsx 不超过 1500 行
- ✅ `V9.c2` 设计工作台没有任何一个组件文件超过 1500 行

### V10 导出 PPTX（0/3）

- ❌ `V10.c1` 幻灯片项目能导出 .pptx（是个像样的 PowerPoint 包）
  - TimeoutError: locator.click: Timeout 3000ms exceeded.
- ❌ `V10.c2` 几页幻灯片就是几页：三页进三页出
  - TimeoutError: locator.click: Timeout 3000ms exceeded.
- ❌ `V10.c3` 每页的文字都在，能在 PowerPoint 里改（不是一张图）
  - TimeoutError: locator.click: Timeout 3000ms exceeded.
