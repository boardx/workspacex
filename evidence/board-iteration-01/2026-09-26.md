# Board Iteration 01 integration evidence

Validated source SHA before evidence-only commit: `5a982b4f021a50622dd7bf719b4d2b66029470d1`.

## Canonical surface and command regression

```text
pnpm --filter whiteboard-core test
4 files, 25 tests passed

pnpm --filter web exec vitest run \
  tests/ui/board-a11y-mirror.test.tsx \
  tests/ui/board-fabric-projection-adapter.test.ts \
  tests/ui/board-fabric-projection-registry.test.ts \
  tests/ui/board-fabric-surface.test.tsx \
  tests/ui/board-fabric-event-command-bridge.test.ts \
  tests/whiteboard/collaborative-editor.test.tsx
6 files, 34 tests passed

pnpm --filter whiteboard-core typecheck
passed

pnpm --filter web typecheck
passed
```

The combined checks cover the Fabric incremental projection registry, visible isolation of unsupported or failed objects, readonly command rejection, gesture-to-command transaction boundaries, stable command idempotency, selection/a11y projection, and truthful undo/redo feedback.

## Primary-session full-stack acceptance

```text
pnpm exec tsx .harness/scripts/with-test-isolation.ts -- \
  pnpm --filter web exec playwright test \
  --config playwright.fullstack-smoke.config.ts \
  --project=seeded-github-import \
  board-fabric-surface.spec.ts

84 passed, 1 skipped (4.9m)
board-fabric-surface.spec.ts: 1 passed (5.6s)
```

The run used isolated PostgreSQL, Redis, MinIO, authenticated API, production WebSocket collaboration, Next.js, and Chromium. The Board scenario created a real Board through HTTP, opened `/studio/board/:boardId`, observed Yjs synchronization, rendered four canonical objects through Fabric, verified no DOM/SVG object fallback, exercised the 5%–800% zoom limits, Fit Selection, Fit Board, keyboard outline selection, and panning. The isolation wrapper removed its Docker project after the run; a follow-up `docker compose ... ps --all` returned no containers.

## Independent reviews

- Undo/redo outcome correction `f1cb8437f309fd05a29c5399ee134dc03985f3c3`: APPROVE, P0/P1/P2 = 0/0/0, 12/12 targeted tests passed.
- Projection isolation `d0f24a2a0b38164fcb1a947b96dfadba27cd0f9d`: APPROVE, P0/P1/P2 = 0/0/0, 19/19 exact-snapshot tests and web typecheck passed.
- Gesture command envelope `5a982b4f021a50622dd7bf719b4d2b66029470d1`: independent verdict is recorded on issue #4238 and the Iteration 01 PR once complete.

No merge authorization is implied by this evidence.
