# 工作台执行反馈精简（#2905）

2026-09-07 用户通过三张 devapp 截图要求改进计划区，并明确指定执行动画使用蝴蝶、删除“准备 → 执行 → 回复”面板。

## 交互方案

执行过程作为唯一可见运行反馈：蝴蝶表示仍在运行，旁边保留实时用时和真实 Tool/Skill 活动；默认折叠，用户展开后流式查看进展摘要和调用详情。终态停止动画。保留现有 journal、回放和控制语义。

输入区上方只展示有内容或需要操作的计划。普通问候不显示“当前计划 0 步”。有步骤时默认显示单行执行计划摘要，点击整行展开；步骤状态按服务端事实呈现，不将 run 结束推断成每一步成功。

暂停恢复、计划确认、失败恢复、待应用修改和孤立约束的处理入口不能被折叠隐藏。切换线程后清除上一线程的编辑和折叠状态。删除重复运行卡仅改变视觉呈现，保留运行订阅、停止按钮和辅助技术状态播报。

视觉沿用应用现有背景、边框、文字、青绿蝴蝶和字号 token。取消重复阶段条和 PLAN 卡头，减少输入区固定高度。尊重 reduced-motion，键盘可切换详情，移动端不横向溢出。

## 验证边界

组件测试覆盖空计划、真实步骤、折叠、确认/恢复、线程切换及运行状态。浏览器验证实际聊天挂载、真实本地流式响应、蝴蝶动画 CSS 与终态；移动端计划状态通过明确的 HTTP fixture 注入，仅证明显示与入口可达，不冒充后台暂停执行验收。

不改 Tools/Skills 实现、模型调用或权限契约。此修复使用独立 worktree，复用现有 PostgreSQL/Redis 测试环境。

## 用户追加的交互闭环

- 确认意图：恢复 CopilotKit generative UI 注册渲染；待处理请求以持久 run/request 身份绑定，显示可交互确认对话框，关闭后保留重新打开入口，避免重复窗口与迟到请求串用。
- 运行中调整方向：复用既有插话入口与队列，在安全步骤边界接收；不取消当前工具。权威线程权限独立于产物/材料加载，跨用户或线程迟到 ACK 不得清除新草稿。
- 刷新恢复：首次 GET 确认运行即结束“恢复中”，展示真实后台状态并继续订阅；长任务持续返回有效快照不得因经过 210 秒被误判停滞，不重复启动。

首轮浏览器环境未进入功能断言：真实项目列表请求与第二项登录导航超时。保留失败证据并继续排查，不能作为验收通过。
