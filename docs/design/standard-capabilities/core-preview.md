# Devapp 核心测试版

2026-09-07。用户要求先交付可测试核心，再继续迭代 PR #2869；不合并 main。

固定候选：`b1a31d8f49354b3586f2dbc02f7534f2a30d9714`。
部署流水线：https://github.com/boardx/workspacex/actions/runs/34100037730
入口：https://devapp.boardx.us/chat

状态：已推送并启动部署门控，尚未宣称部署完成或远端用户链路通过。

## 本次验证顺序

1. 确认部署步骤成功且运行代码对应固定 SHA；受信脚本哈希检查不能跳过。
2. 登录普通成员账号，确认最新工作台 UI、完整 Skill 选择器和会话恢复。
3. 用合成输入生成一个简单表格或 PDF，等待真实产物，下载并独立重开，确认内容正确。
4. 切换会话再返回，确认任务状态和产物仍可见。

## 能力边界

平台标准包、已整合的 peer UI、Office 指引、文本子任务 beta，以及原生工具实现包含在代码中。T042 当前只确认文本子任务，未声称子任务文件产物已交付。S013 网页输出仍有 text/html 发布限制，后续独立版本修复。真实 ASR 仍依赖供应商配置。

现有 devapp 受信部署脚本没有新的 sessions-only 原生沙箱配置。缺少 native socket/key 时运行走 legacy 路径。新版原生 Skill 的本地真实模型证据不能转算成 legacy 远端证据；本次必须另验普通用户文件交付。原生浏览器、并发沙箱与缓存撤权等能力，须完成部署配置后再宣布远端启用。

后续迭代不改变本次 workflow 固定 SHA。部署/冒烟实际结果将在本文件追加，不用 healthz 成功替代用户可见验收。
