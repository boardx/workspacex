# E008 evidence reuse audit after persistent tool snapshots

This is an evidence inventory, not a new execution claim. The scope is the existing E003/E004 migration and drain path; no separate migration router or registry should be built.

- `f5fbf35f4`: `verification.md` / `final-green.txt` in this directory record 12 passing real-PG tests. A newly constructed repository reclaims the persisted native/legacy profile. Existing native approval runs drain when admission is disabled; missing native dependencies fail before model dispatch rather than falling back to legacy. The remote model and owner in the drain test are bounded fixtures, not an external model or real sandbox.
- Persistent capability snapshot evidence is in `../W10-native/verification.md`, `owner-green.txt` (4 real-PG cases), and `python-green.txt` (57 passed, 3 explicitly skipped real-sandbox cases). Candidate-only new tool names do not enter an old binding; removal/change of an existing policy is refused; the three previously reviewed interaction permissions only strengthen. The official graph refuses both unknown persisted tool names and actual calls outside the saved tool set.
- Skill pinning already has dedicated tests: `test_native_factory.py::test_mismatched_package_binding_refuses_before_client`; `test_native_graph.py::test_pins_must_be_full_and_match_mounted_bytes` and `test_checkpoint_cache_rejects_other_session_or_package`; `test_skill_packages.py::test_native_mode_does_not_silently_rebuild_legacy_packages`. These exercise complete-package digest and session/cache bindings, not merely a SKILL.md name. Their existing Python evidence must be used rather than claiming a new test run here.

No additional production routing change follows from this audit. Historical platform alias/catalog compatibility and actual deployed rollback remain distinct from these runtime tests; this inventory does not claim a live deployment rollback, a remote restart, or successful execution of a newly upgraded skill. W08 production document parsing verification takes priority next; avoid re-running the same E008 groups solely to manufacture additional evidence files.
