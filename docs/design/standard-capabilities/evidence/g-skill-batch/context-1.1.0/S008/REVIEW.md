# S008 standard-context 1.1.0 representative default-chain review

The actual model loaded the new immutable 1.1.0 package, searched/read the real authorized current-thread source, published the artifact and completed PG writeback. Canonical wrapper exited 0. The unrelated arithmetic control made zero tool calls. Full sourceId/sourceVersion in the generated text match the actual read response.

Manual review preserves the synthetic facts and unknowns: three-person sample; budget/time unknown; agenda durations and roles are proposals/pending, no attendee or actual date invented. The report states current-files scope; its comment about no organization-index material must not be read as proof that the unqueried index is empty.

This revalidates the default chain under the new package, not live model quality on every optional hybrid route. Configured hybrid/permission/no-configuration behavior is covered by the separate real PG/HTTP seven-test group. Older 1.0.0 evidence remains separate.
