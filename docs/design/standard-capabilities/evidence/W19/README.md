# W19 package component evidence

Executed from worktree root:
```
node --import tsx skills/standard-methods/scripts/build.ts
node --import tsx skills/standard-methods/scripts/verify.ts
```
Both exit 0. Raw output: package-source.txt. Real existing FileSkillStarterPackSource loads the generated manifest, verifySkillStarterPack checks digest/schema, every decoded file equals its source, reference paths resolve, absent deployment root and missing version yield null, tampered contents are rejected.

Content review cases encoded in package references: repeated transcript does not add a participant; unknown identity remains unknown; contradictory evidence preserved; each theme has locator; each planned question maps to objective; sensitive question requires necessity and skipping; unavailable tools block dependent work. These are authored method requirements, not dynamic model-evaluation evidence. No invented respondents/findings/files. No DB, ASR, external model, production import or publication was run. G-SKILL model behavior/production availability remain unverified.
