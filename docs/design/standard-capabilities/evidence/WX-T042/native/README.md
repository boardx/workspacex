# Native dispatch incremental evidence

The existing PostgreSQL queue, claim/cancellation locks, model executor and user status routes remain authoritative. The new `spawn_async_task` callback binds actual run/attempt/lease/binding/tool call identity and checks the actual arguments through existing execution authority. Context references are strict JSON strings of the existing KnowledgeReadInput, e.g. `{"sourceId":"chat-attachment:actual-id","versionId":"sha256:actual-source-digest"}`. Optional projectId must pass the existing context source authorization. Invalid JSON/extra identity fields, duplicate references, unknown versions and truncated content are rejected before enqueue.

The existing queue context field stores a versioned reference envelope, not source bodies. The injected context resolver rechecks current parent visibility and each source version immediately before model consumption. Legacy unmarked context strings retain their prior behavior. Replay returns the actual persisted child status and changed inputs conflict through the existing PostgreSQL idempotency contract.

Contract test red (missing schema) and green are retained. Python `tests/test_standard_subtask_tools.py`: 6/6 passed, including trusted identity, bounded errors without retry and actual completed replay output. API typecheck initially passed implementation, then new HTTP fixture exposed unknown JSON typing (fixed with shared output schema).

First real database group: 13 passed/5 failed. All 12 pre-existing queue tests passed; five new cases correctly encountered source-unavailable because the synthetic extracted attachment fixture omitted the mandatory extracted_excerpt. The fixture was corrected to include its actual synthetic text; no runtime source filter was weakened. Final rerun 91875 passed all 18/18 (new 6 + existing 12), wrapper exited 0 and cleaned its stack in 12 seconds, peak 10 DB connections. The new real HTTP test and source permission revocation-before-execution tests passed.

Boundary: this increment returns a durable child ID/status, not a ready artifact. The existing child executor is text-only; child-produced artifact integration is still separate work. Callback transport/authority substitutions in the focused service fixture are explicit; PostgreSQL queue, actual parent requester, visibility, source reader and object bytes are real. No claim of private-cloud transport acceptance or fully implemented T042 is made here.

Final commands: `pnpm exec tsx .harness/scripts/with-test-isolation.ts -- pnpm --filter @repo/api exec vitest run tests/agent-runtime/standard-subtask-tools.test.ts tests/agent-runtime/subtask-run-store-real-db.test.ts`; `pnpm --filter @repo/contracts exec vitest run tests/standard-subtask-tools.test.ts` (4/4 including generator freshness); `pnpm --filter @repo/api exec tsc --noEmit` exit 0. Python command from apps/deep-agent-service: `.venv/bin/python -m pytest tests/test_standard_subtask_tools.py -q` (6/6).
