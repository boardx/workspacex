Workflow: sum nonnegative counts from a UTF-8 CSV with header count. The Python standard-library script takes the CSV path as argv[1], prints JSON {"total": number}, and exits nonzero for any negative number. No runtime installs or network. Positive: count\n1\n2 gives total 3. Negative: count\n-1 must be rejected. A plain greeting is not a trigger. Create an editable draft, not a published skill.