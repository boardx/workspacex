
════════ 真实模型 PDF 用例 —— 逐条断言结论 ════════
✅ ① 真实 /chat 可用（composer 就绪）
     baseURL=http://127.0.0.1:42168
✅ ② run 真的跑起来并走到终态（不是卡在生成中直到超时，也不是压根没起来）
     发出后 494s，composer data-send-state=disabled；期间观测到 running 态：是（上限 900s）
✅ ③ 全程没有出现工具授权/审批弹窗（pdf-create 是 L0，#2782）
     chat-tool-permission-dialog / chat-approval-card / restored-run-approval 在整轮轮询中一次都没有出现
✅ ④ 全程没有出现错误横幅（#2786 兜底文案 / #2795 terminated 都落在这个节点上）
     copilotkit-v2-error 从未出现
✅ ⑤ 助手气泡没有逐字重复（#2780 规划句重复）
     2 条助手气泡（长度≥12），两两不同
✅ ⑥ 真的产出了一个 PDF 产物（按字节判，不按模型措辞判）
     产出文件字节数=1299987，前 5 字节=「%PDF-」（合格的 PDF 必须是 %PDF-）
✅ ⑦ SSE/WS 全程没有被掐断（#2795 空闲超时/WS 提前关闭）
     流失败事件 0 条；传输层控制台报错 0 条
✅ ⑧ 没有未捕获的页面异常
     pageerror 0 条
── 控制台 error 0 条 · 页面异常 0 条 · 失败请求 16 条 · 流事件 34 条 · WS 事件 86 条
════════ 8/8 通过 ════════
