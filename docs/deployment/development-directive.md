# 持续开发要求（用户 2026-09-11 明确指令）

不等待云端配置，连续完成所有可独立开发与验证的 P0/P1 工作。不得因阶段性提交、创建 PR 或单个模块完成而自行暂停，不再要求用户反复说“继续”。在等待 CI 或云端条件时继续其他可执行工作。

交付目标保持 Starter 与 production 两档、准备条件满足后 300 秒 provision、业务文件 OSS。Mermaid 逐项关联实现和验证证据；目标为所有可执行项通过。尚无真实云端证据的验收不能伪造为 pass，须单列云验收状态，不能借此阻塞代码开发。

用户追加的颜色规则：绿色代表完成，紫色代表验收通过，红色代表有问题需要用户确认或已阻塞。绿色、紫色节点必须显示对应 commit；验收层级必须明确，不将代码 CI 通过冒充真实云交付验收。开发中使用蓝色，待完成使用灰色。
