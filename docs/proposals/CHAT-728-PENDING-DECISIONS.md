# issue #728（chat 主屏保真迭代）待人类裁决事项汇总

> 纯整理文档，不改判据、不改代码、不下结论。来源：
> `phases/phase-01-run-a-project/ui-preview/chat-main-ref/ITERATION-LOG.md`（main，round 0-4/12 摘要）
> + 同名文件在 worktree `compassionate-euler-0c3615` 里保留的更完整版本（round 5-12 逐轮细节，
> 与下方 issue/PR 引用互相印证，未发现矛盾）+ `gh issue view 728 --comments` 全量评论
> + PR #831（已裁决）+ `docs/proposals/PROP-CHAT-P7-INPROGRESS-STATUS-001.md`（已起草未裁决）。
> 实测：本文整理时 main 在 `6f8fe92f`，issue #728 工作分支 HEAD 在 `fe98691f`（尚未合并 main）。

---

## 零、已经裁决过的事项（不要重新问，仅供确认闭环）

### #831 —— D2/D3/D4/D5 契约缺字段（2026-08-09 人类已裁决）
- **裁决内容**（issue #831 评论，逐字）：
  - D-1（agent 角色）：方案 A —— 新增 `role: z.string()`，`duty` 保留为能力描述；
    建 agent 时必填、admin 可改、存量回填 `duty` 前 8 字并标记待人工确认。
  - D-2（D3 线程副行）：方案 A —— 状态徽标从已有数据派生，**不做**「负责 agent」字段。
  - D-3（D4 面包屑）：方案 A —— 只显示真实存在的层级，**不建**「项目阶段/周次」模型。
  - D-4（判据本身）：把 D3「负责 agent」、D4「阶段/周次」两个子句从评分卡中移除。
  - 合格门槛同时从 10 分降到 **9 分**。
- **当前状态**：裁决已落地到 rubric 文字层面（PR #836 "标准(clr): #834 落地 #831 裁决"已合入 main），
  但 **D-1（`role` 字段的契约变更 + ADR-023 签核 + 实现）尚未执行**——round 11/12 的日志仍写
  "D 组 0/10（main 上记录，未本轮复核）"，即 D 组还没有用新判据重新评分过。
- **不需要人类再回答这条**；如果人类想确认，唯一要问的是「D-1 的契约变更工作要不要现在排期」，
  不是要不要采纳裁决本身。

### H3 硬门本身（不是下面 H3-a 那条）—— 已修复
`verify:chat-read` 曾经间歇失败（超时窗口太紧），已由 #733/PR #735 修复，rubric 里也记录了
"H3 曾经的权宜判据，已废止"。这条**不是**开放问题，下面 H3-a 讨论的是另一件事（同一 SHA
上要不要允许重跑取绿），不要混为一谈。

---

## 一、硬门（H1-H4）—— 待裁决

### H3-a｜同一 SHA 上重跑取绿，算不算通过 H3
- **冲突是什么**：rubric 原文只写"`verify:base` 与 `verify:chat-read` 都绿"，没有一句话
  提到"允许重跑"。但 round 10、11、12 里 `verify:chat-read` 都出现过"第一次红（超时/
  `ERR_ABORTED`/停在 `/login`）、原地重跑变绿"的情况，评分员每次都判 H3 通过，并且
  **每次都在报告里明确写"这是我自己行使的裁量，判据没有明文授权"**。
- **涉及文件/判据**：`.harness/rubrics/chat-main-fidelity-rubric.md` H3 行；
  ITERATION-LOG round 10/11/12。
- **目前实现者怎么处理的**：完全没插手——H3 由评分员独立判定，实现者只是如实记录评分员
  的判断和评分员自己标注的"这不是判据明文授权的"。没有自行放宽或收紧。
- **需要人类回答的具体问题**（二选一或给出第三种）：
  - **选项 A**：把"允许在同一 SHA 上重跑（不改动树）取绿，只要失败签名可判定为环境/负载
    型假阴性（如超时、Docker 网络耗尽），不是产品代码回归"写进 H3 判据文字，追认现在
    评分员一直在做的事。
  - **选项 B**：H3 严格化为"第一次跑必须绿"，不允许重跑；已经用重跑判过的几轮（10/11/12）
    需要重新核实是否第一次就绿，若不是则那几轮的总分要改判。
  - **选项 C**：允许重跑，但加数量/条件限制（例如最多重跑一次，且必须同时提供两次运行的
    完整日志作为证据，不能只报告"绿了"）。

### H3-b（新增，2026-08-10）｜H3 要不要把「与本 issue 无关的既有失败」算作不通过
- **冲突是什么**：`verify:base` 里 `lint:templates-doctor` 报的
  `TPL-INSTANCE-SCHEMA-INVALID (.agents/skills/mod-canvas-diagram/SKILL.md)`（PR #827
  canvas 线遗留，与 #728 chat 改动无关）在 round 14（SHA `5a72973c`）和 round 15
  （SHA `e2d15771`）上**性质完全相同**，但两次独立评分给出了相反处理：round 14
  选择"如实记录这条红，但按 H3 通过登记"（`_vp_round14_authoritative_20260810`），
  round 15 选择"按判据字面——硬门不满足则总分直接判 0"（`_vp_round15_h3_gate_20260810`），
  导致 P 组分数从 9 掉到 0，而 P 组自身十维在两轮都实测无回归（P1-P10 逐维仍然满足）。
  与上面 H3-a 不同：H3-a 问的是"允许不允许重跑取绿"，这里问的是"这条红本身是不是
  chat 的责任、算不算数"。
- **涉及文件/判据**：`.harness/rubrics/chat-main-fidelity-rubric.md` H3 行；
  `.harness/state/core-loop-readiness.json` 的 `_vp_round14_authoritative_20260810` /
  `_vp_round15_h3_gate_20260810`；根因 `.agents/skills/mod-canvas-diagram/SKILL.md`
  （PR #827，H3A-018）。
- **目前实现者怎么处理的**：未自行选边——round 15 的分数字段按更保守的数字（0）登记，
  但明确注明这不代表 round 14 的宽松处理被判定为错误，只是"有疑问报保守数字"的默认纪律。
  未去修 `mod-canvas-diagram/SKILL.md` 本身（跨 #728 范围，且已通过 `spawn_task`
  单独跟踪该修复）。
- **需要人类回答的具体问题**：
  - **选项 A**：H3 只看"与当前 issue watch 路径相关的检查是否绿"，`verify:base` 里
    与本 feature 完全无关模块（如 canvas skill 的 schema）的失败不算 H3 不过——
    这需要写清楚"无关"的判定方法（例如：失败文件是否在本 issue 的 `watch` 字段
    路径下），否则又会变成评分员各凭感觉的裁量。
  - **选项 B**：H3 维持绝对字面——`verify:base` 与 `verify:chat-read` 必须全部退出码 0，
    不区分红因归属。在 `mod-canvas-diagram/SKILL.md` 的 schema 问题修好前，
    任何 chat 相关评分都无法通过 H3，P/D 两组都会卡在 0 分——**这意味着尽快修好
    那条 schema 违规（工作量很小，加一个 `scope:` 字段）会成为解锁本 issue 后续
    评分的前置条件，即使它不属于 #728 范围**。
  - **选项 C**：折中——像 H3-a 选项 A 那样，把"经核实为环境/负载假阴性"的豁免范围
    正式扩展到"经核实为其他 issue 遗留、且已有独立 tracking（如本例的 spawn_task）
    的既有失败"，但要求评分员每次都必须证明该失败在 `origin/main` 上、在改动前的
    基线 SHA 上同样红（而不是自己觉得"看起来无关"）。

---

## 二、跨整站的项（不专属 D 组也不专属 P 组）

### 顶栏「项目负责人」与「不在项目上下文中 · 项目角色不适用」同屏矛盾
- **冲突是什么**：全站共用顶栏在个人对话（无项目上下文）场景下，同时显示组织角色标签
  「项目负责人」和「不在项目上下文中 · 项目角色不适用」，字面读起来自相矛盾——
  一边说"项目角色不适用"，一边又印着一个项目角色。
- **涉及文件/判据**：不是 chat rubric 判据点名的项（rubric 对个人对话屏的检查点只覆盖
  "无项目上下文如实呈现"这个 chat 组件本身，不含全站共用顶栏），是评分员在看图时顺带
  发现的、round 10 和 11 两次都重申的问题。属于全站共用壳（app shell），不是 chat 束
  自己的代码。
- **目前实现者怎么处理的**：未动。评分员两轮都**未据此扣 chat rubric 的分**（因为判据字面
  没有覆盖顶栏），只是提请人类注意这个文案矛盾。
- **需要人类回答的具体问题**：
  - **选项 A**：无项目上下文时隐藏组织角色标签本身。
  - **选项 B**：改「项目角色不适用」这半句文案，避免和角色标签同屏时读起来矛盾
    （例如去掉这句提示，只留角色标签本身）。
  - **选项 C**：维持现状——这是 shell 级别的既有模式，超出 #728 范围，另开 issue 单独处理，
    不在本 issue 内解决。

---

## 三、D 组（项目对话 ChatReadScreen，十维 D1-D10）—— 均不阻塞 P 组到 9/10

> D 组截至 round 12 记录为 **0/10（main 上，未按 #831 新判据复核过）**。下面只列尚未
> 裁决过的开放项；D2/D3/D4/D5 已在第零节标注为"已裁决，等实现"，不重复列出。

### D6/D7｜是否允许为了取证专门造带工具调用/产物的 fixture 数据
- **冲突是什么**：D6（AI 过程可见——思考步数+工具调用折叠块）、D7（产物卡）在项目对话侧
  目前的 fixture（`seed-chat-read-e2e.ts` 一类脚本）里没有任何 agent 真的调用过工具、
  产出过结构化产物。要让这两维有截图可评，得让 fixture 里的 agent 走一条真实会调用
  skill/产出产物的代码路径——但"专门为了评分去造一条这样的 fixture 数据"是否算作弊
  （本仓明令禁止"没有真实数据支撑的能力不做假 UI"），评分员没有自行判定。
- **涉及文件/判据**：D6/D7 rubric 行；fixture 种子脚本；`chat-ux-acceptance-criteria.md`
  第一条（没有真实数据支撑的能力不做假 UI）。
- **目前实现者怎么处理的**：未造。个人对话侧（P 组）后来确实这么做了——round 10/11 加了
  "第二个真调工具的 agent"，工具调用走真实 `execute-run.ts` 代码路径，P7 因此从"完全
  没有"转成"只差在途态一项"（见下方 P7）——这被评分员认为是可接受的取证方式（真实代码
  路径，不是脚本拼图）。但这个先例**没有被正式确认可以套用到 D 组**，D6/D7 仍标记待裁决。
- **需要人类回答的具体问题**：
  - **选项 A**：比照 P 组已经做过的方式——只要 agent 走的是真实产品代码路径（真的调用
    了 `execute-run.ts` 的工具调用逻辑、真的产出了结构化数据），fixture 本身"专门为了
    评分而配置"是可以接受的，因为验证的是代码路径而不是伪造 UI。
  - **选项 B**：不接受任何"专门为评分而造"的 fixture，D6/D7 必须等到产品里出现真实的、
    非评分驱动的工具调用/产物使用场景（例如某个真实 skill 已上线并被真实用户触发过）
    才能给分。

### D9｜五分页签里三个假标签 vs 无真实数据支撑
> ⚖ **已裁决（2026-08-21，选项 A）**——人类原话："先做'转录 + 产物'两个真标签，转录
> 控件保留在当前位置（消息面板上方），不搬进右侧栏；'执行/洞察/材料'三个子句比照
> #831 对 D3/D4 的处理方式，从判据中移除或标注'待后端建模'。"
>
> **落地时的字面理解**（与实现方 dev-chat-e2e 沟通确认）：裁决原话的「转录+产物」两个
> 真标签、按字面读会与"转录控件不搬进右侧栏"自相矛盾——转录控件本来就不在右侧栏，
> 不存在"搬不搬"的问题。实际落地选择：**右侧栏画「产物」+「材料」两个真标签**
> （不是"转录"标签——转录控件继续留在消息面板上方，与原来位置逐字节相同，D9 不再
> 提它）；「材料」在本仓已有真实实现路径（`chat_message_attachments` 表 + F150/F151/
> #1584 既有基础设施），凑成两个真标签；「执行/洞察」两个移除、标注待后端建模。
> Rubric D9 行、本节判据已同步改写，见下方。
- **冲突是什么**：原型右侧栏是「转录/执行/洞察/产物/材料」五个分页签。产品侧「转录」
  「产物」两个已有真实数据支撑，但「执行/洞察/材料」三个的计数在后端被硬编码为 0
  （`chat-artifacts-panel.tsx` 文件头已记录，`get-thread.ts` 的 `rightTabs()` 没有
  真实查询）。逐字满足 D9 判据要画出全部五个标签，但画出三个永远显示"0"的假标签正是
  仓库硬规则明令禁止的"没有真实数据支撑的能力不做假 UI"。
  另外调查时发现第二个约束：把"转录"控件（`ChatRecordingPanel`，一个**写操作**——开始/
  停止录音）搬进右侧栏会引入真实回归，因为右侧栏在 <1280px 屏幕上是 `xl:block`（不渲染），
  搬进去等于让这个功能在中小屏上彻底消失，代价比"保真度差一分"更大。
- **涉及文件/判据**：`apps/web/components/chat/chat-artifacts-panel.tsx`；D9 rubric 行；
  `chat-ux-acceptance-criteria.md` 第一条。
- **目前实现者怎么处理的**：调查后未强行做，round 6/7 记录"D9 本轮不动"，把两个约束原样
  写进 issue，未自行折中或拍板。
- **需要人类回答的具体问题**：
  - **选项 A**：先做"转录 + 产物"两个真标签（都有真实数据），正式承认这满足 D9 的"部分"
    判据，把「执行/洞察/材料」三个子句比照 #831 对 D3/D4 的处理方式，从判据中移除或
    标注"待后端建模"，且转录控件保留在当前位置（消息面板上方），**不**搬进右侧栏。
  - **选项 B**：先做后端建模，让「执行/洞察/材料」三个标签有真实计数（这是独立于 chat
    束的一块新工作量，需要单独排期），做完后再让 D9 按原型字面五标签全部满足。
  - **选项 C**：D9 在后端建模完成前维持 0 分，不接受任何部分方案。

---

## 四、P 组（个人对话 PersonalChatScreen，十维 P1-P10）—— round 12 记为 7/10

### P7｜工具调用「正在调用中」在途态缺失 —— **P 组到 9/10 路径上的唯一阻塞项**
- **冲突是什么**：`chat-ux-acceptance-criteria.md` 第 2/3 项要求界面上能看到"正在调用 XX"
  这个进行中状态。当前实现只落库工具调用的终态（成功/失败），调用发起那一刻没有任何
  "正在调用"的数据可读——这是**有文档理由的刻意设计**，不是遗漏：
  `chat-live-message-panel.tsx:704-713` 的注释原文解释"伪造一个『正在调用…』的过渡态
  会是一句界面从未验证过的谎言"。判据字面要求与这条工程立场正面冲突，round 8/10/12
  三次记为"待裁决"。
- **涉及文件/判据**：`packages/contracts/src/wave2-runtime.ts:205,260-289`
  （`AgentRunStepStatus` 枚举、`endedAt` 非空约束）；
  `apps/api/src/application/agent-run/execute-run.ts`（`executeToolLoop`/`record()`，
  291-376/171-196 行）；`apps/api/src/application/agent-run/ports.ts:76`；
  `apps/api/src/infrastructure/agent-run/pg-agent-run-repository.ts:161`；
  `apps/web/components/chat/chat-live-message-panel.tsx:704-790`（`AgentRunToolCallSteps`）；
  `agent_run_steps` 表的 `agent_run_steps_append_only_trg`（append-only 触发器，与
  "running → 终态原地更新"直接冲突）。
- **已有的完整提案**：`docs/proposals/PROP-CHAT-P7-INPROGRESS-STATUS-001.md`
  （2026-08-09 起草，在 issue #728 工作分支上，**尚未合并 main，尚未裁决**）。
  提案核实了 rev-uiux 原始修法描述里四处文件指认有误（真实改动点已在提案里更正），
  并给出：
  - **路径 A（改契约，做出真实的 running 态）**：`AgentRunStepStatus` 扩为
    `running|succeeded|failed`，`endedAt` 改 nullable，新增 migration，并**必须先决定**
    append-only 触发器冲突怎么处理——(a) 给"running→终态"这一条特定路径开白名单允许
    原地更新，或 (b) 保持 append-only、改成同一 `toolName` 插两行（running 一行、终态
    一行），前端做去重/合并渲染。预估工作量 0.5-1 天，风险点详见提案第六节。
  - **路径 B（不改代码，改判据）**：承认"只有终态可见"是可接受的产品行为，由人类修订
    `chat-ux-acceptance-criteria.md` 第 3 项文字（去掉或改写"正在调用 XX"这半句），
    不涉及任何代码改动，但判据文件本身的修订也要走人类裁决（agent 不能自己改判据）。
- **目前实现者怎么处理的**：按判据字面判 0，未自行加码去做一个"看起来在动但没有真实数据
  支撑"的假动画，写了完整提案交给人类，三次记入待裁决未擅自选择路径。
- **需要人类回答的具体问题**：
  - 在路径 A / B 之间二选一；
  - 若选 A：进一步在 append-only 触发器冲突的处理方式 (a)/(b) 之间二选一（提案第六节
    "风险点"已列出两种做法的取舍）。

### P9｜失败态判 1 的口径边界：要不要求"持久可见 + 可重试"
- **冲突是什么**：round 12 把 P9 判为 1 分——理由是真实的 `MODEL_CALL_FAILED` 失败
  如实展示在界面上，不是静默卡住、不是假 UI。但评分员同时指出三个未纳入扣分考量的
  特征：① 展示的是**裸错误码**（`MODEL_CALL_FAILED`，没有人类可读的说明）；
  ② **没有重试入口**；③ **刷新页面后失败提示消失**（不持久）。如果
  `chat-ux-acceptance-criteria.md` 第 7 项"错误处理透明度"的判据应该隐含"失败必须
  持久可见 + 可重试"，那么 P9 应改判 0，P 组总分从 7/10 降到 6/10。
- **涉及文件/判据**：`.harness/instructions/chat-ux-acceptance-criteria.md` 第 7 项；
  rubric P9 行；证据截图 `chat-main-personal-failure.png`；
  失败态渲染代码（`chat-live-message-panel.tsx` / `personal-chat-screen.tsx` 里读取
  `GET /agent-runs/:runId` 终态并展示 `failureCode` 的部分）。
- **目前实现者怎么处理的**：没有改动实现，也没有改判据——评分员按判据原文字面判 1，
  明确写"未自行加码"，把口径边界原样上报。
- **需要人类回答的具体问题**：
  - **选项 A**：维持现状口径——只要真实终态失败被诚实渲染（不静默、不伪造），就满足
    P9，裸错误码/无重试/不持久都不是扣分理由。P 组维持 7/10，不需要新增工作。
  - **选项 B**：收紧口径——P9 要求失败态**持久可见**（刷新后仍看得到）且**有可操作的
    重试入口**。当前实现按新口径重判为 0，P 组降到 6/10，并新增"失败态持久化 + 加重试
    按钮"为后续工作项。
  - **选项 C**：只加"持久可见"这一条门槛（刷新后不消失），不强制要求重试入口——介于
    A/B 之间的折中判据。

---

## 五、按影响的分数维度归类（速查表）

| 分组 | 事项 | 是否阻塞 P 组到 9/10 | 当前状态 |
|---|---|---|---|
| 硬门 | H3-a 同 SHA 重跑取绿 | 否（H3 已连续判过） | 待裁决 |
| 硬门 | **H3-b 无关既有失败算不算 H3 不过** | **是（新的唯一阻塞项）** —— round 15 因此把 P 从 9 判回 0，P 组十维本身无回归 | 待裁决（新增 2026-08-10） |
| 全站共用壳 | 顶栏角色标签矛盾 | 否 | 待裁决，未扣分 |
| D 组 | D2/D5 角色字段 | 否 | **已裁决**（#831），待实现+重新评分 |
| D 组 | D3 负责 agent 子句 | 否 | **已裁决**（#831），待重新评分 |
| D 组 | D4 阶段/周次子句 | 否 | **已裁决**（#831），待重新评分 |
| D 组 | D6/D7 取证 fixture 政策 | 否 | 待裁决 |
| D 组 | D9 三个假标签 | 否 —— **已裁决**（2026-08-21，选项 A：先做「产物+材料」两个真标签，「执行/洞察」移除待后端建模），见 D9 一节标注 | 已裁决并落地 |
| P 组 | P7 工具调用在途态 | 否 —— **已裁决**（2026-08-10，路径B，PR #866），round 15 实测 P7 在新判据下已满足 | 已裁决并落地 |
| P 组 | P9 失败态口径边界 | 否（现判 1，收紧才会掉到 6/10） | 待裁决 |

---

*本文档由 dev-chat-e2e worker 于 2026-08-09 整理，未对任何一条给出结论，仅呈现选项供人类裁决。
建议作为 issue #728 的一条汇总评论发出，或作为独立文档供 coord-main 分发。*
