# 研究规划修复进度（#3441）

用户直接反馈第四步资料研究规划 0/1 暂停，显示 RESEARCH_NODE_STATE_INVALID，任务和来源均为零。

范围：基于 f1b563501，新规划输出 schema/章节覆盖失败时只增加一次自动修复；验证保持严格，来源只来自既有真实检索端口。其余步骤、UI 和错误协议不变。

反证：标准 with-test-isolation 下新增测试在旧实现运行，5 failed / 2 passed。缺必需字段、未知章节和漏覆盖均不能恢复；修复次数及允许章节上下文断言失败。完整输出保留在 /tmp/research-plan-red.log。

实现：共用契约转 JSON Schema 提供完整生成约束；专用校验异常区分 provider/store；修复携带受限原输出、字段错误和精确章节 ID；校验成功前不落盘部分新任务。

现场边界：浏览器调试连接超时，未取得这次线上失败的原始模型输出，不能判定具体哪项字段失败。测试通过注入模型返回验证流程，不代表线上供应商一定产生有效修复输出。

最终验证结果见同目录 session-handoff.md。

## PR 审查补强

远端 P2 指出同一输出同时缺字段和含错误章节 ID 时，只反馈 schema 错误可能浪费唯一修复机会。已用纯函数反例证实：原提交缺少 unknown section 诊断而失败；补强后同一反例通过。现在安全读取原始 tasks 中字符串 sectionId 只作诊断，即使完整 schema 失败也同时报告章节错误和漏覆盖，正式计划仍要求全契约校验通过。新增组合场景测试，全部测试数变为 55。

原提交 0ca496e0568be2691ebed8432978af377e3f0f83 的 CI 为17 SUCCESS / 7 SKIPPED；该结果不代替补强提交的重新验证。
