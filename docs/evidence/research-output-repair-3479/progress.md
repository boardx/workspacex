# 研究来源评估输出修复 #3479

用户截图：已有王者荣耀研究点击开始真实检索，显示相关性评估未通过校验、43个旧来源待复核。

已确认代码缺陷：JSON解析失败发生在校验回调前，无法进入来源输出的有界修复；结构/语义失败仅返回泛化修复提示，没有具体字段/引用错误。未取得线上模型原始响应，不能断言本次具体失败字段。

补修：仅来源筛选注入专用JSON解析器；传递原始无效输出及有界具体诊断，最多重试一次。整批原子性、逐字引用、任务问题范围及相关性要求不变。provider自身SyntaxError和持久化错误不作为输出错误重试。

验证：92项纯API回归通过；旧HEAD570cdcea7在新增初始5例中4失败、1通过。真实PG组合回归在标准资源准入队列。独立预审发现诊断限长不足，已补最多16条/路径6段/消息320字等限制和超长键名反例。

最终本地验证：标准隔离数据库组合回归129/129通过（36真实PG＋93纯API）。API类型检查通过。持久化旧源用例首次坏JSON后恰好两次评估，模型调用failed/succeeded落库。
