# #3449 交接

已验证：147 项针对性回归、API/web 类型检查；原基线在汽车结果用例上失败，修复后通过。详见 verification.txt / baseline-regression.txt。

本轮改动：自动检索源提前语义筛选并逐字引用校验，缓存绑定研究依据，旧会话复核、可重试任务与部分报告恢复，UI区分来源状态；权威契约只加可选服务器来源标记和错误码。

未验证：线上原会话的实际查询/供应商响应未读取；真实外部检索与模型验证被自动审批拒绝（缺少明确凭据使用和摘要外发授权），已向用户询问，未绕过。旧来源无手动标记时统一复核。模型语义判断有误差且增加调用耗时，现有报告证据校验仍保留。

PR #3452 已提交。补修按来源＋任务筛选，缓存绑定任务定义；旧关联裁剪与数据库重试已验证。初始head的fullstack失败为测试模型缺少新阶段回应，已补夹具与汽车负例。后续：推送补修、独立exact SHA review和CI；全绿转交coord-main合并，本模块不自行合并。回收本次隔离资源和工作树，释放模块租约。不启用后台自动化。
