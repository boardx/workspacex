# 检索失败恢复 #3489

用户响应证据：15任务6成功、3SEARCH_EMPTY、6NO_RELEVANT_SOURCES，15条已筛选来源，最新模型调用成功。基线bcb045418只重复task.query。

实现：只在真实空/无相关结果后生成最多两条短query，去重已尝试查询，保留原任务定义与来源相关性校验。searchAttempts记录实际query和结果，上限12；成功任务不重搜。provider抛同名错误归一为不可改写的异常；实际检索入口终结遗留running history，覆盖批准对话提案。普通complete不触发清理，不绕过运行中任务。UI区分已处理/成功/失败并展示查询历史。

验证：147个不同API用例（105纯逻辑、42真实PG），25契约、24 UI用例通过。首轮组合145项143通过2失败，修复新任务fixture历史继承及complete清理边界后，最终PG42项全部通过；入口调整后恢复12项再通过。API/web类型检查通过，init通过，diff检查通过。旧基线两个恢复正例均失败，证明行为差异。PG验证使用标准隔离外壳，资源队列自动放行，未手工绕过负载门禁。

边界：不承诺互联网上一定存在所有内部指标。公开资料不足时仍保留缺口；至少一条真实来源及用户明确部分报告动作后可继续报告，不能伪造全部成功。真实外部模型复测仍待此前外发授权，受控测试不等于原会话恢复。PR及CI最终结果见GitHub #3489关联PR。
