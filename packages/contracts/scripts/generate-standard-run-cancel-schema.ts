import {readFileSync,writeFileSync} from 'node:fs';import {resolve} from 'node:path';import {zodToJsonSchema} from 'zod-to-json-schema';import {RunCancelInput,RunCancelOutput,STANDARD_RUN_CANCEL_TOOL,StandardRunCancelInvocation} from '../src/standard-run-cancel';
const opts={target:'jsonSchema7',$refStrategy:'none'} as const;const content=JSON.stringify({toolName:STANDARD_RUN_CANCEL_TOOL,input:zodToJsonSchema(StandardRunCancelInvocation,opts),toolInput:zodToJsonSchema(RunCancelInput,opts),toolOutput:zodToJsonSchema(RunCancelOutput,opts),limits:{deadlineMs:30000,maxResponseBytes:65536}},null,2)+'\n';const path=resolve(import.meta.dirname,'../../../apps/deep-agent-service/src/deep_agent_service/generated/standard_run_cancel_schema.json');if(process.argv.includes('--check')){if(readFileSync(path,'utf8')!==content)throw new Error('standard run cancel schema stale');}else writeFileSync(path,content);
