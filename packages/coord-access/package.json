{
  "name": "@repo/coord-access",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Cloudflare Access JWT 校验的唯一实现（运营平面共用：devportal / ops-console / ops-telemetry）——缺配置 fail-closed",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: ok'"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "*",
    "@types/node": "*"
  }
}
