const net=require('node:net');
async function connect(authority){return await new Promise((resolve,reject)=>{const s=net.connect(3128,'browser-egress',()=>s.write(`CONNECT ${authority} HTTP/1.1\r\nHost: ${authority}\r\n\r\n`));s.setTimeout(5000,()=>{s.destroy();reject(new Error('timeout'))});s.once('error',reject);s.once('data',d=>{s.destroy();resolve(d.toString().split('\r\n')[0])})})}
(async()=>{for(const target of ['127.0.0.1:443','169.254.169.254:443','10.0.0.1:443','[::1]:443','[::ffff:127.0.0.1]:443','example.com:443']){const status=await connect(target);console.log(target,status);if(!status.includes(target==='example.com:443'?'200':'403'))process.exitCode=1}
await new Promise(resolve=>{const s=net.connect(443,'1.1.1.1',()=>{console.log('direct public bypass: UNEXPECTED connected');process.exitCode=1;s.destroy();resolve()});s.setTimeout(3000,()=>{console.log('direct public bypass: denied (timeout)');s.destroy();resolve()});s.once('error',e=>{console.log('direct public bypass: denied',e.code);resolve()})})})().catch(e=>{console.error(e);process.exitCode=1});
