/**
 * 疑问句判断的公用部分：`memory-intent.ts`（F17，判用户消息里的记住 / 忘掉意图）与
 * `decision-claim.ts`（issue #4181，判已写入 claims 的结论是不是在陈述一个决定）各自
 * 排除「这是一个问句，不是在陈述」时，判定标准完全一样——句末问号、句中疑问词。这两条
 * 原来在两个文件里各写一份、逐字相同，评审 2026-09-25 发现的重复：抽到这里，两边只从
 * 这里 import，不会再悄悄漂开。
 *
 * `QUESTION_TAIL`（句末疑问语气「…吗/呢/是不是」）没有抽到这里：两边对「后面能不能跟
 * 标点」的处理不一样（`memory-intent.ts` 在调用前单独 strip 尾部标点；`decision-claim.ts`
 * 把标点写进正则本身），勉强抽成一份反而要在调用点补差异，不如各自保留更清楚。
 */

/** 句末问号（含中文问号）。 */
export const QUESTION_END = /[?？]\s*$/;

/** 句中带疑问词：问的是内容本身，不是在陈述一件事。 */
export const INTERROGATIVE = /什么|谁|哪|怎么|怎样|咋|多少|几(?!乎)|为何|为什么|是否|是不是|对不对|有没有|能不能|要不要|会不会/;
