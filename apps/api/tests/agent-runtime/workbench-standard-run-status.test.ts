import {expect,it,vi} from 'vitest';import {StandardRunStatusService} from '../../src/application/agent-run/standard-run-status';import {toOrgId} from '../../src/domain/org-id';
it('maps the authorized read and actual produced artifact refs',async()=>{const read=vi.fn().mockResolvedValue({status:'awaiting_tool_permission',steps:[],pendingApproval:{permissionRequestId:null,toolName:'execute',argsSummary:'cmd'}});const artifacts={listProducedByRun:vi.fn().mockResolvedValue([{artifactId:'a',versionId:'a-v2'}])};const service=new StandardRunStatusService({} as never,artifacts,()=>new Date('2026-09-07T00:00:00Z'),read as never);const out=await service.invoke({orgId:toOrgId('o'),userId:'trusted'},'target');expect(read).toHaveBeenCalledWith({}, {orgId:'o',userId:'trusted',runId:'target'});expect(out).toMatchObject({status:'awaiting_tool_permission',waitingRequest:{toolName:'execute'},artifactRefs:[{artifactId:'a',versionId:'a-v2'}]});});
