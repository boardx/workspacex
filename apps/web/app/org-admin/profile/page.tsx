import { OrgProfileScreen } from "@/components/org-admin/org-admin-screen";

/**
 * `/org-admin/profile`——issue #2615：原「组织管理」标签页拆平后的「组织资料」独立路由。
 */
export default function OrgAdminProfilePage() {
  return <OrgProfileScreen />;
}
