import { OrgInvitesScreen } from "@/components/org-admin/org-admin-screen";

/**
 * `/org-admin/invites`——issue #2615：原「组织管理」标签页拆平后的「邀请」独立路由。
 */
export default function OrgAdminInvitesPage() {
  return <OrgInvitesScreen />;
}
