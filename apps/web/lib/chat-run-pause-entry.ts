/**
 * chat 里「暂停」入口的**唯一开关**（issue #3318，2026-09-10 人类裁决）。
 *
 * > chat 中，暂停不了，先取消暂停的动作，只支持取消
 *
 * 实测：devapp 上生成 pptx 的一轮点「暂停」后界面停在「正在暂停 / 暂停中…」，
 * run 并没有停下。真实 kernel 链路上暂停不生效的根因另立 **#3319**
 * （线索：#3307 新增的三条路径在生产侧全部够不到，`run_control_callback` 的生产方
 * 在 `apps/` 与 `packages/` 里一个都不存在，`record.runControl` 恒 `undefined`）。
 *
 * 本文件是**止血**：先别给用户一个按下去没用的按钮。取消（执行条上的「停止生成」，
 * `copilotkit-v2-panel-body.tsx`）是当前唯一真能终止一轮的用户动作，原样保留。
 *
 * ## 为什么是一个常量、而不是把代码删掉
 *
 * 「暂停」的判据（`@path:F3`、`TW-P1-5a`、`TW-P0-3⑤` 的最后一条锚点，以及
 * `tests/ui/copilotkit-v2-plan-control.test.tsx` 的若干条）**不许删** —— 删掉的话
 * 等 #3319 修好、暂停回来时没有任何东西守着它。它们改成读**同一个常量**：
 * 关着就跳过，开了就照旧逐字求值。一处翻牌，入口与判据同时回来，
 * 不会出现"按钮回来了但判据还睡着"的漂移（同一事实不得声明在两处）。
 *
 * ⚠ 标注 `: boolean` 是刻意的：不写的话 TS 把它收窄成字面量类型 `false`，
 * 依赖它的分支会被判成恒不可达，翻牌那天连编译都是错的。
 */
export const CHAT_RUN_PAUSE_ENTRY_ENABLED: boolean = false;
